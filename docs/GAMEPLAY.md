# NIKKE 게임플레이 메커니즘 — 계산기 맥락 참고용

시뮬 결과가 "맞다"는 게 어떤 모습인지 판단 기준. 개별 스킬 구현 명세는 IMPL-STATUS.md.

---

## 전투 기본 설정

- **전투 시간**: 180초. `DEFAULT_CONFIG["duration"]`.
- **적**: 보스 1명. 보스 모드는 **간단 모드**(패턴 없음 — 스탯 고정)와 **패턴 모드** 둘이고, 패턴 모드 안에서
  좌표를 켜고 끈다(`docs/CALCULATOR.md` §보스 패턴의 모드 표). 보스 패턴 `summon`으로 **쫄몹**을 띄울 수 있다 — 쫄몹은
  좌표가 없고, 무엇을 겨누는가는 조준 순서(카메라 니케 저지원 → 쫄몹 → 벌칙 파츠 → 본체, 나머지 쫄몹 → 본체 —
  `docs/CALCULATOR.md` §조준)가 정한다. 쫄몹이 받은 딜은 총딜에 넣지 않는다(`docs/CALCULATOR.md` §쫄몹).
  패턴이 없으면 적은 보스 하나다. 보스의 파츠·저지원·코어는 좌표를 켜면(좌표 모드 — 스크립트가 **화면 좌표**를 준다)
  에임과 탄 분포로 어디에 맞는지 푼다(`docs/CALCULATOR.md` §좌표 모드). 적정거리는 보스 거리(`enemy.distance`)를 주면 니케마다 적정 구간과 비교한다.
  보스 하나가 **시간에 따라 상태를 바꾸는 것**(코어 개폐·방어력 변화·속성보호막·사라짐·파츠·
  저지 표적)은 범위 안이다 — `enemy["patterns"]`, 정본 `calculator/boss_pattern.py`.
  보스가 니케를 때리는 공격(`attack` 패턴)도 범위 안이다 — 보호막·엄폐물·무적·도발·은신·전투불능·
  부활까지 본다(`docs/CALCULATOR.md` §보스 공격). 니케에게 거는 디버프·보스 지속 피해(`debuff` 패턴·
  공격의 `debuffs`)와 보스가 두르는 받는 대미지 증감도 범위 안이다(`docs/CALCULATOR.md` §보스 디버프).
  **패턴을 주지 않으면 니케는 맞지 않는다** — 하네스
  기본 경로가 그렇고, 아래에서 「보스 sim에서 무발동」이라 적힌 피격 계열은 그 경로 이야기다.
- **스쿼드**: 기본 5인.

---

## 컨트롤

유저 조작으로 같은 스쿼드·같은 육성에서도 딜이 달라지는 요소가 있다.
**자동(꺼짐)이 기본**이고, 예외는 캐릭터별 기본 레이어에 등록된 니케뿐이다
(`data/char_defaults.json` — 앨리스·아인·밀크 : 블루밍 바니).

실제 조작은 **좌클릭 누르기**와 **shift 엄폐** 둘뿐이고, 둘 다 시작·끝을 가진 구간이다.
보고 있지 않은 나머지 4명은 언제나 자동으로 싸운다 — **자동이 디폴트다.**

- **톡톡이** — 차지형(SR/RL)에서 차지를 끝까지 하지 않고 짧게 눌렀다 떼기를 반복.
  발당 대미지는 낮아지고 발사 횟수가 늘어난다.
- **장전컨** — 엄폐로 재장전을 유리한 버프 구간에 밀어 넣는다. 엄폐 중에는 재장전을 하므로,
  재장전은 직접 거는 조작이 아니라 **엄폐로 유도되는 결과**다.
- **버스트 엄폐컨** — 본인 버스트 사이클의 풀버스트 동안 엄폐해 **한 발도 쏘지 않는다.**
  발수로 소모되는 버프를 일반 공격에 쓰지 않고 스킬 대미지 구간까지 아끼는 조작이다.
  장전컨과 원시타입은 같지만 목적이 반대다 — 재장전은 여기서 부산물이다.
- **홀드** — 풀차지 후에도 떼지 않고 들고 있다가 원하는 시점에 발사. 전투 내내 하는 운용이
  아니라 특이케이스라, 기본 전략이 아니라 시퀀스로만 지정한다.

메커니즘 수치·계산기 모델·설정 스키마·적용 대상은 **`docs/CONTROL.md`가 정본**이다.

---

## 스쿼드 구성

### 버스트 쿨타임 현황

| 단계 | 일반적 쿨타임 | 비고 |
|------|-------------|------|
| B1 | 20초 | 간혹 40초·60초도 있음 |
| B2 | 20초 | 간혹 40초·60초도 있음 |
| B3 | 최소 40초 | 현 니케 풀에 20초짜리 없음 |

### 유효한 스쿼드의 조건

버스트 사이클 유지 = **각 단계(B1·B2·B3)에 한 사이클 안에 쿨이 도는 캐릭터 필요**.

- B3 쿨 최소 40초 → 항상 2명 필요 (번갈아 사용).
- B1·B2 20초 → 각 1명으로 충분.
- B1·B2 40초 → 2명 필요. B1 20초 1명 + B1 40초 1명 조합도 가능.

전형적 5인 구성: **B1×1 + B2×1 + B3×2 + 버스트 미사용×1**

### 버스트 쿨타임 감소

대부분 실전 스쿼드는 버스트 쿨타임 감소 캐릭터(주로 B1·B2) 포함.

- 실전 쿨 감소량: 아군 전체 기준 **약 6~8초**
- 사이클 주기: 쿨 감소 적용 시 약 **12~14초** (대표값 7.48초)
- 180초 기준: 풀버스트 14회 + 종료 직전 15번째 시작

**계산기 구현**: 두 개의 stat으로 나뉜다.

| stat | 성격 | 처리 |
|------|------|------|
| `burst_cooldown` | 지속형 buff. 거는 동안 쿨이 줄어든 상태 유지 | `BurstManager.tick()`의 `full_burst_start` 분기가 `burst_ready_at`을 당김 |
| `burst_cooldown_reduce` | 순간 차감. 발동 시 남은 쿨에서 한 번 깎음 | `_dispatch_instant()` |

쿨 감소량은 **왜 그 값인가보다 언제 몇 번 발동하는가**가 사이클을 좌우한다.
같은 7.48초라도 매 사이클 발동하면 주기가 고정되고, 3회만 발동하면 초반에만 짧다.

#### 감소 수치 (스킬 레벨 10 기준)

| 캐릭터 | 수치 | 대상 | 발동 |
|--------|------|------|------|
| 아니스 : 스타 | 7.48 | 전체 | 전투 시작 / 풀버스트 종료 (1버 아군 없을 때) |
| 라피 : 레드 후드 | 7.48 | 전체 | 풀버스트 시작 (전투 보조 상태) |
| 리틀 머메이드 | 7.48 | 전체 | 풀버스트 종료 |
| 솔린 : 프로스트 티켓 | 7.48 | 전체 | 풀버스트 시작 — **지속형**(`burst_cooldown`) |
| 목단 | 7.48 | 전체 | 풀버스트 시작 (열혈 상태) |
| D : 킬러 와이프 | 7.0 | 전체 | 풀차지 8회 |
| 루주 | 7.0 | 전체 | 풀차지 8회 |
| 아르카나 | 6.0 | 전체 | 풀버스트 종료 (운명의 수레바퀴 카드) |
| 사쿠라 | 4.84 | 전체 | 풀버스트 시작 |
| 리타 | 2.34 → 2.7 → 3.17 | 전체 | 풀버스트 시작 **1·2·3회차만** |
| 볼륨 | 2.34 → 2.7 → 3.17 | 전체 | 풀버스트 시작 **1·2·3회차만** |
| 헬름 : 아쿠아마린 | 1.82 → 2.2 → 2.6 | 전체 | 풀버스트 시작 **1·2·3회차만** |
| 라피 : 레드 후드 (스3) | 20.0 | 자신 | 버스트 시전 |
| 목단 (스1) | 20.0 | 자신 | 적 등장 — 지속형 |
| 레드 후드 | 40.0 | 자신 | 스쿼드 1버/2버 시전 시 각 1회 |
| 프리카 | **−21.0 (감소가 아니라 증가)** | 자신 | 떼창 |

> 프리카의 음수는 **파싱 오류가 아니라 의도된 값**이다. 자신의 버스트 쿨타임을 21초
> *늘린다*. 부호를 "고치지" 말 것.

`target: self`인 항목(라피 스3, 목단 스1, 레드 후드, 프리카)은 **아군 전체 사이클을 바꾸지 않는다.**
본인의 재사용 간격만 조절한다. 사이클 주기를 논할 때 이들을 전체 감소와 섞지 않는다.

#### 사이클 간격 패턴 (시드 42 실측)

| 구성 | 간격열 | 성격 |
|------|--------|------|
| 7.48 계열 보유 | `12.55` | 균일 — **표준** |
| D : 킬러 와이프 / 루주 (7.0) | `13.0` | 기본 균일 — 13사이클에 1~2칸 짧아진다 (아래 예외) |
| 볼륨 · 리타 | `17.65 → 14.95 → 12.25` 이후 `12.25` | 감쇠 후 정착 |
| 헬름 : 아쿠아마린 | `18.2 → 16.0 → 13.4` 이후 `13.4` | 감쇠 후 정착 |
| 아르카나 + 이사벨 | `7.25 ↔ 12.55` | 무한 교대 |
| 소다 : 트윙클링 바니 | `20.0` | 균일 — 버쿨감 없는 덱 |
| 티아 · 앨리스 : 원더랜드 바니 (B1 재진입, 쿨 40) + 리틀 머메이드 | `12.533 ↔ 12.75` | 무한 교대 — 재진입 대기가 든 사이클만 길다 |
| 아비스타 + 리틀 머메이드 (B1 재진입) | `12.75` | 균일 |
| 아비스타 + 아니스 : 스타 | `20.0` | 균일 — 버쿨감이 없는 조합 (아래 예외) |

**판정은 "12.5초에 가까운가"가 아니라 "그 구성의 프로필에 맞는 패턴인가"로 한다.**
아래 예외들은 전부 정상 동작이다.

#### 예외 케이스

**볼륨 · 리타 · 헬름 : 아쿠아마린 — 감쇠 후 정착**
`full_burst_start_count:1/2/3` 트리거라 **처음 세 사이클만** 쿨을 깎는다.
그래서 첫 간격이 가장 길고 점점 짧아지다가, 4번째부터 감소가 끊겨 고정값에 정착한다.
불규칙해 보이지만 회귀가 아니다. 볼륨과 리타는 수치가 같아 간격열도 소수점까지 일치한다.

**아비스타 + 아니스 : 스타 — 버쿨감이 통째로 꺼진다** (2026-09-22)
아니스 : 스타의 버쿨감 `스타 폴 3`은 condition이 `no_burst1_ally`다. B1인 아비스타가 들어오면
그 분기가 꺼지고 대신 `모두의 별`(`burst_stage_override:reenter1`)이 켜진다 — 스쿼드의 버쿨감이
0이 되어 사이클이 아비스타 본인의 쿨 20초로 고정된다. 소다 : 트윙클링 바니와 같은
「버쿨감 없는 덱」이지 회귀가 아니다. 보조 B1을 리틀 머메이드로 바꾸면 `12.75` 균일이 된다
(재진입 자리를 늘 동료가 받고 아비스타는 쿨 20초로 매 사이클 B1을 연다).

**아르카나 + 이사벨 — 7.25초 교대**
아르카나·이사벨이 함께 버스트를 쓴 사이클 뒤에는 버스트가 약 5초 만에 끝나고
버쿨감이 한 번 더 적용된다. 그 결과 짧은 사이클과 표준 사이클이 무한 교대한다.
`7.25 ↔ 12.55`가 계속 반복되면 정상이다. (등록 스쿼드: `S36_이사벨아르카나`)

**D : 킬러 와이프 · 루주 — 13.0 사이에 한 칸씩 끼는 짧은 간격**
이 둘의 버쿨감(`카드 스로우` · `어썰트 포메이션`, 각 7.0초)은 `full_charge_fire_count:8` 트리거다 —
**버스트 사이클이 아니라 본인 풀차지 8회**마다 발동한다. 두 주기가 맞아떨어지지 않으면 발동
시점이 사이클에 대해 서서히 밀리고, 어느 사이클에는 감소가 두 번 들어가 **그 한 칸만 짧아진다.**
어느 칸이 짧아지는지는 그 덱에서 차지 속도가 얼마나 빠른가에 달렸다 — 조합마다 다르다.

180초 13사이클 기준으로 **한 칸**이 12.2~12.8초로 내려앉는 게 정상이다. 등록 스쿼드 넷이
모두 그렇고, 지금 편성에서는 넷 다 9번째 칸이지만 **값은 서로 다르다** — 그 덱의 차지 속도가
정하기 때문이다. 위치도 조합이 바뀌면 함께 움직인다:

| 스쿼드 | 짧아진 칸 |
|--------|----------|
| `S40_킬러와이프` | 9번째 `12.25` |
| `S36_아인루주` | 9번째 `12.65` |
| `S36_블랑` · `커버_라플라스맥스웰` | 9번째 `12.75` |

**전부 13.0으로 균일하게 나오는 쪽이 오히려 이상하다** — 그 경우 버쿨감이 사이클당 정확히
한 번으로 고정됐다는 뜻이라 발사 쪽이 바뀌지 않았는지 본다.

**소다 : 트윙클링 바니 — 버쿨감을 아예 넣지 않는 덱**
소다는 `fullburst_duration`(풀버스트 지속시간)을 늘린다 — `burst_enter:3` 발동,
골든 칩 10·20스택 조건. **풀버스트 구간 자체가 길어지므로 쿨을 줄일 이유가 없다.**
그래서 이 덱은 버쿨감 캐릭터를 넣지 않고, 사이클도 20초로 길다.
사이클이 길다고 스쿼드가 깨진 게 아니다. 아주 특수한 경우다. (등록 스쿼드: `S39_소다`)

**레드 후드 — `burst_stage: "A"`**
1·2·3단계 어디로든 쓸 수 있어 `burst_order`의 **세 단계 목록에 모두** 등록된다.
따라서 배치 위치가 사이클을 좌우한다 → §스쿼드 구성 §버스트 사용 순서와 배치.

### 버스트 사용 순서와 배치

**스쿼드 멤버 순서 = 버스트 사용 우선순위다.** 유저가 화면 왼쪽부터 누르는 것과 같다.
`BurstController`는 단계별 후보를 **스쿼드 입력 순서**로 세우고(`_rebuild_burst_order()`),
쿨이 도는 사람이 없으면 다음 순위로 넘어간다.

같은 단계에 여러 명이면 **앞사람이 계속 선점한다.** 뒤에 놓인 캐릭터는
앞사람 쿨이 안 돌 때만 쓰게 되고, 앞사람 쿨이 짧으면 **한 번도 버스트를 못 쓴다.**

> 관찰 대상 캐릭터는 반드시 **자기 단계 안에서 맨 앞**에 둔다.
> 뒤로 밀린 채 스냅샷을 찍으면 그 캐릭터의 버스트 스킬이 아예 검증되지 않는다.

예: `[미란다(B1), 에이드(B2), 헬름:아쿠아마린(B2), 에이다(B3), 스노우 화이트(B3)]`
→ 에이드(쿨 20초)가 B2를 독점해 헬름 : 아쿠아마린은 버스트를 한 번도 못 쓴다.
헬름 : 아쿠아마린을 관찰하려면 에이드보다 앞에 놓아야 한다.

#### `burst_stage: "A"` 캐릭터 (레드 후드)

레드 후드는 1·2·3단계 목록에 **모두** 등록된다. 그래서 배치가 특히 민감하다.

- **맨 앞에 두면** 첫 사이클에서 1·2·3단계를 혼자 다 쓴다. 이후 사이클도 흔들린다.
- **맨 뒤에 둬도 안전하지 않다.** 같은 사이클에 1단계와 3단계가 둘 다 비면 레드 후드가
  **두 자리를 혼자 겸한다.** 그 사이클에 밀린 B3의 쿨이 그대로 남아 다음 사이클이 길어진다 —
  `S39_레이븐레드후드`(리타·민트·레이븐·프리카·레드 후드)에서 2번째 칸이 `20.4`로 튀는 게
  이 경우다. 실사용 조합이라 순서를 바꾸지 않고 그대로 둔 상태이며, 회귀가 아니다.
- 스쿼드 dict에 `burst_stage`를 주면 단계를 고정할 수 있지만, **라피 : 레드 후드와
  같이 쓸 때는 고정하면 안 된다.** 라피의 `no_burst1_ally` 조건은
  `burst_stages[n] == "1"`로 판정하는데, 레드 후드를 `"1"`로 고정하면 이 조건이 거짓이 되어
  **라피의 1버 전환이 막힌다.** 레드 후드는 `"A"`로 두고 배치 순서로만 조정한다.

라피 : 레드 후드는 팀에 고정 1버가 없으면 스킬1로 자신이 1버가 된다
(`burst_stage_override:1`). 레드 후드가 `"A"`라 1버로 세지 않으므로, 둘만 있는 팀에서는
라피가 1버를 맡는다. 이때 **라피를 레드 후드보다 앞에** 두는 편이 사이클이 안정적이다.

### 더미 캐릭터 "test_B3"

`parsed_nikke.json`에 **스킬 없는 3버스트 AR 캐릭터** `"test_B3"` 등록 (쿨 40초). 동일 패턴 `"test_B1"`, `"test_B2"`도 등록되어 있음.
스킬 없어 버프·딜 영향 없음. 버스트 사이클 완성 용도.

> **주의**: `"test_B3"` 중복 사용 시 `burst_cooldown` 소급 보정이 중복 계산되어 쿨타임 잘못 단축됨. B3 슬롯 2개 필요 시 한 자리에 `"스노우 화이트 : 헤비암즈"` 사용.

### 표준 테스트 스쿼드

**B1 기준으로는 `리틀 머메이드` (쿨 20초) 사용.**
`아니스 : 스타`는 B1이지만, 팀에 다른 B1이 있으면 B1 재진입이 발동해 사이클이 복잡해지므로 B1 슬롯 대용으로 쓰지 않음.

**대상이 `[버스트 재진입 N단계]`(`burst_reentry`) 또는 `burst_stage_override:reenterN`을 가지면
쿨 20초여도 「쿨 40초」쪽 템플릿(보조 아군 포함 5슬롯)을 쓴다.** 재진입 자리는 건너뛰지 않고
같은 단계의 동료를 기다리는데(§버스트 재진입), 그 단계에 대상 혼자면 **방금 쏜 자기 쿨을 통째로
대기**한다 — 아비스타(B1 쿨 20초)를 「B1, 쿨 20초」 템플릿에 그대로 넣으면 사이클이
12.75s → 40s가 되고 180초 풀버스트가 9회 → 4회로 준다. 티아·앨리스 : 원더랜드 바니가 쿨 40초라
이 함정에 안 걸렸을 뿐이다(2026-09-22, 아비스타 등록 중 확인).

```python
# 대상이 B3, 쿨 40초
squad = [make_char(n) for n in ["리틀 머메이드", "크라운", TARGET, "test_B3"]]

# 대상이 B1, 쿨 20초 (버스트 재진입 보유자는 아래 「쿨 40초」쪽을 쓴다)
squad = [make_char(n) for n in [TARGET, "크라운", "test_B3", "스노우 화이트 : 헤비암즈"]]

# 대상이 B1, 쿨 40초 (보조 B1 필요 → 5슬롯 모두 사용)
squad = [make_char(n) for n in [TARGET, "리틀 머메이드", "크라운", "test_B3", "스노우 화이트 : 헤비암즈"]]

# 대상이 B2, 쿨 20초
squad = [make_char(n) for n in ["리틀 머메이드", TARGET, "test_B3", "스노우 화이트 : 헤비암즈"]]

# 대상이 B2, 쿨 40초 (보조 B2 필요 → 5슬롯 모두 사용)
squad = [make_char(n) for n in ["리틀 머메이드", TARGET, "크라운", "test_B3", "스노우 화이트 : 헤비암즈"]]
```

### 버스트를 쓰지 않는 캐릭터 지정

```python
result = simulate(squad, config={"no_burst_char": "캐릭터명"})
```

지정된 캐릭터는 `BurstController` 단계 목록에서 제외됨.

### 캐릭터 버스트 정보 확인

```python
import json
nikke = json.load(open("data/parsed_nikke.json", encoding="utf-8"))
print(nikke["캐릭터명"]["burst_stage"])    # "1" / "2" / "3"
print(nikke["캐릭터명"]["burst_cooldown"]) # 초 단위
```

---

## 풀버스트 사이클

### 기본 구조

```
버스트 게이지 충전 → [B1 발동] → [B2 발동] → [B3 발동] → 풀버스트 상태 (~10초) → 종료 → 충전 재시작
```

- 버스트 게이지는 스쿼드 전체 공격으로 쌓임.
- B1→B2→B3 순서 연속 발동 필요.

#### 사이클 주기의 구성

계산기는 게이지 사이클을 **두 방식**으로 돌릴 수 있다 (`config["burst_gauge_mode"]`).

| 모드 | 1단계가 나가는 조건 | 쓰는 값 |
|---|---|---|
| `"fixed"` (기본) | 풀버스트 종료 후 **고정 시간** | `burst_regen_time = 2.0초` · 첫 버스트만 `first_burst_time` |
| `"accumulate"` | 게이지가 **실제로 100%에 닿을 때** | 무기·스킬이 만든 게이지. 위 두 값을 **둘 다 무시한다** |

게이지 자체는 두 모드 모두에서 똑같이 계산되고 로그에 남는다 — 갈리는 곳은 진입
게이트 한 곳뿐이라 나란히 비교할 수 있다. 식·실측은 `docs/mechanics/버스트 게이지.md`.

표준 스쿼드(7.48 버쿨감 보유)의 실측 타임라인 — **`"fixed"` 기준** 사이클 12.55초의 내역:

```
13.250  full_burst 종료
15.533  stage:1 사용      (+2.283 = 게이지 충전 2.0 + 쿨 여유)
15.633  stage:2 사용      (+0.1  burst_switch_delay)
15.733  stage:3 사용      (+0.1  burst_switch_delay)
15.783  full_burst 시작   (+0.05)
25.783  full_burst 종료   (+10.0 풀버스트 지속)
28.317  다음 full_burst 시작 → 사이클 간격 12.55
```

즉 **10초(풀버스트) + 2초(게이지 충전) + 0.3초(단계 전환·진입) + 쿨 여유**.
버쿨감이 부족하면 "쿨 여유"가 늘어나 주기가 길어지고, 충분하면 게이지 충전이
병목이 되어 더는 짧아지지 않는다. 12.55초가 사실상 하한선인 이유다.

`"accumulate"`에서는 "게이지 충전 2초"가 스쿼드마다 달라진다. 하네스 33스쿼드 실측으로
**1.85~5.51초**(중앙 2.76초)이고, 풀버스트 합계는 **477 → 449회**, 총딜은 `"fixed"`의
**94.9%**다 — 고정 2.0초가 대부분의 조합에 후했다는 뜻이다. 실제로 `"fixed"`가 1단계를
내보내는 그 2.0초 시점의 게이지는 평균 **71.3%**(40~98%)이고, **14/33 스쿼드는 180초
내내 한 번도 만충에 닿지 못한다.** 버충 버프를 가진 조합(마나·마르차나아쿠아 등)은
반대로 2.0초보다 짧아진다.

> **5인 스쿼드에서는 `"accumulate"`가 실측보다 느리다** — `S39_나가라피`가 실전 15버스트인데
> 시뮬은 13버스트다(×1.6 부족). 원인 미상이고 배제 목록은
> `docs/mechanics/버스트 게이지.md` §미해결. 사이클 절대값을 인용할 때 이걸 감안한다.

풀버스트 지속 10초는 `fullburst_duration` stat으로 연장될 수 있다
(예: 소다 : 트윙클링 바니). 연장되면 사이클 주기도 그만큼 길어진다.

캐릭터별 감소 수치·사이클 패턴·예외는 §스쿼드 구성 §버스트 쿨타임 감소.

### 풀버스트 상태의 전투적 의미

니케 버프·대미지 스킬 대부분이 풀버스트 구간 집중.
- `full_burst_start`, `burst_cast` 타이밍 강력 버프가 이 구간에 집중.
- 풀버스트 자체에서도 전 스쿼드원 딜 계수 +50% (DealForm ③).

### 버스트 단계 전환·진입 타이밍

유저가 B1→B2→B3 버튼 연속으로 누르는 행동 = 계산기에서 **단계 간 0.1초** 고정 모델링 (`burst_switch_delay = 0.1`).

```
idle → stage:1 사용 → switching(0.1초) → stage:2 사용 → switching(0.1초) → stage:3 사용 → full_burst
```

### 버스트 재진입 (Reenter)

일부 캐릭터는 버스트 발동 직후 **같은 단계를 다시 한 번 사용**.
- 재진입 딜레이: **0.5초** (`burst_reenter_delay = 0.5`).
- 계산기 상태: `stage:N → reenter:N(0.5초 대기) → 재진입 발동 → switching(0.1초) → 다음 단계`
- **단계마다 사이클당 1회**(`_reenter_done_stages`). 재진입 자리에서 뽑힌 동료가 또 재진입
  캐릭터여도 슬롯을 다시 열지 않고 다음 단계로 넘어간다 — 늘어나는 것은 "그 동료의 버스트
  한 번"이지 무한 연쇄가 아니다. 상한이 없으면 그 단계에 갇혀 풀버스트가 한 번도 돌지 않는다.
  단계별로 세므로 B1 재진입과 B2 재진입이 한 스쿼드에 있으면 `1 → 1 → 2 → 2 → 3`이 된다.
- 재진입 자리에서 후보 전원이 쿨이면 **기다렸다 쓴다**(건너뛰지 않는다). 재진입을 연 사람은
  방금 썼으므로 이미 쿨이고, 실제로 쓰는 것은 같은 단계의 동료다.

현재 재진입 캐릭터:
- **아니스 : 스타** (B1, 쿨 20초) — 상태 buff `burst_stage_override:reenter1`
  - 스쿼드에 다른 B1 아군이 있으면 자신의 B1 발동 후 B1을 한 번 더 재진입.
  - 버스트 사이클에 0.5초 딜레이 추가 + burst_cast 1회 더 발생.
  - 테스트 스쿼드에서 B1 대용으로 쓰지 않는 이유.
- **티아** · **앨리스 : 원더랜드 바니** (B1, 쿨 40초) — 버스트 스킬의 `[버스트 재진입 1단계]`(instant `burst_reentry`)
  - 조건 없이 자기 버스트마다 재진입한다. 재진입 자리는 같은 단계의 동료가 받으므로 **보조 B1의 쿨을 한 번 더 쓴다** — 표준 스쿼드(`리틀 머메이드` 보조)에서 사이클 간격이 `12.533 ↔ 12.75`로 교대하고, 대상은 2사이클에 1번 버스트한다.

---

## 트리거 발동 의미

PARSING.md §4 표는 텍스트→JSON 키 변환만 담당. 각 트리거가 *실제로 누구에게·언제* 발동하는지는 이 절에 누적 기록한다. `char-add` 시나리오 작성(단계 1·3) 중 일반 규칙(특정 캐릭터에 한하지 않는 동작)이 새로 확인되면 여기 한 줄 추가.

### timing

- **`burst_enter:N`**: 스쿼드의 누군가가 버스트 단계 N으로 진입 시 본인에게 트리거. 본인이 직접 N단계 발동했을 때만 트리거되는 게 아님. → B3 더미 발동 사이클에서도 B3 캐릭터의 `burst_enter:3` 효과가 본인에게 부여됨.
- **`full_burst_start`**: 스쿼드 풀버스트 진입 시 모든 캐릭터에게 트리거. 본인 burst_cast 여부와 무관. → 본인 미사용 사이클에서도 `full_burst_start` 조건 효과가 본인에게 부여됨 (`burst_enter:N`과 동일 패턴).
- **`battle_start`**: t=0 1회. passive 류와 결합 시 영구 유지.
- **`랩쳐 등장 시` / `랩쳐 사망 시`**: 적이 보스 하나면 "등장"은 `battle_start` 1회로 처리, "사망"은 고정 시간 sim이라 보스가 죽지 않아 무발동. 보스 패턴 `summon`이 쫄몹을 띄우면 **쫄몹마다** 등장이, **처치·자폭**하면 사망이 나간다 — 시간이 다해 퇴장한 쫄몹은 사망이 아니다(유저 확인 2026-09-16).
- **`event:full_reload`**: 최대 장탄 재장전 완료 시 1회. 자연 ammo 0 도달 또는 수동/효과 트리거 리로드 후 풀로드 시점. 풀버스트 중에도 ammo 상황에 따라 발생 가능.
  - **`[강제 재장전]`(`force_reload`)의 완료도 정상적으로 `event:full_reload`를 발생시킨다** (유저 확인, 2026-08-16). `[탄환 100% 제거]` + `[강제 재장전]` 조합은 최대 장탄까지 채우므로 "최대 장탄 재장전 완료"에 해당한다. → 버스트로 탄창을 강제로 한 바퀴 돌려 재장전 트리거 패시브를 **풀버스트 안에서 재점화**하는 설계가 성립한다. (질 `슈퍼 캅` — 매그넘탄·산성탄 재부여)
- **`burst_cast`**: 본인이 직접 자신의 버스트 스킬을 발동했을 때만 트리거. 다른 캐릭터(더미 포함)가 같은 단계 버스트를 발동한 사이클에서는 본인에게 트리거 안 됨. ↔ `burst_enter:N`/`full_burst_start`와 대비.
- **피격 계열 트리거(`received_hit_count:N`·`event:cover_hit`·`event:shield_consumed`·`event:ally_down`·`event:self_down`)는 보스 공격 패턴(`enemy.patterns`의 `attack`)이 있을 때만 발동한다.** 패턴 없는 기본 경로(하네스 전부)에서는 영구 무발동이라 시뮬 딜에 기여하지 않는다 → 시나리오 체크리스트에서는 「기본 경로 0회」 네거티브 항목으로 두고, 발동 거동은 패턴을 준 실행으로 확인한다. (슈가 스킬1 `event:cover_hit`, 홍련 `검신합일`)
- **`event:heal_received`(회복 효과 적용 시)는 오버힐에서도 발생한다** (유저 확인, 2026-08-22). 체력이 가득 찬 대상에게 들어간 회복도 "회복 효과 적용"으로 친다 — `timeline._apply_lifesteal()`이 HP를 유효 최대 체력에서 잘라내고도 무조건 notify하는 현행 동작이 맞다. 보스 공격 패턴이 없는 기본 경로에서는 HP가 사실상 늘 최대치이므로, 이 규칙이 아니면 힐 트리거 패시브가 통째로 죽는다. → **라이프스틸만으로 자기 힐 트리거를 자급하는 설계가 성립한다** (아스카 `호승심` — 버스트가 준 `lifesteal_pct`의 회복이 자신의 공격력 96.98% 버프를 켠다).
- **`event:cover_healed`(엄폐물 체력 회복 시)는 가득 찬 엄폐물에 들어간 회복에도 발생한다** (유저 확인, 2026-09-14). 위 `event:heal_received` 오버힐 규칙의 엄폐물판이다. 엄폐물은 보스 공격 패턴이 있을 때만 깎이므로, 이 규칙이 아니면 기본 경로에서 엄폐물 회복 트리거 패시브가 통째로 죽는다. 발생 주체는 **회복을 받은 엄폐물의 주인**이고, 부서진 엄폐물은 회복되지 않으므로 발생하지 않는다. → 자기 엄폐물 회복(버스트)으로 트리거를 자급하고, 아군 전체 엄폐물 회복(나가 `우정의 가드`)으로 반복 발동하는 설계가 성립한다 (티아 `파충류 애호가`).
- **`event:[버프명]`**: named buff가 활성화될 때 **스쿼드 전체에 브로드캐스트**. 해당 buff를 활성화한 캐릭터 외 다른 캐릭터도 반응 가능. 각 캐릭터의 `self_state:` / `target: "self"` 등 조건·타겟은 반응하는 캐릭터 본인 기준으로 평가됨. → 프리카 앵콜이 민트의 `event:떼창`에 반응하는 구조.

#### 공격과 명중 — 원문이 가르는 두 축

원문은 「공격 시」와 「명중 시」를 구분해 쓴다. 같은 스킬 안에 둘이 나란히 오는 경우가
실제로 있으므로(레이 `선두 제압`) 파싱도 계산기도 별개 이벤트로 다룬다.
텍스트→키 매핑은 `PARSING.md` §4-1, 구현 상태는 `IMPL-STATUS.md` timing 마스터가 정본이다.

- **발사(`on_attack`·`on_attack_count:N`·`full_charge_fire`·`last_bullet_fire`)**:
  탄 1발을 쏘면 1회. 총구·펠릿 수와 무관하다. **빗나간 탄도 발동시킨다**
  (유저 확인, 2026-09-04).
- **명중(`hit_count`·`full_charge_hit`·`last_bullet`)**: **탄 단위**라 총구 수만큼 발동한다.
  **탄 1발 소모 = 공격 1회 = 명중 총구 수회**다(유저 확인, 2026-09-04). 펠릿은 한 탄을
  나눈 것이라 곱하지 않는다 — 펠릿 단위 트리거는 `pellet_hit_count:N`이 따로 있다.
  빗나간 탄은 발동시키지 않는다.
- **발동 순서는 발사 → 명중이다.** 쏘고 나서 맞기 때문이고, 그래서 같은 발에 걸린
  「N회 공격 시」 버프가 「N회 명중 시」 딜에 실린다. 다만 둘 다 그 발의 `calc_damage`
  **뒤**에 있어 **자기 발의 대미지는 어느 쪽도 못 바꾼다**(`_BULLET_BOUND_TIMINGS` 규약과
  같은 이야기 — 아래 §무기 메카닉).
- **「공격 명중 시」는 명중으로 읽는다**(유저 판단, 2026-09-04. 아스카 : WILLE
  `안티 AT 필드` — 「일반 공격 50회 공격 명중 시」). 반대로 **「N발 당」은 탄 소모 기준이라
  발사**다(같은 스킬 `섬멸 태세 추가 효과` — 「10발 당」).
- **계산기에는 아직 빗나감 모델이 없다.** 탄착군은 코어히트 확률로만 환산되고 모든 탄이
  명중한다(`docs/mechanics/명중률 탄착군.md`). 그래서 두 계열의 발동 횟수는 총구 2개인
  캐릭터를 빼면 지금 같은 값이다 — 분리는 미스 모델이 들어올 자리를 미리 갈라 둔 것이다.

### target

- **`all_allies`는 시전자 본인 포함**: `target: "all_allies"` 효과는 자신에게도 적용된다. (프리카 스킬3 퍼포먼스 확인)
- **`아군 공용 보호막`은 시전자 본인에게만 부여되며 일반 보호막과 별개 계열이다** (유저 확인). 텍스트에 대상 표기가 없어도 `all_allies`가 아니다. 단 보호막 판정 자체는 성립해 `during_shield`·`event:shield_applied`를 발생시킨다. → 블랑 `럭키 가드`. (보유: 폴리 `폴리스 라인`, 센티 `현장검토`, 라푼젤 : 퓨어 그레이스 `생츄어리` — 모두 미파싱)

### condition

- **`self_state:상태명`**: buff_manager에서 "활성 버프 중 이름이 [상태명]인 항목 존재" 판정. 별도 boolean 플래그 아님.
- **적(랩쳐) 수 조건 (`enemy_count_below:N` / `enemy_count_above:N`)**: 적 수 = 보스 1 + 산 쫄몹(`state["enemy_count"]`, 프레임 맨 앞에 정한다). 쫄몹이 없으면 1 → `랩쳐 N기 이하`는 **항상 참**(1≤N), `랩쳐 N기 이상`(N≥2)은 **항상 거짓 → 영구 무발동**이라 기본 경로에서 적 수 게이팅 효과는 딜에 기여하지 않는다. 보스 패턴 `summon`으로 쫄몹을 띄우면 그 동안 켜진다(맥스웰 `일렉트릭 샷`). (키 이름은 `char-add` 단계 2에서 확정)
- **`hit_count:N`(일반 공격 N회 명중)의 카운트 주체**: 본인 일반공격만 카운트(caster별). ↔ `part_hit_count`/`body_hit_count`는 스쿼드 전체 명중 브로드캐스트. 코어히트는 일반공격 판정이라 포함, 버스트/스킬 딜은 제외.
- **`대상이 타겟이라면` / `타겟에게` / `타겟 등장 시`**: 쫄몹이 없으면 보스가 언제나 타겟이라 항상 참이다. 쫄몹이 있는 보스 패턴에서는 갈린다(유저 결정 2026-09-19) — 한 발을 맞힌 뒤의 「대상이 타겟이라면」은 condition `target_is_boss`(그 한 발의 대상 = 시전자가 겨눈 적이 보스인가, `calculator/boss_pattern.py` §조준), 「대상이 타겟이 아닌 랩쳐라면」은 `target_is_add`, 「타겟에게」와 적 전체 공격 뒤의 「대상이 타겟이라면 동일 적 대상에게」는 대상 `boss`(겨눈 적과 무관하게 보스)다. 2026-09-19 전에는 조건을 비웠다(리버렐리오는 반대 분기 `완만류`를 뺐다). `타겟 등장 시`는 `랩쳐 등장 시`와 같이 `battle_start` 1회. (로산나 · 리버렐리오)
- **`우선적으로 [클래스] 적 N기에게`**: 적이 보스 하나면 **1기에게 1회 적중**. N기 표기라도 딜이 N배가 되지 않는다. 쫄몹이 있으면 효과의 대상 키가 정한다(`calculator/boss_pattern.py` §쫄몹) — 벤데타는 `target`으로 파싱돼 있어 로산나가 겨눈 적 1기가 맞고, 쫄몹 클래스가 없어 클래스 필터는 어디서도 안 건다. (로산나 벤데타 `화력형 적 2기`)
- **`[효과명] 적용 대상 N ▲` / `공격 범위 N% ▲`**: 다른 효과의 타격 대상 수·사거리를 늘리는 버프. 적이 보스 하나면 대상이 이미 1기로 수렴해 있어 **항상 no-op**이고, 쫄몹 모드에서도 대상 수·범위를 늘리는 쪽은 구현하지 않았다 → 파싱은 정상 등록하되 계산기에서는 딜 기여 0, 시나리오 체크리스트에 네거티브 항목("히트 수가 변하지 않아야 함")으로 둔다. (레이 (가칭) 스킬1 — 아스카 : WILLE `섬멸 태세 추가 효과` 대상 +1 / 범위 +500%)
- **`파츠 대미지 ▲`(`part_dmg_pct`)는 인게임에서 파츠에 꽂히는 *일반 공격*에도 실린다** (유저 확인, 2026-08-16).
  계산기가 `hits_parts: true`인 스킬 딜에만 태우는 것은 게임 규칙이 좁아서가 아니라 **계산기가 에임을 다루지 않기 때문**이다 —
  파츠 위치가 상황마다 달라 일반 공격 중 몇 발이 파츠에 맞는지 정할 근거가 없다. 정밀화 대신 **의도적으로 포기한 영역**이다.
  → `part_dmg_pct` 버프는 보스 sim에서 **사실상 무효**로 취급한다(짝이 되는 `hits_parts` 효과는 레이븐 `템페스트` · 신데렐라 :
  크리스탈 웨이브 `모드 스왑 2` · 베스티 : 택티컬 업 `미사일 컨테이너 온라인 3` 셋). 예외는 패턴 모드다 — 좌표 off는 관통·발사체
  폭발이 위치 단계(`reach`)를 적은 파츠에 닿으면 그 **파츠 히트**에 실리고(`calculator/boss_pattern.py` §파츠 다중 타격), 조준
  순서가 파츠(벌칙 파츠)를 겨누면 그 발이 파츠에 통째로 떨어져 실린다(같은 파일 §조준). **좌표 모드**(보스 스크립트가 파츠 좌표를 준다)에서는 파츠에
  떨어진 탄 — 일반 공격 포함 — 이 전부 파츠 히트라 실린다(`calculator/boss_pattern.py` §좌표 모드).
  좌표 모드가 아닌 전투에서 이 버프를 주력으로 삼는 캐릭터는 계산기 딜이 실제보다 **낮게** 나오며, 이는 버그가 아니라 알려진 한계다.
  보고서에 그대로 반영하되 "무효라서 버렸다"가 아니라 "모델 밖"이라고 적는다. (로산나 : 시크 오션 스킬1·2)
- **`[해제 불가]` 태그는 조건 유지와 무관하다**: 적의 버프 해제 스킬에 대한 면역을 뜻할 뿐, `~% 이상일 때 한하여` 같은 게이지·스택 조건이 붙은 `[지속] [해제 불가]` 버프는 **조건이 깨지면 정상적으로 해제된다** → 조건부 상시 재평가로 모델링한다. (치사토 `초감각` 임계 3단 — 게이지 70/55/25% 경계)
- **`self_hp_above:N`은 보스 sim에서도 상수가 아니다** (유저 확인, 2026-08-10). 보스 공격 패턴이 없으면 기본값은 100%지만, `max_hp_only_pct`·`hp_only_caster_based_pct`(**현재 체력 유지** 최대 체력 증가)가 걸리면 `hp_pct`가 떨어져 조건이 **꺼질 수 있다**. "어차피 항상 참"으로 상수화하지 말고 런타임 재평가 조건(`_RUNTIME_COND_PREFIXES`)으로 유지한다. (누아르 `럭키챰` 체력 70% 이상)

- **`일반 공격 N회 공격 시`는 N회마다 반복 발동한다** (유저 확인, 2026-08-17). 누적 N회 도달 시 1회성이 아니라 카운터가 N마다 돌며 계속 재발동한다 → 같은 스킬 안에 5회·15회 블록이 나란히 있으면 각자 자기 주기로 반복한다. (레오나 `우렁찬 포효` — 5회 스택 부여 + 15회 SG 사거리). 키는 `hit_count:N`이 아니라 **`on_attack_count:N`**이다 — 위 §공격과 명중.
- **`hit_count:N` 카운터와 condition의 관계**: 카운터는 condition 충족 여부와 **무관하게 진행**된다. condition은 효과 발동만 게이팅하고 카운트는 계속 누적 → 조건 불충족 구간의 공격도 위상(phase)을 밀어낸다. (라플라스 : 얼티밋 히어로 `일반 공격 12회` + `self_state:일렉트릭 파워 풀 풀 차지`)

### 무기 메카닉

- **RL/SR 차지형 무기**: 기본 일반 공격이 풀차지 공격. 특별한 조건 명시 없으면 "일반 공격" 분류 트리거는 매 풀차지 hit에 발동.
- **차지 시간 stat 변경 (`charge_time_fixed`·`charge_speed_pct` 등)**: 차지 중에 stat이 변경되면 진행분(이미 모은 차지 시간)은 유지되고 새 차지 시간 기준으로 남은 시간만 추가 차징. 예: 0.7s 차징 상태에서 차지 시간이 1.2s→3.2s로 변경 → 남은 2.5s 더 차징.
- **차지 속도 "효과 면역"과 "차지 시간 고정"은 다른 것이다** (유저 확인, 2026-09-02). 둘 다 차지 속도 버프를 무시하지만 사정거리가 다르다.
  - `차지 속도 증가/감소 효과 면역`(`charge_speed_buff_immune`·`charge_speed_debuff_immune`)은 **스킬 버프에만** 면역이다 — 본인 스킬이 건 것도 포함해서 막지만, **오버로드(장비 옵션)와 큐브의 차지 속도는 그대로 걸린다.** 그래서 면역 캐릭터에게도 차지 속도 오버로드를 끼우는 것이 이득이다. (리버렐리오 — `char_defaults.json`이 차지 속도 오버로드를 기본으로 준다)
  - `차지 시간 N초로 고정`(`charge_time_fixed`)은 **소스를 가리지 않는다** — 오버로드·큐브까지 전부 무시하고 그 값이 된다. (리버렐리오 `완만류`, 맥스웰 : 오디너리 미케닉 과전류 단계)
- **`탄환 소모 N발` 표기**: 실제 장탄 소모량이 아니다. 장탄은 1발씩 정상 소모되고, "아군이 소비한 탄의 총합"류 집계에만 N발로 기여한다. (신데렐라 : 크리스탈 웨이브 저격 모드 — `풀 차지 공격 시 탄환 소모 40발`, 최대 장탄 15발과 모순되지 않는 이유)
- **무기 변경(`weapon_change`) 중 탄환 충전 미적용**: `ammo_charge_flat` 등 탄환 충전 계열 효과는 무기 변경 상태에서 적용되지 않는다.
- **`최대 장탄 수 : N발 (사용 무기 변경 시 최대 장탄 수 효과 갱신)` 괄호구**: 변경된 무기의 최대 장탄이 **최대 장탄 수 버프의 영향을 받는다**는 뜻. 이 문구가 없는 무기 변경은 표기된 장탄으로 고정. → 탄수 기반 종료(`duration_bullets`) 무기 변경에서는 장탄 버프가 **모드 지속 시간까지** 바꾸므로 단순 DPS 버프가 아니다. 현재 이 문구 보유: 신데렐라 : 크리스탈 웨이브, 라플라스 : 얼티밋 히어로.
  - **소스를 가리지 않는다**: 스킬 버프뿐 아니라 **장비 오버로드·큐브·소장품**의 최대 장탄도 전부 얹힌다. 기본값은 무기가 아니라 **모드 표기 장탄**이고, 계산은 위 §최대 장탄 반올림 규칙과 같다 (라플라스 : 얼티밋 히어로 — 표기 120발이 오버로드 129.64%만으로 실효 **276발**, 모드 길이 5.97s → 13.77s).
  - **갱신은 장탄을 채우는 사건에만 일어난다** — 모드 진입과 재장전 완료 둘뿐이고, 그 사이에는 진입/재장전 시점의 값을 그대로 쓴다. 두 캐릭터의 괄호구가 `사용 무기 변경 시`(라플라스 : 얼티밋 히어로)와 `재장전 완료 시`(신데렐라 : 크리스탈 웨이브 — 표기 15발이 실효 **35발**)로 갈리는 건 규칙이 달라서가 아니라 **각자에게 있는 사건만 적었기** 때문이다: 라플라스 모드는 안에 재장전이 없고(모든 탄환 발사 = 종료), 신데렐라 모드는 안에서 재장전한다.
  - 매 tick 다시 재면 안 된다 — 모드 도중 장탄 버프가 붙고 끊길 때마다 종료 조건(`모든 탄환 발사 시`)만 흔들려, 장탄과 종료 조건이 어긋난 채 **탄이 마르고도 끝나지 않는 모드**가 생긴다. 구현: `timeline.CharState._full_ammo()`의 `_wc_ammo_full` 캐시를 모드 진입과 `_finish_reload()`에서만 비운다.
- **무기 변경 모드의 차지형 무기도 풀차지 트리거(`full_charge_fire`·`full_charge_hit`)를 발동한다**: 변경된 무기에 `차지 시간`·`풀 차지 대미지`가 명시돼 있으면 그 무기의 풀차지 사격도 `풀 차지 공격 시` 트리거를 정상 발동시킨다. → 모드 밖 스킬(스택 충전·게이지 충전 등)이 모드 안에서도 계속 쌓인다. (맥스웰 : 오디너리 미케닉 — 메티스 버스트 버스터 1발이 스킬1 최대 체력 스택·스킬2 버스트 게이지를 사이클당 1회씩 추가 발동)
- **차지 표기가 없는 무기 변경 모드는 그동안 풀 차지가 아예 없다** (인게임 확인, 2026-07-29): 위 항목의 반대쪽이다. 차지형 캐릭터가 버스트로 MG 등 비차지 무기로 바뀌면 모드 지속 내내 풀차지 트리거가 발동하지 않고, **풀 차지에 걸려 있던 팀 버프도 그 구간 동안 끊긴다.** 끊기는 게 정상이며 버그가 아니다. (벨벳 — 버스트 중 MG 모드 10초 동안 `사랑의 탄환`(아군 차지 대미지)이 사라지고, 그만큼 차지형 동료의 딜이 내려간다)
- **무기 변경 모드가 끝나면 원래 무기의 장탄은 전부 복구된다** (유저 확인, 2026-08-08 · 2026-09-09 재확인): 모드 진입 전 잔탄이나 모드에서 소모한 탄수와 무관하게 **풀로드로 복귀**하며, 복귀 직후 재장전이 삽입되지 않는다. (맥스웰 `피어스 샷` — 장탄 1발 모드 종료 후 기본 SR 6/6)
  - **종료 사유를 가리지 않는다** — 발수 소진이든 지속시간 만료든 토글 해제든 같다. 채우는 값은 표기 장탄이 아니라 그 캐릭터의 **실효 최대 장탄**(장비 옵션·큐브·소장품·스킬 버프 반영)이다.
  - **예외는 모드 종료에 `[탄환 N% 제거]`가 붙은 경우뿐이다** (유저 확인, 2026-09-09). 만탄 복구가 먼저 일어나고 그 뒤 제거가 덮으므로 결과적으로 재장전을 하게 된다 — 라플라스 : 얼티밋 히어로 `일렉트릭 파워 풀 풀 차지`, 드레이크 : 그레이트 빌런 `오버 오버 드라이브`. **순서가 뒤집히면 두 캐릭터의 재장전이 통째로 사라진다.**
  - 구현: 종료 경로가 둘이라 **복구를 정확히 한 번만** 해야 한다 — 발수 소진은 `_tick_weapon_change()`, 지속시간 만료·토글 해제는 `tick()`이 처리하고 `_wc_ammo_restored` 플래그가 중복을 막는다. 발수 소진 쪽은 `bm.end_weapon_change()`(= `event:state_end` 발생) **앞에서** 채워야 위 예외가 성립한다. 모드 안에서 시작된 재장전(`_reload_in_weapon_change`)은 복구와 함께 취소한다.
  - 2026-09-09까지 이 규칙이 **구현돼 있지 않았다.** 종료 시 `orig_ammo`(진입부에서 잡은 잔탄)로 되돌렸는데, 발사가 일어나는 tick에서는 그 값이 **모드의 잔탄**이라 1발 모드가 잔탄 1발로 복귀하고 곧바로 재장전이 삽입됐다. 고친 뒤 츠바이 +10.7% · 스노우 화이트 +7.6% · 맥스웰 +6.1% · E.H. +2.8%이고, 나유타는 모드 종료 시 끼던 재장전이 사라져 SMG 발수가 늘었다. 라플라스 : 얼티밋 히어로는 위 예외라 ±0.04%로 제자리다.
- **최대 장탄·차지 시간의 % 버프는 소스마다 따로 반올림해서 더한다** (유저 확인, 2026-08-19).
  합산 후 한 번 반올림이 **아니다.** 기본값(무기 기본 장탄 / CDN 기본 차지 시간)에 **소스별 %**를
  곱해 눈금에 맞춰 반올림하고, 그 결과들을 더한다. 눈금은 장탄이 **1발**, 차지 시간이 **0.01초**다.

      최대 장탄 = 기본장탄 + Σ 반올림(기본장탄 × 그룹%) + flat        (하한 1발)
      차지 시간 = 기본차지 − Σ 반올림(기본차지 × 그룹%, 0.01초) + flat  (하한 0초)

  - **그룹**: 장비 오버로드는 **종류·레벨이 모두 같으면 부위가 달라도 한 그룹**이라 먼저 합산하고
    한 번만 반올림한다. 단계가 다르면 각각 따로. 큐브·소장품·스킬 버프는 각각 독립 그룹이다.
    같은 종류의 스킬 버프가 여러 번 걸리면 같은 버프이므로 합산 후 1회로 본다(사례 미관측).
  - **flat은 마지막**이다 — `+N발`·`차지 시간 N초 ▼`는 % 반올림이 끝난 뒤에 더한다.
    % 버프가 결국 발수·초 단위의 정수 눈금으로 들어오기 때문이다.
  - **0.5는 올린다** (인게임 확인, 2026-08-19). 음수 쪽도 같은 방향(+∞)이라 −2.5발은 −2발,
    차지 −0.025초는 −0.02초가 된다.
    > **근거 — 유저 계정 크라운(MG, 기본 300발) 실측 536발.** 장탄 옵션 68.93% 1그룹 + 소장품
    > SR15(최대 장탄 9.5%)뿐인데, `300 × 0.095 = 28.5`가 정확히 0.5다.
    > `300 + 반올림(206.79) + 반올림(28.5) = 300 + 207 + 29 = 536` — **올림이어야 맞는다**
    > (내림이면 535). 덤으로 이 한 수치가 **소스별 반올림 자체의 증거**이기도 하다 —
    > 합산 후 한 번 반올림하면 `round(300 × 1.7843) = 535`로 실제와 어긋난다.
    > 같은 계정 라피 : 레드 후드(장탄 옵션 85.37 / 60.71 / 56.6 세 단계 + 소장품 9.5%) 실측
    > **937발**도 `300 + 256 + 182 + 170 + 29`로 정확히 맞는다.
  - **기본 스펙(같은 레벨 2줄)은 한 그룹이라 종전 계산과 값이 같다.** 차이는 단계가 섞인 장비,
    큐브·소장품, 그리고 스킬 버프가 낄 때 난다 — SG 5발에 오버로드 2줄(129.64%)과 프리바티
    `EX 매거진`(−50.66%)이 겹치면 합산식으로는 9발이지만 실제로는 `5 + 6 − 3 = 8발`이다.
  - 차지형은 기본 스펙만으로도 전부 움직인다(1.0초 무기 + 차지속도 2줄 → 0.9074초가 아니라 **0.91초**).
    오차는 수 ms지만 사이클당 발수가 정수라 버스트 창 경계에서 한 발이 들락날락한다.
  - 계산기 구현: 그룹 나누기는 `buff_manager._quant_group_key`, 반올림은 `timeline._quant_sum`.
    `equip_skills`는 스칼라(한 그룹) 또는 **줄별 퍼센트 리스트**(단계가 섞인 장비)를 받는다.
- **차지 시간은 실제로 0초까지 내려간다** (유저 확인, 2026-08-19). 단축량 합이 기본 차지 시간을
  넘으면 그대로 0초이며, 프레임 하한 같은 건 없다.
- **최대 장탄 감소 버프가 겹쳐도 최대 장탄은 1발 아래로 내려가지 않는다** (유저 확인, 2026-08-14).
  합산이 −100%를 넘어도 1발이 하한이라 사격이 멈추지 않는다. → 장탄이 1발로 눌린 캐릭터는
  `마지막 탄환` 트리거가 매 발 발동하므로 **감소 버프가 딜 손실이 아니라 이득이 되는 조합**이 성립한다.
  (프리바티 `EX 매거진` 아군 전체 −50.66%와 아니스 : 스파클링 서머 `스파클링 웨이브` 자신 −73.92%가
  겹치는 구간 — 합 −124.58%이지만 SG 5발은 0발이 아니라 1발이 되고 `스파클링 미사일`이 매 발 터진다)
- **최대 장탄 증가 버프가 만료되면 초과 잔탄은 잘린다** (유저 확인, 2026-08-10). `[N초 유지]`가 붙은
  `max_ammo_flat`·`max_ammo_pct`가 끝나는 순간 현재 잔탄이 원래 최대치를 넘고 있으면 원래 최대치로
  내려간다 — 남은 탄을 다 쏠 때까지 유지되지 않는다. **구현돼 있다** — `CharState.tick()`이 재장전
  중이 아닐 때 매 프레임 `ammo`를 `_full_ammo()`로 clamp하고 잘린 값을 `ammo_log`에 남긴다.
  (2026-09-21 확인: 이 조항은 「계산기는 아직 이 캡을 걸지 않는다 → 누아르 등록에서 구현한다」로
  적혀 있었으나 **코드는 2026-08-23 공개 시점에 이미 이 분기를 갖고 있었다.** 유니 등록 중
  대조하다 드러났고, 계측으로 실제 발동도 확인했다 — 민트를 낀 스쿼드에서 4프레임.)
  잘리는지는 **버프가 실제로 만료되어야** 갈린다. 재부여 간격이 지속보다 짧으면 만료 자체가 없어
  캡이 무의미하다 (유니 `SDS 3` — 풀차지 간격 최대 3.30s < 5초 지속이라 180초 내내 상시).
  (누아르 `래빗 트윈즈 B` 최대 장탄 +5발 10초, 민트 `다 함께 불러주세요! 2` +40% 10초)
- **무기 변경이 끝나 원래 무기로 돌아오면 차지는 처음부터 다시 한다** (유저 확인, 2026-09-02). 모드에 들어가기 전에 잡아 두었던 차지는 물려받지 않는다 — 재장전·엄폐가 차지를 무효로 하는 것과 같다. **초기화하지 않으면 모드를 들고 있는 내내 차지바가 가득 찬 채 얼어 있다가 복귀 프레임에 공짜 풀차지 한 발이 터진다.** 그 한 발이 풀버스트 끝자락에 걸리면 「모드 중에는 불가능해야 할 `full_charge_hit` + `during_full_burst` 효과」가 사이클당 한 번씩 새어 나온다 (벨벳 `사랑의 탄환 1~3`). 진입 쪽은 이미 같은 처리가 있었고(`_wc_new_session`), 발수 소진 종료 경로에도 있었다 — **지속시간 만료·토글 해제 경로만 비어 있었다.** 영향: 벨벳(SR→MG) · 타키나(SR→SG) · 라플라스(RL→SMG) · 레드 후드(SR→SR).
- **무기 변경으로 든 무기의 연사 예열은 무기군 기본값을 그대로 따르지 않는다** (유저 확인, 2026-09-02). 벨벳 `깔끔한 마무리`가 든 MG는 **예열이 없고 연사도 무기군 상한과 다르다** — 유저 실측 10초간 약 560발(=56/s)이고, MG 무기군 기본 곡선(1/s → 70/s를 41.4발에 걸쳐)은 걸리지 않는다. 변경 무기는 CDN에 자기 레코드가 없어 무기군 기본값으로 떨어진다 — **연사만은 예외**로, 버스트가 `ChangeWeapon`이면 스킬 값 칸의 값이 **고정 연사**로 들어온다(2026-09-24, `.agent/skills/char-scrape/SCRAPER.md` §어댑터 매핑 · 모더니아 `섬멸 모드`도 이 값으로 예열 없이 돈다). 벨벳은 그 값(4200rpm = 70/s)도 실측과 달라서, 실측이 다르면 `data/weapon_delays.json`의 `_weapon_change.[캐릭터].[효과명]`에 고정한다(라플라스 : 얼티밋 히어로·타키나가 같은 자리에 연사·펠릿을 고정해 두고 있다). **예열을 없애려면 `fire_rate`(하한)와 `fire_rate_max`(상한)를 실측값으로 같이 눌러야 한다** — 하한만 바꾸면 그 값에서 무기군 상한까지 예열이 다시 생긴다. 인게임에서 벨벳의 MG는 **예열 UI가 있는데 게이지가 100%에서 내려가지 않는다**(엄폐하고 대기해도 그대로 — 유저 확인). 즉 예열이 없는 게 아니라 **항상 최대로 채워져 있고 냉각도 안 되는** 상태다. 연사를 상수로 고정하는 위 방식은 **냉각까지 자동으로 맞는다** — `warmup_shots`가 어떤 값이든 연사가 안 바뀌므로 `_cool_warmup`이 결과를 바꿀 수 없다. 그래서 예열 게이지를 따로 모델링하지 않는다.
- **무기 변경 모드에 들어갈 때 재장전 모션이 한 번 들어간다** (유저 실측 2026-09-03, 드레이크 : 그레이트 빌런 `오버 오버 드라이브`). 풀버스트 시작과 동시에 모드로 바뀌는데 첫 발은 **풀버스트 잔여 8.0초**에 나가고, 그 뒤 6발은 재장전 없이 이어진다. 8.0초를 역산하면 진입에 든 시간이 약 2.0초 = 재장전 + 차지 1.5초라 **재장전이 약 0.5초**인데, 이건 고정 상수가 아니라 **그 캐릭터의 재장전 시간에 재장전 속도 버프가 먹은 값**이다(기본 SG 1.50초 → 표준 스펙에서 0.4초). → 모드 진입은 장탄을 즉시 채우는 게 아니라 재장전을 한 번 태운다. **인게임에서는 모든 무기 변경이 이 모션을 가질 가능성이 높지만, 다른 모드는 실측이 없어 켜지 않았다** — `parsed_skills.json`의 `entry_reload`로 모드마다 선언한다. 켜면 첫 발이 밀려 딜이 움직이므로 실측이 붙는 대로 하나씩 켜고, 전원 확인되면 기본값으로 올린다.
- **무기 변경 모드의 사격도 펠릿 경로를 그대로 탄다** (유저 확인, 2026-09-03). 모드 무기가 펠릿을 여러 개 뿜으면 계수를 펠릿 수로 나눠 **펠릿마다 따로 코어를 판정**하고, `pellet_hit`·`squad_body_hit`·`crit_hit`·`core_hit`도 펠릿 수만큼 발생한다. **탄착군도 기본 무기의 것을 그대로 쓴다** — 무기 변경은 `spread_*`를 갈아끼우지 않는데, 그게 실제 동작과 맞다. 대미지 총량은 분할해도 같으므로 갈리는 것은 **판정 횟수와 팀 브로드캐스트**뿐이다. 차지 무기가 전부 펠릿 1이던 동안에는 통짜 1히트와 같은 말이었고, **펠릿 15인 차지 SG(드레이크 : 그레이트 빌런)가 나오면서 갈라졌다** — 고치기 전에는 모드 사격 1발이 팀에게 15히트가 아니라 1히트로 보였다.
- **무기 변경 모드의 사격은 `weapon_hit:[name]` 이벤트를 쏘지 않는다** (코드 확인, 2026-08-13). 그 이벤트는 `_handle_damage_eff()`가 **named damage 효과**(발사체 등)를 명중시킬 때만 발생하고, `_tick_weapon_change()`의 발사 루프에는 호출처가 없다. → 모드의 **매 발마다** 따라붙는 효과는 `weapon_hit:`이 아니라 **`hit_count:1` + `self_state:[모드명]` 게이팅**으로 센다. 모드 사격도 일반 공격이라 `hit_count`는 정상 누적된다. (라플라스 `라플라스 버스터 4` — 매 발 방어력 무시 딜)
- **모드 사격이 스킬 대미지인 예외가 있다** (유저 확인, 2026-08-25). 위 규칙(모드 사격도 일반 공격)이
  기본이지만, **나유타 `기억 연소`는 변신 중 사격 자체가 스킬 대미지**다. 그래서 그 사격은
  **발수로 소모되는 버프(`duration_bullets`)를 먹지 않는다** — 미란다 `웨이크업! 4`(크리확률 1발
  유지)를 받은 채 변신하면 변신 10초 내내 버프가 살아 있다. 예외는 `weapon_change` 효과의
  `skill_damage: true`로 표시하고, 표시가 없으면 종전대로 일반 공격이다. 구현은
  `timeline._wc_is_skill_damage()`.

  스킬 대미지이므로 **일반 공격에만 붙는 것은 빠지고, 실제 명중에서 오는 것은 남는다**
  (유저 실측 대조, 2026-08-25):

  | 항 | 모드 스킬 사격 | 근거 |
  |---|---|---|
  | ① 「일반 공격 대미지 ▲」 | **빠짐** | 원문이 「최종 공격력 N% 대미지」 — 스킬 대미지 표기다 |
  | ③ 코어 대미지 | **붙음** | 실제로 코어를 때린다 (유저 확인) — `is_weapon_mode_skill`이 ③을 태운다 |
  | ④ 「차지 대미지 ▲」 | **붙음** | 인게임에서 차징 표기가 335%까지 오르는 게 확인됐다 (아래 §차지 배율은 가산이다) |
  | ⑤ 「투사체 폭발 대미지 ▲」 | **빠짐** | 모드가 아니라 **기본 무기**로 판정하기 때문 — 바로 아래 항목 |

  구현은 `is_weapon_mode_skill`이 ①만 끄고 ③은 태우는 것이고, ⑤는 무기 타입 판정이 따로 잡는다.
- **무기 변경 중에도 무기 타입은 기본 무기로 따진다** (유저 확인, 2026-08-25). 「투사체 폭발
  대미지 ▲」는 RL에만 붙는데, 이 RL 판정은 **변경된 모드 무기가 아니라 원래 무기**를 본다.
  → 기본 무기가 RL이 아닌 캐릭터는 변신해서 RL이 되어도 이 버프를 못 받는다.
  구현은 `timeline.CharState.base_weapon_type`(무기 변경 중에도 안 바뀐다).

  | 캐릭터 | 기본 | 모드 | 투사체 폭발 |
  |---|---|---|---|
  | 아니스 : 스타 · 베스티 | RL | RL | 받음 |
  | 나유타 | SMG | RL | **못 받음** |
  | 은화 : 택티컬 업 | SR | RL | **못 받음** |

  실측 대조 (180초 · 철갑 · 코어 있음 · 싱크로 821 · 유저 실제 콘솔):

  | 스쿼드 | 나유타 | 총합 |
  |---|---|---|
  | 아니스 : 스타 · 나유타 · 벨벳 · 홍련 : 흑영 · 리버렐리오 | 1.12배 | 1.01배 |
  | 위에서 아니스 → **리틀 머메이드**(투사체 폭발 버프 없음) | 1.06배 | 0.96배 |

  아니스가 주는 투사체 폭발 92.03%를 나유타에게 태우면 같은 스쿼드에서 1.52배로 튀는데,
  버프 제공자가 없는 아래 스쿼드는 1.06배다. 두 값이 나란히 서려면 나유타가 그 버프를
  받지 않아야 한다 — **이 대조가 기본 무기 판정의 근거**다.
- **클립 무기는 재장전 1회에 최대 장탄의 1/3만 채운다** (유저 확인, 2026-08-19).
  `parsed_nikke.json`의 짧은 `reload_time`(드레이크 SG 0.5s, 센티 RL 0.5s)은 **클립 1회** 시간이고,
  오토는 탄창이 찰 때까지 이 장전을 **3연속**으로 굴린다 → 빈 탄창의 실효 재장전 시간은
  `reload_time × 3`이라 같은 무기군의 일반 캐릭터(레오나 SG 1.5s, 앵커 RL 2.0s)와 사실상 같다.
  **표기값만 보고 "재장전이 3배 빠른 무기"로 읽으면 그 캐릭터 딜이 통째로 부풀려진다.**
  - 클립 1회 충전량은 **현재** 최대 장탄의 1/3을 **반올림**한 값이다 — 장탄 증가 버프가 붙으면
    클립당 발수가 함께 커져 빈 탄창은 대개 3회로 찬다(총 재장전 시간이 장탄 수에 비례해 늘지 않는다).
    단 **반올림이 내려가는 장탄에서는 클립이 4회가 된다**: 31발이면 10발씩 30발을 채운 뒤
    남은 1발을 위해 한 번 더 장전한다 (유저 확인). 올림으로 두면 이 한 번이 통째로 사라진다.
  - `event:full_reload`(`최대 장탄 수 재장전 완료 시`)는 **마지막 클립에서만** 1회 발동한다.
    중간 클립에는 `post_reload_delay`도 붙지 않는다.
  - 실전에는 엄폐를 끊어 1/3·2/3만 채우고 나오는 컨트롤이 있지만 **계산기는 아직 표현하지 않는다**
    (오토 = 3연속 고정). 넣으려면 컨트롤 실행층에서 엄폐 이탈 시 진행 중인 클립을 취소해야 한다.
  - 명단 정본은 **CDN**이다(2026-08-28) — `reload_bullet`(재장전 1회가 채우는 비율)이
    `3300`인 14명이 곧 클립 무기고, `parse_nikke.py`가 `clip_count`(=3)로 접어 내린다.
    `weapon_mechanics.json`의 `clip_characters`(SG 9명·RL 5명)는 CDN 값이 없는 프리뷰
    캐릭터 폴백으로만 남는다 — 전수 대조에서 두 출처는 명단이 정확히 일치했다.
    구현은 `timeline.py _finish_reload()`·`_reload_total_duration()`이고, 1/3은 이제
    상수가 아니라 `clip_count`로 나눈 값이다.
- **무기 변경 모드 진입 시 이전 무기의 차지 진행분은 이어지지 않는다** (인게임 확인): 모드 진입 프레임에 차지를 새로 시작한다. 위 `차지 시간 stat 변경`(진행분 유지)과는 **다른 규칙** — 그쪽은 같은 무기의 stat만 바뀐 경우고, 이쪽은 무기 자체가 교체된다. 이어받게 두면 변경 무기의 차지가 **짧을수록 손해**가 되는 역설이 생긴다: 진입 시점에 이미 `차지 시작 + 짧은 차지`가 지나 있어 즉시 발사되고, 풀버스트 진입(버스트 사용 +0.15초) 전에 쏴 버프를 통째로 놓친다. (맥스웰 : 오디너리 미케닉 — 과전류 5단계 0.4초가 4단계 1.5초보다 대미지가 34% 낮았다)

#### 차지 배율은 가산이다

**무기의 풀차지 배율과 「차지 대미지 ▲」 버프는 곱이 아니라 합이다** (유저 인게임 확인, 2026-08-25).
인게임 차징 표기가 그대로 ④ 차지 배율이고, 나유타 RL 모드(풀차지 250%)에 벨벳 `사랑의 탄환`
스킬 7레벨(차지 대미지 +87.05%)이 걸린 상태에서 **335%**가 찍혔다.

```
가산 (정답):  250% + 87.05%        = 337%   ← 인게임 335%
곱연산 (오류): 250% × (1 + 0.8705)  = 468%
```

곱연산은 **모든 차지 무기(SR·RL)를 통째로 부풀리던 전역 버그**였다. 고친 뒤 유저 실측 대비
홍련 : 흑영 1.16배 → 1.00배, 벨벳 1.36배 → 0.99배, 스쿼드 총합 1.35배 → 1.01배로 내려왔다.

##### 「배율 ▲」은 무기 기본 배율에만 곱한다

원문이 `차지 대미지 N% **배율** ▲`인 것(`charge_dmg_mag_pct`)은 별개 층인데, **곱하는
대상이 합 전체가 아니라 무기 기본 배율뿐**이다. 배율끼리는 서로 가산된다
(유저 인게임 확인, 2026-08-28).

```
④ = full_charge_mult% × (1 + Σ배율%) + Σ평문%
```

헬름(기본 250% · 오버로드 평문 11.11% · 소장품 SR15 배율 9.47% · 버스트
`이지스 캐논 3` 배율 158.4%)의 인게임 차징 표기로 잰다. 다른 버프가 하나도 없는 조합:

| | 계산 | 인게임 |
|---|---|---|
| 평시 | 250 × (1 + 0.0947) + 11.11 = **284.79** | 285 |
| 버스트 | 250 × (1 + 0.0947 + 1.584) + 11.11 = **680.79** | 681 |

결정적인 단서는 **681 − 285 = 396 = 250 × 1.584**가 딱 떨어진다는 것이다 — 버스트가 더하는
양이 평시 표기(285)가 아니라 **기본 배율(250)**에 비례한다. 다른 조합은 전부 어긋난다.

```
합 전체에 곱함    (1 + 1.584) × (250 + 20.58)      = 699.18   ✗
배율을 뒤에 곱함  (250 + 11.11) × 1.0947 × 2.584   = 738.5    ✗
배율끼리 합쳐 곱  (250 + 11.11) × 2.6787           = 699.4    ✗
```

**소장품 RL·SR 효과가 이 층이다.** 원문이 `차지 대미지 N% 배율 ▲`인데 종전에는 평문
`charge_dmg_pct`로 잡고 있었다 — 250% 무기 기준 기여가 +9.47pt가 아니라 +23.68pt다.
정본은 `scraper/cdn_tables.py`의 `COLLECTION_WEAPON_BUFF`.

**소장품 SG·SMG(`일반 공격 대미지 N% 배율 ▲`)도 같은 성격의 곱하는 층이고, 곱하는 대상만
다르다 — ①의 무기 계수 자체다** (유저 확인, 2026-08-28). 이쪽은 `normal_atk_dmg_pct`가
처음부터 `coeff × (1 + Σpct)`로 들어가 있어 바꿀 게 없었다. 「배율 ▲」이라는 같은 표기가
무기군에 따라 ④(차지 배율)와 ①(일반 공격 계수)로 갈린다는 뜻이다.

현재 `charge_dmg_mag_pct`를 주는 **스킬은 헬름 `이지스 캐논 3` 하나뿐**이고, 소장품 쪽은
SR·RL 전원에게 붙는다. 구현은 `damage._factor4()`, 회귀 검산은 `damage.py` 검산 4-B.

⚠ 미해결: 위 나유타 관측(250 + 87.05 = 337 vs 인게임 335)의 2pt 차이는 이 규칙으로도
설명되지 않는다. 벨벳 버프는 평문이고 나유타 소장품은 SMG(차지 무관)라 이 변경의
영향권 밖이다 — `DATA_VERIFY`에 남은 항목.

### 값 산정

- **`시전자 기준 [stat]` 계열은 시전자의 *버프 제외 기본 스탯*으로 환산된다** (유저 확인, 2026-08-10).
  `atk_caster_based_pct`는 `state["base_stats"][시전자]["atk"]`를 읽으므로 시전자에게 걸린 공격력 버프가
  값을 키우지 않는다 → **부여 시점 스냅샷이냐 매 프레임 재평가냐를 따질 필요가 없다.**
  (누아르 `럭키챰` — `[지속]`이라 t=0 1회 부여뿐이지만 이 성질 덕에 문제되지 않는다)
- **버프를 언제 읽는지가 대미지 종류마다 다르다** (유저 조사, 2026-08-11).
  - **순차 대미지(`sequential_damage`)는 풀버스트 *시작 전* 버프로 계산된다.** B3 버스트는 풀버스트보다
    0.05초 앞서 발동하므로, `burst_cast` 시점에 즉시 계산하는 현행 동작이 곧 이 규칙이다 —
    `bonus_damage`처럼 `_pending_burst_dmg`로 풀버스트까지 미루면 **안 된다**.
  - **지속 대미지(`dot_damage`)는 틱이 들어가는 순간의 버프로 계산된다.** 그래서 DoT의 **위상**이
    곧 딜이다 — 같은 틱 수라도 풀버스트 안에 몇 개가 들어가느냐가 달라진다.
  - → 다중 히트가 시간에 걸쳐 들어가는 효과를 **한 시점에 몰아 쏘면 조용히 틀린다.**
    사쿠라 : 블룸 인 서머 `화양연화 2`에서 램프 10틱(배율 합 55)을 버스트 시각에 몰았더니
    램프 전체가 풀버스트 밖으로 밀리고 대신 10중첩 틱이 3회 더 버프를 받았다 —
    상쇄되지 않고 순 손해 쪽으로 어긋났다. `ramp_interval`로 실제 타간 간격을 펼쳐 해결했다.
- **서로 다른 효과가 같은 stat을 걸면 합산된다** (유저 확인). 대상 범위·지속시간이 달라도 각각 별개 버프
  항목으로 병존하며 값이 더해진다 — 큰 값 하나만 적용되는 게 아니다.
  (누아르 `피날레` — 샷건 아군 10초분과 동일 스쿼드 30초분의 명중률·저지 부위가 겹치는 구간에서 합산)
- **`[현재 체력 N% ▼]`는 *현재* 체력의 N%다 — 최대 체력 기준 정액이 아니다** (유저 확인, 2026-08-16).
  자해가 곱연산(`hp ×= 1 − N/100`)이라 **체력은 0에 수렴할 뿐 0이 되지 않는다.** 정액으로 구현하면
  체력이 유한 횟수 만에 0에 닿고, `self_hp_below:N` 임계 도달이 앞당겨져 조건부 버프 가동 시각이
  통째로 밀린다. **`timeline.py` `handle_current_hp_reduce`가 이 규칙대로 구현되어 있다** —
  `hp[name] = max(cur * (1 - val / 100), 0.0)`이고 `git log -S`로 보면 2026-08-23 공개 커밋
  시점부터 그렇다. (2026-09-23 정정: 그 전까지 이 자리에 「현행은 `hp -= base_hp × val/100`
  정액이라 규칙과 어긋난다」가 남아 있었으나 **코드가 아니라 조항이 낡은 것**이었다.
  §무기 메카닉의 「최대 장탄 만료 캡 미구현」 조항과 같은 계통이다.)
  보유 캐릭터는 그레이브 `미래 예지 5`(1%·1초 간격) · 홍련 `육참골단`(4.01%) ·
  길로틴 `유열 2`(2.01%·30명중마다).
  (홍련 등록 중 확인. 홍련은 이 자해가 스킬2·3의 체력 임계 조건을 여는 핵심 메카닉이라 딜에 직결.
  **길로틴에서는 곱연산이라는 점 자체가 기본 경로를 가른다** — 180초 335회 자해 뒤 체력이
  0.111%로 수렴해 살아남는다. 정액이었다면 50회째(t≈14)에 0이 되어 자기 자해로 전투불능이다)
- **`[스킬 N 재사용 시간 X% ▼]`에 `[N초 유지]`·`[N 중첩]`이 둘 다 없으면 즉발형이고, *남은* 재사용 시간에
  곱연산한다** (센티 실측 대조, 2026-08-14). 지속 표기가 있으면(아르카나 `마법사 카드` `[15초 유지]`)
  기존대로 `skill_cooldown_pct` 버프 — `every:Ns` 주기 자체에 곱한다. 표기가 없는 쪽은 버프가 아니라
  **그 순간의 잔여 쿨을 깎는 1회성 사건**이라, 발동 직후(쿨 full)에는 크게·쿨 끝자락에서는 작게 줄인다.
  → 기본값 기준 정액 차감으로 구현하면 과대, 갱신형 단일 버프로 구현하면 과소가 된다.
  (센티 `보수공사` — 스킬2 기본 9.0s, 풀차지 명중마다 ×0.9084. 실효 간격 7.13s로 10스택 71.3s이며
  유저 실측 70~72초와 일치. 정액이면 57.8초, 갱신형 버프면 81.8초로 어긋난다)

- **`[재장전 속도 N% 증가]`는 재장전 *시간*에 `×(1 − N/100)`이다** (유저 확인, 2026-08-16).
  니케의 "속도"는 시간으로 환산되므로 `1/(1+N/100)`이 아니다 — N이 100에 가까우면 재장전이
  사실상 0초가 된다. `timeline.py _reload_duration()`이 이미 이 식이다
  (`reload_time × max(0, 1 − speed_pct)`). → `[재장전 속도 99.96% 증가 상태로 고정]`의
  `reload_time_fixed`는 `1.0 × 0.0004 = 0.0004`초. (질 `슈퍼 캅`)

### 버프 스택

- **`[N 중첩]` 표기가 없는 DoT는 재부여 시 병존하지 않고 갱신된다** (유저 확인, 2026-08-16).
  같은 이름의 지속 대미지가 만료 전에 다시 부여되면 인스턴스가 늘지 않고 지속시간만 새로 시작한다
  → 주기가 짧은 트리거(재장전 등)에 30초짜리 DoT가 걸려 있어도 동시 틱은 **언제나 1개**다.
  (질 `산성탄` — 탄창 주기 vs DoT 30초. 병존으로 모델링하면 딜이 배수로 부풀려진다)

- **스택 부여는 지속시간도 갱신한다** (일반 규칙, 유저 확인). 중첩형 버프에 스택이 추가되면 — 같은 효과가
  재발동하는 경로(`_activate()`)든 별도 항목이 올려주는 경로(`buff_stack_add`)든 — 남은 지속시간이
  **새로 부여된 것과 동일하게 갱신**된다. 원문 `[스택명 : ...] [N 중첩] [M초 유지]`가 "버프를 다시 붙이는"
  문장이기 때문이다. `[지속]`(`duration: -1`)은 만료가 없어 해당 없음.
  → **유한 지속시간 스택 버프는 스택이 계속 들어오는 한 만료되지 않는다.** 스택 공급이 끊긴 시점부터
  M초를 센다. 이걸 "각 스택이 따로 만료된다"로 모델링하면 최대 중첩 도달률이 크게 떨어진다
  (팬텀 `괴도의 단검` 5초·3중첩 — 미갱신 시 최대중첩 proc 42회→23회, 본인 딜 −12.3%).

### 효과 실행 순서 (동일 timing)

같은 timing에 복수 효과가 발동할 때의 순서 규칙 중 **여러 캐릭터에 공통**인 것을 누적한다(캐릭터 한정 순서는 각 시나리오에 둔다). `char-add` `SCENARIO.md` Step 6 최빈 사각지대(효과 실행 순서)에서 일반 규칙이 확인되면 여기 한 줄 추가 — 매번 재발견되지 않도록.

- **스택 부여와 `최대 중첩 시` 판정이 같은 timing이면 같은 프레임에 부여 → 판정 → 소비**까지 처리된다. 결과적으로 최대 중첩 상태의 버프는 한 발도 쓰이지 못하고 해제된다. (라플라스 : 얼티밋 히어로 예열 5중첩 → 즉시 무기 변경 — 5중첩째 차지 속도 +50%는 실효 없음)
- **원문 `■` 블록의 서술 순서가 곧 실행 순서다** (유저 확인). 같은 timing에 딜 블록과 디버프 부여 블록이 함께 있을 때, 딜이 **먼저** 적혀 있으면 그 딜은 뒤에 부여되는 디버프(`받는 대미지 ▲` 등)의 영향을 받지 않는다. → **파싱 시 배열 순서를 원문 블록 순서 그대로 유지**한다. (로산나 벤데타 — 아살토 딜 → 수냉 타겟 `받는 대미지 29% ▲`, 이번 버스트 딜에는 미적용)
- **카운터(스택·게이지) 부여와 그 카운터를 읽는 무기 변경이 같은 timing이면 부여가 먼저 반영된 뒤 무기 변경이 읽는다.** 따라서 첫 발동부터 카운터 1로 센다. → **파싱 시 배열 순서 제약**: 충전 항목을 무기 변경 항목보다 **앞**에 둔다. 뒤집히면 단계 램프가 한 칸씩 밀린다. (맥스웰 : 오디너리 미케닉 — 1회차 버스트에서 과전류 1단계 차지 시간이 적용됨)
- **반대 방향도 같은 규칙이다** — 카운터를 **읽는** 항목이 배열에서 충전 항목보다 **앞**에 있으면 증가 전 값을 읽어 **한 칸 밀린다**. 원문이 「[선행 효과] 적용 시」처럼 *사건*을 가리키면 게이지 조건을 복사하지 말고 **`event:[효과명]`**을 쓴다 — 그쪽은 버프 활성 뒤에 emit되어 배열 순서와 무관하고, 스택 상한에 닿은 뒤의 갱신에도 계속 발생한다. (아르카나 : 포츈 메이트 `청춘의 기록` — 원문은 `행복한 기억 적용 시`인데 게이지 조건을 복사해 4·10·16발째가 아니라 5·11·17발째에 붙었다. 2026-09-02 수정)
- **주기 틱(`tick_interval`)에는 두 유형이 있다** (유저 조사, 2026-08-07). 회수는 양쪽 같고(2초 간격 10초 = **5회**, 1초 간격 10초 = **10회**) 다른 것은 **위상**이다.

  | 유형 | 첫 틱 | 10초·2초 간격의 틱 시각 | 캐릭터 |
  |---|---|---|---|
  | **type 1** | 스킬 발동과 **동시** | 0·2·4·6·8 | 미하라 : 본딩 체인 · 에이다 · 아크레인저 블랙 |
  | **type 2** | 발동 후 **+interval** | 2·4·6·8·10 (**만료 프레임 틱 포함**) | 디젤 : 윈터 스위츠 · 밀크 : 블루밍 바니 |

  type 1은 첫 틱이 **풀버스트 시작 전에** 떨어질 수 있고, type 2는 첫 틱부터 풀버스트 안이다 — 버스트 버프를 몇 틱이 받느냐가 갈린다. 위 목록에 없는 캐릭터(브래디·레이븐 등)는 **미확인**.

  **계산기는 아직 두 유형을 구분하지 않는다** — `_dot_timers`/`_instant_timers`는 type 2 위상이면서 `t >= expires_at`에서 틱 없이 만료해 **4회**가 되고(만료 프레임 틱 누락), `every:Ns`(`_next_fire`)는 전투 시작 기준 **전역 격자**라 스킬 발동 시점과 위상이 무관하다.
  **첫 발동은 `t = N`이다**(`_next_fire[eid] = (interval, interval)`, 코드 확인 2026-08-16) — t=0에 1회 발동하지 않는다.
  → 주기가 사이클(12.5s)과 배수 관계가 아니면 버스트 창과의 위상이 매 사이클 어긋난다. 주기 효과가 DoT거나 DoT 증폭이면 이 위상이 곧 딜이다. (로산나 : 시크 오션 `스피나 디 로사` 30초 주기) 수정 시 해당 캐릭터 baseline이 전부 바뀐다 — 별건으로 처리한다.
- **`[N발 유지]`(`duration_bullets`) 버프를 "발사가 1발을 소모했다"고 셀지는 그 트리거가 대미지 계산보다 앞서는가로 갈린다** (2026-08-07, 아인 파싱 중 발견). 발사 처리 안에서 부여되는 버프는 두 부류다 — `full_charge`(차지 완료 = 발사 **전**)처럼 그 발이 버프를 받는 것은 1발로 세는 게 맞고, `full_charge_hit`(명중 **후**)처럼 이미 계산이 끝난 뒤 붙는 것은 **받지도 못한 발에 소모만 당해 버프가 통째로 사라진다**. 코드에서는 `buff_manager._BULLET_BOUND_TIMINGS` 등록 여부가 이 판정이다. → 새 캐릭터에 발사 트리거 + `duration_bullets` 조합이 나오면 timeline의 `calc_damage` ↔ `notify` 순서를 먼저 확인한다. (아인 `페더 샷` — `full_charge_hit`이 등록돼 있어 차지 대미지 80%가 한 발에도 적용되지 않았다)
- **최대 장탄 증가와 탄환 충전이 같은 블록에 있으면 장탄 증가가 먼저 반영된 뒤 그 최대치를 기준으로 충전한다** (유저 확인, 2026-08-10). 원문 블록 순서 규칙의 한 사례다 — `[최대 장탄 수 N발 ▲]` → `[탄환 충전 M%]` 순서면 충전량·상한 모두 **증가된 최대치** 기준. → 파싱 시 장탄 항목을 충전 항목보다 **앞**에 두고, 충전 핸들러가 읽는 유효 최대 장탄에 `max_ammo_flat`이 포함되어야 한다. (누아르 `래빗 트윈즈 B` — 9발 기준 4발이 아니라 14발 기준 6발)
- **`tick_interval` 주기 instant(회복·게이지 충전·소모 등)의 첫 발동은 현재 등록 +interval이다**(`buff_manager._instant_timers`). 즉시 1회 발동하지 않으므로, 같은 프레임에서 뒤에 오는 항목이 읽는 게이지·스택 값을 앞당겨 바꾸지 않는다. (아크레인저 블랙 — 배터리 드레인이 즉시 1% 깎으면 뒤따르는 `gauge_eq:배터리:100` 긴급 충전 −50%가 발동하지 못해 변신이 10초가 아니라 20초가 된다)
  > ⚠️ **위 type 1/2 분류와 충돌 가능** — 아크레인저 블랙은 type 1(즉시 첫 틱)로 분류됐는데, 배터리 드레인을 즉시 발동으로 바꾸면 변신이 10초 → 20초가 된다. 유형이 **캐릭터 단위인지 효과 단위인지**(주기 *대미지*만 type 1이고 게이지 드레인은 별개인지) 미확인. 주기 틱 수정 작업에서 먼저 확인할 것.

---

## 시뮬레이션 결과 검증 기준

| 관찰 현상 | 의미 |
|-----------|------|
| 풀버스트 사이클 간 긴 갭 (게이지 충전 완료 후 수십 초 대기) | 특정 단계 쿨이 길거나 `burst_cooldown` stat 미반영 |
| 특정 캐릭터가 2~3사이클에 1회 발동 | 쿨타임 정상 (40초 캐릭터가 번갈아 사용). 의도된 동작인지 확인 |
| 180초 기준 풀버스트 횟수가 14~15회 미만 (쿨 감소 덱 기준) | 사이클 주기 이상 — 쿨 감소 미반영 또는 갭 발생 |
| 풀버스트 상태에서 딜이 비풀버스트 대비 예상보다 낮음 | DealForm ③ `full_burst +50%` 미반영 가능성 |
| 버스트 쿨 감소 스킬이 있는데도 대기 발생 | `burst_ready_at` 보정 오류(`_cd_applied_at_cast` 중복 방지 로직) 또는 timing 불일치 |
