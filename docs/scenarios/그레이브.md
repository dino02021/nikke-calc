# 그레이브 동작 시나리오

> 모드: 보강

## 기본 정보
- 버스트 단계: B2
- 본인 쿨다운: 20초
- 무기 유형: AR
- 작성 사이클 수: 1
- 커버 스쿼드: `레이드_미하라에이다`, `레이드_작열짬`, `컨트롤_에이다미하라`

## 효과 요약

수치는 스킬 레벨 10 기준이다.

| ID | 출처 | 트리거 | stat / 효과 | 대상 | 지속 |
|---|---|---|---|---|---|
| `방열 2` | 스킬1 | `event:state_end:미래 예지` | `ammo_charge_pct: -100` | 자신 | 즉시 |
| `방열` | 스킬1 | `event:state_end:미래 예지` | `reload_ratio_pct: -50` | 자신 | 해제 조건까지 |
| `방열 3` | 스킬1 | `on_attack` / `burst_cast` / `event:full_reload` + `self_state:방열` | `remove_named_buff: 방열` | 자신 | 즉시 |
| `방열 4` | 스킬1 | `passive` + `self_state:방열` | `heal_hp_pct: 2`, 1초 간격 | 자신 | 방열 중 |
| `방열 5` | 스킬1 | `passive` + `self_state:방열` | `burst_charge_speed_pct: 38.96` | 아군 전체 | 방열 중 |
| `방열 6` | 스킬1 | `passive` + `self_state:방열` | `pierce_dmg_pct: 48.4` | 아군 전체 | 방열 중 |
| `과열 I` | 스킬2 | `hit_count:15` | `atk_pct: 15.48` | 자신 | 최대 장탄 재장전까지 |
| `과열 I 제거` | 스킬2 | `event:full_reload` | `remove_named_buff: 과열 I` | 자신 | 즉시 |
| `과열 명중 누적` | 스킬2 | `hit_count:1` + `self_state:미래 예지` | `gauge_charge: 과열 명중 +1` | 자신 | 즉시 |
| `과열 명중 초기화` | 스킬2 | `event:미래 예지` | `gauge_consume: 과열 명중 전량` | 자신 | 즉시 |
| `과열 II` | 스킬2 | `passive` + `self_state:미래 예지` + `self_state:과열 I` + `gauge_above:과열 명중:30` | `atk_pct: 20.66` | 자신 | 조건 충족 중 |
| `과열 III` | 스킬2 | `passive` + `self_state:미래 예지` + `self_state:과열 II` + `gauge_above:과열 명중:60` | `atk_dmg_pct: 30.8` | 자신 | 조건 충족 중 |
| `미래 예지 5` | 스킬3 | `burst_cast` | `current_hp_reduce: 1`, 1초 간격 | 자신 | 10초 |
| `미래 예지` | 스킬3 | `burst_cast` | `infinite_ammo` — 상태 담체 | 자신 | 10초 |
| `미래 예지 2` | 스킬3 | `burst_cast` | `pierce_enabled` | 자신 | 10초 |
| `미래 예지 3` | 스킬3 | `burst_cast` | `pierce_dmg_pct: 52.8` | 자신 | 10초 |
| `미래 예지 4` | 스킬3 | `burst_cast` | `crit_rate: 85.19` | 자신 | 10초 |
| `라이터즈 스포일러` | 스킬3 | `burst_cast` | `atk_dmg_pct: 48.2` | 아군 전체 | 10초 |
| `라이터즈 스포일러 2` | 스킬3 | `burst_cast` | `pierce_dmg_pct: 39.98` | 아군 전체 | 10초 |
| `라이터즈 스포일러 3` | 스킬3 | `burst_cast` | `max_ammo_flat: 3` | 아군 전체 | 10초 |

## 검증 스쿼드

- **표준 B2 20초**: `["리틀 머메이드", "그레이브", "test_B3", "스노우 화이트 : 헤비암즈"]`
  — 그레이브가 매 사이클 B2를 사용하며, B3 두 명이 교대한다. 별도 조건부 편성은 없다.
  - 리틀 머메이드의 버쿨감으로 사이클이 12.53초라 **방열 4클립 재장전이 끝나기 전에 다음 버스트가
    온다**(미래 예지 종료 → 다음 B2까지 2.53초, 4클립 완충까지 2.93초). 3클립째 뒤 `재장전 취소(무한 장탄)`로
    끊기므로 `event:full_reload`가 없다 — 이 스쿼드에서 `과열 I 제거` **0회**가 정상이다(아래 Q3).
- **변형 1 (완충 경로 — 버쿨감 없음)**: `["test_B1", "그레이브", "test_B3", "스노우 화이트 : 헤비암즈"]`
  — 사이클 20초. 미래 예지가 끝나고 다음 B2까지 10초라 네 번째 클립이 먼저 차서 `event:full_reload`가
  난다. 실측(2026-09-25): 13.100 방열 → 13.167 재장전 시작 → 클립 13.883·14.600·15.317 →
  **16.033 재장전 완료 · `과열 I 제거` · `방열 3`**, 180초에 9회. 표준 스쿼드가 못 여는 완충 경로의 양성 검증이다.
- **기존 회귀 커버**: `S40_앨리스모더니아`, `S40_아스카` — 관통 딜러에게 주는
  `방열 6`·`라이터즈 스포일러 2`와 실전형 버스트 사이클의 변동을 함께 본다.

## 사이클 타임라인

표준 B2 20초 스쿼드, 고정 버스트 게이지 모드 기준이다. 장비의 재장전 속도에 따라 클립
사이의 실제 초는 달라져도 이벤트 순서는 같아야 한다.

| t (예상) | 이벤트 | 발동 효과 | 대상/지속 | 비고 |
|---|---|---|---|---|
| 0초 | 전투 시작, 일반 공격 | `hit_count` 누적 | 자신 | 15히트마다 `과열 I` 갱신 |
| 약 1.2초 | 일반 공격 15회 | `과열 I` | 자신 / 최대 장탄 재장전까지 | 공격력 +15.48% |
| 3.0초 | B1 사용 | — | — | 리틀 머메이드 |
| 3.1초 | 그레이브 B2 사용 | `미래 예지` 계열, `라이터즈 스포일러` 계열, `방열 3` | 자신·아군 / 10초 | 기존 방열이 있으면 먼저 제거하고 미래 예지 진입 |
| 3.1초 | `event:미래 예지` | `과열 명중 초기화` | 자신 | 카운터 0으로. 과열 II·III는 아직 안 켜진다 |
| 약 5.6초 | 미래 예지 중 일반 공격 30회 | `과열 II` | 자신 / 미래 예지 중 | 공격력 +20.66%. 미래 예지 시작 +약 2.5초 |
| 약 8.1초 | 미래 예지 중 일반 공격 60회 | `과열 III` | 자신 / 미래 예지 중 | 공격 대미지 +30.8%. 미래 예지 시작 +약 5.0초 |
| 3.2초 | B3 사용 | — | — | 첫 사이클 `test_B3` |
| 3.25초 | 풀버스트 시작 | 버스트 중 공격 | 스쿼드 / 10초 | 미래 예지 버프와 중첩 |
| 13.1초 | 미래 예지 종료 | `방열 2`, `방열`, `방열 4·5·6`; 과열 II·III 종료 | 자신·아군 / 방열 해제까지 | 탄창 0, 재장전 비율 25%로 전환 |
| 13.2초 전후 | 자동 재장전 시작 | 25% 클립 반복 | 자신 | 오토는 중간에 사격하지 않음 |
| 다음 약 3초 | 클립 1~3 완료 | 장탄 25% → 50% → 75% | 자신 | 아직 `event:full_reload` 없음 |
| 15.5초 전후 | 다음 B1 사용 | — | — | 표준 주기 약 12.53초 |
| 15.6초 전후 | 다음 그레이브 B2 사용 | `burst_cast` → `방열 3` | 자신 | 완충보다 먼저 버스트가 열리는 대표 경로 |
| 같은 시점 | 미래 예지 재진입 | `infinite_ammo` 및 나머지 버스트 버프 | 자신·아군 / 10초 | 진행 중 재장전에서 무한 장탄으로 전환해야 함 |

수동 조작 변형에서는 1~3번째 클립 뒤 엄폐를 풀어 재장전을 끊고, 첫 일반 공격의
`on_attack`으로 방열을 제거한다. 버스트도 일반 공격도 없으면 네 번째 클립이 최대 장탄을
채우면서 `event:full_reload`를 발생시켜 방열과 과열 I을 함께 제거한다.

## 스킬 간 상호작용

- **미래 예지 종료가 방열 상태를 만든다.**
  - `event:state_end:미래 예지` → 탄환 100% 제거 + `방열(reload_ratio_pct: -50)` 활성.
  - `방열`이 상태 담체이며 `방열 4·5·6`은 `self_state:방열`을 매 순간 재평가한다.
- **과열 II·III는 미래 예지에 종속되고, 그 안에서 30발·60발 뒤에 켜진다.**
  - 미래 예지 적용 시 `과열 명중` 게이지가 0으로 리셋되고, 미래 예지 중 일반 공격이
    명중할 때마다 +1 된다. 30에 닿으면 과열 II, 60에 닿으면 과열 III가 조건을 만족한다.
  - 과열 I이 함께 있어야 과열 II가 성립한다. 미래 예지가 끝나면 게이트가 깨져 둘 다 꺼지고,
    다음 미래 예지에서 카운터가 0부터 다시 오른다.
- **방열의 버스트 게이지 증가가 자신의 해제를 앞당긴다.**
  - 방열 중 아군의 게이지 충전이 빨라져 최대 장탄 완충 전에 다음 사이클이 열릴 수 있고,
    그레이브의 `burst_cast`가 방열을 제거한 직후 미래 예지를 다시 부여한다.
- **방열 해제와 미래 예지 재부여의 동일 timing 순서가 중요하다.**
  - `방열 3`은 스킬1 효과라 스킬3의 미래 예지보다 먼저 실행된다. 기존 방열을 제거한 뒤
    새 미래 예지를 부여해야 새 상태가 방열 조건에 의해 잘못 제거되지 않는다.

## 유저 확인 사항

- **Q1. 과열 II·III는 버스트가 끝난 뒤에도 남는가?** (2026-08-16, 래치 전수 조사 중 확인)
  - **A: 꺼진다.** 버스트(`미래 예지` 10초)가 끝나면 과열 II·III의 공격력·공격 대미지가 사라진다.
  - 원문이 모순처럼 보이는 자리다 — "30회 : 미래 예지와 과열 I 상태일 때 **한하여**
    [과열 II : 공격력 ▲] **[지속]**". `[지속]`만 보면 영구 같지만 `한하여`가 이긴다.
  - **현행 구현이 이대로다.** `duration: -1` + `self_state:미래 예지` 조건이라
    `_has_runtime_cond()`가 상시 재판정 대상으로 잡고, 미래 예지가 끝나면 함께 꺼진다.
  - 만약 래치로 바꾸면 그레이브 딜이 +17~18% 과대 계상된다 (실측).

- **Q2. `방열 5·6`(아군 전체 버스트 게이지 충전 속도·관통 대미지)의 판정 방식은?**
  - **A: 상시 재판정이 맞다.** 원문이 "자신이 방열 상태일 때 **한하여** 아군 전체에게 ... [지속]"이다.
    Q1과 같은 구조 — `[지속]` + `한하여` 조합은 조건이 곧 유효 구간이다.
  - 래치로 바꾸면 팀 전체 관통 대미지가 영구화되어 관통 딜러가 있는 스쿼드가 크게 부풀려진다
    (`레이드_작열짬`에서 앨리스 +25.3%).

- **Q3. 방열 4클립 재장전 도중에 다음 버스트가 오면 과열 I은?** (2026-09-25 유저 확인)
  - **A: 안 지워지는 게 맞다.** 재장전 완료 전에 그레이브가 버스트를 쓰면 장탄 수 무한으로 재장전이
    끊겨 「최대 장탄 재장전 완료」가 없다. 다음 버스트까지 구간이 길면 재장전이 먼저 끝나 지워져야 한다.
  - 계산기가 두 경로를 그대로 낸다 — 표준 스쿼드 0회, 변형 1 매 사이클 1회(§검증 스쿼드).

## 상태 담체 수정 (2026-08-28)

**위 Q1·Q2의 "현행 구현이 이대로다"는 2026-08-28까지 사실이 아니었다.** `미래 예지`가
지속 버프가 아니라 `current_hp_reduce` **instant의 이름**이라 상태가 한 번도 성립하지
않았고, `event:state_end:미래 예지`도 영영 뜨지 않았다. 그 결과:

- `방열`(재장전 비율 ▼)·`방열 2`(탄환 100% 제거) 미발동
- `방열 4·5·6`(회복 · 아군 전체 버스트 게이지 +38.96% · 관통 대미지 +48.4%) 값 0
- `과열 II·III` 미성립 (Q1이 말한 "버스트 중에는 켜진다"조차 아니었다)

이름을 지속 효과로 옮겨 고쳤다(`docs/PARSING.md` §상태의 담체):

| 효과 | 종전 이름 | 현재 이름 |
|---|---|---|
| 장탄 수 무한 (10초 buff) | `라이터즈 스포일러` | **`미래 예지`** ← 상태 담체 |
| 관통 특화 · 관통 대미지 · 크리 확률 | `라이터즈 스포일러 2~4` | `미래 예지 2~4` |
| 현재 체력 1% ▼ (주기 instant) | `미래 예지` | `미래 예지 5` |
| 재장전 비율 50% ▼ (buff) | `방열 2` | **`방열`** ← 상태 담체 |
| 탄환 100% 제거 (instant) | `방열` | `방열 2` |
| 아군 전체 3종 | `라이터즈 스포일러 5~7` | `라이터즈 스포일러 1~3` |

그레이브 개인 딜 +24%(히트 1927 → 1678, 발수는 줄고 배율이 붙었다).
`runner/doclint.py` 검사 K가 같은 종류의 조용한 미발동을 앞으로 막는다.

## 방열 해제 조건 (2026-08-29 유저 확인)

`[방열 제거 조건]`은 다음 세 조건의 **OR**이다. 하나라도 만족하면 이름이 `방열`인
`reload_ratio_pct: -50` 버프를 즉시 제거하고, 이에 종속된 `방열 4·5·6`도 더 이상
유효하지 않다.

| 인게임 조건 | 파싱 timing | 동작 |
|---|---|---|
| 일반 공격 시 | `on_attack` | 수동으로 클립 재장전을 끊고 첫 일반 공격을 하면 제거 |
| 버스트 스킬 발동 시 | `burst_cast` | 그레이브 본인이 다음 B2 버스트를 발동하면 제거 |
| 최대 장탄 재장전 시 | `event:full_reload` | 최대 장탄까지 모두 채운 마지막 클립에서 제거 |

파싱은 제거 효과 `방열 3` 하나에 세 timing을 함께 두어 OR로 표현한다.

```json
{
  "name": "방열 3",
  "type": "instant",
  "trigger": {
    "timing": ["on_attack", "burst_cast", "event:full_reload"],
    "condition": ["self_state:방열"]
  },
  "target": "self",
  "stat": "remove_named_buff",
  "target_effect": "방열"
}
```

### 오토와 수동 조작

- 그레이브의 기본 재장전 비율은 최대 장탄의 1/2이고, 방열의 50% 감소가 곱해지면
  **1회당 1/4(25%)**가 된다. 빈 탄창에서 최대 장탄까지 총 4클립이 필요하다.
- 실전 오토는 중간 클립에서 사격하지 않고 최대 장탄이 될 때까지 4회를 연속 재장전한다.
  따라서 방열이 계속 유지된다면 네 번째 클립의 `event:full_reload`가 해제 조건이 된다.
- 수동 조작은 1~3번째 클립을 채운 뒤 재장전을 끊고 일반 공격으로 돌아갈 수 있다.
  이때 첫 공격의 `on_attack`이 방열을 제거한다.
- 일반적인 실전 운용에서는 방열의 아군 버스트 게이지 충전 속도 증가 효과를 유지한 채
  오토로 두며, 최대 장탄 재장전보다 다음 버스트가 먼저 열리는 경우가 많다. 이때
  `burst_cast`가 방열을 제거하고 `미래 예지`의 장탄 수 무한 상태로 다시 들어간다.

### 계산기 구현 반영 (2026-08-29)

- 유한 `control.sequence` 엄폐가 끝나면 탄이 남은 진행 중 재장전을 즉시 끊는다. 0발이면
  다음 클립 하나가 들어온 직후 끊어 `on_attack` 해제 경로를 재현한다. duration 없는 엄폐와
  무조작 오토는 최대 장탄까지 계속 재장전한다.
- `infinite_ammo` 활성 중에는 장탄이 줄지 않고 탄환 소비 이벤트도 발생하지 않는다.
  0발에서도 발사하며, 진행 중 재장전은 완료 이벤트 없이 취소하고 남은 장탄을 보존한다.

## 과열 명중 카운터 (2026-09-11)

`과열 II`·`과열 III`의 트리거가 `["event:미래 예지", "hit_count:30/60"]`이던 동안, **30회·60회는
장식이었다.** `event:미래 예지`가 버스트 순간에 뜨고 두 버프가 `duration: -1`이라 미래 예지와
동시에 켜졌고, 세 효과의 가동률이 소수점까지 똑같았다(77.7%).

원문은 `미래 예지 상태 적용 **후** 일반 공격 명중 시`라 **미래 예지마다 다시 세는 로컬
카운터**다. 전역 `hit_count:30`은 전투 내내 한 방향으로만 도는 격자라 이 「후」를 표현하지
못한다. 아르카나 : 포츈 메이트 `공격 횟수` 선례대로 가상 게이지로 옮겼다:

| 항목 | 트리거 | 동작 |
|---|---|---|
| `과열 명중 누적` | `hit_count:1` + `self_state:미래 예지` | `과열 명중` +1 |
| `과열 명중 초기화` | `event:미래 예지` | `과열 명중` 전량 소모 (재적용마다 0) |
| `과열 II` | `passive` + `self_state:미래 예지` + `self_state:과열 I` + `gauge_above:과열 명중:30` | — |
| `과열 III` | `passive` + `self_state:미래 예지` + `self_state:과열 II` + `gauge_above:과열 명중:60` | — |

`passive` 등록 + 런타임 재판정은 같은 캐릭터 `방열 4·5·6`과 같은 모양이다 — `한하여` + `[지속]`
이라 게이트가 곧 유효 구간이다.

그레이브 개인 딜 **−8.63%**(두 스쿼드 동일), 스쿼드 총딜 −1.83% / −1.93%. **히트 수는 불변이고
다른 4명은 0.00%** — 배율만 빠진 순수 국소 변화다. `과열 II` 발동 84 → 14회, `과열 III` 56 → 14회로
미래 예지당 1회가 됐다.

`과열 III`의 `self_state:과열 II`는 원문 충실성 표기이고 실효 게이트가 아니다 — `_has_self_state()`가
condition을 보지 않아 조건부 영구 passive는 조건이 거짓이어도 상태로 성립한다
(`PARSING-CHARS.md` 엠마 : 택티컬 업 행).

## 자해 (`미래 예지 5`)

- `[현재 체력 1% ▼] [1초 간격] [10초 유지]` — **현재 체력 비례**다 (일반 규칙,
  `GAMEPLAY.md §값 산정`). 곱연산이라 체력이 0에 닿지 않는다.
- 그레이브는 체력을 조건으로 쓰는 스킬이 없어 이 값이 딜에 실리지 않는다.
  `current_hp_reduce`를 정액 → 비례로 고쳤을 때 커버 스쿼드 3개 모두 딜 불변이었다.

## 해석 선언

| 효과 | 해석 | 근거 | 대안이었다면 |
|---|---|---|---|
| `미래 예지` | `infinite_ammo`인 10초 지속 버프이자 상태 담체. 활성 중 탄환을 소모하지 않고 재장전하지 않는다 | `[장탄 수 무한] [10초 유지]`; 방열이 `미래 예지 상태 종료 시` 발동 | 장탄만 크게 늘리는 해석이면 버스트 중 재장전 여부와 종료 직후 잔탄이 달라짐 |
| `방열` | 기본 1/2 클립 비율에 50% 감소를 곱해 1/4로 만든다 | 유저 실측: 빈 탄창에서 25%씩 4회 | 50%p 차감이면 비율이 0이 되어 재장전 불가 |
| `방열 3` | `on_attack`·`burst_cast`·`event:full_reload` 중 먼저 온 하나가 방열을 제거한다 | 2026-08-29 유저 확인: 제거 조건 세 가지 OR | AND라면 실전에서 거의 제거되지 않고 아군 버프가 영구화됨 |
| `방열 4·5·6` | 방열 상태를 매 순간 재평가하며 방열 제거와 동시에 꺼진다 | `자신이 방열 상태일 때 한하여` + `[지속]` | 1회 래치면 회복·버스트 게이지·관통 버프가 전투 끝까지 남음 |
| `과열 II·III` | 미래 예지 적용 후의 **자체 명중 카운터**(가상 게이지 `과열 명중`)가 30·60에 닿을 때 각각 활성화되고, 미래 예지·선행 과열 상태를 매 순간 재평가한다 | 원문 `미래 예지 상태 적용 **후** 일반 공격 명중 시` + `[공격 명중 횟수 별 효과]`. 전역 `hit_count`는 미래 예지마다 리셋되지 않아 이 「후」를 표현하지 못한다 | 미래 예지와 동시에 둘 다 켜면(2026-09-10까지의 표현) 개인 딜이 +9.4% 과대 계상된다. 독립 영구 래치면 버스트 종료 후에도 남아 더 커진다 |
| `미래 예지 5` | 매 틱 현재 체력의 1%를 곱연산으로 감소시킨다 | `현재 체력 1% ▼`, `GAMEPLAY.md §값 산정` | 최대 체력 기준 정액이면 100틱 뒤 0 도달 |
| `라이터즈 스포일러` 계열 | 그레이브의 `burst_cast`에서 아군 전체에게 10초간 부여되며, 공격 대미지·관통 대미지·최대 장탄을 각각 독립 적용한다 | 스킬3의 동일 clause, `all_allies`, 각 `[10초 유지]` | 그레이브 자신 제외라면 본인 버스트 구간 딜·장탄이 낮아짐 |

## impl/bug-fix용 검증 체크리스트

- [x] 표준 스쿼드의 B2 간격이 약 12.53초이고 그레이브가 매 사이클 B2를 사용할 것
- [x] `burst_cast`에서 `미래 예지`·`미래 예지 2~5`와 `라이터즈 스포일러 1~3`이
      모두 버프/인스턴트 로그에 나타날 것
- [x] 미래 예지 중 탄환을 소비하지 않고 재장전하지 않을 것 (`infinite_ammo`)
- [x] 일반 공격 15회마다 `과열 I`이 발동하고, 최대 장탄 재장전에서 제거될 것
- [x] 버스트 종료 시 `과열 II`·`과열 III`가 함께 꺼질 것 (남아 있으면 래치로 잘못 바뀐 것)
- [x] `과열 II`가 미래 예지 시작과 **동시가 아니라 30발 뒤**(≈2.5초), `과열 III`는 60발 뒤
      (≈5.0초)에 켜질 것 — 셋의 가동률이 서로 달라야 한다 (2026-09-11 실측 `S40_앨리스모더니아`:
      미래 예지 77.7% · 과열 II 58.8% · 과열 III 39.4%. 셋이 같으면 카운터가 죽은 것)
- [x] `방열 5·6`이 방열 상태에서만 아군 전체에 실릴 것 — 2026-08-28 확인
      (t=13.1 `state_end:미래 예지` → `burst_charge_speed_pct` 0 → 38.96, 다음 버스트에 다시 0)
- [x] `과열 I`이 최대 장탄 재장전 완료 시 제거될 것 (`event:full_reload` → `remove_named_buff`) —
      변형 1에서 사이클마다 1회(180초 9회). 표준 스쿼드는 완충 전에 버스트가 와서 `과열 I 제거` **0회**가
      정상이다(Q3) — 0회가 아니면 무한 장탄의 재장전 취소가 새는 것이다
- [ ] 자해로 `hp_pct`가 0에 수렴하되 **0에 도달하지 않을 것**
- [x] 버스트 종료 후 재장전이 4회로 늘 것 (`방열`의 재장전 비율 50% ▼) — 유저 실측과 일치
- [x] `탄환 100% 제거`로 장탄이 **음수가 되지 않을 것** — `handle_ammo_charge_pct`에
      0 하한이 없어 반쯤 남은 탄창에서 음수가 됐다(2026-08-28 수정)
- [x] `방열 3`의 제거 timing이 `on_attack` / `burst_cast` / `event:full_reload` OR로
      파싱되어 있을 것
- [x] 오토에서는 25% 클립 4회 사이에 일반 공격이 없고, 네 번째 클립에서만
      `event:full_reload`가 발생해 방열이 제거될 것
- [x] 수동으로 1~3번째 클립 뒤 재장전을 끊으면 첫 `on_attack`에서 방열이 제거될 것
- [x] 최대 장탄 재장전 전에 그레이브가 다음 버스트를 쓰면 `burst_cast`에서 방열이
      제거되고 `미래 예지`의 `infinite_ammo`로 전환될 것
