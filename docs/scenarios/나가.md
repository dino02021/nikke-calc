# 나가 동작 시나리오

> 모드: 보강 (parsed_skills 반영)
> **역순 작성** — 이미 등록·검증이 끝난 캐릭터의 시나리오를 뒤늦게 채운 문서다.
> 초안(파싱 전) 단계를 거치지 않았으므로 `## 유저 확인 사항`은 파싱 결과에서
> 역으로 확인한 항목만 담는다. 수치·발동 시각은 전부 실측이다.

## 기본 정보
- 버스트 단계: B2
- 본인 쿨다운: 20s (친구들과 함께라면!)
- 무기 유형: SG (9발, 재장전 1.5s, 1.5발/s, 펠릿 10, 계수 204.8%, 코어 200%)
- 속성 코드: 전격 · 클래스: 지원형 · 기업: 미실리스 · 스쿼드: 다즐링 펄
- 애장품: **없음**
- 작성 사이클 수: 1

## 효과 요약 (보강: 효과 단위 stat 키)

| ID (parsed name) | 스킬 | 트리거 (timing+condition) | stat: 값(lv10) | 대상 | 지속 |
|---|---|---|---|---|---|
| 우정의 가드 | 스킬1 | `on_attack_count:12` | `cover_heal_pct`: 14.57% | `all_allies` | — (instant) |
| 우정의 가드 2 | 스킬1 | `event:shield_applied` | `core_dmg_pct`: 85.17% | `all_allies` | 10s |
| 우정의 서포트 | 스킬2 | `on_attack_count:5` | `core_dmg_pct`: 40.07% | `allies_top_atk:2` | 5s |
| 우정의 서포트 2 | 스킬2 | `on_attack_count:5` | `heal_hp_pct`: 9.58% | `allies_lowest_hp:2` | — (instant) |
| 친구들과 함께라면! | 스킬3 | `burst_cast` | `pierce_enabled` | `self` | 10s |
| 친구들과 함께라면! 2 | 스킬3 | `burst_cast` | `atk_caster_based_pct`: 16.18% | `all_allies` | 10s |
| 친구들과 함께라면! 3 | 스킬3 | `burst_cast` + `during_shield` | `atk_caster_based_pct`: 31.02% | `all_allies` | 10s |

**킷의 주력이 코어 대미지다** — 우정의 가드 2(85.17) · 우정의 서포트(40.07)가 딜 기여의
본체이고 둘 다 `core_dmg_pct`(③)다. 그래서 이 캐릭터는 **적 모델에 코어가 있어야만 값이
나온다**(아래 §검증 스쿼드). 나머지는 `atk_caster_based_pct`(②) 두 줄이고,
`cover_heal_pct`는 ✅지만 엄폐물 체력은 보스 공격 패턴(`enemy.patterns`의 `attack`)이 있을 때만 깎이므로, 기본 경로에서는 가득 찬 엄폐물에 회복만 오르고 딜에 영향이 없다.

**나가 본인의 딜은 부산물이다** — 킷에 damage 타입 효과가 하나도 없다.

## 검증 스쿼드

이 캐릭터는 **표준 1개로 안 된다.** 두 축이 스쿼드·적 설정에 걸려 있다.

| 축 | 무엇이 걸리나 | 표준에서 |
|---|---|---|
| **보호막 공급자** | 우정의 가드 2(85.17) · 친구들과 함께라면! 3(31.02) | 둘 다 **영구 미발동** |
| **적 코어 유무** | 우정의 가드 2 · 우정의 서포트 = `core_dmg_pct` | `core_px` 기본 0 = 코어 없음 → **기여 0** |

- **표준 (B2 · 쿨 20s)**: `["리틀 머메이드", "나가", "test_B3", "스노우 화이트 : 헤비암즈"]`
  - 기본 사이클·`hit_count` 주기 검증용. **보호막 계열 2개는 여기서 안 걸린다** — 정상이다.
- **변형 1 (보호막 공급)**: `["리틀 머메이드", "나가", "크라운", "test_B3", "스노우 화이트 : 헤비암즈"]`
  + `config["burst_pattern"] = {"나가": "every:2"}`
  - 크라운 `라스트 킹덤 2`(`shield_from_max_hp_pct`, 15s)가 사실상 유일한 실전 공급원이다
    (전체 공급자는 크라운·플로라·센티·블랑 넷뿐이고, 레이는 작열 코드 아군 한정이라 전격인
    나가에게 안 간다).
  - 패턴이 없으면 왼쪽 우선 순서상 나가가 B2를 독점해 크라운이 영영 버스트하지 않는다 →
    **패턴을 걸어야 보호막 경로가 열린다.**
  - 보호막 15s > 사이클 12.53s라, 크라운이 격 사이클로만 버스트해도 나가 버스트 시점에
    보호막이 살아 있다 → 친구들과 함께라면! 3이 매번 붙는다.
- **변형 2 (코어 보스)**: 표준·변형 1에 `enemy={"core_px": 50}`
  - `core_dmg_pct`가 실제로 곱해지는지 확인. 코어가 없으면 킷의 주력이 통째로 죽는다.

- **실전 조합 (하네스)** — 나가는 1개 스쿼드에 들어 있다:

  | 스쿼드 | 멤버 | 적 | 비고 |
  |---|---|---|---|
  | `S39_나가라피` | 크라운 · **나가** · 아니스 : 스타 · 라피 : 레드 후드 · 프리바티 | 전격 | `burst_pattern {"나가": "every:2"}` · `burst_gauge_mode: accumulate` |

  **크라운과 한 몸이다** — 하네스의 유일한 자리도 크라운과 짝이고 패턴도 위 변형 1과 같다.
  다만 이 스쿼드의 적에는 `core_px`가 없어 **하네스 baseline에서는 코어 계열이 놀고 있다.**

실측 (180s, `--expected`, `first_burst_time=3.0`, 기본 스펙 1층):

| 스쿼드 | 코어 | 스쿼드 총딜 | 나가 |
|---|---|---|---|
| 표준 | 없음 | 1,185,152,492 | 116,630,740 (9.8%) |
| 표준 | 50px | 1,411,204,899 (**+19.1%**) | 117,922,050 (8.4%) |
| 변형 1 | 없음 | 2,089,663,761 | 203,555,060 (9.7%) |
| 변형 1 | 50px | 2,664,125,070 (**+27.5%**) | 206,440,800 (7.7%) |

코어를 켰을 때의 증가폭이 표준 +19.1% → 변형 1 +27.5%로 벌어지는 **+8.4%p가 우정의 가드 2**다.
나가 본인 딜이 코어 유무로 거의 안 움직이는 것(116.6M → 117.9M)이 이 킷의 성격을 그대로 보여준다.

## 사이클 타임라인 (표준)

`hit_count` 두 개가 사이클과 무관하게 자기 주기로 돈다 — 버스트 사이클(12.533s)과 어긋난다.

| t (실측) | 이벤트 | 발동 효과 (ID / stat) | 대상/지속 | 비고 |
|---|---|---|---|---|
| 2.683 | 사격 5회 누적 | 우정의 서포트(`core_dmg_pct` +40.07) · 우정의 서포트 2(heal) | `allies_top_atk:2` / 5s | 첫 버스트보다 먼저 온다 |
| 3.000 | 리틀 머메이드 stage:1 | — | — | |
| 3.100 | **나가 burst_cast** | 친구들과 함께라면!(`pierce_enabled`) · 2(`atk_caster_based_pct` +16.18) | self / all_allies · 10s | **3은 안 붙는다** (보호막 없음) |
| 3.200 | test_B3 stage:3 | — | — | |
| 3.250 | **full_burst 시작** | — | — | 나가 킷에 `full_burst_start` 효과 없음 |
| 6.017 | 사격 5회 누적 | 우정의 서포트 · 2 | 위와 동일 | 주기 **3.333s** |
| 7.350 | 사격 12회 누적 | 우정의 가드(`cover_heal_pct`) | all_allies | 엄폐물 회복 — 딜 무관 |
| 9.333 · 12.667 | 사격 5회 누적 | 우정의 서포트 · 2 | 위와 동일 | |
| 13.100 | 10s 만료 | 친구들과 함께라면! · 2 off | — | 풀버스트 종료(13.250)보다 **0.15s 먼저** |
| 13.250 | full_burst 종료 | — | — | |
| 15.333 | 사격 12회 누적 | 우정의 가드 | all_allies | 주기 **약 8.0s** |
| 15.633 | 나가 burst_cast (2사이클) | 친구들과 함께라면! · 2 | 위와 동일 | 이후 12.533s 등간격 |

사이클 간격: 3.25 → 15.783 → 28.317 → 40.85 → … **12.533s 등간격**(180s에 15회).
나가는 다른 B2가 없으면 **매 사이클 버스트한다**(쿨 20s < 12.533×2).

### 변형 1 (보호막 공급) 타임라인 — 다른 점만

| t (실측) | 이벤트 | 발동 효과 | 비고 |
|---|---|---|---|
| 3.100 | **크라운** stage:2 (`라스트 킹덤 2` 보호막) | **우정의 가드 2**(`core_dmg_pct` +85.17) → all_allies 10s | 나가 버스트가 아니라 **크라운 버스트가 켠다** |
| 13.100 | 10s 만료 | 우정의 가드 2 off | 보호막(15s)보다 먼저 꺼진다 |
| 15.633 | **나가** stage:2 | 친구들과 함께라면! · **2 · 3**(+31.02) | 보호막이 아직 살아 있어 3이 붙는다 |
| 25.650 | 10s 만료 | 셋 다 off | |
| 28.167 | 크라운 stage:2 | 우정의 가드 2 재발동 | 격 사이클로 번갈아 |

즉 **나가의 두 큰 버프가 서로 다른 사이클에 나눠 걸린다** — 우정의 가드 2는 크라운 사이클,
친구들과 함께라면! 3은 나가 사이클. 한 사이클에 몰아 넣을 방법이 없다(보호막 적용 이벤트가
그 순간뿐이라서다).

## 스킬 간 상호작용

- **보호막 공급자 → 우정의 가드 2 → 코어 대미지**
  - 경로: 크라운 `라스트 킹덤 2`(`shield_from_max_hp_pct`, `burst_cast`) → `event:shield_applied`
    → 우정의 가드 2(`core_dmg_pct` +85.17, 10s, all_allies).
  - 시점: 보호막이 *걸리는 순간*에만 평가된다. 보호막이 유지 중인 것으로는 재발동하지 않는다.
  - 위험: 보호막 공급자가 없으면 **킷 최대 버프가 통째로 죽는다.** 표준 스쿼드가 그 상태다.
- **보호막 유지 → 친구들과 함께라면! 3**
  - 경로: 보호막 활성(`during_shield` condition) → 나가 `burst_cast` 시 +31.02 추가.
  - 이쪽은 **유지 상태**를 본다(위와 판정이 다르다). 크라운 보호막 15s > 사이클 12.533s라
    격 사이클 공급으로도 매번 참이 된다.
  - 위험: 보호막이 12.533s보다 짧은 공급원으로 바뀌면 조용히 빠진다.
- **우정의 서포트 2(회복) → 크라운 `로얄 에타이어 4`** *(교차 캐릭터)*
  - 경로: 나가 회복(3.333s 주기) → `event:heal_received` → 크라운 `로얄 에타이어 4`
    (`atk_dmg_pct` +20.99, 7s, all_allies).
  - 주기 3.333s < 지속 7s라 **크라운의 이 버프가 상시로 켜진 채 유지된다.** 크라운 단독
    (표준 스쿼드)에서는 릴렉스 20중첩을 채워야만 나오는, 사이클당 1회짜리 버프다.
  - 나가+크라운 조합의 값어치가 보호막만이 아니라는 것 — 검증 항목으로 둔다.
- **`core_dmg_pct` × 적 코어** *(스쿼드가 아니라 적 설정에 달린 의존)*
  - `core_px = 0`이면 `is_core`가 영영 False라 ③ 계수가 곱해질 자리가 없다.
  - 나가를 평가할 때 **코어 없는 적으로 재면 무조건 과소평가**된다.

## 유저 확인 사항
- 초안 단계를 거치지 않아 신규 질문 없음. 아래는 파싱·코드에서 확인한 사실.
- Q: 우정의 가드(엄폐물 체력 회복)가 `event:shield_applied`를 일으켜 우정의 가드 2를 자가 발동시키나?
  A: **아니다.** `cover_heal_pct`는 엄폐물이고 보호막이 아니다. 표준 스쿼드 실측에서
  우정의 가드는 8s 주기로 도는데 우정의 가드 2는 **한 번도 발동하지 않았다.**
- Q: 우정의 서포트 2의 회복이 로그에 `-> 나가` 한 줄로만 찍히는데 대상이 자신뿐인가?
  A: **아니다.** 적용은 `allies_lowest_hp:2`로 정상 해석된다(`timeline._resolve_targets`).
  로그 쪽이 복합 target 문자열을 시전자로 폴백해 찍을 뿐이다(`buff_manager.py`의 instant
  이벤트 로그 분기). 실제 적용은 크라운의 `event:heal_received` 발동 시각이 정확히
  일치하는 것으로 확인된다.
- Q: `pierce_enabled` 10초가 딜에 기여하나?
  A: **표준·하네스 조합에서는 기여 0이다.** `pierce_enabled`는 일반 공격을 관통 대미지로
  분류해 `pierce_dmg_pct` 버프를 태우는 플래그인데, 두 조합 어디에도 `pierce_dmg_pct`
  공급자가 없다(공급원은 그레이브·츠바이·민트·에이드 : 에이전트 바니·프리카·도로시 :
  세렌디피티·D : 킬러 와이프·밀크 : 블루밍 바니). 단일 표적 sim이라 관통 자체의 다중
  타격 이득도 없다.

## 해석 선언

| 효과 | 해석 | 근거 | 대안이었다면 |
|---|---|---|---|
| 우정의 가드 2 | 보호막이 **걸리는 순간** 1회 평가 (`event:shield_applied`) | 원문 `■ 보호막 적용 시` · 실측에서 크라운 버스트 시각에만 발동 | "보호막 유지 중이면 상시"였다면 15s 내내 켜져 사이클당 커버리지가 10s→15s로 늘어난다 |
| 친구들과 함께라면! 3 | 보호막 **유지 상태** 조건 (`during_shield`) | 원문 `■ 자신이 보호막 적용 상태라면` | "적용된 순간"이었다면 크라운·나가 버스트가 같은 프레임이 아닌 한 영영 안 붙는다 |
| 우정의 가드 2 · 우정의 서포트 | `core_dmg_pct`(③) — 코어 히트에만 곱해진다 | 원문 `[코어 대미지 N% ▲]` · `damage._factor3` | `atk_dmg_pct`였다면 코어 없는 적에서도 전부 살아 표준 총딜이 크게 오른다 |
| 우정의 서포트 | `allies_top_atk:2` — **나가 본인은 거의 못 받는다** | 원문 `공격력 가장 높은 아군 2기` · 실측 대상이 딜러 2인 | `all_allies`였다면 나가 자신 딜도 오른다 (실측상 미미) |
| 친구들과 함께라면! 2·3 | 별개 효과의 **합산**(16.18 + 31.02 = 47.20) | 원문 `추가로` · 파싱이 두 줄 | 배타(31.02로 대체)였다면 보호막 시 총량이 16.18 낮다 |

## impl/bug-fix용 검증 체크리스트
- [ ] 표준: 나가 버스트 시 활성 = 친구들과 함께라면! · 2 (**2개만**)
- [ ] 표준: 우정의 가드 2 · 친구들과 함께라면! 3 **발동 0회** (보호막 공급자 없음 — 네거티브)
- [ ] 변형 1: 크라운 버스트 프레임에 우정의 가드 2가 all_allies로 1회, 10s
- [ ] 변형 1: 나가 버스트 시 활성 = 친구들과 함께라면! · 2 · 3 (**3개**)
- [ ] 우정의 서포트 주기 3.333s (사격 5회) · 우정의 가드 주기 약 8.0s (사격 12회)
- [ ] 사이클 간격 12.533s 등간격 · 180s에 15회 · 나가는 매 사이클 버스트 (표준)
- [ ] `core_px=0`과 `core_px=50`의 총딜 차이가 표준 +19.1% / 변형 1 +27.5% (코어 의존 확인)
- [ ] 발동 금지: 나가 본인 damage 타입 효과 — **킷에 하나도 없다**
- [ ] `cover_heal_pct`(우정의 가드)는 딜에 기여하지 않음 (✅ 엄폐물 회복 — 보스 공격 패턴이 있을 때만 의미)
- [ ] `pierce_enabled`는 `pierce_dmg_pct` 공급자가 없으면 딜 불변
- [ ] 상호작용: 나가+크라운에서 크라운 `로얄 에타이어 4`가 3.333s 주기로 재발동해 상시 유지
