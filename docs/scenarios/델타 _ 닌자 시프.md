# 델타 : 닌자 시프 동작 시나리오

> 모드: 보강 (parsed_skills 반영)
> **역순 작성** — 이미 등록·검증이 끝난 캐릭터의 시나리오를 뒤늦게 채운 문서다.
> 초안(파싱 전) 단계를 거치지 않았으므로 `## 유저 확인 사항`은 파싱 결과에서
> 역으로 확인한 항목만 담는다. 수치·발동 시각은 전부 실측이다.

## 기본 정보
- 버스트 단계: B2
- 본인 쿨다운: **40s** (비기 : 닌자 오버드라이브)
- 무기 유형: MG (300발, 재장전 2.5s, 연사 60→4200rpm 가속, 계수 5.57%, 코어 200%)
- 속성 코드: 수냉 · 클래스: **방어형** · 기업: 엘리시온 · 스쿼드: 스카우팅
- 애장품: **없음**
- 작성 사이클 수: **2** (쿨 40s — 본인 버스트 사이클과 미사용 사이클이 번갈아 온다)

## 효과 요약 (보강: 효과 단위 stat 키)

| ID (parsed name) | 스킬 | 트리거 (timing+condition) | stat: 값(lv10) | 대상 | 지속 |
|---|---|---|---|---|---|
| 인법 애시드 봄 | 스킬1 | `full_burst_start` | `received_dmg_pct`: 12.0% | `all_enemies` | 15s |
| 인법 애시드 봄 2 | 스킬1 | `burst_cast` | `atk_pct`: 15.04% | `self` | 10s |
| 인법 하이퍼 애시드 봄 | 스킬1 | `burst_cast` | `received_dmg_pct`: 8.0% | `enemies_nearest_in_range` | 10s |
| 인법 카모플라쥬 | 스킬2 | `battle_start` + `no_defender_ally` | `shield_from_max_hp_pct`: 12.25% | `self` | 10s |
| 주목 | 스킬2 | `battle_start` + `no_defender_ally` | `taunt` | `self` | 영구 |
| 인법 카모플라쥬 2 | 스킬2 | `battle_start` + `has_defender_ally` | `targeting_exclude` | `self` | — |
| 인법 인젝션 | 스킬2 | `battle_start` + `has_defender_ally` | `lifesteal_pct`: 11.22% | `self` | 영구 |
| 인법 카모플라쥬 3 | 스킬2 | `on_attack_count:200` + `self_state:주목` | `shield_from_max_hp_pct`: 12.25% | `self` | 10s |
| 인법 IFAK | 스킬2 | `every:4s` + `self_state:인법 인젝션` | `heal_overcharge_store_atk_pct`: 165.28% | `self` | 4s |
| 인법 IFAK 방출 | 스킬2 | `event:state_end:인법 IFAK` | `heal_overcharge_discharge` | `all_allies` | — |
| 비기 : 닌자 오버드라이브 | 스킬3 | `burst_cast` | `split_dmg_pct`: 20.0% | `all_allies` | 10s |
| 비기 : 닌자 오버드라이브 2 | 스킬3 | `burst_cast` | `atk_caster_based_pct`: 15.0% | `all_allies` | 10s |
| 비기 : 닌자 오버드라이브 3 | 스킬3 | `burst_cast` | `split_damage`: 170% | `all_enemies` | — |
| 비기 : 닌자 오버드라이브 4 | 스킬3 | `burst_cast` + `self_state:주목` | `next_shield_hp_pct`: 20.13% | `self` | 10s |
| 비기 : 닌자 오버드라이브 5 | 스킬3 | `burst_cast` + `self_state:인법 인젝션` | `accumulate_max_scale_pct`: 20.13% | `self` | 10s |

**킷의 절반이 미구현 stat이다** (`docs/IMPL-STATUS.md`):

| stat | 상태 | 근거 |
|---|---|---|
| `stealth` (카모플라쥬 2) | ✅ | 고르는 보스 공격(`random`·`top_atk`)의 후보에서 빠진다. 2026-09-05에 `targeting_exclude`에서 옮겼다 |
| `heal_overcharge_store_atk_pct` (인법 IFAK) | ❌ | 힐 모델 없음 |
| `heal_overcharge_discharge` (IFAK 방출) | ❌ | 힐 모델 없음 |
| `next_shield_hp_pct` (오버드라이브 4) | ✅ | 델타에게 다음으로 적용되는 보호막(주목 중 `인법 카모플라쥬 3` 등)의 생성량 ×1.2013 후 소모(2026-09-14). 보호막량만 바뀌어 딜 무관 — 방어형 없는 편성 실측 부여 6 · 소모 6 |
| `accumulate_max_scale_pct` (오버드라이브 5) | ❌ | 최대 누적량 증폭 미구현 |
| `taunt` (주목) | ✅ | 고르는 보스 공격의 자리를 먼저 가져간다. 기본 경로(보스 공격 없음)에서는 딜 무관 |
| `lifesteal_pct` (인법 인젝션) | ✅ | 다만 보스 공격 패턴(`enemy.patterns`의 `attack`)이 없는 기본 경로에서는 체력이 안 깎여 회복이 딜로 이어지지 않는다 |

> **`no_defender_ally` / `has_defender_ally`는 2026-09-02에 구현했다.** 그전에는 둘 다
> `_condition_ok`에 분기가 없어 **배타여야 할 두 분기가 동시에 성립**했다(전투 시작에
> 네 효과 전부 발동). 지금은 스쿼드에 방어형 아군이 있으면 `카모플라쥬 2`+`인젝션`,
> 없으면 `카모플라쥬`+`주목` 한쪽만 걸린다. **딜은 한 자리도 안 움직였다** — 딸린 효과가
> 전부 ❌·⚠️ stat이라서다(하네스 `S38_델타레이` L1 무변동, L2만 5건 감소).

**딜에 실제로 기여하는 건 다섯이다** — 인법 애시드 봄(⑥ 12.0, 15s, 매 사이클) ·
인법 하이퍼 애시드 봄(⑥ 8.0, 10s, 본인 버스트 사이클만) · 오버드라이브(⑥ `split_dmg_pct` 20) ·
오버드라이브 2(② 15.0) · 오버드라이브 3(`split_damage` 170%).

## 검증 스쿼드

- **표준 (B2 · 쿨 40s — 보조 B2 필요)**:
  `["리틀 머메이드", "델타 : 닌자 시프", "크라운", "test_B3", "스노우 화이트 : 헤비암즈"]`
  - 격 사이클 버스트와 `full_burst_start`/`burst_cast`의 분리를 잡는다.
  - **크라운이 방어형이라 `has_defender_ally` 분기가 걸린다** — `인법 카모플라쥬 2`
    (`targeting_exclude`) + `인법 인젝션`(`lifesteal_pct`). 보호막·주목 계열은 안 걸린다.
- **변형 1 (방어형 아군 없음)**: 크라운 자리를 **마스트 : 로망틱 메이드**(지원형 · B2 · 쿨 40s)로 교체.
  `["리틀 머메이드", "델타 : 닌자 시프", "마스트 : 로망틱 메이드", "test_B3", "스노우 화이트 : 헤비암즈"]`
  - `no_defender_ally` 분기(`인법 카모플라쥬` 보호막 + `주목`)로 갈린다. 실측 t=0.000에
    이쪽 둘만 붙고 `인법 인젝션`·`카모플라쥬 2`는 안 붙는다.
  - **이 변형이 스킬 2의 절반을 여는 유일한 길이다** — `인법 카모플라쥬 3`(`self_state:주목`)과
    `비기 : 닌자 오버드라이브 4`(같은 조건)가 여기서만 발동한다.
- **변형 2 (분배 대미지 수령자)**: `split_damage` 보유자를 넣는다 — 브래디 · 퀀시 : 이스케이프 퀸 ·
  홍련 : 흑영 · 팬텀 · 밀크 : 블루밍 바니 · 퀸(마코토) · 유키코.
  - `비기 : 닌자 오버드라이브`(`split_dmg_pct` +20, all_allies)가 곱해질 히트가 생긴다.
    표준 스쿼드에서는 **델타 자신의 오버드라이브 3**만 대상이다.
- **변형 3 (코어 보스)**: `enemy={"core_px": 30}` (하네스 `S38_델타레이`와 동일).
  MG 700히트/사이클이라 코어히트율이 개인 딜을 크게 바꾼다.

- **실전 조합 (하네스)** — 델타 : 닌자 시프는 1개 스쿼드에 들어 있다:

  | 스쿼드 | 멤버 | 적 | 비고 |
  |---|---|---|---|
  | `S38_델타레이` | **크라운** · 라피 : 레드 후드 · 아스카 : WILLE · **델타 : 닌자 시프** · 레이 (가칭) | 철갑 · **코어 30px** | `burst_pattern {"델타 : 닌자 시프": "every:2"}` · 라피 장전컨 |

  크라운(방어형 B2 20s)과 B2를 나눠 쓴다 — 표준 스쿼드와 같은 구도다.

실측 (180s, `--expected`, `first_burst_time=3.0`, 기본 스펙 1층):

| 스쿼드 | 스쿼드 총딜 | 델타 : 닌자 시프 |
|---|---|---|
| 표준 | 2,259,645,010 | 179,051,671 (7.9%) |
| `S38_델타레이` (라피 장전컨 미적용) | 6,051,373,736 | 277,292,117 (4.6%) |

## 쿨 40s가 격 사이클로 도는 이유

표준 템플릿의 리틀 머메이드 `버블 오더 2`(풀버스트 종료마다 all_allies `burst_cooldown_reduce`
−7.48s)가 40s를 두 사이클 만에 25.04s로 깎는다 — `docs/scenarios/앵커 _ 이노센트 메이드.md`와 같다.

실측 델타 버스트 시각: 3.100 → 28.167 → 53.233 → 78.300 → … (**간격 25.067s**).
짝수 사이클은 크라운이 채운다. 사이클 간격은 **12.533s 등간격**(180s에 15회).

> 리틀 머메이드가 없는 조합에서는 3~4 사이클에 한 번이 된다. 표준 결과를 실전 값으로
> 읽지 않는다.

## 사이클 타임라인 (표준, 2사이클)

### 사이클 1 — 델타 버스트 사이클

| t (실측) | 이벤트 | 발동 효과 (ID / stat) | 대상/지속 | 비고 |
|---|---|---|---|---|
| 0.000 | battle_start | **인법 카모플라쥬 2**(`targeting_exclude`) · **인법 인젝션**(`lifesteal_pct` 11.22, 영구) | self | 크라운이 방어형 → `has_defender_ally` 분기. 보호막·주목은 안 걸린다 |
| 3.000 | 리틀 머메이드 stage:1 | — | — | |
| 3.100 | **델타 burst_cast** | 오버드라이브(`split_dmg_pct` +20) · 2(② +15.0) · 3(`split_damage` 170%) · 4(❌) · 5(❌) · 인법 애시드 봄 2(`atk_pct` +15.04) · 인법 하이퍼 애시드 봄(⑥ +8.0) | all_allies / self / 적 · 10s | 오버드라이브 3은 **풀버스트 진입 전** 히트 |
| 3.250 | **full_burst 시작 (1회째)** | **인법 애시드 봄**(⑥ +12.0, **15s**) | 적 | |
| 4.017 · 8.017 · 12.000 | 4초 주기 | 인법 IFAK(❌ 미구현) | self / 4s | `self_state:인법 인젝션` |
| (변형 1에서만) 5.800 · 9.133 · 12.467 | MG 200발 누적 | 인법 카모플라쥬 3(보호막 12.25, 10s) | self | 주기 **약 3.33s** · `self_state:주목`이라 표준에서는 무발동 |
| 13.100 | 10s 만료 | 오버드라이브 1·2·4·5 · 애시드 봄 2 · 하이퍼 애시드 봄 off | | 풀버스트 종료(13.250)보다 0.15s 먼저 |
| 13.250 | full_burst 종료 | — | — | |

### 사이클 2 — 델타 미사용 사이클 (크라운이 B2)

| t (실측) | 이벤트 | 발동 효과 | 비고 |
|---|---|---|---|
| 15.633 | 크라운 stage:2 | — | 델타는 쿨 |
| **15.783** | **full_burst 시작 (2회째)** | **인법 애시드 봄**(⑥ +12.0, 15s) 재부여 | **델타가 버스트하지 않아도 걸린다** |
| (변형 1에서만) 16.667 · 20.000 · 23.333 | MG 200발 누적 | 인법 카모플라쥬 3 | 사이클과 무관한 자기 주기 |
| 28.167 | 델타 burst_cast (2회째) | 사이클 1과 동일 | 간격 25.067s |

**인법 애시드 봄이 이 캐릭터의 본체다** — `full_burst_start`라 **매 사이클** 걸리고,
15s > 사이클 12.533s라 **⑥ 12%가 무단절로 유지된다.** 쿨 40s의 손해가 여기엔 안 걸린다.
반면 하이퍼 애시드 봄(8%)과 오버드라이브 계열은 **본인 버스트 사이클에만** 걸려
전체 시간의 40%(10 ÷ 25.07)만 켜져 있다.

사이클 1 개인 딜 분해: 일반공격 13,304,434 (707히트, **100%**).
사이클 0(t<3.25)에만 오버드라이브 3이 283,611(1히트) 잡힌다 —
**버스트 딜이 풀버스트 진입 전 프레임에 터져 이전 사이클로 집계된다.**

## 스킬 간 상호작용

- **`full_burst_start`(애시드 봄) vs `burst_cast`(나머지 전부)** *(쿨 40s를 버티는 구조)*
  - 인법 애시드 봄만 스쿼드 풀버스트에 물려 있어 **델타가 쿨이어도 매 사이클** 걸린다.
    앵커 : 이노센트 메이드의 스킬 1·2와 같은 클래스다.
  - 나머지 8개는 전부 `burst_cast`라 격 사이클이다. **두 그룹의 커버리지가 100% vs 40%로
    갈린다** — 델타를 평가할 때 한 덩어리로 보면 안 된다.
- **`battle_start` 분기가 스킬 2 전체의 갈림길이다** *(스쿼드 구성 의존)*
  - `battle_start`에 켜지는 영구 마커(`주목` / `인법 인젝션`)가 **이후 4개 효과의 게이트**다:

    | 분기 | 켜지는 마커 | 그에 매달린 효과 |
    |---|---|---|
    | `no_defender_ally` | `주목` | 인법 카모플라쥬 3(보호막, 3.33s 주기) · 비기 : 닌자 오버드라이브 4 |
    | `has_defender_ally` | `인법 인젝션` | 인법 IFAK(4s 주기) · 비기 : 닌자 오버드라이브 5 |

  - **전투 시작 1회 판정이고 스쿼드 구성은 안 바뀌므로 전투 내내 한쪽만 산다.**
    `_RUNTIME_COND_PREFIXES`에 넣지 않은 이유이기도 하다.
  - 딜 영향은 양쪽 다 0이다 — 네 효과가 전부 ❌·⚠️ stat이다. 그래도 분기가 없으면
    L2(발동 횟수)가 통째로 거짓말을 하므로 조건을 구현했다.
- **보호막은 자기 자신에게만 간다** *(크라운과 대비)*
  - `shield_from_max_hp_pct` target이 `self`다. 크라운 `라스트 킹덤 2`(all_allies)와 달리
    **나가 `우정의 가드 2` 같은 아군 보호막 트리거를 열지 않는다.**
  - 게다가 `주목` 분기(방어형 아군 없음)에서만 나온다 — 방어형과 함께 쓰는 표준·하네스
    구도에서는 델타가 보호막을 아예 만들지 않는다.
- **`split_dmg_pct`(오버드라이브) × `split_damage` 보유자** *(교차 캐릭터, 조건부)*
  - `split_dmg_pct`는 ⑥에서 `is_split=True` 히트에만 합산된다. 표준 스쿼드에는
    델타 자신의 오버드라이브 3(사이클당 1히트)뿐이라 **버프가 걸려도 곱해질 히트가 거의 없다.**
  - 앵커 : 이노센트 메이드 `불가사리(모양) 오므라이스 2`와 완전히 같은 구조다 —
    L2는 정상인데 L1이 안 움직이는 형태.
- **`lifesteal_pct`(인법 인젝션)는 딜로 이어지지 않는다**
  - `event:heal_received`를 발생시키는 경로이긴 하지만, 실측 표준 스쿼드에서 크라운
    `로얄 에타이어 4`가 **t=17.667 한 번(크라운 자신의 릴렉스 회복)만** 붙었다.
    델타의 흡혈이 크라운 버프를 상시화하지 않는다 — 나가 `우정의 서포트 2`와 다르다.

## 유저 확인 사항
- 초안 단계를 거치지 않아 신규 질문 없음. 아래는 파싱·코드에서 확인한 사실.
- **Q: `[스쿼드 구성 별 효과] [해당되는 효과만 적용]`이 실제로 배타로 도나?**
  A: **지금은 그렇다 (2026-09-02 수정).** 그전에는 `no_defender_ally`·`has_defender_ally`가
  둘 다 `_condition_ok`에 분기가 없어 t=0.000에 네 효과가 전부 붙었다.
  판정은 `parsed_nikke["class"] == "방어형"`(자신 제외)이고, 한 분기에서 두 조건을 함께 처리해
  **한쪽만 구현되는 상태가 다시 생기지 않게** 했다.
  실측: 표준(크라운=방어형) → `카모플라쥬 2`+`인젝션` 2건 / 변형 1(마스트 : 로망틱 메이드)
  → `카모플라쥬`+`주목` 2건. 총딜은 양쪽 다 수정 전과 동일하다.
- Q: 델타가 버스트하지 않은 사이클에도 인법 애시드 봄이 걸리나?
  A: **걸린다.** `full_burst_start`는 스쿼드 풀버스트 진입을 세지 델타의 버스트를 보지 않는다.
  실측 t=15.783(크라운이 B2를 쓴 사이클)에 정상 발동.
- Q: `비기 : 닌자 오버드라이브 3`이 개인 딜 분해에서 사이클 1에 안 보이는데?
  A: **풀버스트 진입 0.15s 전(`burst_cast`)에 터져 이전 사이클로 집계된다.**
  실측 사이클 0(t=0~3.25)에 283,611 / 1히트로 잡혀 있다.
- Q: `인법 IFAK`(4초 충전 후 방출)이 딜에 기여하나?
  A: **0이다.** `heal_overcharge_store_atk_pct`·`heal_overcharge_discharge` 둘 다 ❌ 미구현이다.
  로그에는 `every:4s` 부여와 `event:state_end` 방출이 정상적으로 찍히지만 값이 없다.

## 해석 선언

| 효과 | 해석 | 근거 | 대안이었다면 |
|---|---|---|---|
| 인법 애시드 봄 | `full_burst_start` = **스쿼드 풀버스트마다**(델타 버스트 무관), 15s | parsed timing · 실측 t=15.783 발동 | `burst_cast`였다면 격 사이클로 떨어져 ⑥ 커버리지가 100% → 40%가 된다 |
| 인법 카모플라쥬 / 인법 인젝션 | **배타 분기** — 방어형 아군 유무로 한쪽만 (2026-09-02 구현) | `parsed_nikke["class"]` 판정 · 실측 표준 2건 / 변형 1 2건 | 조건이 없으면(수정 전) 네 효과가 전부 걸려 L2가 두 배가 된다 |
| 인법 카모플라쥬 3 | 보호막 target **`self`** (all_allies 아님) | parsed target | all_allies였다면 아군의 `event:shield_applied` 트리거를 3.33s마다 열어 준다 (나가 등) |
| 비기 : 닌자 오버드라이브 3 | `split_damage` — `split_dmg_pct` 증폭 대상 | parsed stat · `damage._factor6` | `damage`였다면 자기 버프(오버드라이브 1)를 못 받는다 |
| 비기 : 닌자 오버드라이브 | `split_dmg_pct`(⑥) — `is_split` 히트에만 | parsed stat | `atk_dmg_pct`였다면 스쿼드 구성과 무관하게 전 히트에 실린다 |
| 인법 하이퍼 애시드 봄 | `enemies_nearest_in_range` — 쫄몹이 없으면 `__enemy__`, 쫄몹이 있으면 **조준 규칙**이라 조준 가중치가 가장 큰 1기(동률 보스)에 붙는다 | `_resolve_target()` · `boss_pattern` §쫄몹 | 원문 「공격 범위 내 적들」대로 범위 안 전원이었다면 쫄몹마다 받는 대미지 ▲가 붙는다 — 좌표가 없어 범위 판정은 하지 않는다 |

## impl/bug-fix용 검증 체크리스트
- [ ] t=0.000에 스킬 2의 battle_start 효과가 **한 분기 2건만** 발동
      (방어형 아군 있음 → 카모플라쥬 2 · 인젝션 / 없음 → 카모플라쥬 · 주목)
- [ ] 인법 애시드 봄이 **매 풀버스트 진입마다** 발동, 15s → ⑥ 12% 무단절 (델타 미버스트 사이클 포함)
- [ ] 하이퍼 애시드 봄 · 오버드라이브 1~5 · 애시드 봄 2는 **델타 버스트 사이클에만** (커버리지 40%)
- [ ] 델타 버스트 간격 25.067s · 짝수 사이클은 보조 B2가 채움
- [ ] 사이클 간격 12.533s 등간격 · 180s에 15회
- [ ] 인법 카모플라쥬 3은 **`주목` 분기에서만** 발동 (주기 약 3.33s, MG 200발) ·
      대상은 델타 자신뿐 · 표준(방어형 아군 있음)에서는 **0회** (네거티브)
- [ ] 인법 IFAK가 `every:4s` 부여 → 4초 뒤 `event:state_end`로 방출, **딜 기여 0** (❌ 미구현)
- [ ] 오버드라이브 3이 풀버스트 진입 **0.15s 전**에 1히트 (이전 사이클로 집계)
- [ ] 개인 딜이 사실상 100% 일반공격 (MG 707히트/사이클)
- [ ] 딜 무관 확인: `targeting_exclude` · `accumulate_max_scale_pct` ·
      `heal_overcharge_*` (전부 ❌) · `next_shield_hp_pct` · `taunt` (✅, 딜 무관)
- [ ] `split_dmg_pct`는 `split_damage` 보유자가 없으면 곱해질 히트가 델타 자신의 1히트뿐
