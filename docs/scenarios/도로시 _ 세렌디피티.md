# 도로시 : 세렌디피티 동작 시나리오

> 모드: 보강 (parsed_skills 반영)
> **역순 작성** — 이미 등록·검증이 끝난 캐릭터의 시나리오를 뒤늦게 채운 문서다.
> 초안(파싱 전) 단계를 거치지 않았으므로 `## 유저 확인 사항`은 파싱 결과에서
> 역으로 확인한 항목만 담는다. 수치·발동 시각은 전부 실측이다.

## 기본 정보
- 버스트 단계: B3
- 본인 쿨다운: **40s** (위구원)
- 무기 유형: SG (**9발**, 재장전 1.5s, 계수 201.5%, **펠릿 10**, 총구 1, 코어 200%, 연사 1.5/s, 탄착군 250px)
- 속성 코드: 수냉 · 클래스: 화력형 · 기업: 필그림 · 스쿼드: 인헤르트
- **애장품 없음** · **기본 레이어 없음** (`data/char_defaults.json`에 항목이 없다)
- 작성 사이클 수: **2** (쿨 40s)

> **딜의 절반이 히트 수의 3.7%에서 나온다.** 평소에는 펠릿 10~15개를 뿌리지만,
> 펠릿 80회마다 `섬광`이 열려 **3발 동안 펠릿 1개**가 되고 그 한 발이 보통 한 발의
> 1.85배를 때린다. 펠릿은 계수를 나누는 단위라(§펠릿) 개수를 줄여도 총량은 그대로인데,
> `섬광`이 얹는 공격 대미지 72%·관통 대미지 55.08%가 **그 한 방에 통째로 실리기** 때문이다.

## 효과 요약 (보강: 효과 단위 stat 키)

| # | ID (parsed name) | 스킬 | 트리거 (timing+condition) | stat: 값(lv10) | 대상 | 지속 |
|---|---|---|---|---|---|---|
| 0 | 섬광 | 스킬1 | `pellet_hit_count:80` | `pierce_enabled` | `self` | **3발** |
| 1 | 섬광 2 | 스킬1 | `pellet_hit_count:80` | `accuracy_pct`: 98.18% | `self` | 3발 |
| 2 | 섬광 3 | 스킬1 | `pellet_hit_count:80` | `atk_dmg_pct`: 72% | `self` | 3발 |
| 3 | 섬광 4 | 스킬1 | `pellet_hit_count:80` | `pellet_count_fixed`: **1** | `self` | 3발 |
| 4 | 섬광 5 | 스킬1 | `pellet_hit_count:160` | `pierce_range`: 200% | `self` | 3발 |
| 5 | 광익 | 스킬2 | `battle_start` | `pierce_dmg_pct`: 55.08% | `self` | 영구 |
| 6 | 광익 2 | 스킬2 | `passive` + `during_full_burst` | `atk_pct`: 75.24% | `self` | 풀버스트 동안 |
| 7 | 광익 3 | 스킬2 | `passive` + `during_full_burst` | `accuracy_pct`: 40.68% | `self` | 풀버스트 동안 |
| 8 | 위구원 | 스킬3 | `burst_cast` | `attack_speed_pct`: 65% | `self` | 15s |
| 9 | 위구원 2 | 스킬3 | `burst_cast` | `atk_pct`: 88.12% | `self` | 15s |
| 10 | 위구원 3 | 스킬3 | `burst_cast` | `pellet_count`: **+5** | `self` | 15s |

**전 효과가 `self`다.** 아군에게 가는 버프가 하나도 없어 순수 딜러다.

**기본 경로에서 딜에 안 실리는 stat 1건** — `pierce_range`(섬광 5, `IMPL-STATUS §pierce_range`).
보스 스크립트의 reach 표적(좌표 off — 200%라 2단계까지)·좌표 모드의 관통 원에만 쓰인다. 기본 경로에서는
실측 20회 발동하지만 딜을 바꾸지 않는다.

## 검증 스쿼드

- **표준 (B3 · 쿨 40s)**:
  `["리틀 머메이드", "크라운", "도로시 : 세렌디피티", "test_B3"]`
  - 적 코드는 **수냉**(자기 코드 = 우월 코드 불성립)이 기준이다.
- **변형 1 (코어 보스 — 필수)**: 표준 + `enemy={"core_px": 52}`
  - 명중률 버프 두 개(`섬광 2` 98.18 · `광익 3` 40.68)가 여기서만 산다. 개인 딜 **+34.4%**.
- **변형 2 (우월 코드)**: 표준 + `enemy={"code": "작열"}` — 개인 딜 **×2.18**.
  캐릭터 스킬이 아니라 **공통 장비·큐브의 `element_bonus` 107.69%**가 만드는 배수다
  (`damage.py §⑦` = 1 + 0.1 + 1.0769). 도로시 고유 효과는 아니다.

- **실전 조합 (하네스)** — 도로시 : 세렌디피티는 **4개 스쿼드**에 들어 있다:

  | 스쿼드 | 적 | 스쿼드 총딜 | 도로시 : 세렌디피티 | 섬광 |
  |---|---|---|---|---|
  | `S40_토브드레이크` | 풍압 · **코어 52px** | 6,489,542,503 | 1,967,436,665 (30.3%) | 50 |
  | `S38_누아르` | 철갑 (SG 적정 사거리) | 4,251,363,963 | 1,564,454,860 (36.8%) | 51 |
  | `커버_레오나슈가` | 수냉 | 2,261,946,273 | 1,014,721,853 (44.9%) | 59 |
  | `S39_소다` | 전격 | 3,805,034,966 | 955,210,958 (25.1%) | 42 |

  네 자리 모두에서 **딜이 전부 `기본 공격` 한 줄**이다 — 스킬 딜이 0인 캐릭터라
  breakdown이 항상 한 행이다. 조합 차이는 `섬광` 횟수와 버프 총량으로만 나타난다.

실측 (180s, `--expected`, `first_burst_time=3.0`, 기본 스펙 1층):

| 설정 | 스쿼드 총딜 | 도로시 : 세렌디피티 |
|---|---|---|
| 표준 (적 수냉) | 1,589,273,719 | **781,702,760 (49.2%)** |
| 표준 · **코어 52px** | 2,102,429,288 | 1,050,781,819 (50.0%) |
| 표준 · 적 **작열**(우월 코드) | 2,509,258,980 | 1,701,688,021 (67.8%) |

딜 구성 (표준 · 적 수냉) — 스킬 딜이 없으므로 **히트 태그로 가른다**:

| 태그 | 딜 | 비중 | 히트 | 히트당 |
|---|---|---|---|---|
| `pellet:N` (평소 발) | 397,106,135 | 50.8% | 3,215 | 123,516 |
| `normal` (**`섬광` 창의 1펠릿 발**) | 384,596,625 | **49.2%** | **123** | **3,126,801** |

버스트 구간별: **풀버스트(본인) 59.6%** · 풀버스트(타인) 30.4% · 비풀버스트 9.9%.

## 펠릿은 계수를 나누는 단위다 — 그래서 `위구원 3`은 딜을 안 올린다

`_fire()`는 펠릿당 계수를 `damage_coeff / split`으로 준다(`CALCULATOR.md §_fire`,
`DATA_VERIFY.md §총구 수`). 한 발의 **총 계수는 펠릿 개수와 무관하게 201.5%**다.

| 상태 | 펠릿 | 펠릿당 계수 | 실측 펠릿당 딜(풀버스트) | 발당 총딜 |
|---|---|---|---|---|
| 평소 | 10 | 20.15% | — | 201.5% |
| `위구원 3` 중 | **15** | 13.43% | 137,447 | 2,061,705 (= 201.5%) |
| `섬광 4` 중 | **1** | **201.5%** | 3,804,082 | 3,804,082 |

그래서 `위구원 3`(`펠릿 개수 +5`)이 **직접 올리는 딜은 0**이다. 그 대신 두 가지를 한다:

1. **`섬광`을 1.5배 빨리 연다** — 트리거가 `pellet_hit_count:80`이라 발당 펠릿이
   10 → 15가 되면 같은 발수로 1.5배의 카운트를 쌓는다.
2. **버스트 게이지를 1.5배 만든다** — SG는 게이지가 **펠릿당** 0.4다
   (`mechanics/버스트 게이지.md`). `fixed` 모드에서는 사이클이 안 움직이므로
   표준 실측에는 안 나타나지만, `accumulate` 모드·실전에서는 사이클이 당겨진다.

> `섬광 4`(`pellet_count_fixed: 1`)는 `위구원 3`(`pellet_count: +5`)을 **덮는다** —
> `_fire()`가 fixed를 먼저 보고 절대값으로 고정한다. 원문 `[펠릿 개수 1개로 고정]`의
> "고정"을 그대로 읽은 것이다.

## `섬광` 창이 딜의 절반이다

`섬광` 1~4는 한 트리거(`pellet_hit_count:80`)에 같이 붙고 **3발 유지**다.
`섬광 4`가 펠릿을 1개로 만드는 순간 계수 201.5%가 한 히트에 모이고, 거기에
`섬광 3`(공격 대미지 +72)과 `광익`(관통 대미지 +55.08 — `섬광`이 켠 `pierce_enabled`
히트에만 실린다)이 함께 들어간다.

| | 평소 발 (15펠릿) | `섬광` 발 (1펠릿) |
|---|---|---|
| 발당 딜 (표준·풀버스트) | 2,061,705 | **3,804,082** |
| 배수 | 1.00 | **1.85** |

180초에 `섬광` **41회 × 3발 = 123발**. 전체 3,338히트의 **3.7%**가 딜의 **49.2%**를 낸다.

### `섬광`이 다음 `섬광`을 늦춘다 *(자기 억제)*

`섬광` 창의 3발은 펠릿을 1개씩만 만든다. 그래서 그 3발이 다음 80펠릿 카운트에
**45가 아니라 3만** 넣는다. 실측 간격(본인 버스트 사이클, 풀버스트 안):

| 발동 시각 | 간격 |
|---|---|
| 3.750 → 7.383 | 3.633 |
| 7.383 → 10.617 | 3.234 |
| 10.617 → 14.267 | 3.650 |
| 14.267 → 17.500 | 3.233 |

펠릿만 세면 80 ÷ (15 × 2.475발/s) = 2.15초여야 하는데, 창의 3발이 42펠릿을
까먹어 **1.5배로 늘어난다**. `섬광 5`(160펠릿)가 41회 중 **20회**만 나오는 것도
같은 이유다 — 정확히 두 번에 한 번씩 열린다.

## 명중률 두 줄은 코어 보스에서만 산다

`섬광 2`(98.18)와 `광익 3`(40.68)은 `accuracy_pct`다. 이 sim에서 명중률은
**탄착군 → 코어히트 확률**로만 쓰인다(`mechanics/명중률 탄착군.md`).
`enemy.core_px`가 0(기본)이면 계산 경로 자체가 안 돈다.

SG 탄착군은 250px으로 전 무기 중 가장 크다. `D = 250 × (1 − 0.00908 × 명중%)`:

| 구간 | 명중% | 탄착군 D | 코어 확률 (코어 52px) |
|---|---|---|---|
| 비풀버스트 | 0 | 250px | 1.8% |
| 풀버스트 (`광익 3`) | 40.68 | 157.7px | 6.0% |
| **`섬광` 창** (`+섬광 2`) | **138.86** | **1px** (하한) | **100%** |

명중률이 110%를 넘으면 `_current_spread()`의 하한 1px에 붙어 **모든 펠릿이 코어**가 된다.
실측(코어 52px)에서 `섬광` 123히트가 전부 `core` 태그로 잡히고 히트당
3,126,801 → **5,200,108 (×1.66)**이 된다. 나머지 3,215 펠릿은 123,516 → 127,890
(**+3.5%**)뿐이다 — 개인 딜 +34.4%는 거의 전부 `섬광` 창에서 나온 것이다.

## 사이클 타임라인 (표준, 2사이클)

### 사이클 1 — 도로시 버스트 사이클

| t (실측) | 이벤트 | 발동 효과 (ID / stat) | 비고 |
|---|---|---|---|
| 0.000 | 전투 시작 | **광익**(관통 대미지 +55.08, 영구) | 이 시점에는 관통이 없어 놀고 있다 |
| 3.000 · 3.100 | 리틀 머메이드 · 크라운 | | |
| **3.200** | **도로시 burst_cast** | **위구원**(공속 +65) · **위구원 2**(공격력 +88.12) · **위구원 3**(펠릿 **+5**) | 15초 유지 |
| 3.250 | full_burst 시작 | | |
| **3.267** | 풀버스트 판정 | **광익 2**(공격력 +75.24) · **광익 3**(명중률 +40.68) | `passive` + `during_full_burst` |
| 3.350 · 3.750 | 15펠릿 발사 | | 펠릿당 137,447 |
| **3.750** | **펠릿 80회 도달** | **섬광 1~4** (관통 · 명중률 +98.18 · 공격 대미지 +72 · **펠릿 1개 고정**) | 3발 유지 |
| 4.150 · 4.550 · 4.950 | **1펠릿 발사 ×3** | | **발당 3,804,082** |
| 4.950 | 섬광 1~4 소멸 | | 3발을 다 썼다 |
| **7.383** | 펠릿 80회 (2회째) | 섬광 1~4 + **섬광 5**(관통 범위 — 기본 경로 0) | 160펠릿 누적 |
| 10.617 · 14.267 | 펠릿 80회 (3·4회째) | | 14.267에 섬광 5 동반 |
| 11.833 → 12.233 | 재장전 | | **0.4초** (`원 포 올 2` 재장전 +44.35) |
| 13.250 | full_burst 종료 | 광익 2·3 off | |
| 15.467 | 섬광 소멸 | | |

### 사이클 2 — 도로시 미사용 사이클 (test_B3이 B3)

| t (실측) | 이벤트 | 발동 효과 | 비고 |
|---|---|---|---|
| 15.533 · 15.633 · 15.733 | 리틀 머메이드 · 크라운 · test_B3 | | 도로시는 쿨 |
| 15.783 | full_burst 시작 (2회째) | | |
| 15.800 | | **광익 2·3** | 자기 버스트와 무관하게 걸린다 |
| **17.500** | 펠릿 80회 | 섬광 1~4 | 간격 3.233 — 위구원이 아직 살아 있다 |
| **18.200** | **위구원 1~3 만료** | | 여기서부터 펠릿이 10개로 돌아간다 |
| **28.267** | 도로시 burst_cast (2회째) | | **간격 25.067s** |

사이클 간격 **12.533s 등간격**(180초 15회), 도로시 버스트 8회.
사이클별 딜은 **본인 버스트 사이클 ≈74M / 미사용 사이클 ≈35M**로 **2.1배** 차이가 난다
(사이클 1 73,486,539 · 2 36,025,517 · 3 75,746,205 · 4 33,038,166 …).

## 스킬 간 상호작용

- **`위구원 3` → `섬광` 주기** *(펠릿은 딜이 아니라 카운터다)*
  - 경로: 펠릿 10 → 15 → `pellet_hit_count:80`이 1.5배 빨리 찬다.
  - `위구원 3`의 딜 기여는 **직접적으로는 0**이고, 전부 이 경로로만 나온다.
- **`섬광 4` → `광익` · `섬광 3`** *(계수를 한 히트로 모은다)*
  - 펠릿을 1로 고정해 201.5% 전량을 한 히트로 만들고, 그 히트에 `pierce_enabled`가
    붙어 `광익`(관통 대미지 55.08)이 처음으로 실린다.
  - `광익`은 **전투 시작 시 영구**지만 `섬광` 창 밖에서는 죽어 있다 —
    `is_pierce_damage` 히트에만 가산되기 때문이다(`damage.py §⑤`).
- **`섬광 2` ↔ 적 코어** *(적 의존)*
  - 명중률 98.18은 코어가 있는 적에게만 값이 있다. 하네스 4자리 중 코어가 있는 것은
    `S40_토브드레이크`(52px) 하나다.
- **`섬광` → 다음 `섬광`** *(자기 억제)*
  - 창 안의 3발이 펠릿을 3개만 만들어 다음 80펠릿을 늦춘다. 간격이 이론값 2.15초가
    아니라 실측 3.2~3.7초인 이유다.
- **`위구원`(공속 +65) → `섬광` 주기** *(같은 방향)*
  - 발사 간격이 0.667s → 0.404s가 되어 펠릿 누적도 같은 비율로 빨라진다.

## 유저 확인 사항
- 초안 단계를 거치지 않아 신규 질문 없음. 아래는 파싱·코드에서 확인한 사실.
- Q: `위구원 3`(펠릿 +5)이 딜을 하나도 안 올리는 게 맞나?
  A: **이 계산기의 펠릿 해석에서는 맞다.** 펠릿은 계수를 나누는 단위라
  (`DATA_VERIFY §총구 수` — 츠바이 10펠릿 실측으로 세운 규약) 한 발의 총 계수가
  펠릿 수와 무관하다. 실측으로도 10펠릿 발과 15펠릿 발의 발당 총딜이 같다.
  올리는 것은 `섬광` 주기와 버스트 게이지 두 가지뿐이다.
- Q: `섬광` 발이 왜 평소 발의 1.85배인가? 계수 총량은 같은데.
  A: **`섬광 3`(공격 대미지 +72)과 `광익`(관통 대미지 +55.08)이 그 발에만 붙어서다.**
  계수 총량은 같고 DealForm ⑤가 커진다.
- Q: `섬광 5`(관통 범위 200%)는 왜 아무것도 안 하나?
  A: **기본 경로에는 관통 범위가 닿을 표적이 없다.** `pierce_range`는 보스 스크립트의 reach 표적·좌표 모드의
  관통 원에만 쓰인다(`IMPL-STATUS`). 실측 20회 발동하지만 딜에 안 실린다.
- Q: 명중률 두 줄이 표준 스쿼드에서 왜 안 보이나?
  A: **`enemy.core_px`가 0이기 때문**이다. 명중률은 탄착군 → 코어히트 확률로만 쓰인다.
  코어 52px을 켜면 `섬광` 창이 100% 코어가 되고 개인 딜이 +34.4%가 된다.

## 해석 선언

| 효과 | 해석 | 근거 | 대안이었다면 |
|---|---|---|---|
| 섬광 1~4 | 한 트리거(`pellet_hit_count:80`)에 묶인 4개 효과, **3발 유지** | 원문 `■ 펠릿 80회 명중 시` + `[3발 유지]` ×4 | 발 단위가 아니라 초 단위였다면 창 길이가 공속에 안 묶인다 |
| 섬광 4 | `pellet_count_fixed: 1` — `위구원 3`의 가산을 **덮는다** | 원문 `[펠릿 개수 1개로 고정]` · `_fire()`가 fixed 우선 | 가산이었다면 15+1=16펠릿이 되어 이 캐릭터의 딜 구조가 사라진다 |
| 섬광 5 | `pellet_hit_count:160` — 80과 **별도 카운터가 아니라 같은 누적** | 실측 41회 : 20회 = 정확히 2:1 | 별도였다면 두 트리거가 어긋나 동반 발동이 안 나온다 |
| 광익 | `pierce_dmg_pct` — **`pierce_enabled` 히트에만** | `damage.py §⑤` `is_pierce_damage` 분기 | 상시 가산이었다면 평소 발도 1.55배가 된다 |
| 광익 2·3 | `passive` + `during_full_burst` — **누가 버스트했든** | 원문 `풀 버스트 타임 지속 중일 때 한하여` · 실측 15회 | `burst_cast`였다면 격 사이클로 반토막 난다 |
| 위구원 3 | `pellet_count: +5` (고정값, 레벨별 3/4/5) | 원문 `[펠릿 개수 {2}개 ▲]` lv10=5 | — |
| 펠릿 | **계수를 나누는 단위** (총 계수 불변) | `DATA_VERIFY §총구 수` · 츠바이 10펠릿 유저 실측 | 펠릿마다 전량이었다면 `섬광 4`가 딜 1/15짜리 페널티가 된다 |
| 명중률 | 탄착군 직경 → 코어히트 확률로만 (하한 1px) | `mechanics/명중률 탄착군.md` · `_current_spread()` | 명중/빗나감 판정이었다면 코어 없는 적에서도 값이 생긴다 |

## impl/bug-fix용 검증 체크리스트
- [ ] 이탈 보고가 **`기본 스펙(1층) 그대로`** 한 줄 (레이어·애장품 없음)
- [ ] `섬광` 1~4 **41회** · `섬광 5` **20회** (정확히 2:1)
- [ ] `normal` 태그 히트 **123회** = 41 × 3발, 전체 3,338히트의 3.7%
- [ ] `normal` 히트가 개인 딜의 **49.2%** (384,596,625 / 781,702,760)
- [ ] `섬광` 발 3,804,082 : 평소 발 2,061,705 = **1.85배** (풀버스트·표준)
- [ ] 15펠릿 발과 10펠릿 발의 **발당 총딜이 같다** (펠릿은 계수 분할 — 네거티브)
- [ ] `섬광` 간격 실측 **3.23~3.65초** (이론 2.15초가 아니다 — 창의 3발이 42펠릿을 깎는다)
- [ ] 코어 52px에서 `섬광` 히트가 **전부 `core` 태그**, 개인 딜 **+34.4%**
- [ ] 코어 52px에서 나머지 펠릿은 **+3.5%**뿐 (명중률 40.68로는 탄착군이 157.7px)
- [ ] 도로시 버스트 간격 **25.067s**, 180초 8회 · 사이클 간격 12.533s
- [ ] 본인 버스트 사이클(≈74M)이 미사용 사이클(≈35M)의 **2.1배**
- [ ] 스킬 딜이 **0** — breakdown이 `기본 공격` 한 행뿐 (네거티브)
