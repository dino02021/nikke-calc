# 레이븐 동작 시나리오

> 모드: 보강 (parsed_skills 반영)
> **역순 작성** — 이미 등록·검증이 끝난 캐릭터의 시나리오를 뒤늦게 채운 문서다.
> 초안(파싱 전) 단계를 거치지 않았으므로 `## 유저 확인 사항`은 파싱 결과에서
> 역으로 확인한 항목만 담는다. 수치·발동 시각은 전부 실측이다.

## 기본 정보
- 버스트 단계: B3
- 본인 쿨다운: **40s** (템페스트)
- 무기 유형: RL (6발, 재장전 2.0s, 계수 61.3%, 코어 200%, 차지 1.0s, 풀차지 배율 250%)
- 속성 코드: 철갑 · 클래스: 화력형 · 기업: 어브노말 · 스쿼드: 2차 강하 부대
- 애장품: **없음**
- 작성 사이클 수: **2** (쿨 40s)
- 기본 레이어: **없음** (`data/char_defaults.json` 미등록 — 1층 그대로다)

## 효과 요약 (보강: 효과 단위 stat 키)

| ID (parsed name) | 스킬 | 트리거 (timing+condition) | stat: 값(lv10) | 대상 | 지속 |
|---|---|---|---|---|---|
| 쇼크웨이브 (dot) | 스킬1 | `full_charge_fire` | `dot_damage`: 68.46% · **max_stack 10** · `scaling: stack_count` · tick 1.0s | `target` | 5s |
| 쇼크웨이브 2 | 스킬1 | `full_burst_start` | `atk_caster_based_pct`: 47.52% | `self` | 10s |
| 급소 공략 | 스킬2 | `battle_start` | `part_dmg_pct`: 21.12% | `self` | 5s |
| 급소 공략 | 스킬2 | `full_burst_start` | `part_dmg_pct`: 21.12% | `self` | 5s |
| 일점 공격 | 스킬2 | `event:part_destroy` + `not_self_state:A.N. 모드` | `dot_dmg_pct`: 47.32% | `self` | 15s |
| 푸른 칼날 | 스킬2 | `event:part_destroy` + `not_self_state:A.N. 모드` | `remove_named_buff` → 급소 공략 | `self` | — |
| 템페스트 | 스킬3 | `burst_cast` | `burst_damage`: 492.3% | `all_enemies` | — |
| A.N. 모드 2 | 스킬3 | `burst_cast` | `remove_named_buff` → 일점 공격 | `self` | — |
| A.N. 모드 | 스킬3 | `burst_cast` | `dot_dmg_pct`: 89.44% | `self` | 10s |

무효 stat:

| stat | 상태 | 근거 |
|---|---|---|
| `part_dmg_pct` (급소 공략) | ⚠️ **무효** | 구현은 ✅지만 `is_part=True` 히트에만 실린다. **레이븐에게는 짝이 되는 `hits_parts: true` 대미지 효과가 없어** 실릴 히트가 없다 (`IMPL-STATUS.md` `part_dmg_pct` 행에 명시) |

## 검증 스쿼드

- **표준 (B3 · 쿨 40s)**:
  `["리틀 머메이드", "크라운", "레이븐", "test_B3"]`
  - **최소 45초는 돌려야 한다.** `쇼크웨이브`의 10중첩이 t≈29초에야 찬다(아래 §중첩).
- **변형 1 (파츠 보유 보스)**: `enemy={"has_parts": True, "part_break_interval": 45.0}`
  - **표준 스쿼드에서는 `일점 공격`이 실제로 발동한다.** 파괴 시각 45.017 · 90.017 ·
    135.017 중 **앞 둘**만 통과하고(45·90은 A.N. 모드 창 밖), 135.017은 레이븐 버스트
    (128.533~138.533) 안이라 `not_self_state:A.N. 모드`에 막힌다. 실측 2회.
  - 총딜 776,891,722 → **795,135,719 (+2.35%)**.
  - `푸른 칼날`이 같은 프레임에 `급소 공략`을 지운다(실측 45.017 동시).
  - `템페스트`가 다음 버스트에서 `일점 공격`을 지운다 — 실측 45.017에 붙은 15초 버프가
    **53.333(버스트)에 만료 전 제거**된다.
- **변형 2 (하네스 조합에서는 파츠가 무의미하다 — 대조군)**:
  `S39_레이븐레드후드`에 `has_parts` + 45초를 켜도 **총딜이 한 자리도 안 움직인다**
  (실측 양쪽 4,991,186,509). 그 조합은 레이븐이 A.N. 모드를 더 촘촘히 두르고 있어
  파괴 시각이 전부 창 안에 들어간다. `runner/snapshot.py`의 `S39_레이븐레드후드` 주석이
  그 근거로 파츠 조건을 안 켠 것이고, **그 판단은 그 조합에 한해 맞다.**
  일반화하면 안 된다 — 표준 스쿼드에서는 열린다.

- **실전 조합 (하네스)** — 레이븐은 1개 스쿼드에 들어 있다:

  | 스쿼드 | 멤버 | 적 | 비고 |
  |---|---|---|---|
  | `S39_레이븐레드후드` | 리타 · 민트 · **레이븐** · 프리카 · **레드 후드** | 전격 | 파츠 **미사용**(의도) · 레드 후드가 2사이클에서 1·3단계 겸함 |

  baseline 4,907,843,875 중 레이븐 **2,434,944,588 (49.6%)**.
  `쇼크웨이브` 2,080,050,654(170히트)로 개인 딜의 **85.4%**, `템페스트`는 39,224,810(1.6%).

실측 (180s, `--expected`, `first_burst_time=3.0`, 적 철갑, 기본 스펙 1층):

| 스쿼드 | 스쿼드 총딜 | 레이븐 |
|---|---|---|
| 표준 | 2,181,923,960 | 776,891,722 (35.6%) |
| 표준 + 파츠 45s | 2,200,167,957 | 795,135,719 (36.1%) |
| `S39_레이븐레드후드` (baseline, seed 42) | 4,907,843,875 | 2,434,944,588 (49.6%) |

## 쇼크웨이브 — 10중첩까지 25초가 걸린다

`쇼크웨이브`는 **중첩형 지속 대미지**다: `max_stack 10` · `scaling: stack_count` ·
`tick_interval 1.0` · `duration 5.0`. 풀차지 명중마다 1중첩씩 쌓이고, **틱 대미지가
중첩 수에 비례**한다(`doclint` 검사 J가 이 짝을 강제한다).

레이븐의 풀차지 주기는 **2.083초**(차지 1.0s + RL 후딜)다. 180초에 87발.

표준 스쿼드 실측 — 틱 1회 딜을 1중첩 단가로 나눈 값:

| t | 틱 딜 | 중첩 | 비고 |
|---|---|---|---|
| 4.100 · 5.100 | 1,269,491 | 2 | 풀버스트 진입 직후 |
| 6.183 · 7.183 | 1,904,236 | 3 | |
| 8.250 · 9.250 | 2,538,982 | 4 | |
| 10.300 · 11.300 | 3,173,727 | 5 | |
| 12.350 | 3,808,473 | 6 | 풀버스트 종료 직전 |
| 28.967 ~ 32.050 | 6,347,454 | **10 (상한)** | t≈29초에 상한 도달 |

**5초 지속인데 2.083초마다 한 발이라 중첩이 계속 쌓인다** — 새 명중이 기존 중첩의
지속을 함께 갱신하기 때문이다. 사격이 2회 이상 연속으로 끊기지 않는 한 10에서 유지된다.

그래서 **레이븐은 사이클이 진행될수록 세진다.** 개인 딜의 90%가 이 dot이고
(180초 172히트 699,156,261), 일반공격은 8.9%, `템페스트`는 1.1%뿐이다.

## 사이클 타임라인 (표준, 2사이클)

### 사이클 1 — 레이븐 버스트 사이클

| t (실측) | 이벤트 | 발동 효과 (ID / stat) | 대상/지속 | 비고 |
|---|---|---|---|---|
| 0.000 | battle_start | **급소 공략**(`part_dmg_pct` 21.12, 5s) | self | ⚠️ 실릴 히트가 없다 |
| 1.000 | 첫 풀차지 명중 | 쇼크웨이브 dot 1중첩 | 적 · 5s | 이후 2.083s마다 +1 |
| 2.017 | dot 틱 | 97,136 | | tick 1.0s |
| 3.000 · 3.100 | 리틀 머메이드 · 크라운 | — | | |
| **3.200** | **레이븐 burst_cast** | **템페스트**(`burst_damage` 492.3%) · **A.N. 모드 2**(`일점 공격` 제거) · **A.N. 모드**(`dot_dmg_pct` +89.44) | 적 / self · 10s | 버스트 딜은 **풀버스트 진입 전** 히트 |
| 3.250 | **full_burst 시작 (1회째)** | **쇼크웨이브 2**(`atk_caster_based_pct` +47.52, 10s) · **급소 공략** 재부여 | self · 10s / 5s | |
| 4.100 → 12.350 | 1초마다 | dot 틱 (중첩 2 → 6으로 증가) | 적 | A.N. 모드로 틱이 1.89배 |
| 8.267 | 5s 만료 | 급소 공략 off | | |
| 13.200 | 10s 만료 | A.N. 모드 off | | 풀버스트 종료보다 0.05s 먼저 |
| 13.250 | full_burst 종료 | 쇼크웨이브 2 off | | 틱이 3,808,473 → 1,108,556으로 급락 |

### 사이클 2 — 레이븐 미사용 사이클 (test_B3이 B3)

| t (실측) | 이벤트 | 발동 효과 | 비고 |
|---|---|---|---|
| 15.733 | test_B3 stage:3 | — | 레이븐은 쿨 |
| **15.783** | full_burst 시작 (2회째) | **쇼크웨이브 2 · 급소 공략 재부여** | **레이븐이 버스트하지 않아도 걸린다** |
| 16.450 → 24.800 | 1초마다 | dot 틱 (중첩 계속 증가) | **A.N. 모드가 없어** 틱이 사이클 1보다 낮다 |
| 25.783 | full_burst 종료 | 쇼크웨이브 2 off | |
| 28.267 | 레이븐 burst_cast (2회째) | 사이클 1과 동일 | **간격 25.067s** · 여기서 중첩이 10에 닿는다 |

사이클 간격 **12.533s 등간격**(180초 15회), 레이븐 버스트는 8회.

**사이클별 개인 딜 (실측):**

| 사이클 | 레이븐 딜 | 쇼크웨이브 | 일반공격 | 템페스트 |
|---|---|---|---|---|
| 1 (본인 버스트) | 30,083,369 | 12틱 25,276,531 | 6히트 4,806,838 | — |
| 2 (미사용) | **38,305,029** | 12틱 33,200,961 | 6히트 4,130,811 | 1히트 973,257 |

**미사용 사이클이 더 높다.** 중첩이 아직 차오르는 중이라 시간이 지날수록 세지는 쪽이
이기기 때문이다 — 다른 B3 딜러와 정반대 모양이다.
(사이클 2에 `템페스트`가 잡히는 건 t=28.267 버스트가 풀버스트 진입 0.05초 전이라
이전 사이클로 집계되기 때문이다.)

## 스킬 간 상호작용

- **`A.N. 모드`(dot 증폭) × `쇼크웨이브`(dot 본체)** *(이 캐릭터의 축)*
  - 경로: `A.N. 모드`(`dot_dmg_pct` +89.44) → DealForm ⑤ → `is_dot=True` 히트 전부.
    레이븐 딜의 90%가 dot이므로 **사실상 개인 딜 전체에 걸리는 배율**이다.
  - 시점: `burst_cast`에 10초. 본인 버스트 사이클만이라 **커버리지 40%**(10 ÷ 25.07).
  - 위험: `dot_dmg_pct`는 `is_dot` 히트에만 실린다. `쇼크웨이브`가 `dot_damage`가 아닌
    다른 stat으로 파싱되면 **버프는 정상 발동하는데 딜만 안 오른다.**
- **`A.N. 모드`가 `일점 공격`의 게이트를 닫는다** *(자기 스킬끼리 충돌)*
  - `일점 공격`의 조건이 `not_self_state:A.N. 모드`다. **레이븐이 버스트한 10초 동안은
    파츠를 부숴도 `일점 공격`이 안 붙는다.**
  - 게다가 `템페스트`의 `remove_named_buff`가 **이미 붙어 있던 `일점 공격`을 버스트마다
    지운다**(15초 지속인데 다음 버스트에서 잘린다). 실측 45.017 부여 → 53.333 제거.
  - 두 장치가 같은 방향이다 — **버스트를 자주 쓸수록 `일점 공격`이 못 산다.**
    하네스 `S39_레이븐레드후드`에서 파츠가 무의미한 이유가 이것이다.
- **`푸른 칼날`은 `급소 공략`을 지운다 — 무효 버프끼리의 교체다** *(딜 무관)*
  - 두 효과 다 딜에 안 실린다(`급소 공략`은 실릴 히트가 없고, 지우는 쪽은 값이 없다).
    L2(발동 횟수)에만 나타난다.
- **`쇼크웨이브 2`(공격력)와 `쇼크웨이브`(dot)는 별개다** *(파싱 주의 — 2026-09-05 이전에는 이름까지 같았다)*
  - `parsed_skills.json`에 같은 이름의 항목이 둘이다 — `full_charge_fire`의 `dot_damage`와
    `full_burst_start`의 `atk_caster_based_pct`. 원문 스킬 1의 두 `■` 블록이다.
  - `remove_named_buff`·`self_state:` 같은 이름 참조가 생기면 **어느 쪽을 가리키는지
    모호해진다.** 현재는 참조가 없어 문제가 없다.

## 유저 확인 사항
- 초안 단계를 거치지 않아 신규 질문 없음. 아래는 파싱·코드에서 확인한 사실.
- Q: `쇼크웨이브` 중첩이 5초 지속인데 왜 10까지 쌓이나? 2.083초 주기면 최대 3중첩 아닌가?
  A: **새 명중이 기존 중첩의 지속까지 갱신하기 때문이다.** 실측 틱 값이 t=4.1의 2중첩에서
  t≈29의 10중첩(상한)까지 단조 증가한다. 사격이 끊기지 않는 한 상한에서 유지된다.
- Q: `급소 공략`이 매 풀버스트 걸리는데 딜이 안 오른다?
  A: **정상이다.** `part_dmg_pct`는 `is_part=True` 히트에만 실리고, 레이븐에게는 파츠를
  명시한 대미지 효과(`hits_parts: true`)가 없다. `IMPL-STATUS.md`에 명시된 알려진 상태다.
- Q: `S39_레이븐레드후드` 주석은 "파츠를 켜도 총딜이 안 움직인다"는데, 표준 스쿼드에서는 왜 움직이나?
  A: **파괴 시각이 A.N. 모드 창 안에 들어오느냐의 차이다.** 표준은 45·90초가 창 밖이라
  `일점 공격`이 2회 열리고 +2.35%가 된다. 하네스 조합은 전부 창 안이라 0이다.
  **조합 의존이므로 "레이븐의 파츠 효과는 죽었다"로 일반화하지 않는다.**
- Q: 미사용 사이클 딜이 본인 버스트 사이클보다 높은데 버그인가?
  A: **아니다.** 중첩이 차오르는 구간이라 시간이 지날수록 틱이 커진다. 사이클 1은
  중첩 2~6, 사이클 2는 6~10 구간이다.

## 해석 선언

| 효과 | 해석 | 근거 | 대안이었다면 |
|---|---|---|---|
| 쇼크웨이브 (dot) | `dot_damage` + `max_stack 10` + **`scaling: stack_count`** | `PARSING.md §N중첩` · `doclint` 검사 J · 실측 틱 단조 증가 | `scaling`이 없으면 중첩이 쌓여도 틱이 1중첩에 머물러 **개인 딜이 1/5 아래로 떨어진다** |
| 쇼크웨이브 (dot) | 조준선에 가장 가까운 적 1기 → `target`(조준 규칙) | 원문 · `_resolve_target()` · `boss_pattern` §쫄몹 | 쫄몹이 있으면 지속 대미지는 레이븐이 겨눈 적 1기에 붙고 틱은 그 적에게만 간다 — 쫄몹이 살아 있으면 쫄몹을 겨누므로 그동안 건 지속 대미지는 총딜에서 빠진다(`boss_pattern` §조준) |
| A.N. 모드 | `dot_dmg_pct`(⑤) — `is_dot` 히트에만 | parsed stat | `atk_dmg_pct`였다면 일반공격·템페스트에도 실려 커버리지 효과가 달라진다 |
| 일점 공격 | `not_self_state:A.N. 모드` **게이트** (딜 조건이 아니라 부여 조건) | parsed condition · 실측 3회 중 2회 통과 | 조건이 없었다면 파츠 조합에서 상시 +47.32% dot이 된다 |
| A.N. 모드 2 | `remove_named_buff` → **`일점 공격`** 제거 | parsed `target_effect` · 원문은 `[A.N. 모드] : 효과 1`이라 이름이 그 clause 것이다(Step 7) | 대상이 `급소 공략`이었다면 딜 영향이 0이다 (양쪽 다 무효 버프끼리) |
| 푸른 칼날 | `remove_named_buff` → **`급소 공략`** 제거 | parsed `target_effect` | 마찬가지로 딜 무관 |
| 템페스트 (damage) | `burst_damage` 492.3%, `all_enemies` | parsed stat | 개인 딜 기여가 1.1%뿐이라 해석이 바뀌어도 총딜은 거의 안 움직인다 |
| 급소 공략 | `part_dmg_pct` — **현재 무효**로 둔다 | `IMPL-STATUS.md` (짝이 되는 `hits_parts` 효과 없음) | 레이븐에게 `hits_parts` 대미지가 생기면 파츠 조합에서 값이 살아난다 |

## impl/bug-fix용 검증 체크리스트
- [ ] `쇼크웨이브` 틱이 **중첩 수에 비례**해 커진다 (t=4.1 2중첩 → t≈29 10중첩 상한)
- [ ] 중첩 상한이 **10**에서 멈춘다 (6,347,454 = 1중첩 단가 × 10)
- [ ] dot 틱 간격 **1.0초** · 풀차지 주기 **2.083초** · 180초 87발 / dot 172틱
- [ ] `A.N. 모드`가 **본인 버스트 사이클에만** 8회 (커버리지 40%)
- [ ] `쇼크웨이브 2`·`급소 공략`은 **매 풀버스트 15·16회** (레이븐 미버스트 사이클 포함)
- [ ] 풀버스트 종료 프레임에 dot 틱이 급락한다 (3,808,473 → 1,108,556) — 버프 만료 반영
- [ ] 파츠 45초 조합에서 `일점 공격` **2회**(45.017 · 90.017), 135.017은 **막힌다** (네거티브)
- [ ] `푸른 칼날`이 `일점 공격`과 **같은 프레임**에 `급소 공략`을 제거
- [ ] `템페스트`가 버스트마다 `일점 공격`을 제거한다 (15초 지속이 다음 버스트에서 잘림)
- [ ] `급소 공략`은 발동하되 딜 기여 **0** (⚠️ 실릴 히트 없음)
- [ ] 레이븐 버스트 간격 **25.067s** · 180초 8회 · `템페스트` 8히트
- [ ] 개인 딜 구성이 dot **90%** · 일반공격 8.9% · 템페스트 1.1%
