# 리버렐리오 동작 시나리오

> 모드: 보강 (parsed_skills 반영)
> **역순 작성** — 이미 등록·검증이 끝난 캐릭터의 시나리오를 뒤늦게 채운 문서다.
> 초안(파싱 전) 단계를 거치지 않았으므로 `## 유저 확인 사항`은 파싱 결과에서
> 역으로 확인한 항목만 담는다. 수치·발동 시각은 전부 실측이다.

## 기본 정보
- 버스트 단계: B3
- 본인 쿨다운: **40s** (잠기는 세계)
- 무기 유형: SR (6발, 재장전 2.0s, 계수 69.04%, 코어 200%, 차지 1.5s, 풀차지 배율 250%)
- 속성 코드: 풍압 · 클래스: 화력형 · 기업: 필그림 · 스쿼드: 헬레틱
- 애장품: **없음**
- 작성 사이클 수: **2** (쿨 40s)
- **기본 레이어 1건** (`data/char_defaults.json`): 차지속도 옵션 **9.26%**

## 효과 요약 (보강: 효과 단위 stat 키)

| ID (parsed name) | 스킬 | 트리거 (timing+condition) | stat: 값(lv10) | 대상 | 지속 |
|---|---|---|---|---|---|
| 차분한 수심 | 스킬1 | `full_burst_start` | `atk_pct`: 160.0% | `self` | **3s** |
| 차분한 수심 2 | 스킬1 | `full_charge_hit` + **`core_hit`** | `atk_dmg_pct`: 20.83% | `self` | 60s |
| 차분한 수심 3 | 스킬1 | `full_charge_hit` | `bonus_damage:5`: 40.5% | `target` | — |
| 차분한 수심 4 | 스킬1 | `full_burst_start` | `charge_speed_caster_based_pct`: 12.74% | `allies_lowest_atk_burst3:1` | 10s |
| 격류 | 스킬2 | `full_charge_hit` + `target_is_boss` | `atk_dmg_pct`: **231.0%** | `self` | **영구** |
| 완만류 해제 | 스킬2 | `full_charge_hit` + `target_is_boss` | `remove_named_buff` → `완만류` | `self` | — |
| 완만류 | 스킬2 | `full_charge_hit` + **`target_is_add`** | `charge_time_fixed`: **1.0s** | `self` | **영구** |
| 격류 해제 | 스킬2 | `full_charge_hit` + `target_is_add` | `remove_named_buff` → `격류` | `self` | — |
| 차지 속도 증가 효과 면역 | 스킬2 | `battle_start` | `charge_speed_buff_immune` | `self` | 영구 |
| 차지 속도 감소 효과 면역 | 스킬2 | `battle_start` | `charge_speed_debuff_immune` | `self` | 영구 |
| 잠기는 세계 | 스킬3 | `burst_cast` | `atk_dmg_pct`: 50.0% | `self` | 10s |
| 잠기는 세계 2 | 스킬3 | `burst_cast` | `bonus_damage`: 925.0% | `all_enemies` | — |

**미구현 stat은 없다.** `완만류`(차지 시간 1초 고정) 분기는 **쫄몹이 살아 있는 보스 패턴에서만** 켜진다 —
풀차지가 쫄몹에 맞을 때다(`target_is_add`, 아래 §쫄몹 보스). 쫄몹이 없는 기본 경로에서는 영구 무발동이고
`완만류 해제`만 풀차지마다 헛돈다(지울 `완만류`가 없다).

## 검증 스쿼드

- **표준 (B3 · 쿨 40s)**:
  `["리틀 머메이드", "크라운", "리버렐리오", "test_B3"]`
- **변형 1 (코어 보스 — 필수)**: `enemy={"core_px": 30}`
  - **표준에서는 `차분한 수심 2`가 영구 미발동이다.** `core_hit` 조건이라 코어가 없으면
    한 번도 안 걸린다(실측 0회). 코어 30px에서는 **매 풀차지마다 127회** 걸리고 지속 60s라
    사실상 상시 +20.83%가 된다.
  - 총딜 844,652,881 → **1,104,223,644 (+30.7%)**. 코어 배율분과 `차분한 수심 2`가 겹친 값이다.
  - `core_hit` 조건은 **`--expected`(기대값 모드)에서도 정상 판정된다** — seed 모드와 발동
    시각·횟수가 완전히 같다(둘 다 127회, 첫 발동 1.367). 코어히트 *배율*만 기대값으로 녹을 뿐
    조건은 기하 판정이다.
- **변형 3 (쫄몹 보스 — 격류 ↔ 완만류)**: `S36_네온리버` 멤버 + `--boss "data/boss_scripts/솔로 레이드 S40 사치스러운 거미.json"`
  - 투자자 랩쳐(그로기 뒤 쫄몹 12기) 구간에 `완만류`가 켜지고 `격류`가 꺼진다 — §쫄몹 보스
  - 실측(기대값): `격류` 활성 106 · `완만류` 27 · `격류 해제` 27 · 전환 `격류`→`완만류` 4회(35.950 · 74.383 · 110.783 ·
    177.150) · `완만류`→`격류` 3회(48.350 · 76.450 · 122.683). 총딜 2,866,912,615 → **2,816,078,536**, 리버렐리오
    782,311,812 → **750,763,897** (재파싱 전후, 같은 엔진)
- **변형 2 (차지속도 버프 공급자 동반)**: `["리틀 머메이드", "크라운", "리버렐리오", "앨리스"]`
  - 앨리스 `힘나는 당근`(`charge_speed_caster_based_pct`)이 리버렐리오에게 **걸리는 것으로
    로그에 찍히지만 실제로는 무효다** — `차지 속도 증가 효과 면역`이 `get_buffs()` 후처리에서
    양수 기여를 걷어낸다. **레이어의 오버로드 9.26%는 남는다**(`_CHARGE_IMMUNE_EXEMPT_SOURCES`).
  - 동시에 리버렐리오 자신의 `차분한 수심 4`가 앨리스에게 간다 — 서로 못 주고 한쪽만 받는 구도다.

- **실전 조합 (하네스)** — 리버렐리오는 2개 스쿼드에 들어 있다:

  | 스쿼드 | 멤버 | 적 | 비고 |
  |---|---|---|---|
  | `S36_네온리버` | 마스트 : 로망틱 메이드 · 리틀 머메이드 · **네온 : 비전 아이** · **리버렐리오** · 앵커 : 이노센트 메이드 | 수냉 | `burst_gauge_mode: accumulate` · **`camera: "리버렐리오"`** (3번 자리 기본 유도보다 +0.86%, 풀버스트 14 → 15회) |
  | `S40_홍련흑영벨벳` | 리틀 머메이드 · 나유타 · **리버렐리오** · **홍련 : 흑영** · 벨벳 | 풍압 | 홍련 : 흑영 탄충 큐브 커버 |

  baseline: `S36_네온리버` 1,012,908,171 / `S40_홍련흑영벨벳` 946,899,568.
  둘 다 `차분한 수심 3`(bonus_damage) 635·615히트가 개인 딜의 40% 안팎이다.

실측 (180s, `--expected`, `first_burst_time=3.0`, 적 풍압, 기본 스펙 1층 + 차속 레이어):

| 스쿼드 | 스쿼드 총딜 | 리버렐리오 |
|---|---|---|
| 표준 | 1,658,188,770 | 844,652,881 (50.9%) |
| 표준 + 코어 30px | 2,137,935,244 | 1,104,223,644 (51.6%) |
| `S36_네온리버` (baseline, seed 42) | 5,299,059,838 | 1,012,908,171 (19.1%) |
| `S40_홍련흑영벨벳` (baseline, seed 42) | 3,266,177,947 | 946,899,568 (29.0%) |

## 킷 전체가 `full_charge_hit` 하나에 걸려 있다

리버렐리오의 풀차지 주기는 **1.383초**(차지 1.5s + 레이어 차지속도 9.26%)이고,
180초에 **127발**이 나간다. 그 127발이 세 효과를 동시에 끈다:

| 효과 | 무엇 | 커버리지 |
|---|---|---|
| 차분한 수심 3 | `bonus_damage:5` — **한 발에 5히트** (180초 635히트) | 항상 |
| 격류 | `atk_dmg_pct` **+231, 영구** | 첫 발(t=1.367) 이후 항상 |
| 차분한 수심 2 | `atk_dmg_pct` +20.83, 60s | **코어가 있을 때만** |

`격류`가 개인 딜의 뼈대다 — **지속이 `-1`(영구)이라 한 번 켜지면 전투가 끝날 때까지 유지된다.**
로그에 127회 활성으로 찍히지만 전부 같은 버프의 갱신이고 중첩이 아니다.
전투 시작 1.367초를 제외한 **99.2%의 시간 동안 +231%가 켜져 있다.**

## 쫄몹 보스 — 격류 ↔ 완만류

원문 스킬2는 풀차지의 **대상**으로 갈린다 — 타겟(보스)이면 `격류`(+231%, 영구) + `완만류 해제`, 타겟이 아닌
랩쳐(쫄몹)면 `완만류`(차지 시간 1초 고정, 영구) + `격류 해제`. 둘은 서로를 지우는 배타 상태다.

- **대상 = 리버렐리오가 겨눈 적**(`calculator/boss_pattern.py` §조준, 유저 결정 2026-09-19). 산 쫄몹이 있으면 누구나
  쫄몹을 먼저 겨눈다 — 카메라 니케만 산 저지원을 그보다 앞에 둔다. 저지원·파츠를 겨눈 풀차지는 타겟(보스)의
  부위라 `격류` 쪽이다(⬜ 인게임 미확인). 쫄몹이 없으면 늘 보스라 종전과 같다.
- **전환은 명중 뒤다.** 쫄몹에 맞은 첫 풀차지의 딜은 이미 `격류`를 싣고 산정됐고, 다음 발부터 차지 1.0s ·
  `격류` 없음이다. 쫄몹이 사라져 보스에 맞은 첫 풀차지는 `격류` 없이 나가고 그 명중에 `격류`가 다시 켜진다.
- `완만류`는 `charge_time_fixed`라 레이어 차지속도 9.26%까지 무시한다 — 1.383s → **1.0s**(GAMEPLAY §무기 메카닉).
- 쫄몹에 들어간 딜은 총딜 밖이다. 총딜에 보이는 차이는 「보스 복귀 첫 발이 `격류` 없이 나간다」와 「차지 1초로
  쫄몹을 빨리 잡아 보스 복귀가 당겨진다」 둘이다.
- 실측(변형 3): 쫄몹 구간의 풀차지 간격 **1.033s**(차지 1.0 + 발사 지연) — 첫 간격만 2.067s였는데 쫄몹 첫 명중 발에서
  탄이 떨어져 재장전한 것이다. 투자자 랩쳐 처치가 0.02~0.13s씩 당겨진다(타수 기믹이라 발수가 곧 속도).
- **보스 흐름이 계단식으로 흔들린다.** 변형 3에서는 쫄몹 구간 뒤 `격류`가 한 발 늦게 돌아오는 차이로 흐름이 밀려
  알집2가 체력 60.8M 중 59.7M에서 제한시간을 넘겨(재파싱 전에는 73.917s 파괴) 부화 랩쳐 분기가 열렸다 — 총딜 −1.8%의
  대부분이 이 연쇄다. 리버렐리오만 재파싱해도 같은 결과라 네온 : 비전 아이 쪽 변경과는 무관하다.

## 사이클 타임라인 (표준, 2사이클)

### 사이클 1 — 리버렐리오 버스트 사이클

| t (실측) | 이벤트 | 발동 효과 (ID / stat) | 대상/지속 | 비고 |
|---|---|---|---|---|
| 0.000 | battle_start | **차지 속도 증가 효과 면역** · **차지 속도 감소 효과 면역** | self · 영구 | 스킬 차속 버프를 양방향으로 차단 |
| **1.367** | 첫 풀차지 명중 | **격류**(`atk_dmg_pct` +231, **영구**) · 차분한 수심 3(5히트) | self / 적 | 코어가 있으면 **차분한 수심 2**도 여기서 |
| 2.750 | 풀차지 | 격류 갱신 · 차분한 수심 3 | | 주기 **1.383s** |
| 3.000 · 3.100 | 리틀 머메이드 · 크라운 | — | | |
| **3.200** | **리버렐리오 burst_cast** | **잠기는 세계**(`atk_dmg_pct` +50, 10s) · **잠기는 세계 2**(`bonus_damage` 925%, 적 전체) | self / 적 | 버스트 딜은 **풀버스트 진입 전** 히트 |
| 3.250 | **full_burst 시작 (1회째)** | **차분한 수심**(`atk_pct` +160, **3s**) · **차분한 수심 4**(차속 12.74, 10s) | self / test_B3 | 차분한 수심은 3초짜리다 — 풀차지 2발분 |
| 4.133 · 5.517 | 풀차지 | 격류 갱신 · 차분한 수심 3 | | |
| 6.267 | 3s 만료 | 차분한 수심(+160 공격력) off | | **풀버스트가 아직 7초 남았는데 꺼진다** |
| 6.900 → 12.433 | 1.383s마다 | 격류 갱신 · 차분한 수심 3 | | |
| 13.200 | 10s 만료 | 잠기는 세계 off | | |
| 13.250 | full_burst 종료 | 차분한 수심 4 off | | |

### 사이클 2 — 리버렐리오 미사용 사이클 (test_B3이 B3)

| t (실측) | 이벤트 | 발동 효과 | 비고 |
|---|---|---|---|
| 13.817 · 15.200 | 풀차지 | 격류 갱신 · 차분한 수심 3 | 사이클과 무관한 자기 주기 |
| 15.533 · 15.633 · 15.733 | 리틀 머메이드 · 크라운 · test_B3 | — | 리버렐리오는 쿨 |
| **15.783** | **full_burst 시작 (2회째)** | **차분한 수심 · 차분한 수심 4 재부여** | **리버렐리오가 버스트하지 않아도 걸린다** |
| 18.800 | 3s 만료 | 차분한 수심 off | |
| 16.583 → 24.883 | 1.383s마다 | 격류 갱신 · 차분한 수심 3 | |
| 25.783 | full_burst 종료 | 차분한 수심 4 off | |
| 28.267 | 리버렐리오 burst_cast (2회째) | 사이클 1과 동일 | **간격 25.067s** |

사이클 간격 **12.533s 등간격**(180초 15회), 리버렐리오 버스트는 8회.

**사이클별 개인 딜 (실측):**

| 사이클 | 리버렐리오 딜 | 차분한 수심 3 | 잠기는 세계 2 | 일반공격 |
|---|---|---|---|---|
| 1 (본인 버스트) | **75,494,363** | 45히트 26,363,385 | 1히트 24,532,276 | 9히트 24,598,702 |
| 2 (미사용) | **38,166,299** | 45히트 19,743,950 | — | 9히트 18,422,349 |

풀차지 발수가 같은데 딜이 2배다 — 차이는 `잠기는 세계 2`(925%) 한 발과
`잠기는 세계`(+50%)·`차분한 수심`(+160%)의 버프분이다.

180초 누적: 일반공격 127히트 36.8% · 차분한 수심 3 635히트 39.5% · 잠기는 세계 2 8히트 23.7%.
재장전은 180초에 **1회뿐**이다(t=35.967) — 장탄 6 × (1 + 129.64%) = 13발에 풀차지 주기가
1.383초라 탄창이 거의 마르지 않는다.

## 스킬 간 상호작용

- **`격류`가 나머지 전부의 기반이다** *(영구 버프 → 전 히트 스케일링)*
  - 경로: `격류`(`atk_dmg_pct` +231, 영구) → DealForm ⑤ → 일반공격·`차분한 수심 3`·
    `잠기는 세계 2` **전부**에 실린다.
  - 시점: 첫 풀차지 명중(1.367)에 켜지고 이후 갱신만 된다. **끊길 경로가 없다.**
  - 위험: 지속이 `-1`(영구)이라 만료 검증이 불가능하다. `full_charge_hit`가 안 잡히면
    전투 내내 0인 채로 지나간다 — 체크리스트의 "t≈1.367 첫 발동"이 유일한 감시선이다.
- **`차분한 수심`(+160%)의 3초 지속이 풀버스트와 안 맞는다** *(설계 의도 확인 지점)*
  - `full_burst_start`에 켜지지만 **3초짜리**다. 풀버스트 10초 중 첫 3초만 덮는다.
  - 풀차지 주기 1.383s 기준 **딱 2발**이 이 버프를 받는다(3.250~6.267 사이 4.133·5.517).
  - 값이 160%로 큰데 커버리지가 30%다 — 킷에서 가장 시간에 민감한 효과다.
- **`차지 속도 면역` × 아군 차속 버퍼** *(교차 캐릭터, 음의 상호작용)*
  - `charge_speed_buff_immune`은 **스킬 소스의 양수 `charge_speed_pct`만** 제거하고
    장비(오버로드)·큐브 기여는 남긴다(`IMPL-STATUS.md`, 유저 확인 2026-09-02).
  - 따라서 앨리스 `힘나는 당근`·리버렐리오 자신의 `차분한 수심 4`·마스트 : 로망틱 메이드 등
    **아군 차속 버프가 리버렐리오에게는 전부 죽는다.** 로그에는 부여로 찍히므로
    L2(발동 횟수)만 보면 정상으로 보인다 — L1(딜)로만 구분된다.
  - 반대로 차속 **디버프**도 안 통한다. 두 면역이 한 쌍이다.
- **`차분한 수심 4`는 자기가 못 받는다** *(대상 선택)*
  - `allies_lowest_atk_burst3:1` — 기본 버스트 단계가 B3인 아군 중 최종 공격력이 가장 낮은 1기.
    표준에서는 test_B3, 앨리스와 함께면 앨리스에게 간다.
  - 설령 자신에게 왔더라도 위 면역으로 무효다. **리버렐리오는 차속 버프를 주기만 하는 쪽이다.**
- **`잠기는 세계 2`는 풀버스트 진입 전 프레임에 터진다** *(사이클 집계)*
  - `burst_cast`(3.200)가 `full_burst 시작`(3.250)보다 0.05초 빠르다. 사이클 경계를
    풀버스트 진입으로 잡는 breakdown에서는 **직전 사이클로 집계된다** —
    `docs/scenarios/델타 _ 닌자 시프.md`의 `오버드라이브 3`과 같은 형태다.

## 유저 확인 사항
- 초안 단계를 거치지 않아 신규 질문 없음. 아래는 파싱·코드에서 확인한 사실.
- Q: `차분한 수심 2`(코어 명중 시 +20.83%)가 표준 스쿼드에서 왜 안 보이나?
  A: **기본 적에 코어가 없어서다**(`core_px` 0). 실측 표준 0회 / 코어 30px 127회.
  코어 조합에서는 60초 지속 + 1.383초 갱신이라 **사실상 상시 버프**다.
- Q: `--expected`에서 `core_hit` 조건이 판정되나? (코어 hit_tag가 사라진다고 알려져 있다)
  A: **판정된다.** seed 42(확률 판정)와 `--expected`가 발동 시각·횟수 모두 동일하다.
  기대값 모드가 지우는 것은 히트 목록의 표시와 **배율**이지 조건이 아니다.
- Q: 원문 `완만류`(대상이 타겟이 아닌 랩쳐면 차지 시간 1초 고정) 분기는 어디 갔나?
  A: **2026-09-19 복원했다.** 처음 파싱할 때는 단일 적 sim이라 분기째 뺐다. 쫄몹 모델이 생기고 조준 비율
  (`share`)을 조준 순서로 바꾸면서(유저 결정 2026-09-19) 한 발의 대상이 하나로 정해져 분기를 가를 수 있게
  됐다 — §쫄몹 보스.
- Q: 아군이 준 차지속도 버프가 로그에는 붙는데 딜이 안 움직인다?
  A: **정상이다.** `charge_speed_buff_immune`은 버프 부여를 막는 게 아니라 `get_buffs()`
  후처리에서 양수 기여만 걷어낸다. 장비·큐브 소스는 예외라 레이어 9.26%는 살아 있다.

## 해석 선언

| 효과 | 해석 | 근거 | 대안이었다면 |
|---|---|---|---|
| 격류 | `atk_dmg_pct` **영구**(`duration: -1`), 갱신형 단일 버프 | 원문 "[지속]" · 실측 127회 활성이 전부 갱신 | 중첩이었다면 231 × 127로 폭주한다 |
| 격류 / 완만류 | **배타 분기** — 풀차지의 대상이 보스면 `격류` + `완만류 해제`(`target_is_boss`), 쫄몹이면 `완만류`(차지 1초 고정, 영구) + `격류 해제`(`target_is_add`). 대상 = 리버렐리오가 겨눈 적 | 원문 "대상이 타겟이라면" · "대상이 타겟이 아닌 랩쳐라면" · `boss_pattern` §조준(유저 결정 2026-09-19) | 해제 항목이 없었다면 쫄몹 구간 뒤 둘이 함께 켜져 차지 1초 + 231%가 된다. 한 발을 share로 쪼개던 종전 모델이었다면 한 발의 대상이 둘이라 분기를 못 가른다 |
| 차분한 수심 | `atk_pct` **3초**(10초 아님) | 원문 "[3초 유지]" | 10초였다면 풀버스트 전 구간에 +160%가 실려 개인 딜이 크게 오른다 |
| 차분한 수심 2 | `core_hit` **조건**(코어 대미지가 아니라 게이트) | parsed condition · 코어 유무 실측 0 vs 127 | 조건이 없었다면 표준에서도 상시 +20.83%가 붙는다 |
| 차분한 수심 3 | `bonus_damage:5` — **한 트리거에 5히트** | parsed stat suffix · 실측 127발 → 635히트 | 1히트였다면 개인 딜의 39.5%가 1/5로 준다 |
| 차분한 수심 4 | `allies_lowest_atk_burst3:1` — **자신 제외**, B3 아군 1기 | parsed target · 실측 test_B3 / 앨리스 | 자신 포함이었어도 차속 면역으로 무효다 |
| 잠기는 세계 2 | `bonus_damage`(추가 대미지) — `all_enemies` | parsed stat/target | `damage`였다면 DealForm 경로가 달라 격류 증폭을 다르게 받는다 |
| 차지 속도 면역 | **스킬 소스 양수만** 제거, 장비·큐브는 예외 | `IMPL-STATUS.md` `_CHARGE_IMMUNE_EXEMPT_SOURCES` (유저 확인 2026-09-02) | 소스를 안 가렸다면 레이어 9.26%까지 죽어 풀차지 주기가 1.5s로 늘어난다 |

## impl/bug-fix용 검증 체크리스트
- [ ] `격류`가 **첫 풀차지 명중(t≈1.367)**에 켜지고 이후 만료 이벤트가 **한 번도 없다**
- [ ] `격류` 활성 로그 127회가 전부 **갱신**이고 중첩이 아니다 (값이 231 고정)
- [ ] 풀차지 주기 **1.383s** · 180초 **127발** · `차분한 수심 3` **635히트**(발당 5)
- [ ] `차분한 수심`(+160)이 **3초 만에** 꺼진다 — 풀버스트 중 풀차지 2발만 받는다
- [ ] `차분한 수심 2`는 코어 없음 **0회** / 코어 30px **127회** (네거티브 + 포지티브 양쪽)
- [ ] `차분한 수심 4`가 **자신이 아닌 B3 아군**에게 간다 (표준: test_B3)
- [ ] 아군 차속 버프를 받아도 **딜이 안 움직인다** — 부여 로그는 남고 기여만 제거
- [ ] 레이어 차지속도 9.26%는 **면역에 안 걸린다** (걸리면 주기가 1.5s로 늘어난다)
- [ ] 리버렐리오 버스트 간격 **25.067s**, 180초 **8회** · `잠기는 세계 2` 8히트
- [ ] `잠기는 세계 2`가 풀버스트 진입 **0.05s 전**에 터져 이전 사이클로 집계된다
- [ ] 본인 버스트 사이클 75.5M / 미사용 사이클 38.2M ≈ **2배**
- [ ] 기본 경로(쫄몹 없음): `완만류`·`격류 해제` **0회** · `완만류 해제` 풀차지마다(헛돎) · 딜은 재파싱 전과 같다
- [ ] 쫄몹 보스(변형 3): 쫄몹 구간 첫 풀차지 명중에 `격류` 해제 · `완만류` 켜짐 → 이후 풀차지 간격 **1.033s**
  (차지 1.0 + 발사 지연)
- [ ] 쫄몹 보스: 보스로 돌아온 첫 풀차지 명중에 `완만류` 해제 · `격류` 재부여 — 그 발은 `격류` 없이 산정
