# 리타 동작 시나리오

> 모드: 보강 (parsed_skills 반영)
> **역순 작성** — 이미 등록·검증이 끝난 캐릭터의 시나리오를 뒤늦게 채운 문서다.
> 초안(파싱 전) 단계를 거치지 않았으므로 `## 유저 확인 사항`은 파싱 결과에서
> 역으로 확인한 항목만 담는다. 수치·발동 시각은 전부 실측이다.

## 기본 정보
- 버스트 단계: B1
- 본인 쿨다운: 20s (더블 부스트)
- 무기 유형: SMG (120발, 재장전 1.5s, 연사 24발/s, 계수 8.73%, 코어 200%)
- 속성 코드: 철갑 · 클래스: 지원형 · 기업: 미실리스
- 애장품: **없음**
- 작성 사이클 수: 1 (본인 쿨 20s지만 자체 쿨감으로 매 사이클 버스트)

## 효과 요약 (보강: 효과 단위 stat 키)

| ID (parsed name) | 스킬 | 트리거 (timing+condition) | stat: 값(lv10) | 대상 | 지속 |
|---|---|---|---|---|---|
| 리타 부스트 | 스킬1 | `full_burst_start_count:1` | `burst_cooldown_reduce`: 2.34s | `all_allies` | — (instant) |
| 리타 부스트 2 | 스킬1 | `full_burst_start_count:2` | `burst_cooldown_reduce`: 2.7s | `all_allies` | — (instant) |
| 리타 부스트 3 | 스킬1 | `full_burst_start_count:3` | `burst_cooldown_reduce`: 3.17s | `all_allies` | — (instant) |
| 리타 부스트 4 | 스킬1 | `burst_cast_count:1` | `max_ammo_pct`: 45.17% | `all_allies` | 5s |
| 리타 부스트 5 | 스킬1 | `burst_cast_count:2` | `crit_dmg`: 12.46% | `all_allies` | 5s |
| 리타 부스트 6 | 스킬1 | `burst_cast_count:3` | `atk_pct`: 14.42% | `all_allies` | 5s |
| 볼트 부스트 | 스킬2 | `every:15.0s` | `cover_heal_pct`: 52.5% | `allies_lowest_cover_hp:2` | — (instant) |
| 더블 부스트 | 스킬3 | `burst_cast` | `atk_pct`: 66.0% | `all_allies` | 5s |

**`_count:N`은 전부 누적형이다** — `full_burst_start_count:N`·`burst_cast_count:N` 모두
`count >= N`으로 판정하므로(`buff_manager._timing_match`), 원문의 `[하위 효과 중복 적용]`이
그대로 성립한다. 3회째부터는 1·2·3 전부 매번 발동한다. `full_charge_count:N`(N회마다)과
판정식이 다르니 혼동하지 않는다.

정적 딜 기여: 더블 부스트(`atk_pct` 66%, ②) · 리타 부스트 6(`atk_pct` 14.42%, ②) ·
리타 부스트 5(`crit_dmg`, ③) · 리타 부스트 4(`max_ammo_pct`, 재장전 위상).
**리타 본인의 딜은 부산물이다** — 킷 전체가 `all_allies` 버프이고 본인 대미지 효과는 없다.

`cover_heal_pct`는 ✅지만 엄폐물 체력은 보스 공격 패턴(`enemy.patterns`의 `attack`)이 있을 때만 깎이므로 볼트 부스트는
기본 경로에서 딜에 영향이 없다. 대상 `allies_lowest_cover_hp:2`는 남은 비율 순이다.

## 검증 스쿼드
- **표준 (B1 · 쿨 20s)**: `["리타", "크라운", "test_B3", "스노우 화이트 : 헤비암즈"]`
  - 조건부 효과 스캔: `no_burstN_ally` · `back_row` · `allies_weapon/class/code` 류 조건 **없음**.
    모든 효과가 `all_allies` 또는 카운터 기반이라 스쿼드 구성에 무관하다 → **표준 1개로 충분**.
  - 리타를 index 0에 두는 이유는 버프 제공자가 수령자보다 먼저 notify되게 하기 위함이다.

- **실전 조합 (하네스)** — 리타는 4개 스쿼드에 들어 있다:

  | 스쿼드 | 멤버 | 적 |
  |---|---|---|
  | `S37_일레그루드밀라` | 크라운 · **리타** · 팬텀 · 루드밀라 : 윈터 오너 · 일레그 : 붐 앤 쇼크 | 작열 |
  | `S39_레이븐레드후드` | **리타** · 민트 · 레이븐 · 프리카 · 레드 후드 | 전격 |
  | `S40_앨리스모더니아` | **리타** · 그레이브 · 레이 · 앨리스 · 모더니아 | 풍압 |
  | `S40_아스카` | **리타** · 레이 · 아스카 · 모더니아 · 그레이브 | 풍압 |

  전부 리타가 **유일한 B1**이다 — 킷이 순수 쿨감·버프라 B1 슬롯을 놓고 경쟁시킬 이유가 없다.

실측 (180s, `--expected`, 표준): 스쿼드 총딜 **1,653,995,473** / 리타 181,523,991 (11.0%).

## 사이클 타임라인

`_count` 누적이 사이클 간격을 스스로 당기는 구조라, **초반 3사이클이 서로 다르다.**

| t (실측) | 이벤트 | 발동 효과 (ID / stat) | 대상/지속 | 비고 |
|---|---|---|---|---|
| 3.00 | 리타 burst_cast (1회째) | 리타 부스트 4(`max_ammo_pct` +45.17) · 더블 부스트(`atk_pct` +66) | all_allies / 5s | `burst_cast_count:1`만 참 |
| 3.25 | **full_burst 시작 (1회째)** | 리타 부스트(`burst_cooldown_reduce` −2.34s) | all_allies | 리타 쿨 20 → 17.66 |
| 8.00 | 5s 버프 만료 | 리타 부스트 4 · 더블 부스트 off | — | 풀버스트(10s)보다 먼저 끊긴다 |
| 20.667 | 리타 burst_cast (2회째) | 리타 부스트 4 · **5**(`crit_dmg`) · 더블 부스트 | all_allies / 5s | `count>=2` |
| 20.917 | **full_burst 시작 (2회째)** | 리타 부스트 · **2** (합 −5.04s) | all_allies | 쿨 20 → 14.96 |
| 35.633 | 리타 burst_cast (3회째) | 리타 부스트 4 · 5 · **6**(`atk_pct`) · 더블 부스트 | all_allies / 5s | `count>=3` — 이후 상시 |
| 35.883 | **full_burst 시작 (3회째)** | 리타 부스트 · 2 · **3** (합 −8.21s) | all_allies | 쿨 20 → 11.79 |
| 47.883 | 리타 burst_cast (4회째) | 4 · 5 · 6 · 더블 부스트 (포화) | all_allies / 5s | 이후 매 사이클 동일 |
| 48.133 | full_burst 시작 (4회째) | 위와 동일 | all_allies | **사이클 12.25s로 고정** |
| 이후 | 12.25s마다 반복 | 4·5·6 + 더블 부스트 전부 활성 | all_allies / 5s | 정상 상태 |

사이클 간격 실측: 3.25 → 20.917(**17.67s**) → 35.883(**14.97s**) → 48.133(**12.25s**) →
60.383 → 72.633 → 84.883 → … 이후 12.25s 등간격.
쿨감 합계 8.21s는 20−8.21 = 11.79s로 표준 사이클 12.25s보다 짧아, **3사이클째부터는
리타 쿨이 병목이 아니다**(풀버스트 길이 + 버스트 시전 지연이 하한).

## 스킬 간 상호작용

- **`full_burst_start_count` 누적 → 사이클 단축 → `burst_cast_count` 누적 가속**
  - 경로: 리타 부스트 1~3(풀버스트 진입마다 쿨감 누적) → 리타 본인 버스트가 빨라짐 →
    리타 부스트 4~6 단계가 더 일찍 채워짐.
  - 두 카운터가 **서로 다른 이벤트**(full_burst_start / burst_cast)를 세지만, 표준 스쿼드에서
    리타는 매 사이클 버스트하므로 실질적으로 같은 속도로 오른다.
  - 위험: 리타가 버스트를 못 쓰는 사이클이 생기면 4~6 단계만 뒤처진다. 표준 스쿼드에서는
    다른 B1이 없어 발생하지 않는다.
- **5초 버프 vs 10초 풀버스트의 어긋남**
  - 리타 부스트 4~6·더블 부스트는 전부 **5초**인데 `burst_cast`(풀버스트 진입 0.25s 전)에 붙는다.
    → 풀버스트 10s 중 **앞 4.75s만** 덮는다. 뒤 5.25s는 리타 버프가 전부 꺼진 구간이다.
  - 이건 파싱 문제가 아니라 원문 그대로다(`[5초 유지]`). 딜 배치를 앞쪽에 몰아야 이득이 커진다.

## 유저 확인 사항
- 초안 단계를 거치지 않아 신규 질문 없음. 아래는 파싱·코드에서 확인한 사실.
- Q: `[시작 횟수 별 효과] [하위 효과 중복 적용]`이 "N번째에만"인가 "N번째부터 매번"인가?
  A: **N번째부터 매번**. `_timing_match`의 `full_burst_start_count` / `burst_cast_count` 분기가
  `count >= N`이다. 실측으로도 3사이클째부터 1·2·3이 동시에 붙는다.
- Q: 볼트 부스트의 `every:15.0s`는 어디서 나왔나?
  A: **원문에 트리거가 없다**(`■ 남은 엄폐물 체력 수치가 가장 낮은 아군 2기에게`로 시작).
  파싱 시 주기를 부여한 추정치다. `cover_heal_pct`는 엄폐물 회복이라 딜에 영향이 없어 그대로 둔다(보스 공격 패턴이 있으면 주기가 엄폐물 생존에 영향).

## 해석 선언

| 효과 | 해석 | 근거 | 대안이었다면 |
|---|---|---|---|
| 리타 부스트 1~3 | 풀버스트 진입마다 누적 쿨감(3회째부터 8.21s) | `count >= N` 분기 · 실측 사이클 17.67→14.97→12.25 | "N번째에만 1회"였다면 정상 사이클이 20s로 남고 총딜이 크게 떨어진다 |
| 리타 부스트 4~6 | 버스트 3회째부터 세 버프 동시 | 위와 동일 | 배타였다면 3회째에 `atk_pct`만 남아 `crit_dmg`·`max_ammo`가 사라진다 |
| 더블 부스트 · 리타 부스트 4~6 | `burst_cast` 시점 부여, 5초 | 원문 `[5초 유지]` | `full_burst_start` 부여였다면 풀버스트를 0.25s 더 덮는다 (미미) |
| 볼트 부스트 | 15초 주기 추정 | 원문에 트리거 없음 | 주기가 달라도 `cover_heal_pct`는 엄폐물 회복이라 딜 불변 |

## impl/bug-fix용 검증 체크리스트
- [ ] 1사이클 풀버스트 진입 시 활성: 리타 부스트 4 · 더블 부스트 (2개만)
- [ ] 2사이클: + 리타 부스트 5 / 3사이클 이후: + 리타 부스트 6 (총 4개)
- [ ] 사이클 간격 17.67s → 14.97s → 12.25s → 이후 12.25s 등간격
- [ ] 리타 본인 버스트가 매 사이클 발동 (다른 B1 없음)
- [ ] 리타 부스트 4~6 · 더블 부스트가 `burst_cast` +5s에 만료 (풀버스트 종료보다 먼저)
- [ ] 발동 금지: 리타 본인 대미지 효과 — **킷에 damage 타입 효과가 하나도 없다**
- [ ] `cover_heal_pct`(볼트 부스트)는 딜에 기여하지 않음 (✅ 엄폐물 회복 — 보스 공격 패턴이 있을 때만 의미)
