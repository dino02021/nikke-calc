# 브리드 : 사일런트 트랙 동작 시나리오

> 모드: 보강 (parsed_skills 반영)
> **역순 작성** — 이미 등록·검증이 끝난 캐릭터의 시나리오를 뒤늦게 채운 문서다.
> 초안(파싱 전) 단계를 거치지 않았으므로 `## 유저 확인 사항`은 파싱 결과에서
> 역으로 확인한 항목만 담는다. 수치·발동 시각은 전부 실측이다.

## 기본 정보
- 버스트 단계: B2
- 본인 쿨다운: 20s (풀 마스콘)
- 무기 유형: SG (9발, 재장전 1.5s, 1.5발/s, 펠릿 10, 계수 201.5%, 코어 200%)
- 속성 코드: **작열** · 클래스: 지원형 · 기업: 엘리시온 · 스쿼드: 인피니티 레일
- 애장품: **없음**
- 작성 사이클 수: 1

## 효과 요약 (보강: 효과 단위 stat 키)

| ID (parsed name) | 스킬 | 트리거 (timing+condition) | stat: 값(lv10) | 대상 | 지속 |
|---|---|---|---|---|---|
| 기동 | 스킬1 | `full_burst_start` + `target_code:풍압` | `received_dmg_pct`: 15.12% | `enemies_code:풍압` | 10s |
| 기동 2 | 스킬1 | `full_burst_start` | `damage`: 636% | `all_enemies` | — |
| 전도 | 스킬2 | `on_attack_count:10` + `target_code:풍압` | `received_dmg_pct`: 12.12% | `enemies_lowest_hp_code:풍압:1` | 10s |
| 전도 2 | 스킬2 | `on_attack_count:5` | `damage`: 675% | `enemies_lowest_hp:1` | — |
| 풀 마스콘 | 스킬3 | `burst_cast` | `atk_caster_based_pct`: 66.52% | `all_allies_excl_self` | 10s |

**킷이 다섯 줄뿐이고 구조가 단순하다.** 아군 버프 한 줄(풀 마스콘, ②) · 적 디버프 두 줄
(⑥ `received_dmg_pct` 합 27.24%) · 본인 딜 두 줄(기동 2 · 전도 2).

**아군 버프가 자신을 제외한다**(`all_allies_excl_self`) — 브리드 자신은 킷 버프를
하나도 안 받는다. 반대로 본인 딜 지분은 지원형치고 높다(표준 11.8%, 풍압 적이면 22.6%).

## 검증 스쿼드

- **표준 (B2 · 쿨 20s)**: `["리틀 머메이드", "브리드 : 사일런트 트랙", "test_B3", "스노우 화이트 : 헤비암즈"]`
  - 조건부 효과 스캔: `no_burstN_ally` · `back_row` · `allies_weapon/class/code` 류 조건 **없음**.
    스쿼드 구성에 무관 → 표준 1개면 사이클은 다 잡힌다.
- **변형 1 (풍압 적 — 필수)**: 표준 + `enemy={"code": "풍압"}`
  - **코드가 안 맞는 적(예 `{"code": "작열"}`)에서는 기동·전도가 발동하지 않는다** —
    `target_code:풍압` condition이 게이트다(2026-09-02 추가). 코드 미설정은 통과한다.
  - 브리드는 **작열**이고 작열 → 풍압이 우월 코드다(`damage._SUPERIOR`). ⑦ 계수가
    1.1 → 약 2.18배(장비 88.6 + 큐브 19.09)로 뛴다.
  - 실측 총딜 1,864,517,776 → 2,124,512,402 (**+13.9%**),
    **브리드 개인 딜 220,914,632 → 480,909,258 (+117.7%)**.
    다른 3인의 딜은 한 자리도 안 바뀐다 — 순수하게 브리드의 ⑦만 움직인 것이다.
  - 위 +13.9%는 순수하게 ⑦다 — 코드 미설정에서도 `target_code:`는 통과하므로 기동·전도는
    양쪽 다 걸려 있다(아래 §유저 확인 사항).
- **변형 2 (코어 보스)**: `enemy={"core_px": 50}` — SG 펠릿 10개의 코어히트 분포 확인용.
  킷에 `core_dmg_pct`는 없지만 개인 딜 비중이 있어 값이 움직인다.

- **실전 조합 (하네스)** — 브리드 : 사일런트 트랙은 2개 스쿼드에 들어 있고, **둘 다 풍압 적**이다:

  | 스쿼드 | 멤버 | 적 | 비고 |
  |---|---|---|---|
  | `S40_브리드라피` | **브리드 : 사일런트 트랙** · 라피 : 레드 후드 · 아니스 : 스타 · 미하라 : 본딩 체인 · 프리바티 | 풍압 | `accumulate` · 라피 장전컨 · 미하라 엄폐컨 |
  | `S40_플로라` | 디젤 : 윈터 스위츠 · 플로라 · **브리드 : 사일런트 트랙** · 스노우 화이트 : 헤비암즈 · 목단 | 풍압 | |

  **적 코드가 풍압으로 고정된 자리에만 쓴다** — 우월 코드와 `enemies_code:풍압` 두 축이
  동시에 맞아떨어지는 조합이다.

실측 (180s, `--expected`, `first_burst_time=3.0`):

| 스쿼드 | 이탈 | 스쿼드 총딜 | 브리드 : 사일런트 트랙 |
|---|---|---|---|
| 표준 (코드 미설정) | 1층 그대로 | 1,864,517,776 | 220,914,632 (11.8%) |
| 표준 + 풍압 | 1층 그대로 | 2,124,512,402 | 480,909,258 (22.6%) |
| `S40_브리드라피` | 미하라 `atk_pct` 23.22 · 프리바티 `max_ammo_pct` 0 (둘 다 레이어) | 8,423,269,051 | 946,994,133 (11.2%) |

> 하네스 행은 스쿼드 구성·적·게이지 모드만 맞춰 다시 잰 값이다 — 그 스쿼드의 3층
> 오버라이드(라피 : 레드 후드 장전컨 · 미하라 : 본딩 체인 엄폐컨)는 걸지 않았고,
> `--expected` 모드라 골든 스냅샷(고정 시드 `random` 모드)과도 수치가 다르다.

## 사이클 타임라인 (표준 + 풍압)

`hit_count`가 **펠릿이 아니라 사격 수**를 센다 — SG 펠릿 10개는 1발로 친다.
사격 간격 0.667s(1.5발/s) → `on_attack_count:5`는 **3.333s 주기**.

| t (실측) | 이벤트 | 발동 효과 (ID / stat) | 대상/지속 | 비고 |
|---|---|---|---|---|
| 2.683 | 사격 5회 누적 | 전도 2(`damage` 675%) | `enemies_lowest_hp:1` | **첫 버스트보다 먼저** |
| 3.000 | 리틀 머메이드 stage:1 | — | — | |
| 3.100 | **브리드 burst_cast** | 풀 마스콘(`atk_caster_based_pct` +66.52) | `all_allies_excl_self` / 10s | **브리드 자신은 제외** |
| 3.200 | test_B3 stage:3 | — | — | |
| 3.250 | **full_burst 시작** | 기동(`received_dmg_pct` +15.12, 10s) · 기동 2(`damage` 636%) | 적 | 풀버스트 진입 프레임 |
| 6.017 · 9.333 · 12.667 | 사격 5회 누적 | 전도 2 | 적 | 주기 **3.333s** |
| (사격 10회마다) | | 전도(`received_dmg_pct` +12.12, 10s) | 적 | 주기 **6.667s** — 실측 6.017 · 12.667 |
| 13.100 | 10s 만료 | 풀 마스콘 off | 아군 | 풀버스트 종료(13.250)보다 0.15s 먼저 |
| 13.250 | full_burst 종료 · 기동 만료 | 기동 off | 적 | 정확히 10s |
| 15.633 | 브리드 burst_cast (2사이클) | 풀 마스콘 갱신 | | 이후 12.533s 등간격 |
| 15.783 | full_burst 시작 (2사이클) | 기동 · 기동 2 | 적 | |
| 22.000~23.067 | **재장전** (1.067s) | — | — | 전도 2 주기가 여기서 3.333 → 4.417s로 벌어진다 |

사이클 간격: 3.25 → 15.783 → 28.317 → … **12.533s 등간격**(180s에 15회).
브리드는 다른 B2가 없으면 **매 사이클 버스트한다**.

사이클 1 개인 딜 분해(33,076,231): 일반공격 58.5%(190히트 = 19발 × 펠릿 10) ·
전도 2 32.3%(3히트) · 기동 2 9.2%(1히트).
**개인 딜의 6할이 SG 평타**다 — 스킬 딜은 보조다.

`풀 마스콘` 10s < 사이클 12.533s → **사이클당 2.533s 공백**. 기동(10s)도 같다.
`received_dmg_pct` 두 줄이 겹치는 구간에서만 ⑥이 27.24%가 되고, 나머지 구간은 12.12%뿐이다.

## 스킬 간 상호작용

- **`received_dmg_pct` 두 줄의 위상이 다르다** *(⑥ 합산)*
  - 기동(15.12, `full_burst_start`, 10s)은 **사이클에 물려 있고**, 전도(12.12,
    `on_attack_count:10`, 10s)는 **사격 수에 물려 있다**(6.667s 주기, 재장전이 끼면 더 길어짐).
  - 둘이 겹치면 ⑥ = 27.24%. 전도만 있는 구간(풀버스트 종료 후 2.5s)에서는 12.12%.
  - 전도는 주기 6.667s < 지속 10s라 **자기 자신이 상시 유지**된다 — 재장전으로 주기가
    벌어져도 10s를 넘지 않는 한 끊기지 않는다.
- **`hit_count`는 사격 수다 — 펠릿이 아니다** *(SG 공통)*
  - SG 펠릿 10개가 히트 로그에는 10줄로 찍히지만 `hit_count`는 1로 센다.
    실측: 사이클 1의 일반공격 190히트 = 19발인데 전도 2는 3회(19÷5)만 발동했다.
  - 만약 펠릿으로 셌다면 전도 2가 사이클당 38회 터져 개인 딜이 10배가 된다.
- **풀 마스콘은 자신을 제외한다** *(대상 범위)*
  - `all_allies_excl_self`. 브리드가 지원형인데도 자기 버프가 하나도 없는 이유다.
  - 브리드의 개인 딜은 오직 무기·장비·우월 코드와 **다른 아군이 준 버프**에서만 나온다.
- **우월 코드(⑦)가 개인 딜의 절반을 정한다** *(적 설정 의존)*
  - 브리드(작열) vs 풍압 적 → ⑦ = 1 + 0.1 + `element_bonus`(장비 88.6 + 큐브 19.09)/100
    ≈ **2.18배**. 코드가 안 맞으면 1.1배다.
  - 실측 개인 딜 220.9M → 480.9M(+117.7%). **브리드를 코드 미설정 적으로 재면
    반 토막으로 과소평가된다.**

## 유저 확인 사항
- 초안 단계를 거치지 않아 신규 질문 없음. 아래는 파싱·코드에서 확인한 사실.
- **Q: `enemies_code:풍압` 대상만으로 코드 게이팅이 되나?**
  A: **안 된다 — `target_code:풍압` condition이 따로 있어야 한다 (2026-09-02 추가).**
  `enemies_code:코드` / `enemies_lowest_hp_code:코드:N`은 `__enemy__` 센티널만 돌려주고
  **단일 적 시뮬레이터에서는 코드 필터를 무시한다**(`docs/IMPL-STATUS.md`).
  그전에는 브리드 `기동`·`전도`와 이브 `임팩트 타입 엑소스파인 2`에 condition이 없어
  **어느 코드 적에게든 ⑥이 얹혔다** — 마르차나 : 마린 스터디 `고위험 대상`만 양쪽을 달고
  있어 로스터 안에서 관례가 갈려 있었다. 셋 다 마르차나 쪽으로 맞췄다.
  실측: 작열 적에서 기동·전도 **발동 0회**, 풍압 적에서 정상 발동. 코드 미설정은 통과한다
  (`target_code:`의 기존 규칙 — 빈 문자열이면 항상 통과).
  하네스 2스쿼드가 전부 풍압이라 baseline은 안 움직였다.
- Q: `hit_count`가 SG 펠릿을 세나 사격을 세나?
  A: **사격이다.** 사이클 1에서 일반공격 히트 190(=19발 × 펠릿 10)인데 전도 2는 3회 발동했다.
- Q: 전도 2의 `enemies_lowest_hp:1`이 단일 보스에서 의미가 있나?
  A: 단일 보스에서는 없다 — 언제나 그 적이다. 쫄몹이 있으면 **쫄몹 먼저** 규칙이라 남은 체력이 가장 낮은 쫄몹이
  맞고, 그 딜은 총딜 밖이다(`calculator/boss_pattern.py` §쫄몹).
- Q: 기동 2(636%)가 풀버스트 *안*에서 터지나?
  A: **그렇다.** `full_burst_start`(t=3.250)라 풀버스트 진입 프레임이다.
  `burst_cast`(3.100)인 풀 마스콘과 0.15s 차이가 나므로 혼동하지 않는다.

## 해석 선언

| 효과 | 해석 | 근거 | 대안이었다면 |
|---|---|---|---|
| 기동 · 전도 | 코드 게이팅은 **`target_code:풍압` condition이 맡는다** (대상 문자열은 필터가 아니다) | `_resolve_target()`의 `__enemy__` 센티널 · 실측 작열 적 0회 / 풍압 적 정상 | condition이 없으면(수정 전) 다른 코드 적에서도 ⑥ 27.24%가 얹힌다 |
| 기동 2 | `full_burst_start` 시점 (버스트 시전 아님) | parsed timing · 실측 t=3.250 | `burst_cast`였다면 0.15s 빨라지고 풀버스트 진입 버프를 못 받는다 |
| 전도 2 | `on_attack_count:5` = **사격 5회** (펠릿 아님) | 실측 19발/사이클 → 3회 발동 | 펠릿 기준이었다면 사이클당 38회 — 개인 딜이 10배가 된다 |
| 풀 마스콘 | `all_allies_excl_self` — **자신 제외** | parsed target · 실측 대상 3인 | 자신 포함이었다면 브리드 개인 딜이 크게 오른다 (② 66.52는 큰 값이다) |
| 기동 · 전도 | `received_dmg_pct`(⑥, 적이 받는 대미지) — **스쿼드 전체 딜에 곱해진다** | parsed stat · `_factor6` | 브리드 개인 한정이었다면 스쿼드 기여가 대부분 사라진다 |

## impl/bug-fix용 검증 체크리스트
- [ ] 풀버스트 진입 프레임에 기동(디버프 10s) + 기동 2(636% 딜) 각 1회
- [ ] 전도 2 주기 3.333s (사격 5회) · 전도 주기 6.667s (사격 10회) — 재장전 시 벌어짐
- [ ] 전도는 주기 6.667s < 지속 10s → **⑥에서 무단절 유지**
- [ ] 풀 마스콘 대상에 **브리드 자신이 없다** (네거티브)
- [ ] `hit_count`가 펠릿을 세지 않는다 — 사이클당 전도 2가 3~4회 (38회가 아니다)
- [ ] 사이클 간격 12.533s 등간격 · 180s에 15회 · 매 사이클 버스트 (표준)
- [ ] 풀 마스콘 · 기동 10s < 사이클 12.533s → 사이클당 2.533s 공백
- [ ] 풍압 적에서 브리드 개인 딜이 코드 미설정 대비 **+117.7%** (⑦ 우월 코드)
- [ ] 풍압 적으로 바꿔도 **다른 3인의 딜은 불변** (⑦은 시전자 코드에만 걸린다)
- [ ] 작열 등 코드 불일치 적에서 기동·전도 **발동 0회** (`target_code:풍압` — 네거티브)
- [ ] 개인 딜 분해에서 일반공격 비중 약 58% (SG 평타가 본체)
