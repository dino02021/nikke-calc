# 스노우 화이트 동작 시나리오

> 모드: 보강 (parsed_skills 반영)
> **역순 작성** — 이미 등록·검증이 끝난 캐릭터의 시나리오를 뒤늦게 채운 문서다.
> 초안(파싱 전) 단계를 거치지 않았으므로 `## 유저 확인 사항`은 파싱 결과에서
> 역으로 확인한 항목만 담는다. 수치·발동 시각은 전부 실측이다.
> **이격인 `스노우 화이트 : 헤비암즈`와 다른 캐릭터다** — 별칭 `스화`가 이쪽,
> `수화`·`각설`이 헤비암즈다(`ALIASES.md`).

## 기본 정보
- 버스트 단계: B3
- 본인 쿨다운: **40s** (세븐스 드워프 : I)
- 무기 유형: AR (**60발**, 재장전 1.5s, 계수 14.71%, 코어 200%, 연사 12/s, 탄착군 75px)
  - **버스트 중에는 SR로 바뀐다** — 계수 **499.5%**, 차지 **5초**, 풀차지 **1000%**,
    장탄 **1발**, 관통 특화 (`세븐스 드워프 : I`)
- 속성 코드: 철갑 · 클래스: 화력형 · 기업: 필그림 · 스쿼드: 파이오니아
- **애장품 없음** · **기본 레이어 없음**
- 작성 사이클 수: **2** (쿨 40s)

> **버스트마다 딱 한 발.** 그 한 발이 표준 스쿼드에서 개인 딜의 **59.3%**,
> 관통 버퍼가 있는 하네스 조합에서는 **72.0%**를 낸다. 180초에 **7발**이다.
> 나머지 1,656발은 다 합쳐서 그 7발의 절반쯤 된다.

## 효과 요약 (보강: 효과 단위 stat 키)

| # | ID (parsed name) | 스킬 | 트리거 (timing+condition) | stat: 값(lv10) | 대상 | 지속 |
|---|---|---|---|---|---|---|
| 0 | 순백의 의지 | 스킬1 | `hit_count:30` | `bonus_damage`: 82.8% | `target` | — |
| 1 | 순백의 의지 2 | 스킬1 | `hit_count:30` | `atk_pct`: 8.28% | `self` | 5s |
| 2 | 세븐스 드워프 : V&VI | 스킬2 | **`every:15.0s`** | `damage`: 144.73% | `enemies_in_range` | — |
| 3 | 세븐스 드워프 : V&VI 2 | 스킬2 | `every:15.0s` + `during_full_burst` | `crit_rate`: 26.1% | `self` | 10s |
| 4 | 세븐스 드워프 : I | 스킬3 | `burst_cast` | **`weapon_change`** → SR | `self` | **1발** |
| 5 | 세븐스 드워프 : I 2 | 스킬3 | `burst_cast` | `pierce_enabled` | `self` | **1발** |

**미구현 stat은 없다.** 다만 `pierce_enabled`는 **아군이 관통 대미지를 주지 않으면
아무것도 안 한다**(§관통) — 표준 스쿼드에서 `pierce_dmg_pct`가 정확히 0이다.

## 검증 스쿼드

- **표준 (B3 · 쿨 40s)**:
  `["리틀 머메이드", "크라운", "스노우 화이트", "test_B3"]`
  - 적 코드는 **철갑**(자기 코드 = 우월 코드 불성립)이 기준이다.
  - **주의**: GAMEPLAY §표준 테스트 스쿼드가 B3 보조로 쓰는 것은
    `스노우 화이트 : 헤비암즈`이지 이 캐릭터가 아니다. 섞지 않는다.
- **변형 1 (관통 버퍼 — 필수)**: 하네스 `S39_맥스웰츠바이`를 그대로 쓴다.
  츠바이가 `pierce_dmg_pct`를 다섯 줄(합 95.7) 뿌려 **무기 변경 한 발이 ×5.9**가 된다.
- **변형 2 (우월 코드)**: 표준 + `enemy={"code": "전격"}` — 개인 딜 **×2.18**.
  캐릭터 고유 `element_bonus_pct`가 없어 공통 장비·큐브 107.69%만 실린다.
- **변형 3 (코어 보스)**: 표준 + `enemy={"core_px": 52}` — 개인 딜 **+26.5%**.
  AR 탄착군 75px이라 명중률 버프 없이도 코어 확률이 붙는다.

- **실전 조합 (하네스)** — `S39_맥스웰츠바이` **1개**뿐이다:

  | 스쿼드 | 적 | 스쿼드 총딜 | 스노우 화이트 | 무기 변경 7발 |
  |---|---|---|---|---|
  | `S39_맥스웰츠바이` | 전격 (**우월 코드**) | 3,269,287,771 | **1,394,632,089 (42.7%)** | 1,003,632,981 (**72.0%**) |

  멤버가 나유타 · 맥스웰 · **츠바이** · 헬름 : 아쿠아마린이다.
  츠바이의 관통 대미지 버프가 이 캐릭터의 유일한 실전 검증 축이다.

실측 (180s, `--expected`, `first_burst_time=3.0`, 기본 스펙 1층):

| 설정 | 스쿼드 총딜 | 스노우 화이트 | 무기 변경 발당 |
|---|---|---|---|
| 표준 (적 철갑) | 1,716,718,469 | **286,237,781 (16.7%)** | 24,243,097 |
| 표준 · 적 **전격**(우월 코드) | 1,800,665,749 | 623,110,666 (34.6%) | 52,774,798 |
| 표준 · **코어 52px** | 2,072,296,017 | 362,003,580 (17.5%) | 30,990,291 |
| `S39_맥스웰츠바이` (관통 버퍼) | 3,269,287,771 | 1,394,632,089 (42.7%) | **143,376,140** |

딜 구성 (표준 · 적 철갑):

| 태그 / 출처 | 딜 | 비중 | 히트 | 히트당 |
|---|---|---|---|---|
| `full_charge_hit` (**무기 변경 한 발**) | 169,701,680 | **59.3%** | **7** | **24,243,097** |
| `normal` (AR 평타) | 93,383,001 | 32.6% | 1,656 | 56,390 |
| `순백의 의지` (`bonus_damage`) | 17,371,899 | 6.1% | 55 | 315,852 |
| `세븐스 드워프 : V&VI` | 5,781,201 | 2.0% | 11 | 525,563 |

버스트 구간별: **풀버스트(본인) 72.1%** · 풀버스트(타인) 20.0% · 비풀버스트 7.9%.

## 버스트 = 5초 차지 한 발

`세븐스 드워프 : I`은 `weapon_change`이고 `duration_bullets: 1`이다.
버스트를 누르면 무기가 SR(계수 499.5% · 풀차지 1000% · 장탄 1발)로 바뀌고,
**5초를 차지해 한 발을 쏘면 모드가 끝난다**.

실측 발사 시각(표준):

| 버스트 | 무기 변경 발사 | 차지 소요 |
|---|---|---|
| 3.200 | **8.217** | 5.017s |
| 28.267 | 33.283 | 5.016s |
| 53.334 → 158.600까지 25.067s 간격 | 58.350 · 83.417 · 108.483 · 133.533 · 158.600 | 〃 |

**180초에 7발이다.** 버스트는 8회지만 마지막(178.669)은 차지가 끝나기 전에
시뮬이 끝난다. 발사 직후 AR로 돌아와 8.317에 재장전을 시작한다.

한 발이 풀버스트 안에 떨어지는 것도 구조적이다 — 버스트 시각 +5.017초는
풀버스트(버스트 +0.05초부터 10초) 한가운데다.

> 무기 변경 복귀 시 차지가 초기화되지 않아 공짜 풀차지가 나오던 결함(`63a784e`)은
> 벨벳·타키나·라플라스·레드 후드 넷의 문제였다. **스노우 화이트는 원래 정상이었다** —
> `duration_bullets: 1`이라 발사와 동시에 모드가 끝나 재진입 경로를 안 탄다.

## `pierce_enabled`는 표준 스쿼드에서 완전한 no-op다

`세븐스 드워프 : I 2`가 그 한 발에 `pierce_enabled`를 붙인다. 그런데 기본 경로에서
관통은 **`pierce_dmg_pct`를 ⑤에 가산하는 것**뿐이다(`damage.py §⑤` — 관통이 표적을 더 맞히는 것은 보스
스크립트의 reach 표적·좌표 모드에서만이다). 스노우 화이트 본인은 `pierce_dmg_pct`를 **한 줄도 안 갖는다.**

실측 `get_buffs()` — 무기 변경 발사 시점(t≈8.0):

| 스쿼드 | `pierce_dmg_pct` | `crit_rate` | `atk_pct` | 발당 딜 |
|---|---|---|---|---|
| 표준 4인 | **0.0** | 0.15 | 22.22 | 24,243,097 |
| `S39_맥스웰츠바이` | **130.19** | 0.7863 | 65.32 | **143,376,140 (×5.9)** |

130.19를 만드는 것은 **츠바이 한 명**이다 — `관통 계산식`(20.13) ·
`관통 계산식 2`(10.06) · `관통 계산식 3`(24.99, `on_attack`이라 상시 갱신) ·
`과충전 공식 3`(15.48) · `관통학 개론`(25.03)이 전부 `all_allies`다.

**관통 버퍼가 없는 조합에서 이 캐릭터를 재면 스킬3의 절반이 죽은 상태로 잰다.**
표준 스쿼드가 그 상태다.

## AR 구간 — 30발마다 한 방

`순백의 의지`는 `hit_count:30`(**발사 30회마다**)이다. 실측 **55회**
(= 평타 1,656발 ÷ 30). `bonus_damage` 82.8%를 적에게 꽂고, 동시에
`순백의 의지 2`가 자기 공격력 +8.28%를 5초 준다.

30발은 12/s에서 2.5초라 `순백의 의지 2`(5초)가 **끊기지 않고 계속 갱신**된다 —
사실상 상시 +8.28%다.

`세븐스 드워프 : V&VI`는 `every:15.0s`로 **사이클과 무관하게** 15초마다 돈다
(실측 15.000 · 30.017 · 45.017 … 165.000, **11회**). 풀버스트 안에서 발동한
8회만 `V&VI 2`(크리 +26.1, 10초)를 함께 준다 — 11회 중 3회는 창 밖이다.

## 사이클 타임라인 (표준, 2사이클)

| t (실측) | 이벤트 | 발동 효과 (ID / stat) | 비고 |
|---|---|---|---|
| 3.000 · 3.100 | 리틀 머메이드 · 크라운 | | |
| **3.200** | **스노우 화이트 burst_cast** | **세븐스 드워프 : I**(무기 변경 → SR, 장탄 1, 차지 5초) · **I 2**(관통, 1발) | 여기서 AR이 멈춘다 |
| 3.250 | full_burst 시작 | | |
| **8.217** | **무기 변경 한 발** | `full_charge_hit` **21,798,844** | 차지 5.017초 · 관통 |
| 8.217 | 모드 종료 | `세븐스 드워프 : I 2` 소멸 (1발 소모) | AR 복귀 |
| 8.317 → 8.717 | 재장전 | | 0.4초 (`원 포 올 2`) |
| 13.250 | full_burst 종료 | | |
| **15.000** | `every:15.0s` | **세븐스 드워프 : V&VI** 413,203 | **풀버스트 밖이라 `V&VI 2`는 없다** |
| 15.533~15.733 | 리틀 머메이드 · 크라운 · test_B3 | | 스노우 화이트는 쿨 |
| 15.783 | full_burst 시작 (2회째) | | 본인 버프 없음 |
| 24.067 → 24.467 | 재장전 | | 평타 60발 소진 |
| **28.267** | 스노우 화이트 burst_cast (2회째) | 무기 변경 · 관통 | **간격 25.067s** |
| **30.017** | `every:15.0s` | 세븐스 드워프 : V&VI **668,178** + **V&VI 2**(크리 +26.1) | **풀버스트 안이라 둘 다** |
| 33.283 | 무기 변경 한 발 | **26,900,038** | `V&VI 2`의 크리가 실렸다 |

사이클 간격 **12.533s 등간격**(180초 15회), 스노우 화이트 버스트 8회 ·
무기 변경 발사 **7회**.

## 스킬 간 상호작용

- **`세븐스 드워프 : I` → 딜 구조 전체** *(한 발에 몰빵)*
  - 계수 499.5% × 풀차지 1000%가 한 히트로 나간다. AR 평타(14.71%)의 **430배**다.
  - `duration_bullets: 1`이라 **놓칠 수 없다** — 쏘면 끝, 안 쏘면 모드가 안 끝난다.
    차지 5초가 사이클(12.533초)보다 짧아 표준에서는 항상 제때 나간다.
- **`세븐스 드워프 : I 2`(관통) ↔ 아군 `pierce_dmg_pct`** *(조합 의존 · ×5.9)*
  - 본인은 관통 대미지를 안 갖는다. 츠바이 같은 관통 버퍼가 있어야 산다.
  - `is_pierce_damage` 히트에만 붙으므로 **그 한 발에만** 실린다 — 관통 버프의
    가치가 이 캐릭터에서는 통째로 7발에 집중된다.
- **`세븐스 드워프 : V&VI 2`(크리 +26.1) → 무기 변경 발** *(주기 어긋남)*
  - `every:15.0s`와 버스트 주기 25.067초가 서로 어긋나 **어떤 사이클에는 크리 버프가
    얹히고 어떤 사이클에는 안 얹힌다**. 실측 발당 딜이 21,798,844 / 23,605,034 /
    26,900,038 세 값으로 갈리는 이유다.
- **`순백의 의지 2`(공격력 +8.28)** *(사실상 상시)*
  - 30발 = 2.5초, 지속 5초라 갱신이 끊기지 않는다. 다만 무기 변경 5초 동안은
    평타가 멈추므로 그 구간에서 한 번 만료될 수 있다.
- **`세븐스 드워프 : V&VI`의 `enemies_in_range`** *(단일 보스에서 1히트)*
  - 범위 스킬이지만 적이 하나라 히트도 하나다.

## 유저 확인 사항
- 초안 단계를 거치지 않아 신규 질문 없음. 아래는 파싱·코드에서 확인한 사실.
- Q: 버스트 8회인데 무기 변경 발사가 왜 7회인가?
  A: **차지가 5.017초라 마지막 버스트(178.669)의 한 발이 180초 안에 안 떨어진다.**
  실측 발사 시각이 8.217부터 25.067초 간격으로 158.600까지다.
- Q: 표준 스쿼드 수치로 이 캐릭터를 평가해도 되나?
  A: **관통 축이 빠진 값이다.** 표준에서 `pierce_dmg_pct`가 0이라
  `세븐스 드워프 : I 2`가 완전한 no-op다. 츠바이가 있는 `S39_맥스웰츠바이`에서는
  같은 한 발이 24,243,097 → **143,376,140 (×5.9)**이 된다.
- Q: `every:15.0s`의 15초는 어디서 온 값인가?
  A: **원문에도 스크랩 데이터에도 없다.** `쿨타임` 필드가 `null`이라
  `PARSING.md §317~318`의 규칙대로 **유저 인게임 확인**으로 채운 값이다.
  같은 `every:15.0s`를 쓰는 리타·이사벨은 스크랩에 쿨타임이 있는 경우다.
- Q: 무기 변경 복귀 시 공짜 풀차지 결함(`63a784e`)이 이 캐릭터에도 있었나?
  A: **없었다.** `duration_bullets: 1`이라 발사와 동시에 모드가 끝나
  재진입 경로 자체를 안 탄다. 그 결함은 벨벳·타키나·라플라스·레드 후드 넷이었다.

## 해석 선언

| 효과 | 해석 | 근거 | 대안이었다면 |
|---|---|---|---|
| 세븐스 드워프 : I | `weapon_change` + `duration_bullets: 1` — **한 발 쏘면 종료** | 원문 `최대 장탄 수 : 1발` · 실측 발사 즉시 AR 복귀 | 시간 지속이었다면 차지 5초 무기로 여러 발을 쏜다 |
| 세븐스 드워프 : I | 계수 **레벨 스케일링**(lv10 499.5) | 원문 `대미지 : 최종 공격력 {0}%` · `PARSING-CHARS §신데렐라 : 크리스탈 웨이브` 대비 | 하드코딩이었다면 저레벨 결과가 틀린다 |
| 세븐스 드워프 : I 2 | `pierce_enabled`만 — **관통 대미지는 본인에게 없다** | 원문 `추가 효과 : 관통 특화` · 실측 표준 `pierce_dmg_pct` 0 | 관통 대미지를 같이 줬다면 조합 의존이 사라진다 |
| 세븐스 드워프 : V&VI | `every:15.0s` — **원문·스크랩에 근거 없음, 유저 인게임 확인** | `쿨타임: null` · `PARSING.md §317~318` | 주기가 다르면 발동 11회와 크리 창이 통째로 달라진다 |
| 세븐스 드워프 : V&VI 2 | `during_full_burst` **condition** (timing은 같은 `every:15.0s`) | 원문 `풀 버스트 타임 지속 시간 중 발동 시` · 실측 11회 중 8회 | timing이었다면 풀버스트 시작마다 걸려 15회가 된다 |
| 순백의 의지 | `hit_count:30` — **발사 30회**(펠릿·총구 무관) | `_fire()`의 `hit_count` notify가 루프 밖 · 실측 1,656 ÷ 30 = 55 | AR은 1펠릿 1총구라 어느 해석이든 같다 |
| 세븐스 드워프 : V&VI | 대상 `enemies_in_range` — 단일 보스에서 1히트 | 원문 `공격 범위 내 적들에게` | — |

## impl/bug-fix용 검증 체크리스트
- [ ] 이탈 보고가 **`기본 스펙(1층) 그대로`** 한 줄 (레이어·애장품 없음)
- [ ] 무기 변경 발사 **7회**, 시각 8.217부터 **25.067초 간격**으로 158.600까지
- [ ] 차지 소요 **5.017초** (버스트 3.200 → 발사 8.217), 발사 직후 AR 복귀·재장전
- [ ] `full_charge_hit` 7히트가 개인 딜의 **59.3%** (표준) / **72.0%** (하네스)
- [ ] 표준에서 `pierce_dmg_pct`가 **정확히 0** — `세븐스 드워프 : I 2`가 no-op (네거티브)
- [ ] `S39_맥스웰츠바이`에서 발사 시점 `pierce_dmg_pct` **130.19**, 발당 **×5.9**
- [ ] `순백의 의지` **55회** (= 평타 1,656 ÷ 30)
- [ ] `세븐스 드워프 : V&VI` **11회**(15초 등간격) 중 `V&VI 2`는 **8회**만 (풀버스트 안)
- [ ] 무기 변경 발당 딜이 **21,798,844 / 23,605,034 / 26,900,038** 세 값으로 갈린다
      (`V&VI 2` 크리 창과 버스트 주기가 어긋난다)
- [ ] 코어 52px에서 개인 딜 **+26.5%** · 적 전격(우월 코드)에서 **×2.18**
- [ ] 스노우 화이트 버스트 간격 **25.067s**, 180초 8회 · 사이클 간격 12.533s
