# 아니스 : 스타 동작 시나리오

> 모드: 보강 (parsed_skills 반영)
> **역순 작성** — 등록·검증이 끝난 캐릭터의 시나리오를 뒤늦게 채운 문서다.
> 초안(파싱 전) 단계 없음. 수치·발동 시각은 전부 실측이다.

## 기본 정보
- 버스트 단계: B1
- 본인 쿨다운: 20s (스타 아니스)
- 무기 유형: RL (6발, 재장전 2.0s, 차지 1.0s, 계수 61.3%, 풀차지 250%, **풀차지 전용**, 코어 200%)
- 속성 코드: 전격 · 클래스: 방어형 · 기업: 테트라
- 애장품: **없음**
- 작성 사이클 수: 1 (매 사이클 버스트) — 단, **분기가 둘이라 스쿼드는 2개 필요**

## 핵심 전제 — 스쿼드에 다른 B1이 있느냐로 킷이 통째로 갈린다

스킬1의 `[스쿼드 구성 별 효과] [해당되는 효과만 적용]` 분기다. `battle_start`와
**`full_burst_end`마다 재평가**하며, 반대편 상태를 `remove_named_buff`로 지우고 자기 것을 켠다.

| 분기 | 조건 | 켜지는 상태 | 딸려오는 것 |
|---|---|---|---|
| **나만의 별** | `no_burst1_ally` (자신 외 B1 없음) | `atk_pct` +40.01% (self, 영구) | 스타 폴 3(아군 전체 버스트 쿨 −7.48s) · 스타더스트(`atk_caster_based_pct` 35.01%) · 스타 아니스(`atk_dmg_pct` 35.2%) |
| **모두의 별** | `has_burst1_ally` (자신 외 B1 있음) | `burst_stage_override:reenter1` (self, 영구) | 스타더스트 2(회복) · 스타 아니스 2(`hp_caster_based_pct` 15.02%) |

**나만의 별 = 자기 딜러 모드, 모두의 별 = 1버 재진입 인에이블러**다.
`docs/GAMEPLAY.md` §표준 테스트 스쿼드가 "아니스 : 스타를 B1 슬롯 대용으로 쓰지 말라"고
못박은 이유가 이 분기다 — 다른 B1이 끼는 순간 사이클 구조 자체가 바뀐다.

`모두의 별`의 실제 동작(실측): 아니스가 `stage:1`을 쓴 **직후** 스쿼드가 1단계로 재진입해
**다른 B1이 한 번 더** 버스트한다(`_check_reenter`). 즉 아니스가 재진입 슬롯을 가져가는 게
아니라, **아니스가 재진입을 열어 주고 동료가 그 슬롯을 쓴다.**

```
t=3.000  stage:1 사용   아니스 : 스타
t=3.500  reenter:1 사용  리틀 머메이드     ← 모두의 별이 연 슬롯
t=3.600  stage:2 사용   크라운
t=3.700  stage:3 사용   test_B3
t=3.750  full_burst 시작
```

## 효과 요약 (보강: 효과 단위 stat 키)

| ID (parsed name) | 스킬 | 트리거 (timing+condition) | stat: 값(lv10) | 대상 | 지속 |
|---|---|---|---|---|---|
| 스타 폴 | 스킬1 | `battle_start` | `burst_charge_speed_pct`: 6.0% | `all_allies` | -1 |
| 스타 폴 2 | 스킬1 | `battle_start`,`full_burst_end` + `no_burst1_ally` | `remove_named_buff` → `모두의 별` | self | — |
| **나만의 별** | 스킬1 | `battle_start`,`full_burst_end` + `no_burst1_ally` | `atk_pct`: 40.01% | self | -1 |
| 스타 폴 3 | 스킬1 | `battle_start`,`full_burst_end` + `no_burst1_ally` | `burst_cooldown_reduce`: 7.48s | `all_allies` | — |
| 스타 폴 4 | 스킬1 | `battle_start`,`full_burst_end` + `has_burst1_ally` | `remove_named_buff` → `나만의 별` | self | — |
| **모두의 별** | 스킬1 | `battle_start`,`full_burst_end` + `has_burst1_ally` | `burst_stage_override:reenter1` | self | -1 |
| 스타 폴 5 | 스킬1 | `full_charge_hit` | `bonus_damage`: 120.13% | `target` | — |
| 스타더스트 | 스킬2 | `full_burst_start` + `self_state:나만의 별` | `atk_caster_based_pct`: 35.01% | `all_allies` | 10s |
| 스타더스트 2 | 스킬2 | `full_charge_fire` + `self_state:모두의 별` | `heal_hp_pct`: 1.26% | `all_allies` | — |
| 스타더스트 3 | 스킬2 | `full_burst_start` | `projectile_explosion_dmg_pct`: 92.03% | `["self", "allies_below_def"]` | 10s |
| 스타더스트 4 | 스킬2 | `full_burst_start` | `atk_dmg_pct`: 34.0% | `all_allies` | 10s |
| 슈팅 스타1 | 스킬3 | `burst_cast` | `auto_damage`: 40.01% (`damage_formula: normal_attack`, `tick_interval: 0.25`, `tick_start: immediate`, `weapon_type: RL`) | `enemies_random` | 10s |
| 슈팅 스타2 | 스킬3 | `burst_cast` | `charge_time_fixed`: **0.7초** (`fixed_value: 0.7`) | self | 10s |
| 슈팅 스타3 | 스킬3 | `burst_cast` | `explosion_range`: 100.0% | self | 10s |
| 슈팅 스타4 | 스킬3 | `burst_cast` | `def_pct`: 55.01% | self | 10s |
| 스타 아니스 | 스킬3 | `burst_cast` + `self_state:나만의 별` | `atk_dmg_pct`: 35.2% | self | 10s |
| 스타 아니스 2 | 스킬3 | `burst_cast` + `self_state:모두의 별` | `hp_caster_based_pct`: 15.02% | `all_allies` | 10s |

### 딜에 안 실리는 stat 3종

| stat | 상태 | 결과 |
|---|---|---|
| `burst_charge_speed_pct` (스타 폴) | 🚫 보류 (버스트 게이지 모델 단순화) | 스킬1 첫 블록이 통째로 딜 무관 |
| `explosion_range` (슈팅 스타3) | ✅ 보스 패턴 전용 | 폭발 범위 — 기본 경로(단일 보스)에서는 0. 보스 스크립트의 reach 표적(좌표 off)·폭발 원(좌표 모드)에서만 닿는 표적을 늘린다 |
| `def_pct` (슈팅 스타4) | ⚠️ 집계만, DPS 미사용 | 방어형 생존 버프. 버스트 10초 동안 자기 최종 방어력을 올려 스타더스트 3의 `allies_below_def` 판정에만 들어간다 |

## 검증 스쿼드
- **표준 A — 나만의 별 (B1 · 쿨 20s)**:
  `["아니스 : 스타", "크라운", "test_B3", "스노우 화이트 : 헤비암즈"]`
  - 자신 외 B1 없음 → `no_burst1_ally` 참.
- **표준 B — 모두의 별 (B1 2명)**:
  `["아니스 : 스타", "리틀 머메이드", "크라운", "test_B3", "스노우 화이트 : 헤비암즈"]`
  - 자신 외 B1(리틀 머메이드) 있음 → `has_burst1_ally` 참. 재진입 슬롯 검증용.
  - **두 스쿼드 모두 필수다.** 어느 한쪽만 돌리면 킷의 절반이 검증되지 않는다.

- **실전 조합 (하네스)** — 세 조합 모두 **다른 B1이 함께 있어 `모두의 별` 분기**다:

  | 스쿼드 | 멤버 | 같이 든 B1 | 적 |
  |---|---|---|---|
  | `S36_이사벨아르카나` | 이사벨 · **아니스 : 스타** · 신데렐라 · 아르카나 · 크라운 | 신데렐라 | 수냉 |
  | `S39_나가라피` | 크라운 · 나가 · **아니스 : 스타** · 라피 : 레드 후드 · 프리바티 | 라피 : 레드 후드(`전투 보조`) | 전격 |
  | `S40_브리드라피` | 브리드 : 사일런트 트랙 · 라피 : 레드 후드 · **아니스 : 스타** · 미하라 : 본딩 체인 · 프리바티 | 라피 : 레드 후드 | 풍압 |

  **실전에서 아니스 : 스타는 `나만의 별`(자기 딜러)이 아니라 `모두의 별`(재진입 인에이블러)로 쓰인다.**
  아래 표준 A는 분기 자체를 격리해 보기 위한 인공 편성이다.

실측 (180s, `--expected`):

| 스쿼드 | 스쿼드 총딜 | 아니스 딜 | 아니스 비중 |
|---|---|---|---|
| A (나만의 별) | 2,387,294,696 | **748,060,364** | 31.3% |
| B (모두의 별) | **2,793,138,497** | 516,871,655 | 18.5% |

**본인 딜은 A가 +44.7% 높은데 스쿼드 총딜은 B가 +17.0% 높다.** 아니스가 여는 재진입 슬롯을
리틀 머메이드가 채워 659M을 넣으면서 스쿼드 딜이 406M 늘어나는 쪽이 더 크기 때문이다.
이 캐릭터를 총딜로 평가할 때 **본인 지분만 보면 정확히 거꾸로 읽는다.**

## 사이클 타임라인

### A — 나만의 별 (표준 A)

| t (실측) | 이벤트 | 발동 효과 (ID / stat) | 대상/지속 | 비고 |
|---|---|---|---|---|
| 0 | battle_start (`no_burst1_ally`) | 스타 폴(`burst_charge_speed_pct`, 미구현) · 스타 폴 2(모두의 별 제거, no-op) · **나만의 별**(`atk_pct` +40.01) · 스타 폴 3(아군 전체 쿨 −7.48s) | self / all_allies | 아니스 쿨 20 → 12.52 |
| 1.00~ | RL 풀차지 사격 (**1.033s 간격**) | 스타 폴 5(`bonus_damage` 120.13%) 매 발 | 대상 | `full_charge_only` — 모든 발이 풀차지 |
| 3.00 | **아니스 burst_cast** | 슈팅 스타1(자동공격 40.01%, 0.25s×10s) · **슈팅 스타2(차지 1.0 → 0.7s 고정)** · 3·4 · **스타 아니스**(`atk_dmg_pct` +35.2) | self / 10s | `self_state:나만의 별` 통과 · 사격 간격 1.033 → **0.733s** |
| 3.00~13.00 | 슈팅 스타1 틱 | 사이클당 **40히트** (즉시 첫 틱) | 무작위 적 | 실측 40히트 |
| 3.25 | **full_burst 시작** | **스타더스트**(`atk_caster_based_pct` +35.01, 전체) · 스타더스트 3(`projectile_explosion_dmg_pct` +92.03) · 스타더스트 4(`atk_dmg_pct` +34.0, 전체) | all_allies / 10s | |
| 13.25 | full_burst 종료 → `full_burst_end` | 분기 재평가 (여전히 `no_burst1_ally`) → 나만의 별 유지 + 스타 폴 3 재발동(쿨 −7.48s) | | **쿨감이 사이클마다 다시 들어온다** |
| 15.533 | 아니스 burst_cast (2회째) | 위 반복 | | 사이클 **12.53s** |

사이클 간격 실측: 3.25 → 15.783 → 28.317 → 40.850 → 53.383 → 65.917 → … **12.533s 등간격**
(= 20 − 7.48).

사이클당 딜 구성 (실측, 사이클 2 = 15.783~28.317s, 합 54.2M):
일반공격 24.3M(15히트, 44.7%) · 슈팅 스타1 17.4M(40히트, 32.1%) · 스타 폴 5 12.6M(15히트, 23.2%).
**슈팅 스타1이 일반공격의 3/4에 달한다** — 버스트가 딜의 본체다.

### B — 모두의 별 (표준 B)

| t (실측) | 이벤트 | 발동 효과 | 비고 |
|---|---|---|---|
| 0 | battle_start (`has_burst1_ally`) | 스타 폴 · 스타 폴 4(나만의 별 제거, no-op) · **모두의 별**(`burst_stage_override:reenter1`) | `atk_pct` 없음 · 쿨감 없음 |
| 3.000 | 아니스 `stage:1` | 슈팅 스타1~4 · **스타 아니스 2**(`hp_caster_based_pct`, 전체) | 스타 아니스(자기 `atk_dmg_pct`) 없음 |
| **3.500** | **리틀 머메이드 `reenter:1`** | — | 모두의 별이 연 슬롯 |
| 3.750 | full_burst 시작 | 스타더스트 3 · 스타더스트 4 (스타더스트는 **미발동**) | `self_state:나만의 별` 불충족 |
| 13.75 | full_burst 종료 | 분기 재평가 → 모두의 별 유지 | |
| 15.75 | 아니스 `stage:1` (2회째) | | 사이클 **12.75s** |

사이클 간격 실측: 3.75 → 16.5 → 29.25 → 42.0 → … **12.75s 등간격**
(아니스 자체 쿨감이 없어 리틀 머메이드의 `버블 오더 2`(`full_burst_end` −7.48s)에 의존한다).

## 스킬 간 상호작용

- **분기 상태 → 스킬2·3의 조건부 블록 (하나의 상태가 3개 효과를 가른다)**
  - `나만의 별` → 스타더스트(전체 공격력) + 스타 아니스(자기 공격 대미지)
  - `모두의 별` → 스타더스트 2(회복) + 스타 아니스 2(전체 최대 체력)
  - 시점: 상태는 `battle_start`/`full_burst_end`에 확정되고, 소비는 `burst_cast`·`full_burst_start`.
    **부여가 항상 소비보다 한 박자 앞서므로 순서 사고가 없다.**
  - `_has_self_state`는 `_active`의 `target_chars`를 보므로, `target: self`인 두 상태 모두 정상 판정된다.
- **`나만의 별` → 스타 폴 3(쿨감) → 사이클 단축 → 버스트 딜 밀도 상승**
  - 순환 강화. `full_burst_end`마다 재발동하므로 사이클마다 쿨감이 새로 들어온다.
  - 위험: `모두의 별` 분기에는 이 쿨감이 **없다**. B 스쿼드에서 아니스 사이클은 전적으로
    동료 B1의 쿨감에 얹혀 간다.
- **`모두의 별` → 재진입 슬롯 → 동료 B1의 추가 버스트**
  - 경로: `burst_stage_override:reenter1`(self) → `_check_reenter()`가 아니스의 캐스트 직후
    `reenter:1` 페이즈를 연다 → **다른 B1**이 그 슬롯을 쓴다.
  - 아니스 본인은 재진입에 참여하지 않는다(방금 썼으므로 쿨 중).
  - 위험: 다른 B1이 쿨 중이면 슬롯이 비어 이득이 0이다.
- **`슈팅 스타2`(차지 0.7초 고정) → 사이클당 사격 수 → `스타 폴 5` 발동 수**
  - 경로: 버스트 10초 동안 사격 간격 1.033s → 0.733s → 그 구간 사격이 늘고,
    `full_charge_only`라 늘어난 발마다 `스타 폴 5`가 따라붙는다.
  - `fixed_value: 0.7`이 없으면 `_fixed_charge_time()`이 base(1.0s)로 폴백해 **단축이 통째로
    사라진다.** 대조 실측(표준 A): 아니스 632,148,822 → **748,060,364 (+18.3%)**,
    스쿼드 총딜 2,271,383,154 → 2,387,294,696 (+5.1%).
- **`스타 폴 5`(풀차지 명중 추가 대미지) × `full_charge_only`**
  - 아니스는 RL 풀차지 전용이라 **모든 사격이 풀차지 트리거**(`full_charge_fire` + `full_charge_hit`)다.
    → 스타 폴 5가 매 발 터진다(사이클당 15회).
  - 위험: 톡톡이(논차지) 컨트롤을 주면 이 딜이 통째로 사라진다. 아니스에게는 톡톡이를 쓰지 않는다.

## 유저 확인 사항
- 초안 단계 없음. 아래는 파싱·코드·실측에서 확인한 사실.
- Q: `모두의 별`이면 아니스가 재진입 슬롯을 쓰나?
  A: **아니다.** 아니스는 `stage:1`을 정상으로 쓰고, 그 **직후** 열리는 `reenter:1`을 다른 B1이 쓴다.
- Q: 분기는 언제 재평가되나?
  A: `battle_start`와 **매 `full_burst_end`**. 스쿼드 구성이 안 바뀌면 결과도 안 바뀌지만,
  `스타 폴 3`(쿨감)은 재평가 때마다 다시 발동하므로 **사이클마다 쿨감이 들어온다.**
- Q: 버스트 중 차지 시간이 0.7초로 줄어드나?
  A: **줄어든다.** 실측 사격 간격이 비버스트 1.033s → 버스트 **0.733s**(= 0.7 차지 + 0.033 발사 후 딜레이).
  사이클당 사격 수가 11~13발 → **15발**로 늘고, 스타 폴 5도 같이 늘어난다.
- Q: 슈팅 스타4 `[방어력 N% ▲]`는 시전자 기준인가?
  A: **아니다 — 원문대로 `def_pct`** (유저 결정 2026-09-25). 원문에 「시전자 기준」이 없고 같은 문구 선례
  23명이 전부 `def_pct`다. 종전 `def_caster_based_pct`는 `runner.parsecheck` 검사 Q가 찾은 이탈이었다.
  대상이 자신이라 두 stat의 최종 방어력(`_effective_def()`)이 같아 딜·발동·대상이 한 자리도 안 바뀐다
  (S36_이사벨아르카나 · S39_나가라피 · S40_브리드라피 baseline은 L2 stat 이름만 갱신).

## 해석 선언

| 효과 | 해석 | 근거 | 대안이었다면 |
|---|---|---|---|
| 나만의 별 / 모두의 별 | 상호 배타, `full_burst_end`마다 재평가 | `remove_named_buff` 쌍(스타 폴 2·4) · 조건 `no_burst1_ally`/`has_burst1_ally` | 동시 활성이면 스타더스트와 스타 아니스 2가 겹쳐 이중 수혜 |
| 모두의 별 | 아니스가 **재진입을 열고 동료가 쓴다** | `_check_reenter()` 실측 로그 (t=3.0 아니스 stage:1 → t=3.5 리틀 머메이드 reenter:1) | 아니스가 직접 재진입이면 20s 쿨에 막혀 슬롯이 비어 버린다 |
| **슈팅 스타2** | 버스트 10초 동안 차지 **0.7초로 절대 고정**(차지속도 버프도 함께 무효화) | 원문 `[차지 시간 0.7초로 고정]` → `fixed_value: 0.7` · 실측 사격 간격 0.733s | `fixed_value`를 비워 두면 `_fixed_charge_time()`이 base(1.0s)로 폴백해 **단축이 통째로 사라진다** — 표준 A 기준 아니스 본인 딜 차이가 **+18.3%**다 |
| 슈팅 스타1 | 즉시 첫 틱, 0.25s×10s = **40틱**, 일반공격 공식 | 파싱 `tick_start: immediate` (`note`에 "유형 미확인 — 유저 지시") · 실측 40히트 | 지연 첫 틱이면 39틱, 무기 공식이 아니면 계수 체계가 달라진다 |
| 스타 폴 5 | 모든 사격이 풀차지라 매 발 발동 | `full_charge_only: true` · 실측 15히트/사이클 | 논차지 컨트롤을 주면 0회 |
| 스타더스트 3 | 대상이 `["self", "allies_below_def"]` 복합 | 파싱 필드 그대로 | 아니스가 방어형이라 대부분 아군이 조건을 만족한다 — `all_allies`와 실질 차이가 작다 |

## impl/bug-fix용 검증 체크리스트
- [ ] 스쿼드 A(단독 B1): `나만의 별` 활성 · `모두의 별` 0회 / 스쿼드 B(B1 2명): 반대
- [ ] A 풀버스트 진입 시 활성: 나만의 별 · 스타더스트 · 스타더스트 3 · 스타더스트 4 · 슈팅 스타 2·3·4 · 스타 아니스
- [ ] B 풀버스트 진입 시 활성: 모두의 별 · 스타더스트 3 · 스타더스트 4 · 슈팅 스타 2·3·4 · **스타 아니스 2** (스타더스트·스타 아니스는 **없어야** 함)
- [ ] B에서 `reenter:1` 로그가 **아니스가 아닌 다른 B1** 이름으로 찍히는지
- [ ] 사이클 간격: A **12.533s**(= 20 − 7.48) / B **12.75s**(동료 쿨감 의존)
- [ ] `슈팅 스타1`이 사이클당 **40히트**
- [ ] `스타 폴 5`가 사이클당 사격 수와 같은 횟수(**15회**) 발동
- [ ] `스타 폴 3`(쿨감)이 `battle_start` + **매 `full_burst_end`**에 발동 (A 분기에서만)
- [ ] 발동 금지(A): 스타더스트 2 · 스타 아니스 2 / 발동 금지(B): 스타더스트 · 스타 아니스 · 스타 폴 3
- [ ] 버스트 중 사격 간격이 **1.033s → 0.733s**로 짧아지는지 (`슈팅 스타2` 차지 0.7초 고정)
- [ ] `슈팅 스타2`에 `fixed_value: 0.7`이 살아 있는지 — 빠지면 표준 A 아니스 딜이 −15.5%로 조용히 내려앉는다
