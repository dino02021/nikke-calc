# 앵커 : 이노센트 메이드 동작 시나리오

> 모드: 보강 (parsed_skills 반영)
> **역순 작성** — 이미 등록·검증이 끝난 캐릭터의 시나리오를 뒤늦게 채운 문서다.
> 초안(파싱 전) 단계를 거치지 않았으므로 `## 유저 확인 사항`은 파싱 결과에서
> 역으로 확인한 항목만 담는다. 수치·발동 시각은 전부 실측이다.

## 기본 정보
- 버스트 단계: B2
- 본인 쿨다운: **40s** (바다 산책)
- 무기 유형: RL (6발, 재장전 2.0s, 차지 1.0s, 풀차지 250%, 계수 61.3%, 코어 200%)
- 속성 코드: 수냉 · 클래스: 지원형 · 기업: 엘리시온 · **스쿼드: 이지스**
- 애장품: **없음**
- 작성 사이클 수: **2** (쿨 40s — 본인 버스트 사이클과 미사용 사이클이 번갈아 온다)

## 효과 요약 (보강: 효과 단위 stat 키)

| ID (parsed name) | 스킬 | 트리거 (timing+condition) | stat: 값(lv10) | 대상 | 지속 |
|---|---|---|---|---|---|
| 불가사리(모양) 오므라이스 | 스킬1 | `full_burst_start_count:1` | `heal_received_pct`: 30.96% | `all_allies` | 5s |
| 불가사리(모양) 오므라이스 2 | 스킬1 | `full_burst_start_count:2` | `split_dmg_pct`: 30.4% | `all_allies` | 10s |
| 불가사리(모양) 오므라이스 3 | 스킬1 | `full_burst_start_count:3` | `debuff_stack_remove`: 1 | `all_allies` | — (instant) |
| 불가사리(모양) 오므라이스 4 | 스킬1 | `full_burst_start` + `squad_ally_exists` | `heal_hp_pct`: 3.04% (`tick_interval` 1.0) | `all_allies` | 8s |
| 말미잘(모양) 파스타 | 스킬2 | `full_burst_end_count:1` | `accuracy_pct`: 10.13% | `all_allies` | 10s |
| 말미잘(모양) 파스타 2 | 스킬2 | `full_burst_end_count:2` | `atk_caster_based_pct`: 35.02% | `all_allies` | 10s |
| 말미잘(모양) 파스타 3 | 스킬2 | `full_burst_end_count:3` | `reload_speed_pct`: 40.04% | `all_allies` | 15s |
| 저장 | 스킬3 | `burst_cast` | `heal_overcharge_store`: 60.19% | `all_allies` | 25s |
| 바다 산책 | 스킬3 | `burst_cast` | `heal_hp_pct`: 40.18% | `all_allies` | — (instant) |
| 바다 산책 2 | 스킬3 | `burst_cast` | `atk_caster_based_pct`: 30.09% | `all_allies` | 10s |

**스킬 1·2가 본인 버스트와 무관하다.** `full_burst_start_count` / `full_burst_end_count`는
**스쿼드의 풀버스트 횟수**를 세지, 앵커 본인이 버스트했는지를 보지 않는다. 그래서
쿨 40s로 격 사이클밖에 못 쏘는데도 **패시브 6종은 매 사이클 꼬박꼬박 걸린다.**
이 캐릭터의 성립 근거가 여기 있다.

딜에 실제로 기여하는 건 셋뿐이다 — 말미잘 파스타 2(`atk_caster_based_pct` 35.02, ②) ·
바다 산책 2(같은 stat 30.09, ②) · 말미잘 파스타 3(`reload_speed_pct`, 재장전 위상).
나머지는 힐·유틸이고 그중 셋은 아예 미구현이다:

| stat | 상태 | 근거 |
|---|---|---|
| `heal_received_pct` (오므라이스) | ✅ | 회복량에 곱한다(2026-09-14). 기본 경로는 체력이 가득 차 딜 무관 — 보스 공격 패턴이 있을 때만 의미 |
| `heal_overcharge_store` (저장) | ❌ | 초과 회복 저장 미구현 |
| `split_dmg_pct` (오므라이스 2) | ✅ **조건부** | `is_split=True` 히트에만 ⑥으로 합산 — 스쿼드에 `split_damage` 보유자가 있어야 값이 난다 |
| `accuracy_pct` (파스타) | ⚠️ | `core_px > 0`일 때만 코어히트율로 간접 반영 |
| `debuff_stack_remove` (오므라이스 3) | ✅ | 디버프가 걸리는 시뮬이 아니라 실질 무효 |

## 검증 스쿼드

- **표준 (B2 · 쿨 40s — 보조 B2 필요)**:
  `["리틀 머메이드", "앵커 : 이노센트 메이드", "크라운", "test_B3", "스노우 화이트 : 헤비암즈"]`
  - `full_burst_*_count` 단계 진행과 격 사이클 버스트를 잡는다.
  - **`squad_ally_exists`가 거짓이다** — 크라운·test_B3·리틀 머메이드·스노우 화이트 중
    이지스가 없다. 오므라이스 4(주기 회복)는 여기서 안 걸린다.
- **변형 1 (동일 스쿼드 아군)**: 크라운 자리를 **마스트 : 로망틱 메이드**로 교체
  `["리틀 머메이드", "앵커 : 이노센트 메이드", "마스트 : 로망틱 메이드", "test_B3", "스노우 화이트 : 헤비암즈"]`
  - 이지스 = 마스트 · 앵커 · 헬름 · 헬름 : 아쿠아마린 · 마스트 : 로망틱 메이드 ·
    앵커 : 이노센트 메이드. **실전에서 짝이 되는 건 마스트 : 로망틱 메이드다**(아래 하네스 3곳 전부).
  - 마스트 : 로망틱 메이드도 B2 · 쿨 40s라 보조 B2 역할까지 그대로 대체한다. 둘 다 40s면
    마스트의 `3의 배수` 패턴 규칙(`same_stage_cd_max: 20`)이 **안 걸리고**, 격 사이클로
    번갈아 쓰는 평소 순서가 맞다 (`docs/HARNESS.md` §버스트 운용 패턴).
- **변형 2 (분배 대미지 수령자)**: `split_damage` 보유자를 넣는다 — 브래디 · 퀀시 : 이스케이프 퀸 ·
  홍련 : 흑영 · 팬텀 · 델타 : 닌자 시프 · 밀크 : 블루밍 바니 · 퀸(마코토) · 유키코.
  - 없으면 오므라이스 2(30.4%)가 **영구히 0**이다. 표준 스쿼드가 그 상태다.
- **변형 3 (코어 보스)**: `enemy={"core_px": 50}` — `accuracy_pct`(파스타)를 살리려면 필요.

- **실전 조합 (하네스)** — 앵커 : 이노센트 메이드는 3개 스쿼드에 들어 있고,
  **세 곳 모두 마스트 : 로망틱 메이드와 함께**다:

  | 스쿼드 | 멤버 | 적 | 비고 |
  |---|---|---|---|
  | `S36_네온리버` | **마스트 : 로망틱 메이드** · 리틀 머메이드 · 네온 : 비전 아이 · 리버렐리오 · **앵커 : 이노센트 메이드** | 수냉 | `accumulate` · `camera: 리버렐리오` |
  | `S37_브래디퀀시` | **마스트 : 로망틱 메이드** · 퀀시 : 이스케이프 퀸 · **앵커 : 이노센트 메이드** · 브래디 · 라피 : 레드 후드 | 작열 | **분배 대미지 2인** |
  | `S39_크리스탈이브` | **마스트 : 로망틱 메이드** · 신데렐라 : 크리스탈 웨이브 · **앵커 : 이노센트 메이드** · 이브 · 리틀 머메이드 | 전격 · 파츠 | `accumulate` |

  `S37_브래디퀀시`가 유일하게 오므라이스 2가 사는 자리다 — 브래디 `페이버릿 캔디 3`
  (사이클당 8히트) · 퀀시 `위대한 도둑 3`(버스트당 1히트)이 둘 다 `split_damage`다.

실측 (180s, `--expected`, `first_burst_time=3.0`, 기본 스펙 1층):

| 스쿼드 | 스쿼드 총딜 | 앵커 : 이노센트 메이드 |
|---|---|---|
| 표준 | 2,169,840,963 | 84,906,449 (3.9%) |
| `S37_브래디퀀시` (하네스, **라피 : 레드 후드 장전컨 미적용**) | 4,508,544,994 | 195,123,221 (4.3%) |

> 하네스 행은 스쿼드 구성·적만 맞춰 다시 잰 값이다 — 그 스쿼드의 3층 오버라이드
> (`chars`의 라피 : 레드 후드 `finish_by_fb_end` 장전컨)는 걸지 않았고, `--expected`
> 모드라 골든 스냅샷(고정 시드 `random` 모드)과도 수치가 다르다.

**개인 딜 비중이 4% 안팎으로 로스터 최하위권이다** — 킷에 damage 타입 효과가 하나도 없고
RL 6발의 자체 화력도 낮다. 값은 전부 아군에게 붙은 ② 두 줄에서 나온다.

## 쿨 40s가 격 사이클로 도는 이유 — 표준 템플릿의 리틀 머메이드

표준 템플릿의 B1인 리틀 머메이드가 `버블 오더 2`로 **풀버스트 종료마다 all_allies에
`burst_cooldown_reduce` −7.48s**를 준다. 앵커의 40s는 두 사이클마다 −14.96s를 받아
실효 25.04s가 되고, 사이클 12.533s × 2 = 25.07s에 정확히 맞는다.

실측 앵커 버스트 시각: 3.100 → 28.167 → 53.233 → 78.300 → 103.367 → 128.433 → 153.500 →
178.567 (**8회**, 간격 25.067s 등간격). 짝수 사이클은 크라운(변형 1이면 마스트 : 로망틱 메이드)이 채운다.

> 쿨 40s 캐릭터를 이 템플릿으로 재면 **인게임보다 자주 버스트한다.** 리틀 머메이드 없는
> 조합으로 평가할 때는 3~4 사이클에 한 번이 된다는 것을 감안한다.

## 사이클 타임라인 (표준, 2사이클 = 앵커 사용 사이클 + 미사용 사이클)

### 사이클 1 — 앵커 버스트 사이클

| t (실측) | 이벤트 | 발동 효과 (ID / stat) | 대상/지속 | 비고 |
|---|---|---|---|---|
| 3.000 | 리틀 머메이드 stage:1 | — | — | |
| 3.100 | **앵커 burst_cast** | 저장(`heal_overcharge_store`, 25s) · 바다 산책(heal) · 바다 산책 2(`atk_caster_based_pct` +30.09, 10s) | all_allies | ② 기여는 바다 산책 2뿐 |
| 3.200 | test_B3 stage:3 | — | — | |
| 3.250 | **full_burst 시작 (1회째)** | 오므라이스(`heal_received_pct`, 5s) | all_allies | `count:1`만 참 · 딜 무관 stat |
| 8.267 | 5s 만료 | 오므라이스 off | — | |
| 13.100 | 10s 만료 | 바다 산책 2 off | — | 풀버스트 종료(13.250)보다 0.15s 먼저 |
| 13.250 | **full_burst 종료 (1회째)** | 말미잘 파스타(`accuracy_pct`, 10s) | all_allies | `count:1`만 참 |

### 사이클 2 — 앵커 미사용 사이클 (크라운이 B2)

| t (실측) | 이벤트 | 발동 효과 | 대상/지속 | 비고 |
|---|---|---|---|---|
| 15.633 | 크라운 stage:2 | — | — | 앵커는 쿨 |
| 15.783 | **full_burst 시작 (2회째)** | 오므라이스 · **오므라이스 2**(`split_dmg_pct` +30.4, 10s) | all_allies | `count>=2` — 이후 매번 |
| 25.783 | **full_burst 종료 (2회째)** | 말미잘 파스타 · **파스타 2**(`atk_caster_based_pct` +35.02, 10s) | all_allies | `count>=2` |
| 28.117 | 25s 만료 | 저장 off | — | 다음 버스트 직전에 끊긴다 |
| 28.167 | 앵커 burst_cast (2회째) | 저장 · 바다 산책 · 바다 산책 2 | all_allies | 간격 **25.067s** |
| 28.317 | **full_burst 시작 (3회째)** | 오므라이스 · 2 · **3**(`debuff_stack_remove`) | all_allies | `count>=3` — 이후 상시 포화 |
| 38.317 | **full_burst 종료 (3회째)** | 파스타 · 2 · **3**(`reload_speed_pct`, **15s**) | all_allies | `count>=3` — 이후 상시 포화 |

3사이클째부터 **모든 단계가 켜진 정상 상태**다. 파스타 3만 15s라 사이클(12.533s)보다 길어
끊기지 않고, 파스타 1·2는 10s라 **사이클당 2.5s의 공백이 있다.**

### 변형 1에서만 추가되는 줄

| t | 이벤트 | 발동 효과 | 비고 |
|---|---|---|---|
| 매 `full_burst_start` | 오므라이스 4 | `heal_hp_pct` 3.04% · 1초 간격 · 8s | `squad_ally_exists` — 이지스 아군(마스트 : 로망틱 메이드)이 있어야 참 |

## 스킬 간 상호작용

- **`full_burst_*_count`는 본인 버스트와 독립이다** *(이 캐릭터의 핵심)*
  - 경로: 스쿼드 풀버스트 진입/종료 → 카운터 증가 → 단계별 효과. 앵커가 쿨이어도 센다.
  - 그래서 **쿨 40s인데도 버프 커버리지는 20s급**이다. 격 사이클로만 버스트하는 손해가
    스킬 3(바다 산책 2, 30.09)에만 걸리고 스킬 1·2에는 안 걸린다.
  - 위험: 이 판정을 "본인이 버스트한 풀버스트만"으로 잘못 잡으면 킷의 2/3이 반토막 난다.
- **`_count:N`은 전부 누적형이다** (`count >= N`, `buff_manager._timing_match`)
  - 3회째부터 1·2·3이 **매번 동시에** 발동한다. 실측 t=28.317에 오므라이스 1·2·3이,
    t=38.317에 파스타 1·2·3이 함께 붙는다.
- **오므라이스 2(`split_dmg_pct`) → 아군의 `split_damage` 히트** *(교차 캐릭터, 조건부)*
  - 경로: 오므라이스 2 → ⑥ 계수 → `is_split=True` 히트에만 곱해진다.
  - 스쿼드에 `split_damage` 보유자가 없으면 **버프는 걸리는데 곱해질 히트가 없다.**
    L2(발동 횟수)로는 정상이고 L1(딜)만 안 움직이는, 조용히 틀리기 쉬운 형태다.
  - 실전 자리: `S37_브래디퀀시`(브래디 8히트/사이클 + 퀀시 1히트/버스트).
- **바다 산책 2 · 말미잘 파스타 2 — 같은 stat, 다른 시점**
  - 둘 다 `atk_caster_based_pct`(②)이고 **합산**된다(35.02 + 30.09 = 65.11).
  - 다만 파스타 2는 `full_burst_end`, 바다 산책 2는 `burst_cast`(풀버스트 진입 0.15s 전)라
    **겹치는 구간이 짧다.** 앵커 버스트 사이클에서만 둘 다 활성인 구간이 생긴다.
- **저장(25s) vs 본인 쿨(25.067s)** — 갱신 직전에 0.05s 끊긴다
  - 실측 t=28.117 만료 → t=28.167 재부여. `heal_overcharge_store`가 ❌ 미구현이라
    현재는 딜에 무관하지만, 구현되면 이 틈이 의미를 갖는다.

## 유저 확인 사항
- 초안 단계를 거치지 않아 신규 질문 없음. 아래는 파싱·코드에서 확인한 사실.
- Q: `[사용 횟수 별 효과]`의 "사용 횟수"가 앵커 본인의 버스트 사용 횟수인가?
  A: **아니다 — 스쿼드 풀버스트 횟수다.** 파싱이 `full_burst_start_count:N` /
  `full_burst_end_count:N`이고, 실측에서 크라운이 B2를 가져간 2번째 사이클에도
  단계가 정상 진행했다(t=15.783에 `count:2` 효과 발동).
- Q: `[하위 효과 중복 적용]`이 "N번째에만"인가 "N번째부터 매번"인가?
  A: **N번째부터 매번**. `count >= N` 분기다. 3사이클째부터 1·2·3이 동시에 붙는다.
- Q: 말미잘 파스타 계열이 풀버스트 *종료* 시점이면 풀버스트 중에는 못 쓰는 버프 아닌가?
  A: 그렇다. 파스타 1·2는 10s라 **다음 풀버스트 진입(2.533s 후)을 덮고 그 안에서 7.5s쯤
  살아 있다가 끊긴다.** 파스타 3(15s)만 다음 풀버스트를 통째로 덮는다. 원문 그대로이고
  파싱 문제가 아니다.
- Q: `squad_ally_exists`의 "동일 스쿼드"가 편성 스쿼드(팀)인가 소속 스쿼드인가?
  A: **소속 스쿼드**(`parsed_nikke["squad"]` = 이지스). `test_B*` 더미는 스쿼드가 없어
  언제나 거짓이다(`docs/IMPL-STATUS.md`).

## 해석 선언

| 효과 | 해석 | 근거 | 대안이었다면 |
|---|---|---|---|
| 오므라이스 1~3 · 파스타 1~3 | **스쿼드 풀버스트** 횟수 기준, `count >= N` 누적 | parsed `full_burst_start_count` / `full_burst_end_count` · 실측에서 앵커 미버스트 사이클에도 진행 | "본인 버스트 횟수"였다면 격 사이클로만 세어 단계 포화가 3사이클 → 6사이클로 늦어지고 커버리지가 반토막 난다 |
| 파스타 1~3 | 풀버스트 **종료** 시점 부여 | 원문 `■ 풀 버스트 타임 종료 시` | 시작 시점이었다면 풀버스트 10초를 통째로 덮어 ② 35.02가 딜 구간에 정확히 얹힌다 (총딜이 크게 오른다) |
| 오므라이스 2 | `split_dmg_pct`(⑥) — `is_split` 히트에만 | parsed stat · `damage._factor6` | `atk_dmg_pct`였다면 스쿼드 구성과 무관하게 전 히트에 실린다 |
| 바다 산책 2 · 파스타 2 | 같은 `atk_caster_based_pct`의 **합산** | 별개 효과로 파싱 · 시전자 ATK 기준 환산 후 수령자 `atk_flat`에 가산 | 배타(높은 쪽만)였다면 겹치는 구간에서 30.09가 사라진다 |
| 저장 | `heal_overcharge_store` — ❌ 미구현이라 딜 불변 | `docs/IMPL-STATUS.md` | 구현되면 힐 초과분이 보호막처럼 남아 나가 `우정의 가드 2` 같은 보호막 트리거와 얽힐 수 있다 |

## impl/bug-fix용 검증 체크리스트
- [ ] 1사이클 풀버스트 진입 시 활성: 오므라이스만 (1개) / 종료 시: 파스타만 (1개)
- [ ] 2사이클: + 오므라이스 2 · 파스타 2 / 3사이클 이후: + 오므라이스 3 · 파스타 3 (포화)
- [ ] **앵커가 버스트하지 않은 사이클에도** 오므라이스·파스타 계열이 정상 발동 (핵심 네거티브)
- [ ] 앵커 버스트 간격 25.067s · 180s에 8회 · 짝수 사이클은 보조 B2가 채움
- [ ] 사이클 간격 12.533s 등간격 · 180s에 15회
- [ ] 파스타 3(15s) 무단절 / 파스타 1·2(10s)는 사이클당 2.533s 공백
- [ ] 표준: 오므라이스 4 **발동 0회** (`squad_ally_exists` 거짓 — 네거티브)
- [ ] 변형 1: 오므라이스 4가 매 풀버스트 진입마다 1초 간격 8초 발동
- [ ] 변형 2: 오므라이스 2가 걸린 10초 창에서 아군 `split_damage` 히트의 딜이 상승
      (표준에서는 버프만 걸리고 딜 불변 — L2는 정상인데 L1이 안 움직이는 형태)
- [ ] 발동 금지: 앵커 본인 damage 타입 효과 — **킷에 하나도 없다**
- [ ] 딜 무관 확인: `heal_received_pct`(✅, 회복 전용) · `heal_overcharge_store`(❌ 미구현)
