# 앵커 동작 시나리오

> 모드: 보강 (parsed_skills 반영 · **단계 4 구현·검증 완료 2026-09-24**)
> 실측은 스냅샷 하네스와 같은 조건(seed 42 · 180초 · 기본 스펙 1층 · 컨트롤 자동)이다.
> 배치: `미파싱_추가계획.txt` [21] — SR · R 조립 26명 (SR 비콜라보 11 · SR 콜라보 6 · R 9)

## 기본 정보
- 버스트 단계: **B1** / 본인 쿨다운: **20s**
- 무기 유형: **RL** (장탄 6, 재장전 2.00s, 차지 1.0s, 풀 차지 250.0%, 계수 61.3%, 코어 200.0%, 조작 UP)
- `post_fire_delay`·`post_reload_delay`: **CDN 유도값 그대로 — 유저 지시 2026-09-24 「무기 정보는 기본 CDN 데이터 따라」.** `_exceptions`에 등록하지 않는다
- 등급: **SR** · 속성 코드: 풍압 · 클래스: **방어형** · 기업: 엘리시온 · 스쿼드: 이지스(`Aegis`)
- 애장품: 없음 (스킬 3개뿐)
- 작성 사이클 수: **1** (쿨 20s)

## 효과 요약 (보강: 효과 단위 stat 키)

| # | ID (parsed name) | 스킬 | 트리거 (timing · condition) | stat: 값(lv10) | 대상 | 지속 |
|---|---|---|---|---|---|---|
| 1 | GS | 스킬1 | `last_bullet` | `taunt` | `target` | 5s |
| 2 | GS 2 | 스킬1 | `last_bullet` | `def_pct`: 23.82 | `self` | 5s |
| 3 | MDS | 스킬2 | `battle_start` | `projectile_dmg_pct`: 25.6 | `self` | 영구 |
| 4 | CIWS | 스킬3 | `burst_cast` | `burst_damage`: 304.45 | `all_enemies` | — |

| stat | 구현 | 기본 경로에서의 의미 |
|---|---|---|
| `taunt` | ✅ | 도발 — 보스 공격 패턴에서만 의미(`bm.taunters`). 딜 0 |
| `def_pct` | ⚠️/✅ | 적에게 음수면 방어력 ▼(② `enemy_def_down_pct`) · 아군에게면 ⚠️ 딜 식에 안 들어간다(생존) |
| `projectile_dmg_pct` | ❌ | ❌ 발사체에 가하는 대미지 — 적 발사체 모델이 없다. 딜 0 |
| `burst_damage` | ✅ | 버스트 스킬 대미지 — ⑧ `burst_dmg` 버프가 실린다 |

## 검증 스쿼드
- **표준 (B1 · 쿨 20s)**: `["앵커", "크라운", "test_B3", "스노우 화이트 : 헤비암즈"]` — 기본 사이클 동작 검증
  - 실측: 총딜 928,078,203 · 본인 51,255,439(5.52%) · 풀버스트 9회 · 간격 `20.0`×8 · 본인 버스트 9회

## 사이클 타임라인 (예상 · 표준 스쿼드)

| t (예상) | 이벤트 | 발동 효과 | 비고 |
|---|---|---|---|
| 0 | battle_start | `MDS` | 상시·전투 시작 |
| 전 구간 | 사격 트리거 | `GS`, `GS 2` | 발사·명중 카운터 |
| ≈2.x | 본인 버스트(B1) | `CIWS` | 매 사이클 |
| ≈3.25 | 풀버스트 시작 | — | 사이클 ≈12.5s |
| ≈13.25 | 풀버스트 종료 | — | 다음 사이클 ≈15.8 |

## 스킬 간 상호작용
- 스킬끼리 서로를 읽는 경로가 없다(각 효과가 독립)

## 유저 확인 사항
- Q: SR/RL `post_fire_delay`·`post_reload_delay` 실측
  A(유저 지시 2026-09-24): **기본 CDN 데이터를 따른다** — CDN 유도값 그대로, `_exceptions` 미등록
- (자체 판단) **스킬2 「요격」 축(`미파싱_추가계획.txt` [21] 주의)** — 원문 `적 발사체 공격 시 해당 발사체에 가하는 대미지`는 일레그 `쇼트` · 사쿠라 `꽃잎 떨구기`와 같은 문구 → `projectile_dmg_pct`(❌ 미구현 유지). 저지 부위(`intercept_dmg_pct`)가 아니다

## 해석 선언

| 효과 | 해석 | 근거 | 대안이었다면 |
|---|---|---|---|
| `MDS` | `projectile_dmg_pct` — 적 발사체 모델이 없어 딜 0 | 일레그 `쇼트`와 같은 문구 | 저지 부위(`intercept_dmg_pct`, 🚫)로 읽어도 딜 0 |

## 단계 4 결과

- `GS`·`GS 2`는 탄창마다(8회) · `MDS`는 t=0 1회(❌ stat이라 딜 0)

## impl/bug-fix용 검증 체크리스트

### 효과별 기대 발동 (기본 스펙 · 표준 스쿼드)
- [x] `GS` (taunt): 탄창마다(마지막 탄 명중) — **실측 buff 8회(첫 19.1)** · 대상 __enemy__ 8
- [x] `GS 2` (def_pct): 탄창마다(마지막 탄 명중) — **실측 buff 8회(첫 19.1)**
- [x] `MDS` (projectile_dmg_pct): t=0 1회 — **실측 buff 1회(첫 0.0)**
- [x] `CIWS` (burst_damage): 본인 버스트마다(매 사이클) — **실측 hit 9회 2,619,455(첫 3.0)**

### 상호작용·해석 검증
- [x] `MDS`: `projectile_dmg_pct` — 적 발사체 모델이 없어 딜 0
- [x] 사이클 간격이 표준 스쿼드 프로필과 맞는가(`docs/HARNESS.md` §편성 후 사이클 검증) — `20.0`×8 균일 · 버쿨감 없는 B1 쿨20 템플릿 — 이전 배치와 같은 모양
- [x] 기존 33개 회귀 스쿼드 불변
