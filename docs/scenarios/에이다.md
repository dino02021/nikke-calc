# 에이다 동작 시나리오

> 모드: 보강 (parsed_skills 반영)
> **역순 작성** — 이미 등록·검증이 끝난 캐릭터의 시나리오를 뒤늦게 채운 문서다.
> 초안(파싱 전) 단계를 거치지 않았으므로 `## 유저 확인 사항`은 파싱 결과에서
> 역으로 확인한 항목만 담는다. 수치·발동 시각은 전부 실측이다.

## 기본 정보
- 버스트 단계: B3
- 본인 쿨다운: **40s** (시크릿 에이전트)
- 무기 유형: RL (6발, 재장전 2.0s, 계수 61.3%, 코어 200%, 차지 1.0s, **풀차지 배율 250%**)
- 속성 코드: 전격 · 클래스: 화력형 · 기업: 어브노말 · 스쿼드: B.S.T.
- 애장품: **없음**
- 작성 사이클 수: **2** (쿨 40s)
- **조건부 레이어 1건** (`data/char_defaults.json` `_rules`): 홀드컨 `own_full_burst` `lead 0.5`
  — **미란다와 함께일 때만** (택틱 「미란다 크리 보존」). 정본은 `docs/CONTROL.md §에이다`

> **딜의 78%가 일반 공격이 아니라 `섬광 수류탄 투척`이다.** 풀버스트 중에만 도는
> 주기 스킬이고, 본인 버스트가 그 주기를 **2초 → 1초로 반으로 줄인다.**
> 그래서 이 캐릭터의 딜은 「본인 버스트 사이클인가」에 따라 사이클당 **6.2배** 차이가 난다.

## 효과 요약 (보강: 효과 단위 stat 키)

| # | ID (parsed name) | 스킬 | 트리거 (timing+condition) | stat: 값(lv10) | 대상 | 지속 |
|---|---|---|---|---|---|---|
| 0 | 은밀한 지원 | 스킬1 | `full_burst_start` | `atk_caster_based_pct`: 60% | `allies_burst_casted_burst3` | 10s |
| 1 | 은밀한 지원 2 | 스킬1 | `full_burst_start` | `armor_break_dmg_pct`: 50% | 〃 | 10s |
| 2 | 은밀한 지원 3 | 스킬1 | `full_burst_start` | `lifesteal_pct`: 10% | 〃 | 10s |
| 3 | 섬광 수류탄 투척 | 스킬2 | `every:2s` + `during_full_burst` | `armor_break_damage`: **420%** | `enemies_nearest_in_range` | — |
| 4 | 섬광 수류탄 투척 발동 시간 조건 | 스킬2 | `burst_cast` | `effect_interval`: **−1.0초** (`target_effect: 섬광 수류탄 투척`) | `self` | 10s |
| 5 | 시크릿 에이전트 | 스킬3 | `burst_cast` | `atk_pct`: 40% | `self` | 10s |
| 6 | 시크릿 에이전트 2 | 스킬3 | `burst_cast` | `armor_break_dmg_pct`: 42% | `self` | 10s |
| 7 | 특수 개조 | 스킬3 | `burst_cast` | `charge_speed_pct`: **−300%** | `self` | **1발** |
| 8 | 특수 개조 2 | 스킬3 | `burst_cast` | `charge_dmg_pct`: **1500%** | `self` | **1발** |

**미구현 stat은 없다.**

## 검증 스쿼드

- **표준 (B3 · 쿨 40s)**:
  `["리틀 머메이드", "크라운", "에이다", "test_B3"]`
  - 적 코드는 **전격**(자기 코드 = 우월 코드 불성립)이 기준이다.
  - 미란다가 없으므로 **홀드컨 레이어가 안 붙는다** — 조건부 규칙이라 이탈 보고에도 안 뜬다.
- **변형 1 (미란다 동반 = 홀드컨 레이어)**: 하네스 `S36_아인루주`가 그 자리다.
  `["미란다", "타키나", "아인", "루주", "에이다"]` + `enemy={"code": "수냉"}`
  - 이탈 보고에 `control.hold: 없음 → {policy=own_full_burst, lead=0.5} (레이어)`가 뜬다.
  - 손익 측정의 정본은 `CONTROL.md §에이다`다(에이다 585.8M → **628.0M, +7.2%**).
    여기서 다시 적지 않는다.
- **변형 2 (버스트를 못 쓰는 자리)**: 하네스 `S39_라플라스리코리코`
  - B3 자리를 라플라스가 가져가 에이다가 **한 번도 버스트하지 않는다**. 개인 딜이 1/5.6로
    떨어지고 `은밀한 지원`만 남는다(아래).

- **실전 조합 (하네스)** — 에이다는 2개 스쿼드에 들어 있다:

  | 스쿼드 | 멤버 | 적 | 스쿼드 총딜 | 에이다 | 버스트 |
  |---|---|---|---|---|---|
  | `S36_아인루주` | 미란다 · 타키나 · 아인 · 루주 · **에이다** | 수냉 | 3,134,236,286 | **1,338,755,688 (42.7%)** | **씀** (7회) |
  | `S39_라플라스리코리코` | 목단 · 치사토 · 라플라스 · **에이다** · 타키나 | 전격 · 파츠 | 6,489,450,751 | 237,982,660 (3.7%) | **안 씀** |

  `S36_아인루주`에서 에이다는 **스쿼드 1등**이다 — `섬광 수류탄 투척` 104회로 1,213,992,902.
  `S39_라플라스리코리코`에서는 `은밀한 지원`이 **라플라스·치사토**에게 가는 순수 버퍼다.

실측 (180s, `--expected`, `first_burst_time=3.0`, 기본 스펙 1층 · 레이어 없음, 적 전격):

| 스쿼드 | 스쿼드 총딜 | 에이다 |
|---|---|---|
| 표준 | 1,821,242,908 | **604,566,559 (33.2%)** |
| `S36_아인루주` (baseline, seed 42, 적 수냉) | 3,134,236,286 | 1,338,755,688 (42.7%) |
| `S39_라플라스리코리코` (baseline, seed 42, 적 전격·파츠) | 6,489,450,751 | 237,982,660 (3.7%) |

딜 구성 (표준):

| 출처 | 딜 | 비중 | 히트 |
|---|---|---|---|
| `섬광 수류탄 투척` | 468,875,258 | **77.6%** | 106 |
| 기본 공격 | 135,691,301 | 22.4% | 112 |

버스트 구간별: **풀버스트(본인) 82.7%** · 풀버스트(타인) 15.5% · 비풀버스트 **1.9%**.
비풀버스트가 2%도 안 되는 것은 주력 스킬이 `during_full_burst` 게이트이기 때문이다.

## 본인 버스트가 수류탄 주기를 반으로 줄인다

`섬광 수류탄 투척`은 `every:2s` + `during_full_burst`다. 여기에 `섬광 수류탄 투척 발동
시간 조건`(`effect_interval −1.0`, `target_effect`로 그 효과를 지목)이 **본인 버스트에만**
10초간 붙어 주기를 **1초**로 만든다.

실측 — 같은 스킬이 사이클에 따라 이렇게 갈린다:

| | 본인 버스트 사이클 (t=3.25~13.25) | 미사용 사이클 (t=15.78~25.78) |
|---|---|---|
| 발동 시각 | 3.617 · 4.617 · … · 12.617 | 16.017 · 18.017 · 20.017 · 22.017 · 24.017 |
| 주기 | **1.000초** | **2.000초** |
| 횟수 | **10회** | **5회** |
| 히트 딜 | **5,529,074** | 1,784,505 |
| 구간 합계 | **55.3M** | 8.9M (**1/6.2**) |

히트 딜까지 3.1배 갈리는 것은 방어력 무시 대미지 버프가 같은 시점에 겹치기 때문이다 —
`시크릿 에이전트 2`(+42) · `은밀한 지원 2`(+50)가 둘 다 본인 버스트 사이클에만 붙는다.
180초 합계 **106회**(= 8사이클 × 10 + 7사이클 × 5에서 위상으로 몇 개가 잘린 값).

> `effect_interval`은 `target_effect`로 **특정 효과 하나의 주기만** 가감한다
> (`skill_cooldown_pct`가 대상의 모든 `every:Ns`에 일괄로 걸리는 것과 다르다 —
> `IMPL-STATUS §effect_interval`). 에이다가 그 stat의 유일한 사용처다.

## `특수 개조` — 4초를 들여 한 발에 6.5배를 싣는다

`특수 개조`(`charge_speed_pct −300`)와 `특수 개조 2`(`charge_dmg_pct +1500`)는 둘 다
**1발 유지**(`duration_bullets: 1`)다. 음수 차지 속도는 단축량이 음수가 되어 **차지 시간이
늘어난다**: `1.0 − (1.0 × −3.00) = 4.0초` (`_effective_charge_time()`).

표준 실측 — 버스트 직후 한 발:

| | 시각 | 딜 |
|---|---|---|
| 직전 평상시 한 발 | 2.400 | 226,590 |
| **특수 개조 실린 한 발** | **6.800** | **8,061,707** |
| 그 뒤 평상시 한 발 | 8.183 | 1,243,907 |

버스트(3.200) 후 **3.6초** 만에 나가고, 그 순간 두 버프가 함께 사라진다(1발 소진).
같은 구간의 평상시 한 발과 비교하면 **6.5배**다.

**이 한 발이 홀드컨의 표적이다.** 미란다가 있으면 크리 확률 버프(1발 유지)까지 겹치므로,
풀버스트 종료 0.5초 전까지 들고 있다가 떼서 **수류탄 10회 + 이 한 발을 모두 크리로** 만든다.
조작의 근거와 손익표는 `CONTROL.md §에이다`에 있다.

## `은밀한 지원`은 「직전에 버스트를 쓴 B3」에게 간다

대상이 `allies_burst_casted_burst3` — `all_allies_burst_casted` ∩ `allies_burst3`다
(`IMPL-STATUS §target`). **에이다 자신도 포함**되며, 자기 버스트 사이클에는 자기가,
남의 사이클에는 그 B3가 받는다:

| 스쿼드 | 실측 대상 | 횟수 |
|---|---|---|
| 표준 | `['test_B3', '에이다']` | 15 (매 풀버스트) |
| `S36_아인루주` | `['아인', '에이다']` | 14 |
| `S39_라플라스리코리코` | `['라플라스', '치사토']` | 15 — **에이다 자신은 한 번도 못 받는다** |

세 번째 줄이 이 효과의 성격을 보여 준다. **에이다가 버스트를 못 쓰는 조합에서 그는
자기 버프를 하나도 못 받는 순수 서포터**가 되고, 개인 딜이 604.6M → 238.0M(하네스 기준)로
떨어진다. `시크릿 에이전트`·`특수 개조`·수류탄 주기 단축이 전부 `burst_cast`에 매여 있어서다.

## 사이클 타임라인 (표준, 2사이클)

### 사이클 1 — 에이다 버스트 사이클

| t (실측) | 이벤트 | 발동 효과 (ID / stat) | 대상/지속 | 비고 |
|---|---|---|---|---|
| 1.000 · 2.400 | 풀차지 사격 | 기본 공격 238,033 / 226,590 | | 차지 1.0초 · 간격 1.383초 |
| 3.000 · 3.100 | 리틀 머메이드 · 크라운 | — | | |
| **3.200** | **에이다 burst_cast** | **시크릿 에이전트**(공격력 +40) · **2**(방무 대미지 +42) · **특수 개조**(차지속도 −300) · **2**(차지 대미지 +1500) · **수류탄 주기 −1초** | self · 10s / **1발** / 10s | 네 버프가 한꺼번에 |
| **3.250** | full_burst 시작 (1회째) | **은밀한 지원 1·2·3** (시전자 기준 공격력 +60 · 방무 +50 · 라이프스틸 10) | **직전 B3 = 에이다** · 10s | |
| **3.617 → 12.617** | `every:1s` | **섬광 수류탄 투척** 5,529,074 **×10** | 적 | 주기가 반으로 줄어 있다 |
| **6.800** | 특수 개조 실린 **한 발** | 기본 공격 **8,061,707** · 특수 개조 1·2 소멸 | | 차지 4.0초 · 평상시의 **6.5배** |
| 13.200 | 10s 만료 | 시크릿 에이전트 1·2 · 수류탄 주기 조건 off | | |
| 13.250 | full_burst 종료 | 은밀한 지원 1·2·3 off | | **수류탄이 멈춘다** |

사이클 1 수류탄 구간 합계 **55.3M**.

### 사이클 2 — 에이다 미사용 사이클 (test_B3이 B3)

| t (실측) | 이벤트 | 발동 효과 | 비고 |
|---|---|---|---|
| 15.533 · 15.633 · 15.733 | 리틀 머메이드 · 크라운 · test_B3 | — | 에이다는 쿨 |
| **15.783** | full_burst 시작 (2회째) | **은밀한 지원 1·2·3** → **test_B3** | 에이다 자신은 대상에서 빠진다 |
| 16.017 · 18.017 · 20.017 · 22.017 · 24.017 | `every:2s` | 섬광 수류탄 투척 1,784,505 **×5** | 주기 2초 · 히트 딜도 1/3.1 |
| 25.783 | full_burst 종료 | | 구간 합계 **8.9M** (사이클 1의 **1/6.2**) |
| **28.267** | 에이다 burst_cast (2회째) | 사이클 1과 동일 | **간격 25.067s** |

사이클 간격 **12.533s 등간격**(180초 15회), 에이다 버스트 8회.

## 스킬 간 상호작용

- **`burst_cast` 네 버프가 한 벌로 움직인다** *(이 캐릭터의 전부)*
  - `시크릿 에이전트 1·2`(공격력·방무) + `특수 개조 1·2`(한 발) + `수류탄 주기 −1초`.
    **주기 단축과 딜 버프가 같은 10초 창**이라 수류탄 10회가 전부 강화된 값으로 나간다.
  - 위험: 넷 중 주기 단축만 빠져도 발동이 10 → 5회로 반이 된다.
- **`은밀한 지원` → 자기 자신** *(대상 선택의 순환)*
  - `allies_burst_casted_burst3`. 자기가 B3를 썼으면 **자기가 받는다**(방무 +50이 수류탄에
    그대로 실린다). 남이 썼으면 그쪽에 간다.
  - `full_burst_start` 1회 평가라 이후 상태가 바뀌어도 대상은 안 바뀐다.
- **`특수 개조` ↔ 미란다 크리확률** *(교차 캐릭터 · 조작)*
  - 둘 다 **1발 유지**라 아무 발에나 흘리면 낭비다. 홀드컨(`own_full_burst`, lead 0.5)이
    풀버스트 종료 0.5초 전까지 들고 있게 해 **둘을 같은 한 발에** 얹는다.
  - 조건부 레이어이므로 **미란다가 없으면 안 붙는다.** 손익은 `CONTROL.md §에이다`.
- **`섬광 수류탄 투척`은 `during_full_burst` 게이트다** *(구간 의존)*
  - 풀버스트 밖에서는 한 발도 안 나간다. 에이다의 비풀버스트 딜 비중이 **1.9%**인 이유다.
  - 풀버스트 길이를 줄이는 아군(이사벨 `소닉 체이서 5`)과 같이 쓰면 이 스킬이 직접 손해를 본다.
- **`armor_break_damage`는 적 방어력을 0으로 본다** *(계산 경로)*
  - `damage.py §②`. 그래서 `armor_break_dmg_pct` 버프 두 개(합 +92%)가 곧바로 배율로 실린다.

## 유저 확인 사항
- 초안 단계를 거치지 않아 신규 질문 없음. 아래는 파싱·코드에서 확인한 사실.
- Q: 표준 스쿼드에서 홀드컨이 왜 안 붙나?
  A: **조건부 레이어라 미란다가 없으면 규칙이 안 맞는다.** `spec.resolve_rules()`가
  스쿼드를 다 조립한 뒤 판정하고, 안 걸린 컨트롤은 캐릭터 dict에 아예 안 들어가므로
  이탈 보고에도 안 뜬다(`HARNESS.md §부착`).
- Q: `특수 개조`가 차지 속도를 −300% 하는데 왜 차지가 빨라지지 않고 느려지나?
  A: **음수라 단축량이 음수가 되어 시간이 늘어난다.** `1.0 − (1.0 × −3.00) = 4.0초`.
  대신 `특수 개조 2`가 차지 대미지 +1500%를 얹어 한 발이 6.5배가 된다.
- Q: 수류탄이 사이클마다 10회 / 5회로 갈리는 게 맞나?
  A: **맞다.** `effect_interval −1.0`이 본인 버스트에만 10초 붙어 주기를 2초 → 1초로 만든다.
  `target_effect`로 이 효과 하나만 지목한다.
- Q: `S39_라플라스리코리코`에서 에이다 딜이 왜 그렇게 낮은가?
  A: **버스트를 한 번도 못 써서다.** B3 자리를 라플라스가 가져가 `시크릿 에이전트`·
  `특수 개조`·주기 단축이 전부 안 걸리고, `은밀한 지원`도 라플라스·치사토에게 간다.

## 해석 선언

| 효과 | 해석 | 근거 | 대안이었다면 |
|---|---|---|---|
| 섬광 수류탄 투척 | `every:2s` + `during_full_burst` — **풀버스트 안에서만** 도는 주기 스킬 | 원문 `풀 버스트 타임 지속 중 2초마다` · 실측 비풀버스트 히트 0 | 상시였다면 딜 분포가 완전히 달라지고 사이클 편차가 사라진다 |
| 섬광 수류탄 투척 발동 시간 조건 | `effect_interval` **−1.0초**, `target_effect`로 **그 효과 하나만** | `IMPL-STATUS §effect_interval` · 실측 1.000초 ↔ 2.000초 | `skill_cooldown_pct`처럼 일괄이었다면 다른 `every:Ns`까지 당겨진다(에이다에겐 없지만 일반화가 위험하다) |
| 특수 개조 | `charge_speed_pct` **−300%** → 차지 시간 **4.0초**(늘어난다) | `_effective_charge_time()` (`base − base × pct/100`) · 실측 3.200 → 6.800 | 속도 배율로 나눗셈 해석이었다면 시간이 0.25초로 **줄어** 정반대가 된다 |
| 특수 개조 1·2 | `duration_bullets: 1` — **시간이 아니라 한 발** | 실측 6.800에 두 버프 동시 소멸 | 10초 지속이었다면 그 구간의 모든 발이 1500%를 받아 딜이 폭증한다 |
| 은밀한 지원 1~3 | `allies_burst_casted_burst3` — **자신 포함**, `full_burst_start` 1회 평가 | `IMPL-STATUS §target` · 실측 대상이 사이클마다 갈린다 | 자신 제외였다면 자기 버스트 사이클의 방무 +50이 사라져 수류탄 딜이 크게 준다 |
| 섬광 수류탄 투척 | `armor_break_damage` — ②에서 **적 방어력 0** | `IMPL-STATUS §armor_break_damage` | 일반 대미지였다면 방어력을 그대로 맞아 딜이 크게 준다 |
| 섬광 수류탄 투척 | `enemies_nearest_in_range` — 쫄몹이 없으면 `__enemy__`, 쫄몹이 있으면 **조준 규칙**이라 한 발이 에이다가 겨눈 적 1기에 통째로 간다(히트 수는 안 는다 — `boss_pattern` §조준) | `_resolve_target()` · `boss_pattern` §쫄몹 | 원문 「공격 범위 내 적들」대로 범위 안 전원이었다면 히트가 적 수만큼 는다 — 좌표가 없어 범위 판정은 하지 않는다 |
| 홀드컨 | **레이어(조건부)** — 미란다가 있을 때만 | `data/char_defaults.json` `_rules` · `CONTROL.md §에이다` | 상시 부착이었다면 미란다 없는 조합에서 한 발을 헛되이 미룬다 |

## impl/bug-fix용 검증 체크리스트
- [ ] 표준 스쿼드에서는 이탈 보고가 **비어 있다** (조건부 레이어 미적용)
- [ ] 미란다를 넣으면 `control.hold: 없음 → {policy=own_full_burst, lead=0.5} (레이어)`가 뜬다
- [ ] `섬광 수류탄 투척` 주기: 본인 버스트 사이클 **1.000초 · 10회** / 그 밖 **2.000초 · 5회**
- [ ] 180초 수류탄 **106회** · 딜 비중 **77.6%**
- [ ] 비풀버스트 딜 비중이 **2% 미만** (`during_full_burst` 게이트)
- [ ] `특수 개조` 실린 한 발이 **버스트 +3.6초**에 나가고 그 즉시 두 버프가 소멸한다
- [ ] 그 한 발이 평상시 한 발의 **≈6.5배** (8,061,707 vs 1,243,907)
- [ ] `은밀한 지원` 1·2·3이 **매 풀버스트 15회**, 대상은 그 사이클의 B3 발동자
- [ ] 에이다가 버스트를 못 쓰는 조합에서 `은밀한 지원` 대상에 **에이다가 없다**
      (`S39_라플라스리코리코` = 라플라스·치사토)
- [ ] 에이다 버스트 간격 **25.067s**, 180초 8회 · 사이클 간격 12.533s
