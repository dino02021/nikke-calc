# 에이드 : 에이전트 바니 동작 시나리오

> 모드: 보강 (parsed_skills 반영)
> **역순 작성** — 이미 등록·검증이 끝난 캐릭터의 시나리오를 뒤늦게 채운 문서다.
> 초안(파싱 전) 단계를 거치지 않았으므로 `## 유저 확인 사항`은 파싱 결과에서
> 역으로 확인한 항목만 담는다. 수치·발동 시각은 전부 실측이다.

## 기본 정보
- 버스트 단계: B2
- 본인 쿨다운: 20s (최첨단 요원 장비)
- 무기 유형: SR (6발, 재장전 2.0s, 차지 1.0s, 풀차지 250%, 계수 69.04%, 코어 200%)
- 속성 코드: 철갑 · 클래스: 지원형 · 기업: 테트라 · 스쿼드: 메이드 포 유
- 애장품: **없음**
- 작성 사이클 수: 1

## 효과 요약 (보강: 효과 단위 stat 키)

| ID (parsed name) | 스킬 | 트리거 (timing+condition) | stat: 값(lv10) | 대상 | 지속 |
|---|---|---|---|---|---|
| 요원의 시선 | 스킬1 | `full_charge_hit` + `optimal_range` | `atk_caster_based_pct`: 15.2% | `all_allies` | 5s |
| 스파이 렌즈 | 스킬1 | `full_charge_fire` | `optimal_range_min`: 4.44 (**10중첩**) | `self` | 5s |
| 요원의 움직임 | 스킬2 | `full_charge_hit` + `optimal_range` | `pierce_dmg_pct`: 18.36% | `all_allies` | 5s |
| 요원의 움직임 2 | 스킬2 | `passive` + `self_stack_above:스파이 렌즈:10` | `pierce_enabled` | `self` | 영구 |
| 요원의 움직임 3 | 스킬2 | `passive` + `self_stack_above:스파이 렌즈:10` | `atk_pct`: 16.0% | `self` | 영구 |
| 최첨단 요원 장비 | 스킬3 | `burst_cast` | `optimal_range_min`: 55.56 | `self` | 10s |
| 최첨단 요원 장비 2 | 스킬3 | `burst_cast` | `atk_dmg_pct`: 55.04% | `all_allies` | 10s |
| 최첨단 요원 장비 3 | 스킬3 | `burst_cast` | `pierce_dmg_pct`: 10.13% | `all_allies` | 10s |

**관통 특화 버퍼다.** 아군에게 가는 값이 `pierce_dmg_pct` 두 줄(18.36 + 10.13 = 28.49)과
`atk_dmg_pct` 55.04 · `atk_caster_based_pct` 15.2다. 관통 두 줄은 **받는 쪽이 관통 히트를
낼 때만** ⑤에 실리므로 스쿼드 구성에 크게 달려 있다.

**`optimal_range_min`은 보스 거리(`enemy.distance`)가 있을 때만 산다**(2026-09-18 — 최소 거리를 N% 줄인다,
`docs/IMPL-STATUS.md`). 거리 없는 기본 경로에서는 `스파이 렌즈`와 `최첨단 요원 장비`(스킬 3의 자기 버프)가
**딜에 직접 기여하지 않는다.**
다만 스파이 렌즈는 **스택 카운터로서** 요원의 움직임 2·3의 게이트를 여는 실질 역할이 있다 —
값이 죽어도 스택은 살아 있어야 한다.

> **아군 버프 두 줄은 `optimal_range` 게이트를 탄다 (2026-09-02 추가).** 원문
> `풀 차지 공격으로 **적정 사거리의 대상** 명중 시` 조건이 그전에는 파싱에 없어 항상 참이었다.
> 판정은 ③ 고정 +30%와 같은 함수(`buff_manager.in_optimal_range`)다 — 보스 거리가 없으면 **적 스펙
> `optimal_range_weapons`**에 시전자 무기군(SR)이 들어 있는지, 있으면 에이드의 적정 구간(SR 45~100 —
> 스파이 렌즈·최첨단 요원 장비가 최소를 줄인다)에 거리가 드는지(2026-09-18). **기본값이 빈 목록·거리 없음이라
> 스쿼드 스펙이 명시하지 않으면 무발동**이고, 지금 하네스에서 이 목록을 쓰는 스쿼드는
> `S38_누아르`(SG) · `S39_마르차나아쿠아`(SMG) · `S40_퀸유키코`(SG·SMG) · **`S36_서머메이든`(SR)** 넷이다.
> `스파이 렌즈`(원문에 사거리 조건 없음)와 스킬 3은 게이트를 타지 않아 그대로다.

## 검증 스쿼드

- **표준 (B2 · 쿨 20s)**: `["리틀 머메이드", "에이드 : 에이전트 바니", "test_B3", "스노우 화이트 : 헤비암즈"]`
  - 조건부 효과 스캔: `no_burstN_ally` · `back_row` · `allies_weapon/class/code` 류 조건 **없음**.
    모든 조건이 자기 스택(`self_stack_above`)이라 스쿼드 구성에 무관 → 표준 1개로 사이클은 다 잡힌다.
  - **`optimal_range_weapons`가 비어 있어 요원의 시선·요원의 움직임이 발동하지 않는다.**
    표준으로 재면 에이드의 아군 버프 절반이 빠진 값이 나온다 — 아래 변형 2가 짝이다.
  - **관통 수령자는 우연히 들어 있다** — 스노우 화이트 : 헤비암즈가
    `어나더 화이트 관통특화`(`pierce_enabled`, `full_charge`)를 갖고 있어
    `pierce_dmg_pct`가 실제로 실린다(표준에서는 최첨단 요원 장비 3의 10.13만).
- **변형 1 (관통 소비자 없음)**: 스노우 화이트 : 헤비암즈를 관통 없는 B3로 바꾼다.
  - `pierce_dmg_pct` 두 줄이 **걸리기는 하는데 곱해질 히트가 없는** 상태를 만든다.
    L2(발동 횟수)는 정상이고 L1(딜)만 안 움직이는 형태 — 조용히 틀리기 쉬운 자리다.
- **변형 2 (적정거리 보스 — 아군 버프를 여는 유일한 길)**: `enemy={"optimal_range_weapons": ["SR"]}`
  - ③에 고정 +30%가 붙고(일반 공격 한정) **동시에 `optimal_range` condition이 참이 되어
    요원의 시선·요원의 움직임이 살아난다.**
  - 실측 총딜 1,366,833,989 → **1,572,560,659 (+15.1%)**, 에이드 70,042,222 →
    105,812,890 (+51.1%). 요원의 시선/움직임 발동 각 250회(풀차지마다).
  - `optimal_range_min` 버프(스파이 렌즈·최첨단 요원 장비)는 **무기군 목록 판정에는 관여하지 않는다.**
    보스 거리 판정에는 관여한다 — 아래 변형 3.
- **변형 3 (보스 거리, 2026-09-18)**: `enemy={"distance": 70}`이면 SR 45~100 안이라 변형 2와 **원 단위까지 같다**
  (기대값 1,572,560,659). `enemy={"distance": 30}`이면 처음에는 구간 밖이다가 첫 버스트의 최첨단 요원 장비
  (최소 −55.56% → 20)가 걸린 뒤부터 적정거리 안이다 — 요원의 시선 첫 발동 **3.8s**(변형 2는 1.0s),
  총딜 1,576,632,875(리틀 머메이드 SMG 15~35도 30에서 적정이라 오르고, 헤비암즈 SR은 빠진다).

- **실전 조합 (하네스)** — 에이드 : 에이전트 바니는 1개 스쿼드에 들어 있다:

  | 스쿼드 | 멤버 | 적 |
  |---|---|---|
  | `S36_서머메이든` | 아니스 : 스파클링 서머 · **에이드 : 에이전트 바니** · 목단 · 메이든 : 아이스 로즈 · 프리바티 | 수냉 · **SR 최적거리** |

  에이드가 **유일한 B2**라 매 사이클 버스트한다. 그리고 **SR도 에이드 하나뿐**이라
  `optimal_range_weapons: ["SR"]`은 다른 멤버의 ③에 손대지 않는다 — 에이드의 일반공격
  +30%와 그의 아군 버프 두 줄만 연다.

실측 (180s, `--expected`, `first_burst_time=3.0`):

| 스쿼드 | 이탈 | 스쿼드 총딜 | 에이드 : 에이전트 바니 |
|---|---|---|---|
| 표준 (적정거리 없음) | 1층 그대로 | 1,366,833,989 | 70,042,222 (5.1%) |
| 표준 + `optimal_range_weapons: ["SR"]` | 1층 그대로 | 1,572,560,659 | 105,812,890 (6.7%) |
| `S36_서머메이든` (SR 최적거리) | 아니스 : 스파클링 서머 · 프리바티 `max_ammo_pct` 0 (둘 다 레이어) | 6,040,605,018 | 166,110,662 (2.7%) |

> **`S36_서머메이든`에는 `"optimal_range_weapons": ["SR"]`을 적었다 (2026-09-02, 유저 확인).**
> 이 스쿼드의 에이드는 적정 사거리에서 쏘는 자리라는 판단이다. 골든 스냅샷 기준으로
> 다른 네 명은 게이트 도입 전 값으로 **정확히 되돌아왔고**, 에이드만 ③ 고정 +30%가 새로
> 붙어 **개인 +19.28%**(141,326,646 → 168,567,964), 스쿼드 **+0.45%**가 됐다.
> `optimal_range_weapons`를 안 적은 조합에서 에이드를 재면 아군 버프 두 줄이 통째로 빠진다는
> 점은 그대로다 — 위 표의 첫 행이 그 값이다.

## 스파이 렌즈 — 10중첩까지 13.5초

SR 풀차지 주기 **약 1.4s**, 첫 발이 t=1.000(버스트보다 먼저).
`buff_stack_add`가 스택과 함께 지속시간도 갱신하므로(`docs/IMPL-STATUS.md`)
**5초 지속인데도 1.4s마다 갱신되어 스택이 계속 쌓인다.**

| 발 | t (실측) | 스택 (값) |
|---|---|---|
| 1 | 1.000 | 1 (4.44) |
| 2 | 2.400 | 2 (8.88) |
| … | … | … |
| **10** | **13.533** | **10 (44.4) — 상한** |
| 이후 | 1.4s마다 | 10 유지 (값 44.4 고정) |

**t=13.550에 요원의 움직임 2(`pierce_enabled`) · 3(`atk_pct` +16)이 켜지고,
180초 동안 한 번도 꺼지지 않는다**(만료 이벤트 0건). 갱신 주기 1.4s가 지속 5s보다 짧고,
유일한 재장전 공백(t=35.900 → 38.717, 2.817s)도 5s를 넘지 않기 때문이다.

> **에이드는 전투 시작 13.5초까지 반쪽이다.** 1사이클 전체와 2사이클 초반은
> `pierce_enabled`·`atk_pct` 없이 돈다. 짧은 창으로 재면 과소평가된다.

## 사이클 타임라인 (표준)

| t (실측) | 이벤트 | 발동 효과 (ID / stat) | 대상/지속 | 비고 |
|---|---|---|---|---|
| 1.000 | 풀차지 1발 | 스파이 렌즈 1중첩 | self | **첫 버스트보다 먼저**. 요원의 시선·움직임은 `optimal_range` 거짓이라 무발동 |
| 2.400 | 풀차지 2발 | 스파이 렌즈 2중첩 | self | 주기 **1.4s** < 지속 5s → 스택 유지 |
| 3.100 | **에이드 burst_cast** | 최첨단 요원 장비(`optimal_range_min` — 보스 거리가 있을 때만) · 2(`atk_dmg_pct` +55.04) · 3(`pierce_dmg_pct` +10.13) | self / all_allies 10s | |
| 3.250 | **full_burst 시작** | — | — | 킷에 `full_burst_start` 효과 없음 |
| 3.800 · 5.200 · 6.600 · 8.000 · 9.383 · 10.767 · 12.150 | 풀차지 | 스파이 렌즈 갱신 (변형 2에서는 요원의 시선 · 움직임도 함께) | | |
| 13.100 | 10s 만료 | 최첨단 요원 장비 1~3 off | | 풀버스트 종료(13.250)보다 0.15s 먼저 |
| **13.533** | 풀차지 10발째 | **스파이 렌즈 10중첩(상한)** | self | |
| **13.550** | 조건 재평가 | **요원의 움직임 2**(`pierce_enabled`) · **3**(`atk_pct` +16) | self / 영구 | 한 프레임(0.0167s) 뒤 |
| 15.633 | 에이드 burst_cast (2사이클) | 최첨단 요원 장비 1~3 갱신 | | 이후 12.533s 등간격 |
| 36.283~37.700 | **재장전** (1.417s) | — | — | 180초에 몇 번뿐 — 리틀 머메이드 `세이렌 송 2`가 매 사이클 탄약을 채운다 |

사이클 간격: 3.25 → 15.783 → 28.317 → … **12.533s 등간격**(180s에 15회).
에이드는 다른 B2가 없으면 **매 사이클 버스트한다**.

`최첨단 요원 장비 2·3`은 10s < 사이클 12.533s → **사이클당 2.533s 공백**.
반면 스킬 1·2의 5초 버프는 1.4s 갱신이라 **무단절**이다 — 다만 아군에게 가는 두 줄은
`optimal_range`가 참인 스쿼드에서만 그렇고, 표준에서는 아예 안 걸린다.

## 스킬 간 상호작용

- **스파이 렌즈(스택) → 요원의 움직임 2·3** *(자기 게이트)*
  - 경로: 풀차지 → 스택 +1 → 10 도달 → `self_stack_above:스파이 렌즈:10` 참 →
    `pierce_enabled` + `atk_pct` +16.
  - **거리 없는 기본 경로에서 `optimal_range_min` 값이 쓰이지 않아도 스택 자체는 살아 있어야 한다.**
    값이 안 쓰인다고 버프를 등록하지 않으면 이 게이트가 영영 안 열린다 — 네거티브 검증 항목.
  - `self_stack_above`는 런타임 조건이라 **스택이 떨어지면 즉시 꺼진다**(`_RUNTIME_COND_PREFIXES`).
    실측에서는 갱신 주기 1.4s < 지속 5s라 한 번도 안 꺼졌다.
- **요원의 움직임 2(`pierce_enabled`) × `pierce_dmg_pct` 두 줄** *(자기 완결 순환)*
  - 에이드 자신이 10중첩 뒤 관통 특화가 되므로, **자기가 뿌린 `pierce_dmg_pct` 28.49를
    자기가 소비한다.** 13.5초 전후로 에이드 개인 딜의 성격이 바뀐다.
  - 아군 쪽은 각자 `pierce_enabled`(또는 관통 히트)를 가져야 한다. 표준 스쿼드에서는
    스노우 화이트 : 헤비암즈만 해당한다.
- **`optimal_range_min`은 무기군 목록과는 무관하고 보스 거리와는 얽힌다** *(혼동 주의)*
  - 적 설정이 무기군 목록(`optimal_range_weapons`, 하네스가 쓰는 쪽 — `docs/HARNESS.md`)이면 에이드의 사거리
    버프를 켜도 적정거리 판정이 안 바뀐다.
  - 적 설정이 보스 거리(`distance`)면 킷의 `optimal_range_min`이 SR 구간의 최소를 줄여 **가까운 거리에서도
    적정거리가 열린다**(4.44% × 10중첩 + 55.56% = 100%면 0~100). 변형 3.
- **`optimal_range` 게이트가 스킬 1·2의 아군 몫을 통째로 여닫는다** *(적 설정 의존)*
  - 요원의 시선(② 15.2) · 요원의 움직임(⑤ 18.36)만 게이트를 타고, 같은 풀차지 트리거인
    `스파이 렌즈`는 원문에 사거리 조건이 없어 **그대로 돈다.** 그래서 게이트가 닫혀도
    10중첩 → 요원의 움직임 2·3 경로는 살아 있다.
  - 즉 게이트가 닫힌 스쿼드에서 에이드는 **자기 강화(관통 특화 + `atk_pct` 16)와 스킬 3만
    남은 캐릭터**가 된다. 아군에게 가는 건 최첨단 요원 장비 2·3뿐이다.
  - 실측 차이: 총딜 1,366,833,989 → 1,572,560,659 (+15.1%), 에이드 개인 +51.1%.

## 유저 확인 사항
- 초안 단계를 거치지 않아 신규 질문 없음. 아래는 파싱·코드에서 확인한 사실.
- **Q: `적정 사거리의 대상 명중 시` 조건은 어떻게 표현했나?**
  A: **`optimal_range` condition을 새로 만들었다 (2026-09-02).** 그전에는 parsed condition이
  비어 있어 매 풀차지마다 무조건 발동했다. 계산기에 사거리 항이 따로 없으므로 판정은
  **적 스펙 `optimal_range_weapons`**를 재사용한다 — ③ 고정 +30%를 태우는 `timeline`의
  `is_optimal_range`와 같은 기준이다. 시전자 무기군은 로스터 값(`parsed_nikke["weapon_type"]`)이라
  무기 변경 모드는 반영하지 않는다(현 사용처에 모드 전환이 없다).
  → 2026-09-18 보스 거리 모델이 생기면서 판정이 `buff_manager.in_optimal_range` 한 함수로 모였다(거리가 없으면
  종전 목록 그대로).
  **남은 판단**: `S36_서머메이든`이 `optimal_range_weapons`를 안 적어 에이드의 아군 버프
  두 줄이 그 스쿼드에서 통째로 빠진다(스쿼드 총딜 −6.94%). 스펙에 `["SR"]`을 적을지는
  운용 판단이라 미결로 뒀다.
- Q: 스파이 렌즈가 5초 지속인데 어떻게 10중첩까지 쌓이나?
  A: **갱신되기 때문이다.** `buff_stack_add`는 스택을 올리면서 지속시간도 다시 붙인다
  (`docs/IMPL-STATUS.md`). 풀차지 주기 1.4s < 5s라 끊기지 않고 누적된다.
- Q: `optimal_range_min`이 미구현이면 스파이 렌즈를 파싱에서 빼도 되나?
  A: **안 된다.** 요원의 움직임 2·3이 이 버프의 **스택 수**를 게이트로 쓴다.
  값은 죽었어도 카운터는 살아 있어야 한다. (2026-09-18부터는 보스 거리가 있으면 값도 산다.)
- Q: 요원의 움직임 2·3이 `passive`인데 왜 t=13.550에 켜지나?
  A: `self_stack_above:`는 런타임 재평가 조건이다. 조건이 참이 되는 첫 프레임에 활성화되고,
  거짓이 되면 즉시 꺼진다. `passive`는 "상시 후보"라는 뜻이지 "전투 시작부터 활성"이 아니다.

## 해석 선언

| 효과 | 해석 | 근거 | 대안이었다면 |
|---|---|---|---|
| 요원의 시선 · 움직임 | `optimal_range` = **`in_optimal_range`** — 보스 거리가 없으면 적 스펙 `optimal_range_weapons`에 SR이 있는가, 있으면 에이드의 적정 구간에 거리가 드는가 | ③ +30%와 같은 판정 | 조건이 없으면(수정 전) 사거리 밖에서도 ② 15.2 · ⑤ 18.36이 걸려 낙관적으로 계산된다 |
| 스파이 렌즈 | 같은 풀차지 사격인데 **게이트를 타지 않는다** — 원문이 「풀 차지 공격 시」(발사, `full_charge_fire`)이고 나머지 둘은 「풀 차지 공격으로 적정 사거리의 대상 명중 시」(명중, `full_charge_hit` + `optimal_range`)다 | 원문 블록에 사거리 조건이 없다 | 함께 게이팅했다면 요원의 움직임 2·3까지 죽어 자기 강화가 통째로 사라진다 |
| 스파이 렌즈 | 값(`optimal_range_min`)은 보스 거리가 있을 때만 쓰인다, **스택은 늘 유효** | `docs/IMPL-STATUS.md` · 실측 10중첩에 게이트 열림 | 버프를 아예 등록하지 않았다면 요원의 움직임 2·3이 영영 안 켜진다 |
| 요원의 움직임 2·3 | `self_stack_above:10` — **런타임 조건**(스택 빠지면 즉시 off) | `_RUNTIME_COND_PREFIXES` · `stack_reach` timing이 아니다 | `stack_reach:스파이 렌즈:10` timing이었다면 한 번 켜진 뒤 스택이 빠져도 유지된다 |
| 요원의 움직임 · 최첨단 요원 장비 3 | `pierce_dmg_pct`(⑤) — **관통 히트에만** 가산 | `damage._factor5`의 `is_pierce_damage` 분기 | `atk_dmg_pct`였다면 스쿼드 구성과 무관하게 전 히트에 실린다 |
| 최첨단 요원 장비 (1) | 자기 `optimal_range_min` 55.56 — 보스 거리가 있을 때만 SR 구간 최소를 줄인다. 기본 경로 **딜 기여 0** | `buff_manager.in_optimal_range` | 최소를 늘리는 쪽으로 읽었다면 가까운 거리에서 오히려 적정거리가 닫힌다 |

## impl/bug-fix용 검증 체크리스트
- [ ] 풀차지 주기 약 1.4s · 첫 발 t=1.000 (첫 버스트보다 먼저)
- [ ] 표준(적정거리 미설정): 요원의 시선 · 요원의 움직임 **발동 0회** (`optimal_range` — 네거티브)
- [ ] 변형 2(`optimal_range_weapons: ["SR"]`): 둘이 **매 풀차지마다** 갱신 → 무단절 (각 250회)
- [ ] 스파이 렌즈는 게이트와 무관하게 **양쪽 모두** 매 풀차지 갱신
- [ ] 스파이 렌즈 10중첩 도달 t=13.533 (10번째 풀차지), 값 44.4에서 상한
- [ ] 요원의 움직임 2·3 활성 t=13.550, 이후 180s 동안 **만료 0건**
- [ ] 13.533s **이전에는** 요원의 움직임 2·3 미활성 (네거티브 — 1사이클 전체가 반쪽)
- [ ] 버스트 프레임에 최첨단 요원 장비 1·2·3, 10s (사이클당 2.533s 공백)
- [ ] 사이클 간격 12.533s 등간격 · 180s에 15회 · 매 사이클 버스트 (표준)
- [ ] `pierce_dmg_pct` 두 줄이 관통 히트에만 실린다 — 관통 소비자 없는 스쿼드에서
      L2는 정상인데 L1이 안 움직인다 (변형 1)
- [ ] 무기군 목록 적에서는 `optimal_range_min` 버프를 켜도 ③ 적정거리 +30%나 `optimal_range` condition이
      켜지지 **않는다** (네거티브)
- [ ] 보스 거리 70 = 변형 2와 원 단위 같음 · 거리 30이면 첫 버스트(최첨단 요원 장비) 뒤부터 요원의 시선 발동 (변형 3)
- [ ] 발동 금지: 에이드 본인 damage 타입 효과 — **킷에 하나도 없다**
