# 이사벨 동작 시나리오

> 모드: 보강 (parsed_skills 반영)
> **역순 작성** — 이미 등록·검증이 끝난 캐릭터의 시나리오를 뒤늦게 채운 문서다.
> 초안(파싱 전) 단계를 거치지 않았으므로 `## 유저 확인 사항`은 파싱 결과에서
> 역으로 확인한 항목만 담는다. 수치·발동 시각은 전부 실측이다.

## 기본 정보
- 버스트 단계: B3
- 본인 쿨다운: **40s** (소닉 체이서)
- 무기 유형: SG (9발, 재장전 1.86s, 계수 210.7%, 코어 200%, **펠릿 10**, 연사 1.5/s, 탄착군 250)
- 속성 코드: 전격 · 클래스: 화력형 · 기업: 필그림 · 스쿼드: 인헤르트
- 애장품: **없음**
- 작성 사이클 수: **2** (쿨 40s)
- 기본 레이어 **없음** (`data/char_defaults.json`에 항목 없음)

> **이 캐릭터의 특이점은 딜이 아니라 사이클이다.** 버스트가 **아군 전체의 풀버스트를 5초 줄인다**
> (`소닉 체이서 5`, `fullburst_duration -5.0`). 로스터에서 이 값이 **음수인 캐릭터는 이사벨뿐**이고,
> 아르카나는 그 단축을 전제로 설계된 캐릭터다 — `docs/scenarios/아르카나.md §핵심 전제`.
> **혼자 쓰면 순손해**이고, 아르카나와 함께여야 이득으로 뒤집힌다.

## 효과 요약 (보강: 효과 단위 stat 키)

배열 순서 = 실행 순서다. 아래 표는 `data/parsed_skills.json` 순서 그대로다.

| # | ID (parsed name) | 스킬 | 트리거 (timing+condition) | stat: 값(lv10) | 대상 | 지속 |
|---|---|---|---|---|---|---|
| 0 | 타겟 마킹 1 | 스킬1 | `burst_cast_count:1` | `crit_rate`: 6.26% | `self` | 45s |
| 1 | 타겟 마킹 2 | 스킬1 | `burst_cast_count:2` | `crit_dmg`: 18.03% | `self` | 45s |
| 2 | 타겟 마킹 3 | 스킬1 | `burst_cast_count:3` | `atk_pct`: 17.28% | `self` | 45s |
| 3 | 페더 이젝션 | 스킬2 | `every:15.0s` | `damage`: 170.58% | `enemies_top_def:5` | — |
| 4 | 소닉 체이서 | 스킬3 | `burst_cast` | `burst_damage`: 149.85% | `all_enemies` | — |
| 5 | 소닉 체이서 2 | 스킬3 | `burst_cast` + `self_state:타겟 마킹 1` | `received_dmg_pct`: 39.96% | `all_enemies` | 5s |
| 6 | 소닉 체이서 3 | 스킬3 | `burst_cast` + `self_state:타겟 마킹 2` | `bonus_damage`: 299.7% | `all_enemies` | — |
| 7 | 소닉 체이서 4 | 스킬3 | `burst_cast` + `self_state:타겟 마킹 3` | `bonus_damage`: 349.65% | `all_enemies` | — |
| 8 | 소닉 체이서 5 | 스킬3 | `burst_cast` | `fullburst_duration` **−5.0s** | `all_allies` | 영구 |

**미구현 stat은 없다.**

## 검증 스쿼드

- **표준 (B3 · 쿨 40s)**:
  `["리틀 머메이드", "크라운", "이사벨", "test_B3"]`
  - 적 코드는 **전격**(자기 코드 = 우월 코드 불성립)이 기준이다.
- **변형 1 (아르카나 동반 — 필수)**: 하네스 `S36_이사벨아르카나`를 그대로 쓴다.
  `["이사벨", "아니스 : 스타", "신데렐라", "아르카나", "크라운"]` + `enemy={"code": "수냉"}`
  - 이 조합에서만 `소닉 체이서 5`의 단축이 **이득**이 되고, `페더 이젝션` 주기도 바뀐다(아래).

- **실전 조합 (하네스)** — 이사벨은 1개 스쿼드에 들어 있다:

  | 스쿼드 | 멤버 | 적 | 비고 |
  |---|---|---|---|
  | `S36_이사벨아르카나` | **이사벨** · 아니스 : 스타 · 신데렐라 · 아르카나 · 크라운 | 수냉 | `7.25 ↔ 12.55` 무한 교대 (`GAMEPLAY §버스트 쿨타임 감소`) |

  baseline 11,239,985,395 중 이사벨 **3,565,127,576 (31.7%)** · 풀버스트 **18회**(표준 15회).

실측 (180s, `--expected`, `first_burst_time=3.0`, 기본 스펙 1층 · 레이어 없음):

| 스쿼드 | 적 | 스쿼드 총딜 | 이사벨 |
|---|---|---|---|
| 표준 | 전격 | 1,472,805,905 | 308,267,878 (20.9%) |
| 표준 · `소닉 체이서 5` 제거(실험) | 전격 | 1,592,703,208 | 327,459,356 (20.6%) |
| `S36_이사벨아르카나` (baseline, seed 42) | 수냉 | 11,239,985,395 | 3,565,127,576 (31.7%) |

딜 구성 (표준 · 적 전격):

| 출처 | 딜 | 비중 | 히트 |
|---|---|---|---|
| 기본 공격 | 266,058,970 | **86.3%** | 2,650 (= 265발 × 10펠릿) |
| `소닉 체이서 4` (마킹 3 게이트) | 16,043,484 | 5.2% | 6 |
| `소닉 체이서 3` (마킹 2 게이트) | 15,663,067 | 5.1% | 7 |
| `페더 이젝션` | 7,494,408 | 2.4% | 11 |
| `소닉 체이서` (버스트 본체) | 3,007,949 | 1.0% | 8 |

버스트 구간별: 풀버스트(본인) 40.3% · 풀버스트(타인) 32.0% · 비풀버스트 27.7%.

## `소닉 체이서 5` — 혼자 쓰면 −7.5%다

`fullburst_duration -5.0`은 **자기가 버스트한 사이클의 풀버스트만** 줄인다. 버프 자체는
아군 전체에게 영구(`~inf`)로 붙지만, 풀버스트 길이는 **그 사이클 발동자 기준**으로 읽힌다
(`_fb_caster` 판정 — `docs/scenarios/모더니아.md §신세계` 참조).

표준 스쿼드 실측:

| 사이클 | B3 발동자 | 풀버스트 | 길이 |
|---|---|---|---|
| 1 | **이사벨** | 3.250 ~ 8.250 | **5.0s** |
| 2 | test_B3 | 15.783 ~ 25.783 | 10.0s |
| 3 | **이사벨** | 28.317 ~ 33.317 | **5.0s** |
| 4 | test_B3 | 40.850 ~ 50.850 | 10.0s |

**그런데 사이클 간격은 안 줄어든다** — 풀버스트 시작이 3.250 / 15.783 / 28.317 / 40.850으로
**12.533초 등간격**이다. 표준 스쿼드에서는 1단계가 리틀 머메이드의 버스트 쿨에 걸려 있어,
풀버스트를 일찍 끝내도 다음 사이클이 당겨지지 않는다. 결과적으로 180초 안의 풀버스트 총
시간이 **150초 → 110초**로 줄기만 한다.

실험(파싱에서 `소닉 체이서 5`만 빼고 재실행, 되돌림):

| | 있음(현행) | 없음 |
|---|---|---|
| 스쿼드 총딜 | 1,472,805,905 | 1,592,703,208 (**+8.1%**) |
| 이사벨 | 308,267,878 | 327,459,356 (**+6.2%**) |

**이사벨 자신도 손해를 본다.** 이 스킬은 자기 이득이 아니라 **아르카나에게 넘기는 패스**다.

아르카나가 있으면 이야기가 뒤집힌다 — 짧아진 풀버스트가 아르카나의 버스트 쿨감을 한 번 더
불러 `7.25 ↔ 12.55` 교대가 성립하고, 180초 풀버스트가 15회 → **18회**가 된다.
그 메커니즘의 정본은 `docs/scenarios/아르카나.md`와 `GAMEPLAY §버스트 쿨타임 감소`이므로
여기서 다시 적지 않는다.

## 타겟 마킹은 「N번째 이후 매번」이다 — 계단이 세 칸 오른다

`burst_cast_count:N`은 **N번째 버스트에서 한 번**이 아니라 **N번째 이후 매 버스트**다
(`buff_manager.py` — `count >= N`). 45초 유지이고 이사벨 버스트 간격이 25.067초라
**한 번 오른 마킹은 다시 안 내려간다.**

| 버스트 | t | 새로 붙는 마킹 | 그 버스트에 열리는 `소닉 체이서` |
|---|---|---|---|
| 1회째 | 3.200 | **마킹 1** (크확 6.26) | 본체만 |
| 2회째 | 28.267 | 마킹 1·**2** (크뎀 18.03) | 본체 + **3** (299.7%) |
| 3회째 | 53.333 | 마킹 1·2·**3** (공격력 17.28) | 본체 + 3 + **4** (349.65%) |
| 4~8회째 | 78.400 ~ | 셋 다 갱신 | 셋 다 |

180초 실측 발동: 마킹 1 **8회** · 2 **7회** · 3 **6회** = 버스트 8회의 계단.
`소닉 체이서 2/3/4`도 정확히 같은 8/7/6이다 — **같은 `burst_cast` 안에서 마킹이
먼저 붙고 게이트가 그 뒤에 평가되기 때문**이다(배열 순서 #0~2 → #5~7).
순서가 뒤집히면 전부 한 칸씩 밀려 7/6/5가 된다.

버스트 본체(`소닉 체이서`)의 히트 딜도 마킹을 따라 오른다:
**304,873 (1회째) → 315,436 (2회째) → 424,564 (3회째)**.

## 버스트 본체보다 추가 대미지가 6배 세다

같은 `burst_cast`인데 계산 시점이 다르기 때문이다.

| 효과 | stat | 계수 | 계산 시점 | 히트 딜(3회째) | 1%당 |
|---|---|---|---|---|---|
| 소닉 체이서 | `burst_damage` | 149.85% | **`burst_cast` 즉시** (53.333) | 424,564 | 2,833 |
| 소닉 체이서 3 | `bonus_damage` | 299.7% | **풀버스트 시작으로 보류** (53.383) | 2,441,454 | 8,146 |
| 소닉 체이서 4 | `bonus_damage` | 349.65% | 〃 | 2,848,363 | 8,146 |

**B3의 `bonus_damage`만 `_pending_burst_dmg`로 풀버스트까지 미뤄진다**(유저 확인 —
`IMPL-STATUS §bonus_damage`). 풀버스트는 B3 버스트 0.05초 뒤에 시작하므로, 미뤄진 쪽만
풀버스트 버프를 받아 **1%당 딜이 2.9배**가 된다. 버스트 본체는 그 버프를 못 받는다.

`burst_dmg_aoe_pct`(전체 대상 버스트 대미지 ▲)도 **본체에만** 붙고 `소닉 체이서 3·4`에는
안 붙는다(유저 확인, `IMPL-STATUS`). 이 캐릭터가 그 규칙의 근거 사례다.

## `페더 이젝션`은 전투 시작 기준 15초 격자다

`every:15.0s`는 버스트·풀버스트와 무관한 **전역 격자**다(`tick()`의 `_next_fire`).
표준 실측 첫 발이 **t=15.000**, 이후 30·45·… 로 180초에 **11회**.

**아르카나가 있으면 이 주기가 통째로 바뀐다.** 아르카나 `마법사 카드`가
`skill_cooldown_pct −75%`를 걸고, 이 stat은 `target_effect` 미지원이라 대상의
**모든 `every:Ns`에 일괄** 적용된다(`IMPL-STATUS §skill_cooldown_pct`). 이사벨의
`every:Ns`는 `페더 이젝션` 하나뿐이라 정확히 이 스킬만 빨라진다:

| | 표준 | `S36_이사벨아르카나` |
|---|---|---|
| `페더 이젝션` 발동 | **11회** (15초 주기) | **38회** |
| 그 딜 | 7,494,408 (2.4%) | 311,003,141 (8.7%) |

## 사이클 타임라인 (표준, 2사이클)

### 사이클 1 — 이사벨 버스트 사이클 (마킹 1만)

| t (실측) | 이벤트 | 발동 효과 (ID / stat) | 대상/지속 | 비고 |
|---|---|---|---|---|
| 3.000 · 3.100 | 리틀 머메이드 · 크라운 | — | | |
| **3.200** | **이사벨 burst_cast** | **타겟 마킹 1**(크확 +6.26) · **소닉 체이서 2**(적 받는 대미지 +39.96) · **소닉 체이서 5**(풀버스트 −5s) · **소닉 체이서** 304,873 | self 45s / 적 5s / 아군 전체 영구 / 적 | 마킹 2·3은 아직 조건 미달 |
| **3.250** | full_burst 시작 (1회째) | — | | `소닉 체이서 3·4`는 게이트에 막혀 **보류 대미지 없음** |
| **8.250** | **full_burst 종료** | | | **5초 만에 끝난다** (평소 10초) |
| 8.217 | 소닉 체이서 2 만료 | | | 5초 지속이 단축된 풀버스트와 거의 정확히 겹친다 |
| **15.000** | `every:15s` | **페더 이젝션** 473,764 | 적 | 전투 시작 기준 격자 |

### 사이클 2 — 이사벨 미사용 사이클 (test_B3이 B3)

| t (실측) | 이벤트 | 발동 효과 | 비고 |
|---|---|---|---|
| 15.533 · 15.633 · 15.733 | 리틀 머메이드 · 크라운 · test_B3 | — | 이사벨은 쿨 |
| 15.783 | full_burst 시작 (2회째) | — | 이사벨이 안 썼으므로 **10초 정상 길이** |
| 25.783 | full_burst 종료 | | |
| **28.267** | 이사벨 burst_cast (2회째) | **타겟 마킹 2 추가** · 소닉 체이서 315,436 | **간격 25.067s** |
| 28.317 | full_burst 시작 (3회째) | **소닉 체이서 3** 1,911,511 (보류분) | 여기서 처음 열린다 |
| 33.317 | full_burst 종료 | | 다시 5초 |

사이클 간격 **12.533s 등간격**(180초 15회), 이사벨 버스트 8회.

## 스킬 간 상호작용

- **마킹 계단 → 버스트 효과 게이트** *(스택 생성 → 소비)*
  - 경로: `타겟 마킹 N`(`burst_cast_count:N`, 45s) → `self_state:타겟 마킹 N` → `소닉 체이서 N+1`.
  - 시점: **같은 `burst_cast` 안에서 마킹이 먼저**다(배열 #0~2 → #5~7). 그래서 2회째 버스트에
    이미 `소닉 체이서 3`이 열린다.
  - 위험: 순서가 뒤집히면 전부 한 칸씩 밀리고, 45초 유지가 버스트 간격(25.067s)보다 짧아지는
    조합(버스트 쿨감이 없고 B3가 셋 이상)에서는 계단이 무너진다.
- **`소닉 체이서 5` → 아르카나** *(교차 캐릭터 · 이 캐릭터 설계의 축)*
  - 경로: 풀버스트 −5s → 버스트 사이클 단축 → 아르카나 버스트 쿨감 추가 발동 → `7.25 ↔ 12.55` 교대.
  - 이사벨 단독으로는 **−6.2%**(본인) / **−8.1%**(스쿼드)의 순손해다.
  - 정본은 `docs/scenarios/아르카나.md`·`GAMEPLAY §버스트 쿨타임 감소`.
- **아르카나 → `페더 이젝션` 주기** *(스킬 쿨타임)*
  - `skill_cooldown_pct −75%`가 이사벨의 모든 `every:Ns`에 걸리고, 그게 `페더 이젝션` 하나다.
  - 실측 11회 → **38회**, 딜 비중 2.4% → 8.7%.
- **`소닉 체이서 2`는 스쿼드 버프다** *(적 받는 대미지)*
  - `received_dmg_pct` 39.96%를 적에게 5초. **단축된 풀버스트(5초)와 길이가 같다** —
    이사벨의 버스트 사이클에서는 풀버스트 전 구간을 덮는다.
  - 마킹 1 게이트라 **첫 버스트부터** 걸린다(8회 전부).

## 유저 확인 사항
- 초안 단계를 거치지 않아 신규 질문 없음. 아래는 파싱·코드에서 확인한 사실.
- Q: `타겟 마킹 2`가 왜 7회인가? 3회째 버스트부터가 아닌가?
  A: **2회째부터 매번이라 8−1 = 7회다.** `burst_cast_count:N`은 `count >= N`이다
  (`buff_manager.py`). 마킹 3은 6회.
- Q: 버스트 본체(`소닉 체이서`)가 왜 추가 대미지보다 약한가?
  A: **계산 시점이 다르다.** B3의 `bonus_damage`만 풀버스트로 보류돼 풀버스트 버프를 받는다.
  본체는 `burst_cast` 시점에 즉시 계산된다.
- Q: 풀버스트가 5초로 끝나는데 사이클도 빨라져야 하지 않나?
  A: **표준 스쿼드에서는 안 빨라진다.** 1단계가 리틀 머메이드의 버스트 쿨에 걸려 있어
  다음 사이클 시작이 그대로다. 아르카나가 있어야 단축이 사이클로 전달된다.
- Q: `페더 이젝션`의 15초는 어디서 나온 값인가?
  A: **CDN에 쿨타임이 `null`로 오는 유형**이라 원문·스크랩 어디에도 주기가 없다
  (`PARSING.md §4-1` — 정규 절차는 유저 인게임 확인). **15.0은 그 인게임 확인값이다**
  (유저 재확인, 2026-09-03).

## 해석 선언

| 효과 | 해석 | 근거 | 대안이었다면 |
|---|---|---|---|
| 타겟 마킹 1·2·3 | `burst_cast_count:N` = **N번째 이후 매 버스트** | `buff_manager.py`(`count >= N`) · 실측 8/7/6 | "N번째에 한 번"이면 45초 유지가 끊기는 순간 계단이 영구히 무너진다 |
| 타겟 마킹 ↔ 소닉 체이서 | 같은 `burst_cast` 안에서 **마킹이 먼저** | parsed 배열 순서 #0~2 → #5~7 · 실측 게이트 8/7/6 일치 | 뒤집히면 2회째 버스트에 `소닉 체이서 3`이 안 열려 전부 한 칸 밀린다 |
| 소닉 체이서 (본체) | `burst_damage` + `all_enemies` → **`burst_dmg_aoe_pct` 대상** | `IMPL-STATUS §burst_dmg_aoe_pct` (유저 확인) | `소닉 체이서 3·4`까지 대상이었다면 추가 대미지가 한 번 더 증폭된다 |
| 소닉 체이서 3·4 | `bonus_damage` → **풀버스트로 보류** (B3라서) | `IMPL-STATUS §bonus_damage` (유저 확인) · 실측 1%당 2.9배 | 즉시 계산이었다면 본체와 같은 배율로 떨어져 버스트 딜이 크게 준다 |
| 소닉 체이서 5 | `fullburst_duration -5.0`, `duration: -1`, `polarity: harmful` — **자기가 발동한 사이클만** 단축 | 실측 사이클 1·3만 5초 · `모더니아 §신세계` 선례와 동형 | 유한 `duration`을 넣으면 `_fb_caster` 판정 경로가 달라져 남의 사이클까지 줄인다. `duration`은 `null` → `-1`, `polarity`는 `beneficial` → `harmful`로 2026-09-05 정정(둘 다 동작 불변, `PARSING-CHARS.md` 이사벨 행) |
| 소닉 체이서 5 | 대상 `all_allies` — **자신 포함** | 원문 `아군 전체에게` · 실측 4명 전원 | 자기 제외였다면 풀버스트 길이 판정이 캐릭터별로 갈려 모델이 무너진다 |
| 페더 이젝션 | `every:15.0s` — **전투 시작 기준 전역 격자** | 실측 첫 발 t=15.000 · `IMPL-STATUS §every:Ns` | 스킬 발동 기준 위상이었다면 버스트와의 정렬이 조합마다 달라진다 |
| 페더 이젝션 | 주기 **15.0초**라는 값 자체 | CDN 쿨타임 `null`이라 원문에 근거가 없다 — **유저 인게임 확인값**(재확인 2026-09-03) | 30초였다면 딜 비중이 절반, 아르카나 조합에서는 38회 → 19회로 갈린다 |
| 페더 이젝션 | `enemies_top_def:5` — 쫄몹이 없으면 `__enemy__`, 쫄몹이 있으면 **보스 먼저** 최대 5기가 한 발씩 맞는다(쫄몹 몫은 총딜 밖) | `_resolve_target()` · `boss_pattern` §쫄몹 | 쫄몹 방어력 모델이 없어 순서는 보스 → 등장 순이다 — 쫄몹 방어력이 생기면 5기를 넘는 무리에서 누가 맞는지가 바뀐다 |

## impl/bug-fix용 검증 체크리스트
- [ ] 레이어 없음 — 이탈 보고가 비어 있다
- [ ] `타겟 마킹` 1/2/3 = **8/7/6회** 계단 (버스트 8회)
- [ ] `소닉 체이서 2/3/4`가 마킹과 **같은 횟수** (8/7/6) — 실행 순서 회귀 항목
- [ ] 이사벨이 버스트한 사이클의 풀버스트 길이 = **5.0초**, 아닌 사이클 = **10.0초**
- [ ] `소닉 체이서 5` 대상 = 스쿼드 전원(자신 포함) · 사이클당 인원수만큼 발동 (표준 8×4 = 32회)
- [ ] 사이클 간격은 **줄지 않는다** (표준 12.533s 등간격 · 풀버스트 15회)
- [ ] `bonus_damage` 2종이 **풀버스트 시작 시각**(버스트 +0.05s)에 찍힌다
- [ ] 1%당 딜: `bonus_damage` ≈ `burst_damage`의 **2.9배**
- [ ] `페더 이젝션` 첫 발 **t=15.000** · 180초 **11회** (표준)
- [ ] 아르카나 동반 시 `페더 이젝션` **38회**로 뛴다 (`skill_cooldown_pct` 일괄 적용)
- [ ] 기본 공격 히트 = 발수 × **10펠릿** (표준 2,650 = 265발)
- [ ] `소닉 체이서 5` 제거 시 스쿼드 총딜 **+8.1%** — 단독 사용은 순손해다
