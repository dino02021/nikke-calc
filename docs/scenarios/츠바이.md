# 츠바이 동작 시나리오

> 모드: 보강 (parsed_skills 반영)
> **역순 작성** — 등록·검증이 끝난 캐릭터의 시나리오를 뒤늦게 채운 문서다.
> 초안(파싱 전) 단계 없음. 수치·발동 시각은 전부 실측이다.

## 기본 정보
- 버스트 단계: B1
- 본인 쿨다운: 20s (과충전 공식)
- 무기 유형: SG (9발, 재장전 1.5s, 연사 1.5발/s, 계수 100.75%, **펠릿 5 × 총구 2 = 사격당 10히트**, 코어 200%)
- 속성 코드: 전격 · 클래스: 지원형 · 기업: 엘리시온
- 애장품: **있음 — 네잎클로버 책갈피** (`favorite_slots: [3, 2, 1]` → 1단계=스킬3, 2단계=스킬2, 3단계=스킬1)
- 기본 레이어: `char_defaults.json`에 **버스트 패턴 전담**(같은 단계에 다른 B1이 있을 때만 적용)
- 작성 사이클 수: 1 (본인 쿨 20s가 그대로 사이클 병목)

## 효과 요약 (보강: 효과 단위 stat 키)

기본 스펙은 **애장품 3단계**라 세 슬롯 전부 애장 판본이다. 아래는 그 상태 기준.

| ID (parsed name) | 스킬 | 판본 | 트리거 (timing+condition) | stat: 값(lv10) | 대상 | 지속 |
|---|---|---|---|---|---|---|
| 관통 계산식 | 스킬1 | 애장3 | `full_burst_start` | `pierce_dmg_pct`: 20.13% | `all_allies` | **1발** |
| 관통 계산식 2 | 스킬1 | 애장3 | `full_burst_start` | `pierce_dmg_pct`: 10.06% | `all_allies` | 10s |
| 관통 계산식 3 | 스킬1 | **애장3 전용** | `on_attack` + `during_full_burst` | `pierce_dmg_pct`: 24.99% (max_stack 3) | `all_allies` | 1발 |
| 골조 해석 | 스킬2 | 애장2 | `on_attack_count:5` | `cover_heal_pct`: 7.52% | `all_allies` | — (instant) |
| 골조 해석 2 | 스킬2 | 애장2 | `full_burst_start` | `crit_rate`: 18.63% | `all_allies` | **10s** (비애장 5s) |
| 골조 해석 3 | 스킬2 | **애장2 전용** | `on_attack` + `self_state:관통학 개론` | `crit_rate`: 15.0% (max_stack 3) | `all_allies` | 5s |
| 과충전 공식 | 스킬3 | 애장1 | `burst_cast` | `weapon_change` (SR · 계수 50.69% · 장탄 1 · **차지 1.2s** · 풀차지 300% · 관통특화) | self | 1발 |
| 과충전 공식 2 | 스킬3 | 애장1 | `burst_cast` | `pierce_enabled` | self | 1발 |
| 관통학 개론 | 스킬3 | **애장1 전용 이름** | `burst_cast` | `pierce_dmg_pct`: 25.03% | `all_allies` | 10s |

비애장 판본과의 차이:
- 스킬3: 차지 1.5s → **1.2s**, 마지막 버프 이름이 `과충전 공식 3`(15.48%) → **`관통학 개론`(25.03%)**
- 스킬2: `골조 해석 2` 지속 5s → **10s**, `골조 해석 3` 신설
- 스킬1: `관통 계산식 3` 신설

**이름이 곧 배선이다** — 애장2의 `골조 해석 3`은 `self_state:관통학 개론`으로 게이팅되는데,
그 이름을 만드는 것이 애장1의 스킬3 버프다. 애장 1단계가 없으면 애장 2단계의 `골조 해석 3`도
영원히 꺼진다. 실측 총딜이 이 의존을 그대로 보여준다(아래 §애장품 단계별 실측).

정적 딜 기여: `pierce_dmg_pct` 3종(⑤, **관통 히트에만** 가산) · `crit_rate` 2종(③).
`cover_heal_pct`는 ✅지만 엄폐물 체력은 보스 공격 패턴(`enemy.patterns`의 `attack`)이 있을 때만 깎여 딜 무관.

## 검증 스쿼드

조건부 효과 스캔: `no_burstN_ally` · `back_row` · `allies_weapon/class/code` 류 조건 **없음**.
`self_state:관통학 개론`은 본인 버스트로 자기완결이라 **효과 검증만 놓고 보면 스쿼드 1개로 충분**하다.
다만 **사이클이 조합에 크게 좌우되므로**(아래) 실전 조합을 함께 돌린다.

- **실전 조합 (하네스 `S39_맥스웰츠바이`)** — 커버 정본:
  `["나유타", "스노우 화이트", "맥스웰", "츠바이", "헬름 : 아쿠아마린"]`, `enemy={"code": "전격"}`
  - 츠바이가 유일한 B1이라 **매 사이클 전담**. 본인 킷에 쿨감이 없어도 나유타·헬름 : 아쿠아마린이
    깎아 주는 몫으로 사이클이 20s → **13.38s까지 당겨진다.**
  - 실측 (180s, `--expected`): 스쿼드 총딜 **3,278,362,738** / 츠바이 210,146,276 (6.4%).
- **전담 패턴 검증 (버쿨감 B1 동석)**:
  `["츠바이", "리틀 머메이드", "크라운", "test_B3", "스노우 화이트 : 헤비암즈"]`
  - `char_defaults.json`의 츠바이 레이어는 `_burst_patterns: {"전담": "every:1"}`와
    `_rules: [{when: {same_stage_other: true}, apply: {control: {burst: {pattern: "전담"}}}}]`다 —
    **같은 단계에 다른 멤버가 있을 때만** 걸리는 등급 0(그 단계를 전담) 패턴이라,
    이 스쿼드에서만 실제로 발동한다(러너가 `[츠바이] control.burst: 없음 → {pattern=전담} (레이어)`로
    이탈을 보고한다).
  - 실전 운용대로 **1버는 츠바이가 거의 전담**한다. 리틀 머메이드는 버스트를 쓰지 않고
    `버블 오더 2`(`full_burst_end` −7.48s)로 츠바이의 쿨만 깎는 자리다.
  - 실측 B1 사용 로그: t = 3.0(츠바이) → 15.25(**리틀 머메이드**) → 27.783 → 40.317 → 52.85 →
    … 이후 끝까지 전부 츠바이. **15회 중 14회를 츠바이가 가져간다.**
    2사이클만 넘어가는 이유는 쿨 −7.48s를 받은 츠바이의 준비 시각(15.52s)이
    B1 슬롯이 열리는 15.25s보다 **0.27초 늦기 때문**이다. 등급 2가 없는 리틀 머메이드가
    그 틈에 나간다(단계를 막지 않는 설계 — `docs/HARNESS.md` §버스트 운용 패턴).
  - 실측 (180s): 스쿼드 총딜 **2,286,951,305** / 츠바이 264,276,884 (11.6%).
- **격리 / 애장품 대조**: `["츠바이", "크라운", "test_B3", "스노우 화이트 : 헤비암즈"]`
  - 다른 B1이 없어 전담 패턴이 걸리지 않고, 쿨감 공급자도 없어 **사이클이 본인 쿨 20s에 묶인다.**
    효과 자체의 유무를 격리해서 볼 때만 쓴다. 애장품 단계 대조는 여기서 냈다.

### 애장품 단계별 실측 (180s, `--expected`, **격리 스쿼드** 총딜)

| 단계 | 총딜 | 직전 대비 | 무엇이 켜지나 |
|---|---|---|---|
| 0 | 1,073,699,631 | — | 기본 판본 3개 |
| 1 | 1,082,553,464 | +0.82% | 스킬3 차지 1.5→1.2s, `관통학 개론`(25.03%) |
| 2 | **1,246,649,839** | **+15.16%** | `골조 해석 2` 10s화 + `골조 해석 3`(crit_rate 15%×3) |
| 3 | 1,290,359,095 | +3.51% | `관통 계산식 3`(pierce 24.99%×3) |

애장 2단계의 도약이 이 캐릭터의 본체다. `골조 해석 3`이 실제로 발동한다는 증거이기도 하다
(`self_state:관통학 개론` 게이팅이 성립).

실측 (180s, 애장3, 격리 스쿼드): 스쿼드 총딜 **1,290,359,095** / 츠바이 187,157,923 (14.5%).

## 사이클 타임라인 (애장 3단계 · 격리 스쿼드 기준)

> 아래 표의 **20.0s 사이클은 격리 스쿼드에서만 성립한다.** 실전 조합에서는 동료가 주는
> 쿨감으로 13.38s(하네스) ~ 12.53s(리틀 머메이드 동석)까지 당겨진다 — 사이클 값 외의
> 효과 순서·수치는 세 스쿼드 모두 같다.

| t (실측) | 이벤트 | 발동 효과 (ID / stat) | 대상/지속 | 비고 |
|---|---|---|---|---|
| 0 | battle_start | (없음 — 상시 버프 없음) | — | SG 자동사격 시작, 사격당 10히트 |
| 0~3 | 일반 공격 | `on_attack_count:5`마다 골조 해석(엄폐 회복) | all_allies | 딜 무관 |
| 3.00 | **츠바이 burst_cast** | 과충전 공식(**무기 변경 SR**) · 과충전 공식 2(`pierce_enabled`) · 관통학 개론(`pierce_dmg_pct` +25.03) | self · all_allies / 10s | 이 시점부터 `self_state:관통학 개론` 참 |
| 3.25 | **full_burst 시작** | 관통 계산식(20.13%, 1발) · 관통 계산식 2(10.06%, 10s) · 골조 해석 2(`crit_rate` +18.63, 10s) | all_allies | |
| 3.25~ | 츠바이 일반 공격 | 관통 계산식 3(pierce, 1발·3중첩) · 골조 해석 3(`crit_rate` 15%, 5초·3중첩) | all_allies | `during_full_burst` / `self_state` 게이팅 |
| **4.217** | 무기 변경 SR **1발 발사** | 892,407 (실측) — 직후 SG로 복귀 | 대상 | 차지 1.2s → 3.0+1.2 |
| 4.233 | SG 사격 재개 | 펠릿 10히트 (87,927 ×10, 실측) | 대상 | |
| 4.917 | 재장전 시작 | — | — | SR 1발 소모 후 SG 탄창 |
| 13.00 | 관통학 개론 만료 | `골조 해석 3` 게이팅 닫힘 | — | 이후 `on_attack`으로 새 스택 안 붙음 |
| 13.25 | **full_burst 종료** | 관통 계산식 2 · 골조 해석 2 만료 | — | |
| 23.00 | 츠바이 burst_cast (2회째) | 위 반복 | | 본인 쿨 20s |
| 23.25 | full_burst 시작 (2회째) | | | **사이클 20.0s** |

### 스쿼드별 사이클 실측

| 스쿼드 | 츠바이 버스트 간격 | 병목 |
|---|---|---|
| 격리 (`츠바이·크라운·test_B3·스노우 화이트 : 헤비암즈`) | 3.25 → 23.25 → 43.25 → … **20.0s 등간격** | **츠바이 본인 쿨 20s.** 킷에 쿨감이 하나도 없고 공급자도 없다 |
| 하네스 `S39_맥스웰츠바이` | 3.25 → 21.433 → 37.417 → 50.800 → 64.183 → … **13.38s로 수렴** | 동료 쿨감이 붙으면서 3사이클에 걸쳐 짧아진다 |
| 리틀 머메이드 동석 | 3.0 → (15.25 리틀 머메이드) → 27.783 → 40.317 → … **12.53s 등간격** | 표준 사이클. 20 − 7.48 |

**츠바이 킷에는 버스트 쿨감이 없다** — 사이클은 전적으로 동료가 만든다. 그래서 실전에서는
버쿨감을 가진 B1을 함께 넣고 **버스트는 츠바이가 전담**하는 형태가 정석이고,
`char_defaults.json`의 전담 레이어가 그 운용을 코드로 옮긴 것이다.

## 스킬 간 상호작용

- **애장1 `관통학 개론`(이름) → 애장2 `골조 해석 3`(게이팅)**
  - 경로: 스킬3(애장1)이 `all_allies`에 `관통학 개론` 이름의 버프 부여 → `_has_self_state("츠바이",
    "관통학 개론")`이 참 → 스킬2(애장2)의 `on_attack` crit_rate가 열린다.
  - 시점: `burst_cast` 부여 후 10초 구간. **풀버스트(10s)와 거의 정확히 겹친다**(0.25s 어긋남).
  - 위험: 애장 1단계 미보유 시 이름이 `과충전 공식 3`이라 `골조 해석 3`이 통째로 죽는다.
    애장 단계를 낮춰 돌릴 때 **딜이 계단식으로 꺼지는 게 정상**이다(위 표).
- **무기 변경(SR 1발) → 관통 계열 전부의 수혜 대상**
  - 경로: 과충전 공식(무기 변경, 관통특화) + 과충전 공식 2(`pierce_enabled`) → 그 1발이
    `is_pierce_damage=True` → `pierce_dmg_pct` 3종(관통 계산식 1·2·3 + 관통학 개론)이 전부 실린다.
  - **동일 timing(`burst_cast`) 배열 순서**: 무기 변경 → `pierce_enabled` → `관통학 개론` 순서를 유지한다.
  - 위험: `duration_bullets: 1`이라 **딱 한 발**이다. 그 한 발이 실측 892,407로 SG 한 사격
    (10히트 × 87,927 = 879,270)과 맞먹는다.
- **`관통 계산식`(1발 유지) vs `관통 계산식 2`(10초)**
  - 같은 stat·같은 시점인데 수명만 다르다. 1발짜리는 사실상 **무기 변경 그 한 발 전용**이다
    (풀버스트 진입 직후 첫 사격에 소모).

## 유저 확인 사항
- 초안 단계 없음. 아래는 파싱·코드·실측에서 확인한 사실.
- Q: `골조 해석 3`이 정말 발동하나? (`self_state:관통학 개론`은 남이 만든 이름)
  A: **발동한다.** `_has_self_state`는 `_active` 버프의 `target_chars`에 캐스터가 있으면 참이고,
  `관통학 개론`은 `all_allies` 대상이라 츠바이 자신도 포함된다. 애장 1→2 총딜 +15.16%가 증거.
- Q: 무기 변경 SR은 몇 발 쏘나?
  A: **1발**. `max_ammo: 1` + `duration_bullets: 1`. 실측에서 t=4.217 단발 후 즉시 SG 복귀.
- Q: 사이클이 왜 20s인가?
  A: 츠바이 킷에 버스트 쿨감이 **없다**. 본인 쿨 20s가 그대로 사이클이 된다.

## 해석 선언

| 효과 | 해석 | 근거 | 대안이었다면 |
|---|---|---|---|
| 과충전 공식 (무기 변경) | SR 1발만 발사 후 원래 SG 복귀 | `max_ammo:1` · `duration_bullets:1` · 실측 t=4.217 단발 | 지속형이었다면 SG 10히트/사격을 잃고 오히려 손해 |
| 관통학 개론 (애장1) | 이름 자체가 애장2의 게이팅 키 | `골조 해석 3`의 `self_state:관통학 개론` | 이름이 `과충전 공식 3`이면 애장2 crit 스택이 전부 죽는다 (실측 −15%) |
| 관통 계산식 (1발 유지) | 무기 변경 그 한 발에 소모 | `duration_bullets:1` + 풀버스트 진입 직후 사격 | 10초였다면 SG 사격 전부에 20.13%가 더 실린다 — 단, SG는 관통 히트가 아니라 무의미 |
| 골조 해석 3 · 관통 계산식 3 | `max_stack:3` 누적, 스택마다 값 가산 | `buff_manager` 스택 처리 | 갱신형이었다면 crit_rate 15%·pierce 24.99%가 3배가 아니라 1배 |

## impl/bug-fix용 검증 체크리스트
- [ ] 풀버스트 진입 시 활성(애장3): `과충전 공식 2` · `관통학 개론` · `관통 계산식` · `관통 계산식 2` · `골조 해석 2`
- [ ] `burst_cast` 후 1.2s 지점에 무기 변경 SR **단발** 히트 (차지 1.2s, 비애장이면 1.5s)
- [ ] 그 1발에 `pierce_dmg_pct` 4종(20.13+10.06+25.03+관통 계산식 3 스택)이 전부 실리는지
- [ ] `골조 해석 3`이 `관통학 개론` 활성 10초 구간에만 붙고 3중첩까지 오르는지
- [ ] 사이클 간격: 격리 **20.0s** / 하네스 `S39_맥스웰츠바이` **13.38s** / 리틀 머메이드 동석 **12.53s**
- [ ] 리틀 머메이드 동석 시 **전담 패턴이 걸려 츠바이가 B1을 15회 중 14회 가져가는지**
      (`_rules[].when.same_stage_other` — 다른 B1이 없으면 패턴 자체가 안 붙는다)
- [ ] 애장 단계별 총딜(격리): 0 → 1(+0.8%) → 2(**+15.2%**) → 3(+3.5%)
- [ ] 발동 금지: `cover_heal_pct`(골조 해석)의 딜 기여 — 엄폐물 회복(✅)이라 딜 무관
- [ ] 발동 금지(애장 1단계 미만): `골조 해석 3` — 이름 게이팅이 닫혀 0회
