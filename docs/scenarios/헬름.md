# 헬름 동작 시나리오

> 모드: 보강 (parsed_skills 반영)
> **역순 작성** — 이미 등록·검증이 끝난 캐릭터의 시나리오를 뒤늦게 채운 문서다.
> 초안(파싱 전) 단계를 거치지 않았으므로 `## 유저 확인 사항`은 파싱 결과에서
> 역으로 확인한 항목만 담는다. 수치·발동 시각은 전부 실측이다.

## 기본 정보
- 버스트 단계: B3
- 본인 쿨다운: **40s** (이지스 캐논)
- 무기 유형: SR (6발, 재장전 2.0s, 계수 69.04%, 코어 200%, 차지 1.0s, **풀차지 배율 250%**)
- 속성 코드: 수냉 · 클래스: 화력형 · 기업: 엘리시온 · 스쿼드: 이지스
- **애장품: 「낡은 나침반」 — 슬롯 1·2·3 전부** (기본 스펙은 **3단계**)
- 작성 사이클 수: **2** (쿨 40s)
- **기본 레이어 1건** (`data/char_defaults.json`): 최대 장탄 옵션 **0%**

> **애장품이 이 캐릭터의 절반 이상이다.** 3단계 → 0단계로 내리면 헬름 개인 딜이
> **1/3.86**로 떨어진다(458.9M → 118.8M). `이지스 캐논` 계수가 1237.5% → **8236.8%**로
> 바뀌고, 딜 지분 22%를 차지하는 `포문 개방 3`은 **애장품 2단계에서 처음 생기는 효과**다.
> 애장품 단계를 안 밝힌 헬름 수치는 읽을 수 없다.

## 효과 요약 (보강: 효과 단위 stat 키)

애장품 슬롯마다 **판본이 둘**이다(기본 / 애장품 N단계). 기본 스펙은 3단계라
아래 표의 **애장품 판본이 실제로 도는 것**이고, 기본 판본은 0~2단계에서만 쓰인다.

| # | ID (parsed name) | 스킬 | 판본 | 트리거 | stat: 값(lv10) | 대상 | 지속 |
|---|---|---|---|---|---|---|---|
| 0 | 진두지휘 | 스킬1 | 기본 | `last_bullet` | `normal_atk_crit_rate`: 14.64% | `all_allies` | 5s |
| 1 | 진두지휘 | 스킬1 | **애장품1** | `last_bullet` | `normal_atk_crit_rate`: 14.64% (동일) | `all_allies` | 5s |
| 2 | 진두지휘 2 | 스킬1 | **애장품1** | `full_charge_fire` | `heal_hp_pct`: 0.59% (`scaling: max_hp`) | `all_allies` | — |
| 3 | 진두지휘 3 | 스킬1 | **애장품1** | `full_charge_fire` | `burst_charge_pct`: **14.31%** | `all_allies` | — |
| 4 | 포문 개방 | 스킬2 | 기본 | `passive` | `intercept_dmg_pct`: 3.08% | `all_allies` | 영구 |
| 5 | 포문 개방 2 | 스킬2 | 기본 | `full_burst_start` | `atk_dmg_pct`: 11.85% | `all_allies` | 10s |
| 6 | 포문 개방 | 스킬2 | **애장품2** | `passive` | `intercept_dmg_pct`: 3.08% (동일) | `all_allies` | 영구 |
| 7 | 포문 개방 2 | 스킬2 | **애장품2** | `full_burst_start` | `atk_dmg_pct`: **27.87%** | `all_allies` | 10s |
| 8 | 포문 개방 3 | 스킬2 | **애장품2** | `full_charge_hit` | `bonus_damage`: 178.98% | `target` | — |
| 9 | 이지스 캐논 | 스킬3 | 기본 | `burst_cast` | `burst_damage`: 1237.5% | `enemies_top_atk:1` | — |
| 10 | 이지스 캐논 2 | 스킬3 | 기본 | `burst_cast` | `lifesteal_pct`: 54.45% | `all_allies` | 10s |
| 11 | 이지스 캐논 | 스킬3 | **애장품3** | `burst_cast` | `burst_damage`: **8236.8%** | `enemies_top_atk:1` | — |
| 12 | 이지스 캐논 2 | 스킬3 | **애장품3** | `burst_cast` | `lifesteal_pct`: 54.45% (동일) | `all_allies` | 10s |
| 13 | 이지스 캐논 3 | 스킬3 | **애장품3** | `burst_cast` | `charge_dmg_mag_pct`: 158.4% | `self` | **10발** |

**영구 미발동 1건**: `포문 개방`(`intercept_dmg_pct`)은 **구현하지 않기로 한 stat**이다
(🚫, 유저 결정 2026-08-11 — `IMPL-STATUS`). 계산기 적 모델에 저지 부위가 없어 기여가 0이다.
파싱은 정상이고 네거티브 항목으로 둔다.

## 검증 스쿼드

- **표준 (B3 · 쿨 40s)**:
  `["리틀 머메이드", "크라운", "헬름", "test_B3"]`
  - 적 코드는 **수냉**(자기 코드 = 우월 코드 불성립)이 기준이다.
- **변형 1 (애장품 단계 — 필수)**: 표준 + `--favorite "헬름:0"` (1·2도 각각)
  - 이 캐릭터에서 가장 큰 변수다. 아래 표 참조.
- **변형 2 (장탄 레이어 이탈)**: 표준 + `chars={"헬름": {"equip_skills": {"max_ammo_pct": 129.64}}}`
  - `진두지휘`가 `last_bullet` 트리거라 **탄창이 커지면 발동이 줄어든다** — 실측 16회 → **4회**.

- **실전 조합 (하네스)** — 헬름은 1개 스쿼드에 들어 있다:

  | 스쿼드 | 멤버 | 적 | 비고 |
  |---|---|---|---|
  | `S39_마르차나아쿠아` | 미란다 · 나유타 · 마르차나 : 마린 스터디 · **헬름** · 헬름 : 아쿠아마린 | 전격 · SMG 적정 사거리 | `accumulate` · 헬름에 **클릭컨 `burst_chain` `hold_until_close`**(`CONTROL.md §클릭`) |

  baseline 5,875,119,523 중 헬름 **555,349,884 (9.5%)** · 풀버스트 13회.
  `진두지휘` 85회(17 발동 × 5명) · `진두지휘 2·3` 각 **107회**.
  여기서는 딜러가 아니라 **버충·크리 공급원**이다.

실측 (180s, `--expected`, `first_burst_time=3.0`, 기본 스펙 1층 + 장탄 레이어, 적 수냉):

| 설정 | 스쿼드 총딜 | 헬름 |
|---|---|---|
| 표준 (애장품 **3**) | 1,476,860,590 | **458,916,709 (31.1%)** |
| 애장품 2 | 1,247,422,363 | 229,478,482 (18.4%) |
| 애장품 1 | 1,064,124,531 | 122,096,574 (11.5%) |
| 애장품 0 | 1,045,276,314 | 118,799,914 (11.4%) |
| 표준 · 장탄 옵션 129.64% (레이어 이탈) | 1,472,264,821 | 464,019,200 |
| 표준 · `accumulate` | 1,467,312,365 | 459,062,540 |
| `S39_마르차나아쿠아` (baseline, seed 42, 적 전격) | 5,875,119,523 | 555,349,884 (9.5%) |

딜 구성 (표준):

| 출처 | 딜 | 비중 | 히트 |
|---|---|---|---|
| 기본 공격 | 210,118,923 | 45.8% | 123 |
| `이지스 캐논` (버스트) | 148,951,840 | **32.5%** | 8 (히트당 18.6M) |
| `포문 개방 3` (풀차지마다) | 99,845,946 | 21.8% | 123 |

버스트 구간별: 풀버스트(본인) 39.2% · 풀버스트(타인) 19.5% · 비풀버스트 41.2%.

## 애장품 단계별로 무엇이 켜지는가

| 단계 | 새로 켜지는 것 | 헬름 딜 | 스쿼드 총딜 |
|---|---|---|---|
| 0 | — (기본 판본만) | 118,799,914 | 1,045,276,314 |
| 1 | `진두지휘 2`(힐) · `진두지휘 3`(**버충 14.31%**) | 122,096,574 (+2.8%) | 1,064,124,531 (+1.8%) |
| 2 | `포문 개방 2` **11.85 → 27.87** · `포문 개방 3`(추가 대미지 178.98%) | 229,478,482 (**+87.9%**) | 1,247,422,363 (+17.2%) |
| 3 | `이지스 캐논` **1237.5 → 8236.8** · `이지스 캐논 3`(차지 대미지 배율 158.4, 10발) | 458,916,709 (**+100.0%**) | 1,476,860,590 (+18.4%) |

**계단이 2·3단계에 몰려 있다.** 1단계는 딜에 거의 기여하지 않고(버충·힐), 2단계에서
스쿼드 버프(`포문 개방 2`)와 본인 딜(`포문 개방 3`)이 동시에 붙으며, 3단계가 버스트를
**6.66배**로 키운다. 스쿼드 총딜 기준으로도 0 → 3이 **+41.3%**다.

1단계에서 `리틀 머메이드`가 580.8M → 589.5M로만 오르고 2단계에서 638.1M로 뛰는 것도
같은 이유다 — 아군 버프 증가분(`포문 개방 2` +16.02%p)이 2단계에 있다.

## 최대 장탄 옵션 0% 레이어 — `진두지휘`를 살리려는 것이다

`진두지휘`는 `last_bullet`(마지막 탄환 명중) 트리거라 **탄창이 클수록 드물게 터진다.**
기본 스펙의 장탄 옵션 129.64%를 그대로 쓰면 발동 간격이 3배 이상 벌어진다:

| | 장탄 0% (레이어) | 장탄 129.64% |
|---|---|---|
| `진두지휘` 발동 | **16회** (부여 64건) | **4회** (부여 16건) |
| 첫 발동 시각 | 10.767 | 35.900 |
| 발동 간격 | ≈ 11.7초 | ≈ 36초 |
| 헬름 딜 | 458,916,709 | 464,019,200 (+1.1%) |
| 스쿼드 총딜 | **1,476,860,590** | 1,472,264,821 (−0.3%) |

**표준 스쿼드에서 이 레이어의 순이득은 −0.3%p 수준으로 작다.** 헬름 본인은 오히려
장탄이 많은 쪽이 낫고(재장전이 줄어 발수가 123 → 127), 스쿼드가 그보다 조금 더 잃는다.
레이어의 값어치는 **일반 공격 비중이 큰 아군이 여럿일 때** 커진다 —
`진두지휘`가 `all_allies`의 **일반 공격 크리티컬 확률**이기 때문이다.
`S39_마르차나아쿠아`(SMG·AR 다수)가 그 자리이고, 거기서는 17회 발동한다.

## `이지스 캐논 3`은 시간이 아니라 발수로 끝난다

`charge_dmg_mag_pct` 158.4%가 `duration_bullets: 10`으로 붙는다 — 로그에는 만료가
`~inf`로 찍히지만 **10발을 쏘면 사라진다.**

실측: 3.200 부여 → **16.850 소멸** (13.65초). 풀버스트는 13.250에 끝나므로
**이 버프는 풀버스트 밖으로 3.6초를 더 산다.** 반대로 사격이 빠른 조합에서는
풀버스트 안에서 다 타 버린다 — 지속시간형 버프와 계산이 다르다는 점을 놓치면 안 된다.

## 사이클 타임라인 (표준, 2사이클)

### 사이클 1 — 헬름 버스트 사이클

| t (실측) | 이벤트 | 발동 효과 (ID / stat) | 대상/지속 | 비고 |
|---|---|---|---|---|
| **0.000** | passive 평가 | **포문 개방**(`intercept_dmg_pct` 3.08) | 아군 전체 · 영구 | **영구 미발동 stat** (🚫) |
| 1.000 · 2.400 | 풀차지 사격 | **진두지휘 2**(힐) · **진두지휘 3**(버충 14.31) · 기본 공격 + **포문 개방 3** | 아군 전체 / 적 | 풀차지 한 발에 4건이 함께 난다 |
| 3.000 · 3.100 | 리틀 머메이드 · 크라운 | — | | |
| **3.200** | **헬름 burst_cast** | **이지스 캐논** 18,618,980 · **이지스 캐논 2**(라이프스틸 54.45) · **이지스 캐논 3**(차지 대미지 배율 158.4) | 적 / 아군 10s / self **10발** | 버스트 본체는 풀버스트 **전**에 계산된다 |
| **3.250** | full_burst 시작 (1회째) | **포문 개방 2**(공격 대미지 +27.87) | 아군 전체 · 10s | 애장품 2단계 값 |
| 3.800 → 12.150 | 풀차지 1.4s | 기본 2,673,353 + 포문 개방 3 1,034,894 | | 버스트 밖 대비 약 9배 |
| **10.767** | **마지막 탄환 명중** | **진두지휘**(일반 공격 크확 +14.64) | 아군 전체 · 5s | 11.150 재장전 시작 |
| 13.250 | full_burst 종료 | 포문 개방 2 off | | |
| **16.850** | — | **이지스 캐논 3 소멸** | | 시간이 아니라 **10발 소진** |

### 사이클 2 — 헬름 미사용 사이클 (test_B3이 B3)

| t (실측) | 이벤트 | 발동 효과 | 비고 |
|---|---|---|---|
| 15.533 · 15.633 · 15.733 | 리틀 머메이드 · 크라운 · test_B3 | — | 헬름은 쿨 |
| 15.783 | full_burst 시작 (2회째) | **포문 개방 2** (아군 전체) | 헬름 자신도 받는다 |
| **22.450** | 마지막 탄환 명중 | **진두지휘** | 간격 **11.683s** |
| 25.783 | full_burst 종료 | | |
| **28.267** | 헬름 burst_cast (2회째) | 사이클 1과 동일 | **간격 25.067s** |

사이클 간격 **12.533s 등간격**(180초 15회), 헬름 버스트 8회.

## 스킬 간 상호작용

- **풀차지 한 발이 네 가지를 동시에 낸다** *(이 캐릭터의 리듬)*
  - 풀차지 한 발에 `진두지휘 2`(힐) · `진두지휘 3`(버충) · `포문 개방 3`(추가 대미지)이
    묶여 있고, 거기에 기본 공격 자체가 더해진다. 실측 표준 **각 123회**.
    원문이 앞의 둘은 「풀 차지 공격 시」(`full_charge_fire`), 셋째는 「풀 차지 공격 명중 시」
    (`full_charge_hit`)라 **키는 다르지만 발동하는 발은 같다** — 빗나감 모델이 생기면 갈린다.
  - 그래서 **헬름에게 톡톡이(논차지)를 걸면 안 된다** — `CONTROL.md §풀차지 섞기`가 헬름을
    명시적으로 제외 사례로 든다. 논차지로 바꾸는 순간 버충 본체가 사라진다.
- **`진두지휘 3` → 버스트 게이지** *(모드 의존)*
  - `burst_charge_pct` 14.31%/발. **`fixed` 모드에서는 사이클 판정에 아무 영향이 없다**
    (게이지가 결과를 안 정한다). `accumulate`에서만 산다 — `docs/mechanics/버스트 게이지.md`.
  - 표준 스쿼드는 `accumulate`로 돌려도 풀버스트가 **15회로 같다**(1단계가 리틀 머메이드의
    버스트 쿨에 묶여 있어서다). 이 효과의 값어치는 `S39_마르차나아쿠아` 같은 실누적 조합에서 난다.
- **`진두지휘` ↔ 장탄 옵션** *(레이어의 근거)*
  - `last_bullet`은 탄창 크기의 역수로 터진다. 장탄 옵션을 0으로 두는 레이어가
    발동을 4배로 만든다(위 표).
- **애장품 2단계 → 스쿼드 · 3단계 → 본인** *(판본 계단)*
  - 2단계는 `포문 개방 2`(아군 전체 공격 대미지)를 2.35배로 키워 **스쿼드가 먼저 오르고**,
    3단계는 `이지스 캐논`을 6.66배로 키워 **본인만 오른다.**
- **`이지스 캐논`의 대상은 `enemies_top_atk:1`** *(단일 보스에서는 무의미)*
  - 쫄몹이 없으면 `__enemy__`와 같다. 쫄몹이 있으면 보스와 쫄몹(`attack`의 `atk`)을 공격력 순으로 줄 세워 1기를
    고른다 — 쫄몹이 보스보다 세면 쫄몹이 맞고 그 딜은 총딜 밖이다(`calculator/boss_pattern.py` §쫄몹).

## 유저 확인 사항
- 초안 단계를 거치지 않아 신규 질문 없음. 아래는 파싱·코드에서 확인한 사실.
- Q: `포문 개방`(저지 부위)이 왜 한 번도 딜에 안 잡히나?
  A: **구현하지 않기로 한 stat이다**(`intercept_dmg_pct` 🚫, 유저 결정 2026-08-11).
  적 모델에 저지 부위가 없어 조건이 영구 미달성이다. 파싱은 정상이다.
- Q: 애장품 판본이 둘씩 있는데 어느 쪽이 도는가?
  A: **기본 스펙의 애장품 단계가 3이므로 애장품 판본이 전부 돈다.** `--favorite "헬름:N"`으로
  내리면 그 단계까지만 적용된다.
- Q: 장탄 옵션 0% 레이어가 헬름 본인에게는 손해 아닌가?
  A: **맞다** — 본인 딜은 장탄이 많은 쪽이 +1.1%다. 레이어는 `진두지휘`(아군 전체 크확)를
  4배로 돌리기 위한 것이고, 표준 스쿼드에서는 순이득이 −0.3%p로 작다.
  일반 공격 비중이 큰 아군이 여럿인 조합이 이 레이어의 자리다.
- Q: `이지스 캐논 3`의 만료가 왜 `inf`로 찍히나?
  A: **발수 기반(`duration_bullets: 10`)이라 시각 만료가 없다.** 10발을 쏘면 사라진다
  (실측 3.200 → 16.850).

## 해석 선언

| 효과 | 해석 | 근거 | 대안이었다면 |
|---|---|---|---|
| 이지스 캐논 (애장품3) | `burst_damage` 8236.8% — **애장품 판본이 기본 판본을 대체**한다(합산 아님) | `buff_manager.char_effects()` 판본 선택 · 실측 히트 18.6M 1발 | 합산이었다면 버스트 딜이 1.15배 더 나온다 |
| 이지스 캐논 | `burst_cast` 즉시 계산 — 풀버스트 버프 없음 | `GAMEPLAY §값 산정`(순차·버스트 대미지) · 실측 t=3.200 | `bonus_damage`처럼 보류였다면 버스트 딜이 크게 오른다 |
| 이지스 캐논 3 | `duration_bullets: 10` — **시간이 아니라 발수** | 실측 3.200 → 16.850(13.65초) | 10초 지속이었다면 풀버스트 안에서 정확히 끝나 사격 속도와 무관해진다 |
| 포문 개방 3 | `full_charge_hit` `bonus_damage` — **`burst_cast`가 아니므로 보류 대상이 아니다** | `_handle_damage_eff()`는 `burst_cast`만 보류 | 보류 대상이었다면 풀차지마다 나는 딜이 사라진다 |
| 포문 개방 | `intercept_dmg_pct` — **영구 미발동** | `IMPL-STATUS` 🚫 (유저 결정) | 구현하면 저지 부위 모델이 필요하고 레이드 전용 수치가 된다 |
| 진두지휘 | `last_bullet` — 탄창을 비운 **마지막 한 발이 명중할 때** | 실측 재장전 시작 0.383초 전 | 재장전 완료 시점이었다면 위상이 재장전 시간만큼 밀린다 |
| 진두지휘 3 | `burst_charge_pct` — `fixed` 모드에서는 **딜·사이클 모두에 영향 0** | `docs/mechanics/버스트 게이지.md` · 표준 `accumulate` 실측 풀버스트 15회로 동일 | `accumulate`가 기본이었다면 이 값이 사이클을 직접 당긴다 |
| 이지스 캐논 | `enemies_top_atk:1` — 쫄몹이 없으면 `__enemy__`, 있으면 보스·쫄몹 중 공격력 1위 | `_resolve_target()` · `boss_pattern` §쫄몹 | 쫄몹 공격력이 보스보다 크면 버스트 딜이 쫄몹으로 가 총딜에서 빠진다 |

## impl/bug-fix용 검증 체크리스트
- [ ] 이탈 보고에 **`equip_skills.max_ammo_pct: 129.64 → 0 (레이어)`** 한 줄
- [ ] 애장품 3단계 판본이 돈다 — `이지스 캐논` 히트가 **8회에 148.9M**(히트당 18.6M)
- [ ] 애장품 0단계로 내리면 헬름 딜이 **1/3.86** (458.9M → 118.8M)
- [ ] `포문 개방 2` 값이 **27.87**(애장품2)이지 11.85가 아니다
- [ ] 풀차지 3종(`진두지휘 2`·`진두지휘 3` = `full_charge_fire`, `포문 개방 3` = `full_charge_hit`)이 **전부 123회**로 같다
- [ ] `진두지휘` 발동 **16회**(부여 64건 = 16 × 4명) · 간격 ≈ 11.7초
- [ ] 장탄 옵션을 129.64%로 되돌리면 `진두지휘`가 **4회**로 줄어든다 (레이어 회귀 항목)
- [ ] `이지스 캐논 3`이 **10발 뒤에** 사라진다 (표준 3.200 → 16.850, 풀버스트 밖까지)
- [ ] `포문 개방`(`intercept_dmg_pct`)은 부여만 되고 **딜 기여 0** (네거티브)
- [ ] `이지스 캐논 2`(라이프스틸)는 보스 sim에서 **딜에 영향 없음** (네거티브)
- [ ] 헬름 버스트 간격 **25.067s**, 180초 8회 · 사이클 간격 12.533s
