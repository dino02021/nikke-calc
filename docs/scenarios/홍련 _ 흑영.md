# 홍련 : 흑영 동작 시나리오

> 모드: 보강 (parsed_skills 반영)
> **역순 작성** — 이미 등록·검증이 끝난 캐릭터의 시나리오를 뒤늦게 채운 문서다.
> 초안(파싱 전) 단계를 거치지 않았으므로 `## 유저 확인 사항`은 파싱 결과에서
> 역으로 확인한 항목만 담는다. 수치·발동 시각은 전부 실측이다.

## 기본 정보
- 버스트 단계: B3
- 본인 쿨다운: **40s** (화무십일홍 · 만개)
- 무기 유형: RL (9발, 재장전 2.0s, 계수 57.29%, 코어 200%, 차지 0.3s, 풀차지 배율 150%)
- 속성 코드: 풍압 · 클래스: 화력형 · 기업: 필그림 · 스쿼드: 갓데스
- 애장품: **없음**
- 작성 사이클 수: **2** (쿨 40s — 본인 버스트 사이클과 미사용 사이클이 번갈아 온다)
- **기본 레이어 2건** (`data/char_defaults.json`): 차지속도 옵션 **9.26%** · 장전컨 **탄충 취소**

## 효과 요약 (보강: 효과 단위 stat 키)

| ID (parsed name) | 스킬 | 트리거 (timing+condition) | stat: 값(lv10) | 대상 | 지속 |
|---|---|---|---|---|---|
| 파죽 게이지 | 스킬1 | `full_charge_fire_count:3` | `gauge_charge`: +1 (`파죽`) | `self` | — |
| 화무십일홍 · 파죽 | 스킬1 | `full_charge_fire_count:3` + `gauge_eq:파죽:1` | `damage`: 283.03% | `enemies_lowest_def:1` | — |
| 화무십일홍 · 파죽 2 | 스킬1 | `full_charge_fire_count:3` + `gauge_eq:파죽:2` | `split_damage`: 565.0% | `enemies_in_range` | — |
| 화무십일홍 · 파죽 3 | 스킬1 | `full_charge_fire_count:3` + `gauge_eq:파죽:3` | `split_damage`: 848.03% | `all_enemies` | — |
| 화무십일홍 · 파죽 4 | 스킬1 | `full_charge_fire_count:3` + `gauge_eq:파죽:3` | `gauge_consume`: 3 (`파죽`) | `self` | — |
| 화무십일홍 · 수라 | 스킬2 | `full_burst_start` | `max_ammo_pct`: 60.0% | `self` | 10s |
| 화무십일홍 · 수라 2 | 스킬2 | `full_burst_start` | `ammo_charge_pct`: 100.0% | `self` | — |
| 화무십일홍 · 만개 | 스킬3 | `burst_cast` | `trigger_count_reduce`: 2.0 → `파죽 게이지` | `self` | 10s |
| 화무십일홍 · 만개 2 | 스킬3 | `burst_cast` | `atk_pct`: 115.12% | `self` | 10s |
| 화무십일홍 · 만개 3 | 스킬3 | `burst_cast` | `charge_dmg_pct`: 169.63% | `self` | 10s |

**미구현 stat은 없다.** 열 효과 전부가 딜에 실린다 — 이 배치에서 드문 축이다.

## 파싱 값을 원문 기준으로 되돌린 이력 (2026-09-02)

이 문서를 쓰면서 `data/parsed_skills.json`이 `scraper/nikke_scraped.json`(수집 원시 데이터의
정본)보다 **정확히 1.13배 낮은** 값을 들고 있는 것이 드러났다:

| 효과 | stat | 종전 parsed | 원문 = 현재 |
|---|---|---|---|
| 화무십일홍 · 파죽 | `damage` | 250.47 | **283.03** |
| 화무십일홍 · 파죽 2 | `split_damage` | 500.00 | **565.00** |
| 화무십일홍 · 파죽 3 | `split_damage` | 750.47 | **848.03** |
| 화무십일홍 · 만개 3 | `charge_dmg_pct` | 150.12 | **169.63** |

레벨 1~10 전 구간이 같은 배수였고, 같은 템플릿의 `만개 2`(`atk_pct` 115.12)와
`수라`(60.0 / 100.0)는 원문과 일치했다 — **대미지·차지 대미지 계열만** 어긋난 셈이다.
전 캐릭터 89명을 대조해도 이런 모양은 홍련 : 흑영뿐이었다.

**원인은 상향 패치다** (유저 확인 2026-09-02). 패치 뒤 원시 데이터는 갱신됐는데
`parsed_skills.json`이 따라가지 못한 채로 남아 있었다. 원문 값으로 되돌렸고,
아래 수치는 전부 되돌린 뒤 기준이다. 개인 딜 658,557,501 → **728,854,657 (+10.7%)**.

> **doclint에는 원문 ↔ parsed 값 대조 검사가 없다.** 스킬 텍스트가 바뀌지 않고
> 숫자만 바뀌는 밸런스 패치는 지금 구조에서 아무 데도 안 걸린다. 같은 종류의
> 잔류가 다른 캐릭터에도 있는지는 이번에 한 번 훑었을 뿐이다(다른 27명의
> "원문에만 있는 값"은 `배율 ▼` 변환·항 합성 같은 정상 파싱 결정으로 보였다).

## 검증 스쿼드

- **표준 (B3 · 쿨 40s)**:
  `["리틀 머메이드", "크라운", "홍련 : 흑영", "test_B3"]`
  - `GAMEPLAY.md §표준 테스트 스쿼드`의 B3·쿨40 템플릿 그대로다.
  - 격 사이클 버스트와 `full_burst_start`(수라) / `burst_cast`(만개)의 분리를 잡는다.
- **변형 1 (코어 보스)**: `enemy={"core_px": 30}`
  - RL 단발 풀차지라 코어히트율이 개인 딜을 크게 흔든다. 실측 728,854,657 → **856,481,068 (+17.5%)**.
  - 파죽 1~3은 `damage`/`split_damage`라 코어 배율을 안 받는다 — 늘어난 건 전부 일반공격이다.
- **변형 2 (탄충 취소 미적용)**: `chars={"홍련 : 흑영": {"control": {}}}`
  - **표준 스쿼드에서는 총딜이 한 자리도 안 움직인다** (728,854,657 동일).
    표준 조합에서 홍련 : 흑영은 **180초 내내 재장전을 한 번도 하지 않기 때문**이다(아래 §탄약).
    `CONTROL.md §탄충 취소`가 말하는 "재장전이 풀버스트 경계를 물 때"가 성립하지 않는다.
  - 이 레이어를 검증하려면 재장속 버프가 없거나 탄창이 마르는 조합이 필요하다 —
    실전 커버는 `S40_홍련흑영벨벳`(택티컬 베어 큐브)이 맡는다.

- **실전 조합 (하네스)** — 홍련 : 흑영은 1개 스쿼드에 들어 있다:

  | 스쿼드 | 멤버 | 적 | 비고 |
  |---|---|---|---|
  | `S40_홍련흑영벨벳` | 리틀 머메이드 · 나유타 · **리버렐리오** · **홍련 : 흑영** · 벨벳 | 풍압 | 홍련 : 흑영만 **택티컬 베어 큐브**(탄충) — 유일한 instant 큐브 커버 |

  baseline 기준 스쿼드 3,266,177,947 중 홍련 : 흑영 **1,078,341,434 (33.0%)**.
  파죽 히트 64 / 63 / 63으로 1:1:1이 유지된다. **재파싱으로 이 스쿼드의 골든 스냅샷이
  함께 움직였다** — 종전은 스쿼드 3,171,285,878 / 홍련 : 흑영 983,449,365 (31.0%)였고,
  **히트 수는 하나도 안 바뀌었다**(311 / 64 / 63 / 63). 값만 오른 변화다.

실측 (180s, `--expected`, `first_burst_time=3.0`, 적 풍압, 기본 스펙 1층 + 레이어 2건):

| 스쿼드 | 스쿼드 총딜 | 홍련 : 흑영 |
|---|---|---|
| 표준 | 1,528,674,836 | 728,854,657 (47.7%) |
| 표준 + 코어 30px | 1,876,476,958 | 856,481,068 (45.6%) |
| `S40_홍련흑영벨벳` (baseline, seed 42 · 재파싱 후 재생성) | 3,266,177,947 | 1,078,341,434 (33.0%) |

## 파죽 게이지가 이 캐릭터의 전부다

스킬 1은 **가상 게이지 `파죽`(최대 3)** 하나로 표현돼 있다 (`PARSING-CHARS.md §홍련 : 흑영`).

```
풀차지 N회 누적 → 파죽 게이지 +1 → 게이지 값에 따라 파죽1(=1) / 파죽2(=2) / 파죽3(=3)
                                   파죽3이 나가면 게이지 −3 (0으로 리셋)
```

- **평소 N = 3**(`full_charge_fire_count:3`) — 풀차지 3발마다 한 단계.
- **버스트 중 N = 1** — `화무십일홍 · 만개`의 `trigger_count_reduce` 2.0이
  `파죽 게이지` 항목의 `full_charge_fire_count:3`을 **3 → 1로 깎는다**. 풀차지 1발마다 한 단계다.

홍련 : 흑영의 풀차지 주기는 **0.75초**로 버스트 안팎이 같다(차지 0.3s + 레이어 차지속도 9.26%).
바뀌는 건 **문턱값뿐**이다:

| 구간 | 게이지 1칸에 필요한 발수 | 실측 간격 | 사이클(12.533s)당 파죽 발동 |
|---|---|---|---|
| 만개 활성 (본인 버스트 10초) | 1발 | **0.75s** | 13~14회 |
| 그 외 | 3발 | **2.25s** | 5회 |

**본인 버스트 사이클과 미사용 사이클의 딜이 5.8배로 갈리는 이유가 이것이다** (아래 §타임라인).

## 사이클 타임라인 (표준, 2사이클)

### 사이클 1 — 홍련 : 흑영 버스트 사이클

| t (실측) | 이벤트 | 발동 효과 (ID / stat) | 대상/지속 | 비고 |
|---|---|---|---|---|
| 0.000 | battle_start | — | — | **스킬에 `battle_start`가 하나도 없다.** 장비·큐브·소장품만 붙는다 |
| 0.283 · 1.017 · 1.767 | 풀차지 사격 | — | | 주기 0.75s |
| **1.767** | 풀차지 3발 누적 | **파죽 게이지**(+1 → 1) → **화무십일홍 · 파죽**(283.03%) | 적 1기 | 첫 파죽. 401,582 |
| 3.000 | 리틀 머메이드 stage:1 | — | | |
| 3.100 | 크라운 stage:2 | — | | |
| **3.200** | **홍련 : 흑영 burst_cast** | **만개**(`trigger_count_reduce` 2.0) · **만개 2**(`atk_pct` +115.12) · **만개 3**(`charge_dmg_pct` +169.63) | self · 10s | 여기서부터 문턱 3 → 1 |
| 3.250 | **full_burst 시작 (1회째)** | **수라**(`max_ammo_pct` +60, 10s) · **수라 2**(`ammo_charge_pct` 100%) | self | 탄창을 통째로 채운다 |
| 3.267 | 풀차지 | 파죽 게이지(+1 → 2) → **파죽 2**(565%, 분배) | 범위 내 적 | |
| **4.017** | 풀차지 | 파죽 게이지(+1 → 3) → **파죽 3**(848.03%, 분배) → **파죽 4**(`gauge_consume` 3 → 0) | 적 전체 / self | 사이클 최대 단타 |
| 4.767 → 12.900 | 0.75s마다 | 파죽 1 → 2 → 3 순환 (consume 6.267 · 8.500 · 10.700 · 12.900) | | 버스트 창에서 **파죽3만 5회** |
| 13.200 | 10s 만료 | 만개 · 만개 2 · 만개 3 off | | 풀버스트 종료(13.250)보다 **0.05s 먼저** — 문턱이 3으로 돌아간다 |
| 13.250 | full_burst 종료 | 수라 off | | |
| 15.100 | 풀차지 3발 누적 | 파죽 게이지(+1) | | 간격이 다시 2.25s로 |

### 사이클 2 — 홍련 : 흑영 미사용 사이클 (test_B3이 B3)

| t (실측) | 이벤트 | 발동 효과 | 비고 |
|---|---|---|---|
| 15.533 · 15.633 · 15.733 | 리틀 머메이드 · 크라운 · test_B3 | — | 홍련 : 흑영은 쿨 |
| **15.783** | **full_burst 시작 (2회째)** | **수라 · 수라 2 재부여** | **홍련 : 흑영이 버스트하지 않아도 걸린다** |
| 17.333 · 19.583 · 21.833 · 24.083 | 풀차지 3발마다 | 파죽 게이지 → 파죽 1/2/3 (consume 19.583) | 간격 **2.25s** |
| 25.783 | full_burst 종료 | 수라 off | |
| 28.267 | 홍련 : 흑영 burst_cast (2회째) | 사이클 1과 동일 | **간격 25.067s** |

사이클 간격은 **12.533s 등간격**(180s에 15회) — 리틀 머메이드 `버블 오더 2`가 쿨 40s를
두 사이클 만에 25.067s로 깎아 격 사이클이 된다.

**사이클별 개인 딜 (실측):**

| 사이클 | 홍련 : 흑영 딜 | 파죽 1/2/3 히트 | 일반공격 |
|---|---|---|---|
| 1 (본인 버스트) | **87,174,796** | 5 / 5 / 5 | 17히트 21,849,880 |
| 2 (미사용) | **15,089,381** | 1 / 2 / 2 | 17히트 5,311,821 |

일반공격 히트 수는 같은데 딜이 4.1배다 — `만개 2`(공격력 +115.12)와 `만개 3`
(차지 대미지 +169.63)가 본인 버스트 사이클에만 걸리기 때문이다.

180초 누적: 파죽 **49 / 48 / 48회**(1:1:1), 게이지 충전 145회, consume 48회.
개인 딜 분해 — 파죽 3 36.4% · 파죽 2 24.2% · 파죽 12.6% · 일반공격 26.8%.

## 탄약 — 표준 조합에서는 재장전이 0회다

- 기본 장탄 9 × (1 + 장비 `max_ammo_pct` 129.64%) = **20발**,
  풀버스트 중에는 `수라`가 +60%p를 얹어 **26발**.
- 사이클당 실제 사격은 약 17발이고, 매 풀버스트 진입에 `수라 2`가 **탄창을 100% 채운다**.
- 결과: **180초 동안 `재장전 시작` 이벤트가 한 건도 없다.** 재장속 버프
  (렐릭 베어 큐브 29.69% · 크라운 `원 포 올 2` 44.35%)가 전부 죽은 값이 된다.
- 그래서 표준 스쿼드에서는 **탄충 취소 레이어도 무효**다(변형 2). 재장전이 풀버스트
  경계를 무는 상황 자체가 안 나온다.

## 스킬 간 상호작용

- **`만개`(문턱 축소) → `파죽 게이지` → 파죽 1/2/3** *(이 캐릭터의 유일한 축)*
  - 경로: `만개`(`trigger_count_reduce` 2.0, `target_effect: 파죽 게이지`) → `파죽 게이지`의
    `full_charge_count` 3 → 1 → 게이지가 3배 속도로 돈다 → 파죽 3(848.03%)이 5배 자주 나간다.
  - 시점: `burst_cast`(3.200)에 켜져 10초. **풀버스트 종료보다 0.05초 먼저 꺼진다.**
  - 위험: `trigger_count_reduce`가 `target_effect` 이름으로 붙어 있다 —
    `파죽 게이지` 항목의 이름이 바뀌면 **조용히 무효가 되고 딜만 1/3로 준다.**
    체크리스트의 "버스트 중 게이지 간격 0.75s"가 그 감시선이다.
- **동일 timing 안의 실행 순서 — 충전 → 판정 → 소비** *(최빈 사각지대)*
  - `full_charge_fire_count:3` 하나에 **다섯 효과가 매달려 있다.** `parsed_skills.json` 배열 순서가
    `파죽 게이지`(충전) → `파죽`(=1) → `파죽 2`(=2) → `파죽 3`(=3) → `파죽 4`(소비)다.
  - **충전이 판정보다 앞서야** 3발째에 파죽1이 나간다. 실측 첫 파죽1이 t=1.767(3발째)이므로
    현재 순서가 맞다. 뒤집히면 아르카나 : 포츈 메이드 `청춘의 기록`과 같은 **한 발 밀림**이 된다.
  - 소비가 판정보다 앞서면 파죽3이 영원히 안 나간다. 실측 파죽3 48회로 정상.
- **`수라`(장탄) × `수라 2`(탄충)는 딜이 아니라 사이클 유지 장치다**
  - 둘 다 `full_burst_start`라 **홍련 : 흑영이 버스트하지 않는 사이클에도 걸린다**(15회).
    킷에서 유일하게 커버리지 100%인 효과다.
  - 표준에서는 재장전이 애초에 안 나므로 **총딜 기여가 0에 수렴한다**(변형 2). 값이 사는 건
    탄창이 마르는 실전 조합 쪽이다.
- **`split_damage`(파죽 2·3)는 `split_dmg_pct` 보유자의 증폭 대상이다** *(교차 캐릭터)*
  - 델타 : 닌자 시프 `비기 : 닌자 오버드라이브`(`split_dmg_pct` +20, all_allies)와 팬텀
    `괴도의 시선 7`이 홍련 : 흑영의 파죽 2·3에 곱해진다. 표준 스쿼드에는 없다.
  - 반대 방향도 성립한다 — 홍련 : 흑영은 `docs/scenarios/델타 _ 닌자 시프.md` 변형 2의
    "분배 대미지 수령자" 목록에 이름이 올라 있다.

## 유저 확인 사항
- 초안 단계를 거치지 않아 신규 질문 없음. 아래는 파싱·코드에서 확인한 사실.
- **Q: 파싱 값이 원문과 1.13배 어긋나 있었는데 어느 쪽이 맞나?**
  A: **원문이 맞다 — 그 사이 상향 패치가 있었다** (유저 확인 2026-09-02).
  `parsed_skills.json`을 원문 값으로 되돌렸다. 위 §파싱 값을 원문 기준으로 되돌린 이력 참조.
- Q: `PARSING-CHARS.md`는 "전투 시작 시 게이지 1로 초기화"라고 적혀 있었는데 실제로도 그런가?
  A: **아니다. 게이지는 0에서 시작한다.** `parsed_skills.json`에 `battle_start` 게이지 항목이
  없고, 실측 첫 파죽이 3발째(t=1.767)에 **파죽 1**이다. 게이지가 1에서 시작했다면 3발째에
  파죽 2가 먼저 나왔어야 한다. 순 동작(3/6/9발에 1/2/3단계)은 같아 딜 영향은 없다.
  **`PARSING-CHARS.md` 해당 행은 2026-09-02에 구현대로 고쳤다** (배열 순서 설명도 함께).
- Q: 홍련 : 흑영이 버스트하지 않은 사이클에도 수라가 걸리나?
  A: **걸린다.** `full_burst_start`는 스쿼드 풀버스트 진입을 센다. 실측 t=15.783 정상 발동.
- Q: 탄충 취소 레이어가 표준 스쿼드에서 딜을 바꾸나?
  A: **한 자리도 안 바꾼다.** 재장전 이벤트가 180초에 0건이라 발동 조건 자체가 없다.
- Q: 코어가 있으면 파죽도 같이 오르나?
  A: **아니다.** 파죽 1~3은 `damage`/`split_damage`라 코어 배율 밖이다. 코어 30px에서 늘어난
  +17.5%는 전부 일반공격 몫이다.

## 해석 선언

| 효과 | 해석 | 근거 | 대안이었다면 |
|---|---|---|---|
| 파죽 1/2/3 | **가상 게이지 `파죽`(0~3) 순환**. 3발마다 한 칸, 3칸째에 소비 | `PARSING-CHARS.md §홍련 : 흑영` · 실측 히트 49/48/48 | `full_charge_fire_count:3/6/9` 세 timing으로 뒀다면 리셋 시점이 없어 파죽3이 6발마다 중복 발동한다 |
| 실행 순서 | **충전 → 게이지 판정 → 소비** (배열 순서) | 실측 첫 파죽1 t=1.767 = 3발째 | 판정이 앞서면 한 발씩 밀리고, 소비가 앞서면 파죽3이 영구 미발동 |
| 화무십일홍 · 만개 | `trigger_count_reduce` **1건으로 통합** — `파죽 게이지`의 문턱만 3→1 | `PARSING-CHARS.md §홍련 : 흑영` 스킬3 · 실측 버스트 중 게이지 간격 0.75s | 파죽1/2/3 각각의 timing을 따로 줄였다면 게이지 회전과 어긋나 단계가 건너뛴다 |
| 화무십일홍 · 파죽 2 | `split_damage` (`enemies_in_range`) | parsed stat/target | `damage`였다면 `split_dmg_pct` 증폭을 못 받는다 |
| 화무십일홍 · 파죽 3 | `split_damage` (`all_enemies`) | parsed stat/target | 쫄몹이 있어도 파죽 2와 대상이 같다 — 좌표가 없어 `enemies_in_range`도 전원이고, 둘 다 맞은 적 수로 나눈다(`boss_pattern` §쫄몹) |
| 화무십일홍 · 파죽 | `enemies_lowest_def:1` — 쫄몹이 없으면 `__enemy__`, 쫄몹이 있으면 **쫄몹 먼저**라 쫄몹 1기 | `_resolve_target()` · `boss_pattern` §쫄몹 | 쫄몹이 살아 있는 동안 파죽 딜이 쫄몹으로 가 총딜에서 빠진다 |
| 화무십일홍 · 수라 2 | `ammo_charge_pct` 100% = **탄창 전체 즉시 충전** | parsed stat · 실측 재장전 0회 | 30/60%(스킬 레벨 1~6 판본)였다면 표준에서도 재장전이 살아나 탄충 취소 레이어가 값을 갖는다 |
| 만개 2 / 만개 3 | 각각 `atk_pct`·`charge_dmg_pct`, 대상 `self` | 원문 "[공격력 ▲][차지 대미지 ▲]" 자기 대상 | 아군 대상이었다면 스쿼드 총딜 구도가 통째로 달라진다 |

## impl/bug-fix용 검증 체크리스트
- [ ] 첫 파죽이 **3발째**(t≈1.767)에 **파죽 1**로 나간다 — 한 발 밀림·단계 건너뜀 없음
- [ ] 버스트 중 게이지 충전 간격 **0.75s**(풀차지 1발), 버스트 밖 **2.25s**(3발)
- [ ] 파죽 1 : 2 : 3 히트 비가 **1 : 1 : 1** (180초 49 / 48 / 48)
- [ ] `파죽 4`(`gauge_consume` 3)가 파죽 3과 **같은 프레임**에, 파죽 3 뒤에 실행
- [ ] `수라`·`수라 2`는 **매 풀버스트 진입마다** 15회 (홍련 : 흑영 미버스트 사이클 포함)
- [ ] `만개` 3종은 **본인 버스트 사이클에만** 8회 · 만료가 풀버스트 종료보다 0.05s 빠름
- [ ] 홍련 : 흑영 버스트 간격 **25.067s** · 사이클 간격 12.533s 등간격 · 180초 15회
- [ ] 본인 버스트 사이클 딜 ÷ 미사용 사이클 딜 ≈ **5.8배** (87.2M / 15.1M)
- [ ] 표준 조합에서 **재장전 이벤트 0건** — 탄충 취소 레이어를 빼도 총딜 불변 (네거티브)
- [ ] 코어 30px에서 늘어나는 건 일반공격뿐 — 파죽 1/2/3은 코어 배율 밖
- [ ] `trigger_count_reduce`의 `target_effect`가 `"파죽 게이지"`를 계속 가리킨다
      (이름이 어긋나면 버스트 중 게이지 간격이 0.75 → 2.25s로 조용히 돌아간다)
